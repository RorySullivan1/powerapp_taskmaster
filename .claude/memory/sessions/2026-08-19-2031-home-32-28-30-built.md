# 2026-08-19 20:31 · home-32-28-30-built

**Goal:** Land #32 (dedupe the dashboard fold), #28 (local project-name map) and #30 (single
open-issues fetch) on scrHome — all three touch the same block, #32 first so the other two are
one-place edits.

## What happened
- **#32 — `a8592c6`.** The KPI fold lived twice in `scrHome`: `OnVisible` and the section
  header's `Refresh Stats` `OnAction`, byte-for-byte, under a comment conceding there was no
  shared function to call. Extracted to **`btnHomeRecompute`** — hidden, 1x1, declared FIRST in
  the screen's children, the exact `btnPrjHealth` / `btnRptLoad` shape. `OnVisible` is now the
  `gUserEmail` self-heal + `Select()`; `OnAction` is `Concurrent(Refresh x3)` + toast + `Select()`.
  The three `Refresh` calls used to run in series.
- **#28 — `a4f57e9`.** `cellMyTaskProject` / `cellMyIssueProject` looked the parent project's
  name up against `taskmaster_projects` PER VISIBLE ROW, re-firing on scroll. Now `LookUp(
  colPrjNames, ... )` — a fourth `Concurrent` arm, `ForAll( ActiveProjects As p, {ID, project_name} )`.
- **#30 — `0073ba5`.** `gIssLed` was one delegable query PER PROJECT MANAGED. Now a fifth
  `Concurrent` arm `colIssByPrj = ForAll( OpenIssues As i, { PId: i.issue_project_id.Id } )` and a
  local count, with the old per-project queries kept as the **`CountRows(colIssByPrj) >= 2000`
  overflow fallback**.

## Gotchas & dead ends
- **`Select()` QUEUES the target `OnSelect` for AFTER the calling formula finishes** — it does not
  run it inline and does not wait (MS Learn, function-select). Two consequences, both commented
  in the source: the `OnVisible` self-heal is GUARANTEED to land before the fold reads
  `gUserEmail`; and **the Refresh toast now fires BEFORE the numbers do** — "Refreshed" means the
  refresh was issued. Moving the toast into `btnHomeRecompute` would fix the order but would then
  toast on every screen open, so it stays in `OnAction`. Keep `Select` LAST in both callers.
- **`Select()` OF A SCREEN CONTROL FROM A COMPONENT INSTANCE'S EVENT HANDLER WORKS — SETTLED IN
  STUDIO** (user, 2026-08-19). `hdrMyWork.OnAction` names the sibling `btnHomeRecompute` and it
  resolves. This was the one thing in the three changes that could have failed outright rather
  than degrade; the fallback (inline the fold in `OnAction`, keep `OnVisible` on the button) was
  never needed. The reasoning that predicted it: the instance's event binding is authored in
  SCREEN scope, so component isolation does not apply — isolation applies to formulas INSIDE the
  component definition, which still cannot see screen controls. Every prior `Select( btnRptLoad )`
  / `Select( btnPrjRecompute )` caller in the repo was a screen `OnVisible`, `OnChange` or a plain
  control `OnSelect`, so this is the first grounded case of the pattern here. Reuse it.
- **#28's archived-parent edge was ACCEPTED, not solved.** `colPrjNames` covers active projects
  only, so a row whose parent archived since its `*_project_archived` flag was last stamped shows
  "—" instead of a name; today's raw-list LookUp resolves it. Considered and REJECTED a
  `If( IsBlank(local), <server LookUp> )` fallback: `If` laziness is grounded in BEHAVIOUR
  formulas, but this is a DECLARATIVE `Text` property where the row may still register the data
  source dependency — a construct that MIGHT not remove the per-row call is worse than one that
  definitely does, when the goal of the issue is removing per-row calls and nothing can be
  measured across the gap.
- **#30 amends the 2026-08-13 decision, it does not reverse it.** There is still no delegable
  single query for "children of the projects I manage" and `issue_project_id.Id in
  colMyProjects.ID` still does not delegate. What changed is the guard: the `>= 2000` sentinel
  means a partial page can no longer undercount silently, which is the risk that decision was
  protecting against. Both `If` arms are live; the comment says so, so neither reads as dead
  code later.
- **`ForAll` projection, never `ShowColumns`, over anything rooted in a named formula** — both
  new collections read a field back, so both use `ForAll`. `colRptIss` on `scrReports` gets away
  with `ShowColumns( OpenIssues, ... )` only because nothing reads a column off it.
- **The three `LookUp( taskmaster_projects, ... )` left in `scrHome` row `OnSelect` handlers are
  correct and out of scope** — one call per TAP, and a deliberate live re-read before navigating
  (rule 5 in the screen header).

## State at end
- 22/22 valid. Three commits on `claude/powerapp-repo-init-xymvlm` and on `main`.
- **LANDED (user, 2026-08-19) AND #32 / #28 / #30 ARE CLOSED.** `scrHome` pasted clean first
  time; the paste queue is empty again.
- **LANDING IS A PASTE CONFIRMATION, NOT A MEASUREMENT.** #30's "Done when" asked for a Live
  Monitor count and that step was NOT performed, so every round-trip saving claimed across these
  three is what the source now does, not an observed figure.

## Open threads
- Unexercised paths, believed correct from the source but never seen either way: **the `>= 2000`
  fallback arm in `gIssLed` has never run** (needs a 2000+ open-issue tenant, so the overflow
  path is correct by construction and unverified); a work-list row whose parent archived
  mid-session (should show "—"); the toast-before-numbers ordering that `Select`'s queueing
  introduces, which has not been judged in use; and the `DisplayMode` trap — `Select` does
  nothing on a Disabled target, and `btnHomeRecompute` leaves `DisplayMode` at its default, so
  this only becomes a hazard if a future edit sets it.
- Perf backlog still open after these: #33, #34, #35, #37, #38, #39, #40.

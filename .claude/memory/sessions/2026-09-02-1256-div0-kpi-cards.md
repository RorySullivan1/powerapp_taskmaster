# 2026-09-02 12:56 · div0-kpi-cards

**Goal:** Diagnose and fix the "Division by 0" at app start; audit main vs the other branches.

## What happened
- User: *"Upon start an error 'division by 0' occurs. What is the cause of this?"* — near-identical
  wording to the 2026-08-19 report, but WITHOUT "sometimes".
- **The 2026-08-19 answer is dead code.** That session traced the error to `/ gTlSpan` in
  `imgTimeline.Image` and fixed it with `sp: Max(7, gTlSpan)`. **#24 then deleted the timeline
  outright** (`feat: add the projects gallery and swap the timeline for two charts`), so
  `gTlSpan`, `gTlStart`, `gTlEnd`, `colTimeline` and `imgTimeline` appear NOWHERE in `src/`.
  Verified with `git log -S"gTlSpan"` — two commits, one adding, one removing. Do not go looking
  for the timeline again.
- **THE ANSWER: `scrHome.pa.yaml:325` and `:384` — `lblPrjLedSub` / `lblTaskLedSub`.**
  `RoundDown(gPrjLedOverdue / gPrjLed * 100, 0)` behind `If( gPrjLed = 0, "none open", … )`.
  Property formulas on the START screen; `OnVisible` reaches `btnHomeRecompute` with `Select()`,
  which QUEUES, so both globals are Blank at first paint. Blank is not reliably `= 0` for an
  untyped global, the else arm runs, `Blank()/Blank()` coerces to `0/0`.
- Re-ran the full divisor enumeration over `src/` (script in scratchpad; strip `#` and `//`
  comments, single- and double-quoted strings, then `Classic/X@n.n.n` tokens, then take the token
  after each surviving `/`). ~200 naive grep hits reduce to ~140 real ones, and **these two were
  the only unguarded divisors left in the app.** Everything else is a literal constant divisor,
  `Max(1, …)` at the point of division, or a `Max(1, …)` global read only inside the behaviour
  formula that set it (`gRptAudTot`/`gRptRgnTot`/`gRptPhTot`/`gRptPrdTot`/`gRptPr2Tot`).
  `scrReports:1979/2332` `n` is `Max(1, CountRows(colRptBuckets))` and `sk` is `If(… > 8, 2, 1)`
  — both clear.

## Gotchas & dead ends
- **The two prior decisions on `= 0` guards CONTRADICT each other, and the empirical one wins.**
  2026-08-17 recorded Studio *actually reporting* Division by 0 on `scrReports` formulas already
  guarded with `If( gRptOpenN = 0, … )`. 2026-08-19 then reasoned from MS Learn's
  `If( false, 1/0, 3 )` example that `If(gPrjLed = 0, …)` is "genuinely safe" and wrote
  **"RULED OUT — do not re-audit these"** into the session log. That ruling is why the bug
  survived the timeline fix and the timeline's deletion. See the Decisions entry.
- The MS Learn example is not wrong, it is just about a LITERAL `false`. It says nothing about
  whether a blank untyped global satisfies `= 0` at the moment a property formula first renders.
- `scrReports` states the correct invariant in its own file header (rule 3: *"Every denominator in
  a PROPERTY formula is wrapped Max(1, …) at the point of use"*). `scrHome`'s header has no such
  rule — that asymmetry is what let the two lines through.

## State at end
- **FIXED IN THE REPO, NOT IN STUDIO.** `dfb5080` on `claude/powerapp-repo-init-xymvlm`.
  Both labels divide by `Max(1, gPrjLed)` / `Max(1, gTaskLed)`; the `= 0` arm is KEPT because it
  is what produces "none open" rather than a misleading "0% overdue". 22/22 valid.
- A 3-line comment on `lblPrjLedSub` and a 1-line pointer on `lblTaskLedSub` say why the
  `Max(1, …)` is not redundant with the `= 0` arm — it reads as belt-and-braces and a later pass
  will delete it otherwise.
- **PASTE QUEUE: `scrHome` (code view, full file).** Cheap to verify: cold start, dashboard with
  at least one non-complete project led, no banner.

## Branch audit
- Only two branches exist, local and remote: `main` and `claude/powerapp-repo-init-xymvlm`.
- `main` = `3b24298`, the merge of **PR #41 from this branch**. The branch tip `0d67cdf` is an
  ancestor of `main`, `origin/main..branch` is EMPTY, and `git diff origin/main HEAD` was empty —
  **identical trees. `main` was the most up to date; nothing was stranded on a side branch.**
- Because #41 is merged, the branch was restarted from `origin/main` (`checkout -B`, same name)
  and the fix committed on top, per the merged-PR rule. No force push needed — `0d67cdf` is an
  ancestor of `main`, so the push fast-forwards.

## Open threads
- **NOT EXERCISED.** "Fixed" means the source is right and validates; the banner has not been
  observed gone. Same trap as the timeline fix, which was recorded as landed and was never proven.
- The `= 0`-guard question is now settled by decision, not by probe. A `tests/` probe screen could
  settle empirically whether a blank untyped global satisfies `= 0` in a property formula — worth
  it only if a third instance of this bug appears.

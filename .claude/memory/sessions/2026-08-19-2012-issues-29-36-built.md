# 2026-08-19 20:12 · issues-29-36-built

**Goal:** Build #29 (hoist + parallelise pre-Patch reads) and #36 (gate the taskproduct reconcile)

## What happened
- **#29 — `713b4d9`.** `scrTaskEdit.btnTkSave.OnSelect`: the parent project row was read TWICE per
  save — inline in the Patch record for `task_project_archived`, and again for the C3 rollup. One
  `Concurrent` at the top of the handler now reads it once into `gTkParentProj`, paired with the
  `colTkHlIss` health fetch that used to sit serialized ahead of it. Both the mirror and the rollup
  read that record; `gTkRollupProj` is gone. The rollup's own `Concurrent` collapsed to a plain
  `Set` — one arm left, and Concurrent needs two.
- **#29 — `scrIssueEdit`.** The parent-project LookUp and the stored-`issue_date_close` LookUp both
  sat inside the Patch record. Now one `Concurrent` above the `IfError`. The close-date arm KEEPS
  its `gEditMode = "Edit" && StartsWith(gIssStatus,"Closed")` guard, so the ordinary (non-closing)
  save still issues exactly one read — hoisting it unconditionally would have ADDED a round trip
  to the common path.
- **#36 — `a146eec`.** `scrTaskEdit.OnVisible` baselines the loaded links into
  `colTkProductsLoaded` (copied from `colTkProducts`, not a second junction read, and INSIDE the
  resume guard). The reconcile + summary block is gated on
  `CountRows(colTkProducts) <> CountRows(colTkProductsLoaded) || !IsEmpty(Filter(colTkProducts, !(Id in colTkProductsLoaded.Id)))`
  — both tests collection-vs-collection, no query behind either.

## Gotchas & dead ends
- **The mirror LookUp's key changed from `gSelProject.ID` to the screen's own parent id**
  (`gTkProject.Id` / `gIssProject.Id`). Merging two LookUps forces one key, and the screen-local
  one is strictly more correct: it is the record's OWN stored parent, where `gSelProject` is only
  equal to it because every entry path happens to come from that project. Both entry paths into
  `scrTaskEdit` (`scrProject`, `scrProjectEdit`) were checked. The "read LIVE, never a snapshot"
  invariant is untouched — the id is a key, the record is still fetched at save time.
- **`scrTransactionEdit` was deliberately NOT changed**, despite #29 naming it. It has exactly ONE
  pre-Patch read and nothing independent to pair it with, so hoisting the LookUp out of the Patch
  record moves code without removing a round trip. Same shape of call as #31's skip there.
- **`Concurrent` needs ≥ 2 arms** — that is why the rollup block became a bare `Set`, not a
  one-armed `Concurrent`.
- **`If(cond, value)` with no else** is how the conditional close-date arm stays conditional
  inside a `Concurrent` arm; it yields `Blank()` when false, which `Coalesce(..., Now())` already
  handles.
- **`task_product_summary` is now left unwritten on a products-untouched save**, so a brand-new
  task with no products keeps `Blank()` where it used to get `""`. No control in `src/` reads that
  column (checked) — it is a denormalised column for SharePoint views — so blank vs empty string
  is not observable in the app.
- **The `tglTkOutput` trap stays fixed.** The gate compares staged state to loaded state, never
  toggle visibility, so deliberately clearing every product is still a set difference and still
  deletes.

## State at end
- 22/22 valid. Two commits on `claude/powerapp-repo-init-xymvlm` and on `main`.
- **BOTH ARE AUTHORED AND UNPASTED.** `scrTaskEdit` and `scrIssueEdit` are queued for the human to
  carry into Studio. Nothing about either is confirmed until they report back.

## Open threads
- Paste `scrTaskEdit` and `scrIssueEdit`, then confirm: a task save still stamps the archived
  mirror correctly; closing an issue still preserves its original close date on a re-save; a
  products-untouched save issues no junction reads; a save that adds/removes/clears products
  reconciles as before.
- Perf backlog still open: #28, #30, #32, #33, #34, #35, #37, #38, #39, #40.

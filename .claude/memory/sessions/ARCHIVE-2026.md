# ARCHIVE 2026 — the full INDEX.md contents as of 2026-08-13

Moved here verbatim when INDEX.md was pruned from 620 lines / 231KB back toward its
~80-line budget. NOTHING was rewritten or shortened on the way in: this is the
complete Decisions ledger, Threads list and Log as they stood. INDEX.md keeps a
one-line digest of the entries still load-bearing and points here for the rest.

Append-only, like every file under sessions/. Do not edit or reorder it.

## State            (rewrite in place — current truth only, ≤ ~10 lines)
- **Phase: BUILT — now editing and refining.** 11 screens, 9 components and the App object
  exist in Studio, the lists are added, `App.OnStart` + `App.Formulas` are in. Work is fixing and
  refining, not creating. **THE PUBLISHED APP RENDERS PROPERLY as of 2026-08-12 (user confirmed)** —
  the squished-and-black era is over; `gTheme` resolves in the player.
- **HOVER-FIX PASTE IN PROGRESS 2026-08-12.** Landed so far: `App.OnStart` (carries the new
  `gTheme.Color.Hover` / `.Press` tokens) and `cmpAppBar`. Remaining: `cmpPicker`,
  `cmpLookupField`, `cmpNestedSelect`, `cmpToast`, then every screen except `scrHome`.
  **`App.OnStart` MUST land before any screen** — the screens read the two new tokens, and a
  missing token is blank, which coerces to black.
- **LANDED 2026-08-12 (user confirmed): `cmpLookupField`, `cmpPicker`, and all five re-pasted
  `scr*Edit` screens are IN STUDIO**, along with the four code-review batches. **THERE ARE NO
  ORPHANED CONTROLS — the user DELETES a screen before pasting it back** (confirmed 2026-08-12),
  so a re-paste is a genuine replace and there are no `_1` suffixes either. Stop carrying an
  orphan backlog. Still outstanding: deleting the superseded COMPONENTS in Studio
  (`cmpPeoplePicker`, `cmpRecordPicker`, `cmpUiKit`, `cmpEditableGrid`, `cmpStatusPill`,
  `cmpChoicePill`) — instance-free but still shipping inside the `.msapp`.
- **Repo restructured 2026-08-05 to plain source**: `src/App.pa.yaml`, `src/Screens/*.pa.yaml`,
  `src/Components/*.pa.yaml`. 22/22 valid. Component definitions are WHOLE files again — the
  contract/body split, the skeleton variants, the `.msapp` packing and the `studio/` tree are all
  deleted, along with the paste runbooks. `docs/build-history.md` is the closed historical record.
- **The shell is ONE component, `cmpAppBar`** (header bar + fly-out nav rail). The four `scr*Edit`
  screens deliberately KEEP a local close-only header — the fly-out rail would let a user navigate
  away from an editor holding unsaved staged children.
- **`scrProject` REWORKED 2026-08-07 — no tabs.** Info card (name + 8 explicit facts at 12pt, KPI
  ring, stacked edit/delete icons) over a work band: tasks and transactions stacked left, issues
  full height right with 5 columns. Containers throughout below the header; `gProjTab` is gone.
  **Padding-overflow fixed 2026-08-11 — IN STUDIO.**
- **STANDING LAYOUT RULE: a child's `Width` must subtract its parent's horizontal padding.**
  `Parent.Width` in a container is the OUTER width, not the content box — `Width: =Parent.Width`
  under 16px padding hangs 32px off the right. Write `=Parent.Width - 48` / `- 32`, as
  `scrProjectEdit` always has. **SWEPT 2026-08-11: all screens audit clean via `tools/audit_container_padding.py`.**
- **`scrTaskEdit` is the SOLE task writer — `scrTask` was DELETED 2026-08-10.** A task row on
  `scrProject` now goes straight to the editor, like transactions and issues already did.
- **`scrTaskEdit` REWORKED 2026-08-07 — no tabs, containers.** Order: Task (name+description) ·
  Status (colour-coded combos) · People and dates (2 cols x 2 rows: Lead/Supporter | Start/Due) ·
  **Client** (toggle) · **Output requirements** (toggle). Project derived, never asked for;
  completion date derived from stage and NOT on the form at all. 116 controls.
- **`scrProjectEdit` is the container-layout REFERENCE.** Fixed footer = one auto-layout container
  + opaque backing rect on the identical anchor; fields = `row*` of `col*`; results galleries expand
  inline as siblings; 57 containers, 0 children with X/Y. Copy this shape, not the older
  absolute-overlay screens — the other three edit screens are still absolute.
- **Modern versions are PER-CONTROL:** TextInput/Combobox `@1.1.1`, DatePicker `@1.0.1`,
  Dropdown `@1.0.2`, Radio `@1.0.1`, DataGrid `@1.5.0`. **Unsure of one → use the CLASSIC
  control.** ModernButton/NumberInput/TabList/Slider are unknown, so they are not used.
- **Transfer:** screens paste via code view; `App.Formulas` goes through the formula bar (the App
  object has no code view); a component's custom properties are typed by hand. **Re-pasting a
  landed screen re-freezes X/Y/Width/Height and re-suffixes colliding names** — for a small edit,
  change the property in Studio directly.
- **Backend: LISTS ARE LIVE (2026-08-04).** `provisioned: live`; `asset_library` is defined
  minimally (`asset_name` only, by instruction). `schema/schema.yaml` is the golden source and
  carries a provisioning-verification block. Managed metadata is RETIRED (C11) — three live
  `mapping_*` lists (**`mapping_regions` plural**, the other two singular) whose columns are
  **exactly `level1 / level2 / level3`** — there is no `active` — read through `cmpNestedSelect`.
  **`asset_approval` RETIRED 2026-08-09** — approvals moved to an external portal; task now holds
  free-text `task_output_approval_id`. The list is commented out in the golden source and **must be
  deprovisioned in SharePoint** (pending).
  **`migration.blocking_now` is RESOLVED 2026-08-10** — the user retired every Managed Metadata
  column and replaced them with CHOICE columns, so `"Project_Type" is required` can no longer
  reject a save. **The replacement columns' names and values are NOT recorded**, so nothing in
  `src/` may target them yet — see `schema.yaml -> choice_replacement_UNRECORDED:` and Threads.
- **Every combo box is `ModernCombobox@1.1.1`** (10 of them) with `ItemDisplayText: =ThisItem.Value`.
  **Modern controls are ENABLED and all stay.** Versions are per-control and **NO TWO MATCH**
  (TextInput/Combobox `@1.1.1`, DatePicker/Radio `@1.0.1`, Dropdown `@1.0.2`, DataGrid `@1.5.0`) —
  **none is `@1.0.0`**. Do not pattern-match a version across the family.
- **Open gates:** Office 365 Users connection; arity of the other 8 Person and 6 Managed Metadata
  columns (three were provisioned multi and corrected).


## Decisions        (append-only; supersede, never delete)
- [2026-08-10] **All four edit screens are now container/grid-based with the shared `cmpPeoplePicker` modal — deployment complete.** (1) `scrTransactionEdit` was 100% ABSOLUTE → full one-pass rework to a `frmTxScroll` container scroll host + 4 `GridLayout` bands + fixed footer (34→47 controls; `selTxCurrency` wrapped in a col per the component-in-grid rule; overlays absolute). (2) People-picker deployed to the last two screens: `scrIssueEdit` Assignee+Owner → `cmpIssPeople` (−5 controls), `scrTransactionEdit` Sales → `cmpTxPeople` (−2) — routed by `gIssPicker`/`gTxPicker` + a `g*PeopleQuery` global, mirroring `cmpPrPeople`/`cmpTkPeople`; the per-field search boxes + result galleries removed; the Person-write shape (expanded-user + `ClaimPrefix`) is UNCHANGED. Every change ran the FULL validator: 24/24, 0 component-contract errors; all agent-added comments scrubbed. **PENDING STUDIO — whole-screen re-lands of scrProjectEdit, scrIssueEdit, scrTransactionEdit** (scrTaskEdit's grids the user already confirmed render). All need the Office 365 Users connection (unchanged) and a rename-check if a `cmp*People` instance lands suffixed. — src/Screens/scr{ProjectEdit,IssueEdit,TransactionEdit}.pa.yaml
- [2026-08-10] **Grid deployment across screens — scrProjectEdit −12, scrIssueEdit −4 landed; scrTransactionEdit reverted; a NEW paste rule learned.** After the People/dates 2×2 confirmed grid rows render equal-fraction, ran 3 parallel `powerapp-canvas-developer` agents doing the leaf-col-eliminating grid conversion on swap-free, always-visible caption+field bands (K cols × 10-row fine mesh; caption rows 1/3, field rows 4/10; grounded `LayoutGridColumns`/`LayoutGridRows` + `LayoutGrid{Column,Row}{Start,End}`). **scrProjectEdit 39→27 containers (−12)** — Details rows 1+2 + task/transaction modal metadata; kept the people-picker swaps, staged+linked galleries, footer. **scrIssueEdit 26→22 (−4)** — 5 bands→grid (see the rule below). **scrTransactionEdit REVERTED** — it's 100% ABSOLUTE (no nested scaffolding), so grid-optimizing only added 2 islands (+2, no reduction, inconsistent, whole-screen re-land for little gain); **an absolute screen needs a FULL absolute→container rework FIRST (like scrIssueEdit got) before grid pays off.** **NEW PASTE RULE (validator caught it, paste would have failed): a CanvasComponent instance CANNOT take `LayoutGrid*` props directly — wrap the component in a GroupContainer col and put `LayoutGrid*` on the COL** (proven via `gridTkPeopleDates`); only plain CONTROLS take `LayoutGrid*` directly. scrIssueEdit's 3 `cmpSelection` fields were re-wrapped (cost 3 containers back: −7→−4). **And: ALWAYS run the FULL validator (`python tools/validate_pa_yaml.py` no-args) after a grid conversion** — the single-file run skips the cross-file component-contract check that flagged the 12 errors. See tools/studio-enums.json ▸ `_gridContainerNotes.component_children_WARNING` — src/Screens/scrProjectEdit.pa.yaml, scrIssueEdit.pa.yaml
- [2026-08-10] **scrIssueEdit reworked from 100% ABSOLUTE → auto-layout containers, matching scrProjectEdit/scrTaskEdit.** `frmIssScroll` vertical scroll host over an opaque `formSheet` rect + 7 `row*`-of-`col*` bands (form-left / status-dates-right) + fixed footer (`actionBarRow` over `actionBarBg` on the identical `Y = Parent.Height - 88` anchor); header stays absolute; **the 5 overlay picker galleries STAY ABSOLUTE, declared last** (a container child can't overhang the row below). LAYOUT-ONLY re-home — every formula, OnVisible seed, guarded save Patch and control type UNCHANGED. Buys responsiveness + **paste-freeze immunity** (was ~140 frozen X/Y coords). Control count 56→85 (container scaffolding; a later GRID pass is what reduces it — that optimization is deferred). **NO grid authored — tokens still ungrounded.** Built via the powerapp-canvas-developer agent, which AGAIN added ~22 rationale comments despite the no-comment instruction — scrubbed in review (kept the author's, removed the agent's via a HEAD-diff of comment-only lines); **scrubbing agent-added comments is now a standing post-build step.** Studio: WHOLE-SCREEN re-land; no SharePoint change; the overlay gallery `Y` values are best-effort (a dropdown sitting offset is a one-property formula-bar nudge, no re-land). Follow-ups to FULL parity: `cmpPeoplePicker` for Assignee/Owner, `cmpSelection`→`ModernCombobox` for the Status/Type/Impact strips. — src/Screens/scrIssueEdit.pa.yaml
- [2026-08-10] **Grid-container opportunity assessed on the 3 edit screens (analysis only — nothing authored).** *(Its "tokens still ungrounded" is PARTLY SUPERSEDED the same day — the Control token, the Variant and the four CHILD placement tokens are now confirmed; only the CONTAINER's shape tokens remain missing. See the GridLayout entry below.)* Three read-only agents. Findings: scrTaskEdit 38 AutoLayout containers → grid saves ~10 (the People/dates band, 7 nested containers → 1 grid, is the single best change in the app; + the Status band); scrProjectEdit 39 → saves ~11–14 (Details rows 1+2 as one 3×4 grid, the 2-col modal metadata rows); scrIssueEdit is **100% absolute, 0 containers** → grid saves ~0 CONTROLS but erases ~120–140 frozen X/Y coordinates and buys responsiveness + **paste-freeze immunity** (a different, arguably higher, value than control-count). **THE RULE:** grid is safe on an always-visible static field-grid band; it BREAKS wherever the layout relies on (a) hidden-child-collapse — chip↔box same-slot swaps and Visible-toggled rows (tglTkOutput/tglTkClient), since a hidden grid CHILD still reserves its cell; (b) FillPortions flex — staged-gallery scroll balancing, label-grows action bars; (c) positional z-order overlays — the picker galleries that overhang the next row. Clean line: you CAN Visible-gate a whole per-band grid container (a hidden group collapses), you just can't put a toggled/swapped/overlaying child in a LIVE grid. **BLOCKER (all three):** grid pa-yaml tokens (Control/version/Variant/every property name) are ungrounded — authoring now = blind paste that fails whole. **Next action:** get one Studio code-view of a Grid container with 2 placed children → ground tokens → first conversion is scrTaskEdit's People/dates band. See tools/studio-enums.json ▸ _gridContainerNotes.
- [2026-08-10] **scrProjectEdit reworked — shared people picker + linked children on Edit.** (1) The three per-field people pickers (Manager/Requestor/Supporter: `txtPr*Search` boxes + inline `SearchUserV2` galleries `galPrManager/galPrRequestor/galPrSupporter`) were collapsed into ONE shared `cmpPeoplePicker` instance (`cmpPrPeople`, declared last, full-screen), routed by new globals `gPrPicker` / `gPrPeopleQuery` — mirrors scrTaskEdit's `cmpTkPeople` port; chips stay as screen controls. (2) On Edit (any existing project), the lower two staging columns now also load the project's linked tasks & transactions (`colLinkedTasks` / `colLinkedTx` via delegable `Filter(taskmaster_tasks, task_project_id.Id = gEditProject.ID)` etc.) and show them as CLICKABLE rows that open the child's edit screen (cockpit navigate-with-guard pattern); New-mode staging is unchanged, and only ONE gallery shows per mode so the fixed column heights don't fight. Built via the powerapp-canvas-developer agent from a full spec; validator 24/24. **Caveat:** opening a linked child `Navigate`s AWAY from scrProjectEdit, discarding unsaved project-form edits — inherent to the clickable choice (save first). **Studio hand-off: WHOLE-SCREEN re-land** (structural add/remove of controls), no SharePoint change; new globals create themselves at runtime; if `cmpPrPeople` lands suffixed on paste, rename it and recheck its `.Query`/`.Picked`/`Reset()` refs — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-09] **Output-approval de-lookuped: `task_output_approval` (Lookup→asset_approval) → `task_output_approval_id` (Text).** User decision: approvals now live in a SEPARATE portal (out of scope); the task just stores that approval task's id as free text. Reworked end to end: schema.yaml (column retyped; `asset_approval` list commented out as RETIRED + **must be deprovisioned in SharePoint**; C6 updated — region now modelled TWO ways, `approval_region` gone); scrTaskEdit (the people-picker-style approval control — `gTkApproval` global, `txtTkApprovalSearch`, `chipTkApproval`, and the `galTkApproval` results gallery over `asset_approval` — all REMOVED, replaced by a single `txtTkApprovalId` ModernTextInput seeded via Default; save writes `task_output_approval_id: Trim(txtTkApprovalId.Text)` still gated by `tglTkOutput`, clears on edit-with-section-off); context/schema.md (list table, relationship diagram, C6, C8 uniqueness note, index shortlist). Validator 1/1 valid; schema.yaml valid YAML; no residual refs to the old lookup/list. `incoming-lists.md` left as-is (provenance/historical). **NOTE the Studio hand-off:** scrTaskEdit must be re-landed (or the approval field edited per-property) AND the `task_output_approval_id` column must exist on the live `taskmaster_tasks` list (add Text column) — a paste that writes to a non-existent column fails. Log: sessions/2026-08-09-2305-claude-asset-audit-fixes.md
- [2026-08-09] **The three `*_project_id` lookups DISPLAY the numeric ID live, not the name.** `taskmaster_{tasks,transactions,issues}.*_project_id` were provisioned with their SharePoint display column = the target's numeric ID, so `task_project_id.Value` returns a number. `schema.yaml` still declares `show: project_name` (the fresh-provision IDEAL) — but **do NOT change the display column in place on the live lists: it unassociates existing items and breaks the `*_project_id` references** (user hit exactly this). Non-destructive fix = resolve the name app-side from the stored FK: `LookUp(taskmaster_projects, ID = <rec>.task_project_id.Id, project_name)` — one indexed, delegable read, and the name is only shown in single-record spots (no galleries) so cost is negligible. **Applied to all four project-referencing screens** — the OnVisible seed on scrTaskEdit (`gTkProject`), scrTransactionEdit (`gTxProject`), scrIssueEdit (`gIssProject`), and the `lblContext` header on scrTask. Also fixed same session: `scrTaskEdit.lblTkTargetCap` had `Color: =|` (should be `Color: |` — the stray `=` folded into the value on paste); and the `schema.yaml` tasks `join_cost` "cap is 12" note corrected to the >8-join block. **Studio hand-off pending:** 5 formula-bar edits (scrTaskEdit ×2, scrTask/scrTransactionEdit/scrIssueEdit ×1 each) — all per-property, no re-land, no SharePoint change — schema.yaml / src/Screens/scrTaskEdit.pa.yaml / scrTask.pa.yaml
- [2026-08-09] **`/code-review [max]` audited the `.claude/` knowledge assets themselves — 15 corrections landed across 13 files, none touching `src/`/`schema/` (nothing crosses the gap).** 8 finder agents by category, each grounded against source + MS Learn. Fixed: editable-table's two worked examples (used `.ThisItem` on `AllItems` rows — the alias IS the record — and a malformed `Patch(Source, ForAll(...))` wrapper); `studio-enums.json` keyed concrete `@1.0.0` on the four version-UNKNOWN controls (Button/NumberInput/TabList/Slider) → renamed `_modernX_TOKEN_UNKNOWN`; enums ModernCombobox self-contradiction (@1.0.0 vs @1.1.1); pre-paste-review's "confirm freshness" gate (contradicted the one-way gap → would block every paste) and its column check pointed at `context/schema.md` (no columns) not `schema/schema.yaml`; SharePoint "12-join wall" → **8** (MS Learn: blocked above 8 joins; 12 is only maximal-view return); Graph `$filter` needs `Prefer: HonorNonIndexedQueriesWarningMayFailRandomly` + `eq` is case-INsensitive; DAX `TOTALYTD` year-end is the **4th** arg; `=Blank()` delegates on simple columns only, not Person/Lookup/Choice; DAX running-total `ALL('Date'[Date])`→`ALL('Date')`; `pac canvas download -d` and `-f`+Expand-Archive are two ALTERNATIVES not a sequence; power-query-m multi-value Person = `[Table]` needs `Table.ExpandTableColumn`. **REJECTED one candidate after MS-Learn check:** the column-formatting `float` "no longer supported" note is CORRECT (verbatim in the formatting-syntax reference) — verifying negatives before editing prevented deleting a right note. **`validate_pa_yaml.py` `KNOWN_CONTROLS` never listed the four bogus tokens** so the enums file had drifted ahead of the paste-gating allow-list. Full log: sessions/2026-08-09-2305-claude-asset-audit-fixes.md
- [2026-08-05] **The validator now checks BRACKET BALANCE in every formula — I broke `lblPrMissing` exactly this way.** Removing three arms from a long `&`-chain with a regex took the closing `) },` with them, leaving `With( { m: Trim(` unclosed and a dangling `&`. Studio renders that as one red control with no indication of which bracket is missing, so across the gap it returns as *"its erroring"* and costs a round trip. The check strips `//` comments and string literals (handling `""` escapes) before counting `()[]{}`, so a `)` inside `"a ) stray"` and a balanced `RGBA(...)` both pass. **Lesson beyond the check: never delete arms from a multi-line Power Fx expression with a regex** — edit the whole expression as one unit so its closers stay attached — tools/validate_pa_yaml.py
- [2026-08-05] **REPO RESTRUCTURED TO PLAIN SOURCE — every paste-route artefact deleted.** By instruction: *"drop the paste route related dirs and focus just on housing the screens, components, formulas, and app items… no sort of trick method… just having the yaml correctly organized."* Layout is now `src/App.pa.yaml` + `src/Screens/` + `src/Components/`. **Deleted:** `studio/` (inert pulled/ scaffolding), `build/` (the `.msapp` packing experiment), `src/patches/` (folded into `src/App.pa.yaml` as a real `App: Properties: Formulas:` block), `components/bodies/` (derived from the component files — the contract/body split existed ONLY because a two-part paste needed it), `variants/` (skeleton scaffolding), `BUILD-SHEET.md`, `BUILD-BOOK.md`, `HANDOFF.md`, `tools/split_components.py`, `tools/change_sheet.py`. `paste-log.md` → `docs/build-history.md`, kept as a closed record because it holds every defect diagnosis. **Nothing authored was lost** — the bodies and the build sheet were generated FROM the component files, so those files were always the source of record. Validator retargeted at `src/**` and the list-rooted-fragment wrapper removed with the bodies — src/
- [2026-08-05] **CORRECTED: the paste route is NOT closed — but edits should still be per-property.** I read *"the paste route does not work"* as the channel closing and rewrote the records around that; the user clarified it is the documented HYBRID working as designed (screens paste, App.Formulas via the bar, component bodies paste with properties typed by hand). **The real change is the PHASE — the app is fully built and populated, so the work is editing, not creating.** For edits the unit is `<control>.<property> = <formula>`, generated by **`tools/change_sheet.py`** — `--since <git-ref>` emits only what changed, which is the sustainable refinement loop. The leading `=` is stripped: the formula bar renders it outside the editable area, so pasting it yields `==` and an error that reads like a fault in the formula. **This is the correct unit even if paste returns** — re-pasting a screen re-freezes X/Y/Width/Height into constants and re-suffixes colliding names, while a property edit touches only what it names. **Cost: adding a control is now expensive** (hand-insert + rename + apply properties), so prefer changes that re-use the existing control tree. **Not diagnosed** — clipboard permission, formula bar switched off (hides View code), or pasting into a container would each look like this and are all recoverable — tools/change_sheet.py
- [2026-08-05] **`asset_library` UNBLOCKED — bind `asset_name` only.** User: *"only the name is used as a lookup just assume asset_name"*. `schema/schema.yaml` now defines the list with that single column and `task_output_asset` binds `show: asset_name`; the list's other columns are out of scope because the app neither reads nor writes them. **`scrTaskEdit` still carries the 'not editable' placeholder label** and needs a real picker — that is now an ADD, so it is the expensive kind of change under the new channel — schema/schema.yaml
- [2026-08-05] **A responsive anchor and a hardcoded one in the same band is the floating-button bug.** `scrProjectEdit`'s action bar was `Y: =Parent.Height - 132` while its buttons were `Y: =706` — fine at 768 tall, mid-screen anywhere else, and the freeze rule guarantees they land inconsistently rather than drift. **Rule: a fixed footer is ONE auto-layout container plus an opaque backing rectangle sharing the identical anchor expression, and nothing inside it carries X/Y.** Applies to every screen with an action bar; scrProjectEdit is the reference implementation.
- [2026-08-05] **Ordering children for a grid inside a VERTICAL container silently destroys the form.** `frmPrScroll`'s children were sequenced `nameCap, phaseCap, nameInput, phaseCombo, …` — a two-column reading order in a one-column container, so it rendered caption/caption/field/field and no label belonged to anything. Nothing errors; it just reads as nonsense. **Rule: a multi-column row is a horizontal `row*` container of vertical `col*` containers, one caption + one control per column.** Never rely on flow order to pair a label with its field.
- [2026-08-05] **`LayoutAlignItems.Start` is no longer needed anywhere — size every container child to the full inner width instead.** With each direct child at `Parent.Width - 48` (or an explicit share of a row), the inferred `.Start` and the CONFIRMED `.Center` render identically, so `.Center` is used throughout `scrProjectEdit`. That removed the screen's last inferred layout token. Keep new children full-width and it stays true.
- [2026-08-05] **A staged-list gallery's Height must be a CONSTANT, gated by `Visible`, never `CountRows()`-driven.** A `CountRows(col…)` height freezes at paste, when every staging collection is empty, and clips the list permanently. `scrProjectEdit`'s three staged galleries are `Visible: =!IsEmpty(col…)` + `Height: =200`, with a one-line empty state in their place — replacing 1,160px of unconditional dead space.
- [2026-08-05] **A modal's picker gallery gated on the modal flag alone is always open and always covering its own chip.** All five in `scrProjectEdit` were `Visible: =gN*Open`, positioned at the identical X/Y as their selection chip and querying `SearchUserV2` with an empty search term — so a user never saw what they had picked. **Gate a results gallery on its own search state (`Len(search.Text) >= 2` and nothing yet chosen), never on the container's visibility.**
- [2026-08-05] **A `ModernDatePicker` with no `DefaultDate` on an edit screen writes `Blank()` over the stored date.** Found on `scrProjectEdit`'s `project_date_start` / `project_date_target`: the control opened empty on Edit and the Patch wrote `dtp.SelectedDate` unconditionally. Silent — no error, just a lost date, the exact quiet-wrong class the one-way gap cannot detect. **Every date picker on an edit screen needs `DefaultDate` seeded from the edit record AND a `Reset()` in `OnVisible`.** Audit the other three edit screens for the same shape.
- [2026-08-05] **Combobox `Items` are a schema surface — validate them against `schema.yaml`, not against memory.** `scrProjectEdit` offered "In Progress"/"Resolved" for `issue_status` (really [Open, Review, Waiting, Blocked, Closed]) and "Medium" for `issue_impact` (really [Low, Moderate, High, Critical]), and staged `i_impact` on every row without ever writing it. A hardcoded `Items` array drifts from the list exactly like an invented column name does, and the column-token validator does not look inside array literals. **Candidate for the write-time hook: check every `Items: =[...]` against the matching Choice column's `values:`.**
- [2026-08-05] **`Classic/Slider@2.1.0` + `Classic/Radio@2.3.0` GROUNDED; `ModernSlider@1.0.0` from MS Learn's YAML sample.** Outs: slider `.Value`, radio `.Selected.Value` (`SelectedText` deprecated). **Radio is the third control taking the dotted `Items.Value:` display column** — classic-family convention now, not a quirk; the modern family uses `ItemDisplayText`. **`Classic/` rule re-tested and held** on both. Traps: a slider's `ValueFill` is the FILLED side and `RailFill` the empty track (swap = reads backwards, no error), and **a slider can never be `Blank()`** so it cannot carry an unset optional column. **`ModernSlider` WAS REVISED** — input `Value`→`Default`, `Value` now read-only output, `Layout: ="Horizontal"`→`LayoutDirection: =LayoutDirection.Horizontal`; any online sample assigning `Value:` is stale. **Orientation enums differ per control with no rule**: `Layout.Vertical` (modern Radio), `LayoutDirection.Horizontal` (modern Slider), `LayoutDirection.Vertical` (GroupContainer). Modern radio group's TOKEN stays ungrounded — author `Classic/Radio` — tools/studio-enums.json
- [2026-08-05] **`Classic/Toggle@2.1.0`, `RichTextEditor@2.7.0`, `Rating@2.1.0` GROUNDED — and the `Classic/` prefix has a rule after all.** The prefix appears on exactly those names a modern Fluent control also uses (Icon, TextInput, Button, ComboBox, DropDown, Toggle) and is absent where none exists (Timer, ListBox, RichTextEditor, Rating, Gallery, Image, HtmlViewer, Label, Rectangle) — **predictive only**, supersedes the 2026-08-04 "no rule to infer, check the catalogue" line as guidance for *which form to ask for*, not as a licence to author unground tokens. Outputs: Toggle `.Value`, Rating `.Value` (**`0` when unrated, never `Blank()`** → guard with `> 0`), Rich text **`Default` in / `.HtmlText` out — the one control whose in and out names differ**, and it emits MARKUP, so its SharePoint column must be *Enhanced rich text* and its read side an `HtmlViewer`, never a Label. A toggle is two-state only, so a Yes/No column that must distinguish "unanswered" needs a dropdown. **The validator now audits that every allow-listed token also appears in `studio-enums.json` AND the controls skill** — grounding that isn't findable next session isn't grounded — tools/validate_pa_yaml.py
- [2026-08-04] **`DefaultSelectedItems` CONFIRMED — no inferred property remains in the repo.** Its own MS Learn page: *"the initial items selected… must be a TABLE OF RECORDS from the Items data source"*, so `=[gVar]` against an array-literal `Items` is correct. Flag removed from scrProjectEdit's header. Also grounded: **`Classic/DropDown@2.3.1`** (capital D) and **`ListBox@2.2.0`** — **no `Classic/` prefix**, same trap as `Timer`. **The four single-select controls all seed differently** — DropDown/ListBox `Default`, ModernDropdown `Default` (a value), ModernCombobox `DefaultSelectedItems` (a table) — and getting it wrong is SILENT: the control renders, it just never shows the current value. New syntax fact: a property key can be **dotted** (`Items.Value: =Value` names the display column) — tools/studio-enums.json
- [2026-08-04] **`Classic/ComboBox@2.4.0` grounded — and it corroborates `DefaultSelectedItems`.** A different control from `ModernCombobox@1.0.0` (Studio generates the classic one inside data cards; it adds `Chevron*` styling). Its card wiring is `Items: =Choices([@list].'col')` + `DefaultSelectedItems: =Parent.Default` + `SelectMultiple: =false`. **`DefaultSelectedItems` remains THE ONLY inferred property in the repo** — now confirmed on the classic control and listed for the Fluent family, but never seen on ModernCombobox itself, so it stays corroborated-not-proven. Also: Studio quotes a column inside `Choices()` as a single-quoted IDENTIFIER; unquoted snake_case is equally valid — tools/studio-enums.json
- [2026-08-04] **Edit form GROUNDED: `Form@2.4.4` (Variant: Classic, Layout: Vertical) + `TypedDataCard@1.0.7` (Variant: ClassicTextualEdit).** The card is the contract: `Default` in, `Update` out, children bound through `Parent.*` and tagged `MetadataKey: FieldName|FieldValue|ErrorMessage`. `Layout:`, `MetadataKey:` and `IsLocked:` are first-class pa-yaml keys — the schema already accepted them, only my allow-list didn't. `DataSourceInfo`'s column arg is a **single-quoted identifier** (`'Title'`). **Not adopting Forms for the edit screens**: they need a guarded Patch PER COLUMN (optional Choice, expanded-user, expanded-taxonomy) plus child staging before the parent exists, and one `SubmitForm` cannot express that — but a Form is right for a simple one-list CRUD screen — .claude/skills/powerapp-canvas-controls/SKILL.md
- [2026-08-04] **Four generic `powerapp-canvas-*` skills + a `powerapp-canvas-developer` agent** built from an MS instructions doc, expanded with everything this project paid for in failed pastes. Split by ownership: **controls** (tokens, variants, enums, OUTPUT properties), **development** (pa-yaml v3.0 + Power Fx rules), **design** (bands, collision arithmetic, z-order, paste-freezing, containers), **project-management** (records, provisioning, one-sentence diagnosis). New workflows: `screen-build`, `control-grounding`. **The source doc is vendored UNEDITED at `docs/reference/powerapps-canvas-yaml.instructions.md` with a correction table on top** — it is right about structure and Power Fx semantics, wrong about every control token (`Button` vs `Classic/Button@2.2.0`), so following it literally fails a paste — .claude/skills/powerapp-canvas-*
- [2026-08-04] **`ModernCombobox@1.0.0` + `ModernDatePicker@1.0.0` GROUNDED** (user pasted working Studio YAML). Reads are **`cbo.Selected.Value`** (SelectMultiple false → Selected is a record) and **`dtp.SelectedDate`** (Blank when unset) — both confirmed on MS Learn. `Items` takes an array literal: `=["Low", "High"]`. scrProjectEdit's 6 Choice strips and 4 typed-date pairs are now these controls; **every echo label is deleted because a date picker cannot produce an unparseable date**, and dates travel as real date values end to end (no `DateValue()` left on the screen). Only `DefaultSelectedItems` is inferred — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-04] **Auto-layout container GROUNDED: `GroupContainer@1.5.0`, `Variant: AutoLayout`** (photo of Studio code view). One control for both Horizontal and Vertical container; direction is `LayoutDirection`. Confirmed: `LayoutDirection`, `LayoutAlignItems`, `Padding*`, child `LayoutMinWidth`/`LayoutMinHeight`. **Children carry NO X/Y — so container-laid-out content is immune to Studio's paste-freezing of X/Y/Width/Height**, which makes it the only layout that survives this gap intact. **Second photo CONFIRMED the scroll switch**: `LayoutGap: =8`, `LayoutOverflowX/Y: =LayoutOverflow.Scroll`. Only `LayoutJustifyContent`, `LayoutWrap`, `FillPortions` still inferred. **A hidden child takes no space, so an inline results gallery expands/collapses the column — replacing the whole absolute-overlay picker pattern** — .claude/skills/studio-transfer/SKILL.md
- [2026-08-04] **Tab strips are plain buttons now — `cmpSelection` raised `OnChange` in the same pass as `Select(Parent)`.** Consumers read `<strip>.Selected.Id` inside that event, i.e. the gallery's selection is read in the behaviour pass that sets it — same race shape as cmpToast's `Reset()` vs `Start`. Tabs didn't switch. Replaced on 3 screens with `Classic/Button` + `Set(gTab, n)`: **the value travels in the control, so nothing has to propagate.** ⚠️ **The ~20 Choice strips still use cmpSelection and would lag by one click — invisible until save. If a Choice writes the PREVIOUS pick, this is why** — src/Screens/_EDIT-NOTES.md §1b
- [2026-08-04] **`Office365Users.SearchUser` (V1) matches the address only in practice** — the doc table claims display name / given name / surname, but users could not be found by name. All 8 people pickers now use **`SearchUserV2({…}).value`**, the form MS's own current example uses. **Note the `.value` unwrap — V2 returns a wrapper, V1 returns the table directly** — src/Screens/_EDIT-NOTES.md §3
- [2026-08-04] **ARITY applies to Managed Metadata too, not just Person.** `project_region` and `project_coverage` were provisioned multi-value and corrected to single — the app assumes single on both sides (`Coalesce(col.Value, …)` read, `LookUp(Choices(…), Path = …)` write). **`Choices()` is arity-independent**, so a multi MM column leaves the picker working and breaks only the current-value label and the save — quieter than the Person case, not safer. All 8 MM + 9 Person columns now carry explicit `multi: false`; 6 MM and 8 Person still unverified against the live lists — schema/schema.yaml
- [2026-08-04] **A Person column's ARITY is part of the provisioning contract.** `project_requestor` was provisioned *Allow multiple selections = Yes*, so the connector returns a TABLE: `Coalesce(col.DisplayName, "")` errors ("expecting a Table" — Coalesce types from its first argument) and the expanded-user WRITE would have failed too. The tell: `gPrManager` and `gPrSupporter` use the identical formula and were fine — **when one instance of a repeated formula fails, suspect the DATA, not the code**. Fix is in SharePoint (repo is golden source, says single). All 9 Person columns now carry explicit `multi: false` — schema/schema.yaml
- [2026-08-04] **LAYOUT FORMULAS FREEZE INTO CONSTANTS ON PASTE — first-party documented.** *"After you write formulas for the X, Y, Width and Height properties of a control, your formulas will be overwritten with constant values if you subsequently drag the control in the canvas editor"* (Create responsive layouts) — and a paste positions controls. So layout formulas land as **the value they evaluated to at the instant of the paste**. This is the TRUE cause of `galProjects` landing at Y=193, not an unresolved reference: the cross-control term was evaluated mid-paste and frozen. Consequences: **the landed app is not responsive**, Theme changes never propagate to layout, and a wrong position must be corrected in the FORMULA BAR because re-pasting re-freezes it. **Authoring rule: plain integers for X/Y/Width/Height wherever the value is static** — .claude/skills/studio-transfer/SKILL.md
- [2026-08-04] **Never position a control off another control's geometry across this gap.** Studio suffixes a colliding name on paste (`SearchBox` → `SearchBox_1`); an unresolved reference collapses the term and the control silently JUMPS — `scrProjects`' gallery was `Y: =SearchBox.Y + SearchBox.Height + Gap` and landed on top of the filter/search row. Fixed to pure Theme arithmetic; control renamed to the `txt*` house convention. Validator now NOTEs any Y/Height that references another control (X/Width anchoring to a backdrop card is left alone — small blast radius). **This failure is invisible from my side: the authored bands compute correctly, so only the render shows it** — src/Screens/scrProjects.pa.yaml
- [2026-08-04] **Overlay picker dropdowns need a one-at-a-time gate AND sideways placement.** Fields 66px apart with 132px result galleries meant two open dropdowns overlapped by half (later-declared wins, earlier one's rows unclickable) and an open dropdown covered the next two search boxes. Fixed across **21 pickers on 4 edit screens**: `OnChange: =Set(gXPicker, "<key>")` per search box + `gXPicker = "<key>"` in each gallery's Visible; gallery moved to `X = input.X + input.Width + 8` at the same Y. Also `cmpSelection` gained a **`FontSize` input (default 11)** — a component can't read `Theme.Size.*`, and a 7-option strip needs smaller text than a 3-option one. **Lesson: absolute-positioned overlays need their collision geometry checked at author time — I can't see the render, so field pitch vs overlay height is a calculation I must do** — src/Screens/_EDIT-NOTES.md §3
- [2026-08-04] **AN `.msapp` CONTAINS THE ENUM TABLES — unzip it before declaring anything ungroundable.** `References/Templates.json` holds them as pipe-delimited runs; `grep -oE '[A-Za-z0-9_]+(\|[A-Za-z0-9_]+){20,}'` recovered the **complete 180-value classic `Icon` enum** from the example export that has been in `build/` all along. It exposed three invented names (`Icon.Back`→`BackArrow`, `Icon.Documents`→`Document`, `Icon.Table`→`DetailList`) and showed that `Icon.Hamburger`, which I had declared ungroundable and worked around with three Rectangles, is perfectly valid. Committed as `tools/studio-enums.json`; validator rejects any icon off the list and suggests the nearest real name. **Lesson: 'MS Learn doesn't document it' is not the same as 'ungroundable' — the artefacts in the repo are evidence too** — .claude/skills/studio-transfer/SKILL.md
- [2026-08-04] **`IfError` requires ALL arguments to be type-compatible — the documented idiom does not compile.** MS Learn prints `IfError( Patch(…), Notify(…) )` and then notes that *currently* every argument's type must match, not just the ones that could be returned. `Patch` returns a record, `Notify` a boolean → Studio: *"expecting a record"*. **28 call sites across 5 screens** were wrong. Fix: make every arm a `Set`, stash `FirstError.Message` (only in scope inside the replacement) and Notify afterwards; accumulate optional-column failures into one `gXWarn` banner. Inside `ForAll` — where `Set` is unsafe, iteration order being unguaranteed — force the arms to text instead: `IfError( Text(Patch(…).ID), FirstError.Message, "" )`, empty meaning written. **Lesson: a construct straight out of MS Learn is still not grounded — the docs describe intent, Studio enforces today's implementation** — src/Screens/_EDIT-NOTES.md §5a
- [2026-08-04] **The dead-row gallery bug was NEVER fixed app-wide — only on the nav rail.** An audit found **24 more galleries** across 6 screens still relying on the gallery's own `OnSelect`: every person/lookup search dropdown on all four edit screens, `galProjects`, and the task/transaction/issue rows on `scrProject`. Every one would have rendered correctly and done nothing — the entire app's clickability. All now carry a transparent full-template `Classic/Button` declared LAST doing `Select(Parent); <action>`, and the validator now **rejects a gallery `OnSelect` outright**. **Lesson: when a bug class is confirmed, sweep the whole repo for it the same day — fixing only the instance that was reported leaves the rest to be discovered one painful round trip at a time** — tools/validate_pa_yaml.py
- [2026-08-04] **New cross-file lint: component-instance contracts.** Every property a `CanvasComponent` instance sets must be a declared custom property of that component or a built-in, else the paste fails on an unrecognised name. Currently clean across all 23 files. This is the one class of paste failure that was previously invisible until a human hit it — tools/validate_pa_yaml.py
- [2026-08-04] **A STALE STUDIO REPORTS WORKING CODE AS BROKEN — refresh before believing any failure.** The toast bug ("duration doesn't work", then "doesn't even appear") was Studio serving an old component definition; a browser refresh cleared it. **I rewrote `cmpToast` twice chasing a phantom.** Across a one-way gap a false negative is indistinguishable from a real failure, so the first question on any "it didn't work" is now *did you refresh Studio after the edit?* — before any diagnosis, and certainly before touching code. Recorded in the studio-transfer skill (principle 4 + watch-out 6) and BUILD-BOOK's golden rules — .claude/skills/studio-transfer/SKILL.md
- [2026-08-04] **The toast never dismissed — TWO sources of truth plus a Reset race.** `cmpToast` kept an internal `_show` while the screen's `gToastShow` gated the instance's `Visible`, and its `Show()` action did `Set(_show, true); Reset(tmrToast)` — resetting the timer in the same behaviour pass that sets the variable its `Start` reads. Rebuilt around ONE flag: `IsOpen` Input, timer `Start: =IsOpen` / `Reset: =!IsOpen` (documented properties, canonical restart idiom), no action, no internal variable. **General rule: when a component and its consumer both track the same state, the component should take it as an INPUT — never mirror it.** Ruled out: hidden timers are fine (MS Learn endorses `Visible: false` for background timers) and timers only run in Preview — src/Components/cmpToast.pa.yaml
- [2026-08-04] **Header + nav merged into `cmpAppBar`, a FLY-OUT rail** (user asked for a pop-out nav). Closed it is a 64px header with a hamburger; open, the rail overlays the content behind a scrim that closes it. This deletes `cmpNavMenu` AND the `HeaderBar`/`AppTitle`/`ScreenTitle` trio from 8 screens, and frees the permanent 240px nav gutter — content now starts at `Theme.Space.Gutter` and runs full width. **The instance `Height: =If(gNavOpen, Parent.Height, Theme.Space.HeaderH)` is the whole design**: a component swallows every click in its bounds (the scrHome lesson), so full-screen is only legal while open — and then the scrim WANTS the click. Validator's full-screen-component lint now accepts a conditional Height as a gate. Still cannot Navigate, so the screen keeps the `Switch` on `SelectedKey` and adds `Set(gNavOpen, false)` — src/Components/cmpAppBar.pa.yaml
- [2026-08-04] **The hamburger is three Rectangles, not `Icon.Hamburger`.** No first-party source enumerates the classic `Icon` enum — MS Learn documents the property and never lists its values — and one unknown token fails the ENTIRE body paste, costing a round trip measured in hours across a one-way gap. Drawing it uses only tokens already proven here and cannot be rejected. Applies generally: **when an enum can't be grounded, draw the thing** — src/Components/cmpAppBar.pa.yaml
- [2026-08-03] **RETRACTED: "a screen can't live in a table" was WRONG** (user-corrected). `Screen: scrHome` in a NavMenu record is legal; their *"expecting a table"* was an error in how the definition had been typed. I rewrote 8 files on a bad inference — second time today I generalised a rule from one failure. The Key-based NavMenu is KEPT, for a real reason instead: **nav is now the `cmpNavMenu` component, and a component cannot Navigate (no access to app screens)**, so it raises `OnNavigate` + exposes `SelectedKey` and the SCREEN does the Navigate — docs/build-history.md
- [2026-08-03] **A gallery's `OnSelect` does NOT reliably fire when the click lands on a child control** — that is why nav items looked right and did nothing. Fix: a transparent full-template `Classic/Button` per row, declared LAST so positional z-order puts it on top, doing `Select(Parent); <event>()`. Applies to ANY gallery row meant to be clickable — src/Components/cmpNavMenu.pa.yaml
- [2026-08-03] **TWO runtime bugs found in preview.** (1) **A full-screen CanvasComponent instance with no `Visible` swallows EVERY click** — `cmpConfirmHome` (Parent.Width x Parent.Height, declared last) made all of scrHome dead: nav, buttons, everything. Transparent fill is irrelevant, it still hit-tests; `IsOpen` only governs what's drawn INSIDE. Gate the INSTANCE. Toast had it too (corner only). Validator lints the shape. (2) **A screen cannot live in a table** — `NavMenu` held `Screen: scrHome` per record; a screen is a control reference, not a value, so the named formula failed to type and every gallery's `Items: =NavMenu` errored *"expecting a table"*. Now a numeric `Key` + `Switch` per gallery; active-item highlight compares `ThisItem.Key` to the screen's own key instead of `App.ActiveScreen`. 8 files — src/Screens/_SHELL-NOTES.md
- [2026-08-03] **The black cmpTermPicker was the invalid gallery `Variant`, NOT the zero-alpha `Fill`** — it loads correctly once the variant is real. **Zero-alpha transparency works**, so the earlier `RGBA(0,0,0,0)` theory is retracted and the 8 components still using it are fine. Galleries + row buttons reverted to transparent; only the component-level Fill stays opaque white, as panel styling rather than repair. Lesson: I blamed the nearest suspicious-looking property when the fault was a token three lines above — confirm tokens first — src/Components/cmpTermPicker.pa.yaml
- [2026-08-03] **Gallery `Variant` is `BrowseLayout_<Orientation>_<Template>_ver5.0`, NOT `Vertical`/`Horizontal`.** `BrowseLayout_Vertical_TwoTextOneImageVariant_ver5.0` read straight off Studio's code view (photo); horizontal + OneText + Flexible/SocialFeed corroborated from published .pa.yaml. **`Variant: Vertical` was a guess carried since Phase 1 and was never valid** — replaced across 49 galleries in 20 files. Probably the real cause of cmpTermPicker's BLACK render (no template to style from), so the zero-alpha-fill theory was at best secondary; the explicit surfaces are kept as good design. The template part only picks the DEFAULT child layout, which pasted children replace, so any vertical variant works. **Lesson: a photo of code view carries far more than works/doesn't — ask for one whenever a token is in doubt** — tools/validate_pa_yaml.py
- [2026-08-03] **A control's `Items` is WRITE-ONLY** — `CountRows(gal.Items)` is invalid; the readable form is `AllItems`. But `Self.AllItems` in a `Visible` is a LATCH: a hidden gallery's rows aren't realised, so once false it never recovers. cmpTermPicker's level visibility now asks the SOURCE DATA (`does any term start with parentPath & delim`) — a tree has no gaps, so 'something deeper exists' implies 'something at the next level exists', needing no depth arithmetic and no sentinel compensation (`> 0`, not `> 1`). Component custom props NAMED `Items` are unaffected and the lint distinguishes them — src/Components/cmpTermPicker.pa.yaml
- [2026-08-03] **The Timer control is `Timer`, NOT `Classic/Timer`** (user-confirmed in Studio). cmpToast's authored token had the wrong class. Written without a version suffix — optional per the schema, and Studio uses the current version, which beats inventing an uncheckable number. **This was the LAST unverified token: every control token in the kit is now confirmed and the validator emits zero token warnings.** Full confirmed set: `Label@2.5.1`, `Rectangle@2.3.0`, `Classic/Icon@2.5.0`, `Classic/TextInput@2.3.2`, `Classic/Button@2.2.0`, `Gallery@2.15.0`, `Image@2.2.3`, `HtmlViewer@2.1.0`, `Timer`, `CanvasComponent` — tools/validate_pa_yaml.py
- [2026-08-03] **`RGBA(0, 0, 0, 0)` can render as opaque BLACK, not transparent.** cmpTermPicker showed a black fill. All 11 components use that value, but it only SHOWS where the body doesn't cover the component — cmpSelection's single gallery is Parent.Width/Height so it never surfaced. Fixed cmpTermPicker by removing every dependence on alpha (component + 4 galleries explicit white; row buttons mirror TemplateFill). **8 components still rely on a zero-alpha fill** and are listed with their intended backdrop in `_COMPONENTS-NOTES.md`; components can't read Theme, so any replacement must be a literal — src/Components/
- [2026-08-03] **Column names are IDENTIFIERS, not strings — Power Apps 3.24042 (Apr 2024).** `Ungroup(t, "v")` errors *"expecting an identifier name"*; must be `Ungroup(t, v)`. Applies to Add/Drop/Rename/ShowColumns, GroupBy/Ungroup, Search, DataSourceInfo. **Existing apps were auto-migrated — we author from scratch, so nothing migrates us, and most online examples predate the change.** Fixed 13 call sites repo-wide. Validator checks it PER ARGUMENT POSITION (AddColumns' even args are formulas, Search's 2nd is the search text) — a window-based first attempt false-positived on `Set(g, "Edit")` nearby — tools/validate_pa_yaml.py
- [2026-08-03] **A component Output CAN reference its own child controls, conditionally or otherwise — the earlier "bare reference only" rule was WRONG and is retracted.** Root cause of the error: the property had been created as an **Input**. An Input's `Default` evaluates in the CONSUMER's scope and cannot see the component's children; an Output's formula evaluates INSIDE and can. Original formulas restored (`cmpSelection.Selected` conditional; `cmpTermPicker` back to 9 controls, no `lblPickLabel`/`lblPickDone`), and the validator lint removed. **Lesson: when a property errors on a child reference, check its KIND before touching the formula** — docs/build-history.md
- [2026-08-03] **`HtmlViewer@2.1.0` CONFIRMED** — cmpStatusPill's body pasted with it; the only failure was the HtmlText VALUE (four `IsMatch` patterns carrying their own `^`/`$` while IsMatch already defaults to MatchOptions.Complete → double-anchored). Rewritten as `in` membership over a word list — `in` on a single-column table is membership AND case-insensitive by default, so `(?i)` was never needed. Also confirmed en route: `(?i)` inline flags ARE supported ([imsx] at pattern start), and `EncodeHTML` IS a real canvas function (v24034). **`Classic/Timer@2.1.0` is now the ONLY unverified token in the kit** — docs/build-history.md
- [2026-07-26] Project is parent; Task/Ticket/Issue are peers (not variants) — why: distinct kinds — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-07-26] Three lists, not one discriminated list — why: a discriminator nulls ⅔ of columns + misleads Owner/PercentComplete — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-07-26] Only Tasks roll up into completion; Tickets/Issues surface alongside — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-07-26] No snapshot/metrics list — why: existed only for Sum-delegation; Power BI imports whole; do NOT reintroduce — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-07-26] Products/indices are stubs; ISIN is the egress contract, sync one-way in — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-07-26] Notional is the only value column (bias: favours large low-margin trades = issuance volume) — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-07-26] Not a book of record — internal KPI only, no retention obligation — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-07-26] Q1 → FULL ticket-level rows as primary store — cost: max delegation/threshold exposure, indexing mandatory — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-07-26] Q2 → NOT everyone Power BI-licensed → native nav primary + empty state; dashboard can't carry nav — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-07-26] Q11 → MANUAL (SharePoint UI) provisioning — cost: _x0020_ name risk + no term-store sync route — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-07-26] Adopt list approved "as recommended"; column-token validator hook proposed not built — sessions/2026-07-26-1726-repo-init-decisions.md
- [2026-08-02] PM-tracker template = blueprint not plumbing (SQL-backed; reuse screens/nav, NOT its data layer/SQL-view aggregation) — sessions/2026-08-02-1702-external-repo-review.md
- [2026-08-02] Authored dual-use skills `power-apps-svg` + `power-apps-editable-table` (SVG grounded on MS Learn data-URI/EncodeUrl) — sessions/2026-08-02-1702-external-repo-review.md
- [2026-08-02] Q11-bis: RECOMMEND flow-as-list-provisioner over manual UI IF Power Automate available (Q12) — repeatable, kills _x0020_ risk; not yet chosen — .claude/context/open-questions.md
- [2026-08-02] Phase-1 nav built as per-screen gallery bound to `NavMenu`, NOT a reusable component — why: components are hardest to paste across the air gap; NavMenu already gives T6's DRY intent. Component upgrade deferred until channel proven — sessions/2026-08-02-1411-phase1-core-shell.md
- [2026-08-02] Paste/code-view dialect = modern STRUCTURED schema (Control:Type@version, positional z-order, no ZIndex); `pac canvas unpack`'s inline `As type:` is the RETIRED format — do not author to it — sessions/2026-08-02-1411-phase1-core-shell.md
- [2026-08-02] Components authored in v3.0 `.pa.yaml` `ComponentDefinitions`; display pills/chips delivered as `cmpUiKit` OutputFunction HTML-builders (canvas components CAN'T sit in a gallery), interactive chip stays a component; components inline Theme (can't read app globals) — sessions/2026-08-02-1411-phase1-core-shell.md
- [2026-08-02] **Air gap is ONE-WAY** (user-corrected): repo→Studio only; sole return signal is binary "works/doesn't." Kills pull/round-trip/pull-reconcile; repo is authoritative source not mirror. → resolve unknown tokens from PUBLIC sources; ship safe fallbacks; maximise first-try paste success — sessions/2026-08-02-1411-phase1-core-shell.md
- [2026-08-02] SCHEMA: snake_case `taskmaster_*`/`asset_approval` is canonical, supersedes the PascalCase tm* model (user-confirmed) — schema/incoming-lists.md
- [2026-08-02] Schema promoted to canonical (7 lists, snake_case). Model uses Lookup + Managed Metadata + multi-Person throughout — accepted as the user's design; consequences documented rather than relitigated — .claude/context/schema.md
- [2026-08-02] task_status split into TWO Choice cols: `task_status`=health (Green/Amber/Red) + `task_stage`=lifecycle (values TBD); `task_output_format`+`task_client_stage` MM→Choice — why: Choice costs no join & sorts; tasks 11→8 joins, J1 resolved — .claude/context/schema.md
- [2026-08-02] **`schema/schema.yaml` is the GOLDEN SOURCE** — repo DEFINES the SharePoint lists, SharePoint applies them (`provisioned:` per list). Inverts the old 'capture true names from SP' model; mirrors the one-way gap. context/schema.md keeps shape+costs only, never columns (DRY) — schema/README.md
- [2026-08-02] `task_stage` values fixed: Not Started, Planning, Drafting, Under Review, Finalizing, Complete, Archived — unblocks the delegable Or-of-equals open-task filter — schema/schema.yaml
- [2026-08-03] Schema recs C4/C5/C8 APPLIED: `task_date_start` Calculated→DateTime(indexed); added `transaction_notional_usd` (only cross-currency-safe column); renamed `Issue_owner`→`issue_owner`(Person), `product_UID`→`product_uid`. schema.yaml v1.2.0 — schema/schema.yaml
- [2026-08-03] C1 RESOLVED: multi-person → single `task_supporter`/`project_supporter` (1 extra person, indexed). 'Mine' now fully delegable via `lead.Email = me || supporter.Email = me`; join cost unchanged (tasks 8) — schema/schema.yaml
- [2026-08-03] C3 RESOLVED: `project_perc_completion` = weighted mean of child task-stage weights (0/10/35/60/85/100, Archived excluded), WRITER = **app-side** — app patches parent on task-stage change. `StageWeights` named formula + write-back snippet in `src/App.pa.yaml`. Cost: stale if SP edited directly — schema/schema.yaml
- [2026-08-03] C6 BY DESIGN: three region columns are intentional — approval_region is broad-stroke, project/client_region are granular, never used together. No conformed dimension; separate PBI dimensions; do NOT 'fix' — .claude/context/schema.md
- [2026-08-03] Phase-2 DATA bound on scrHome/scrProjects/scrReports: one delegable Filter per screen → local aggregation (CountRows/Average never delegate). galProjects uses If-of-independent-Filters so each branch folds. Reports rings deliberately SCOPED (my tasks / active projects) — org-wide counts can't be exact in-app. Trends zeroed, not faked — src/Screens/
- [2026-08-03] Pre-paste audit → DO-NOT-PASTE; 6 real defects fixed: `Sort(If(..))` does NOT fold (Sort moved INSIDE each branch); unscoped colActiveProjects → scoped to my projects; redundant+risky IsBlank search guard dropped; Navigate-to-self doesn't re-fire OnVisible (refresh now inlines the collects); PASTE ORDER now App.Formulas BEFORE data screens (they need StageWeights); cmpConfirmDialog custom prop `Visible` collided with the base prop → renamed `IsOpen` — src/Screens/
- [2026-08-03] C9 raised: task_stage/task_status/issue_status are OPTIONAL → blank rows drop out of every enumerated Choice filter (silent undercount). Recommend required-with-default. UNDECIDED — schema/schema.yaml
- [2026-08-03] C9 APPLIED: task_stage/task_status/issue_status now REQUIRED with defaults (Not Started/Green/Open) — no blank-value gap; green%+amber-red% now sum to 100 — schema/schema.yaml v1.6.0
- [2026-08-03] Added `HANDOFF.md` — the one-way delivery runbook (repo→work machine→Studio). Stage 1 is a SMOKE TEST using scrReference/scrAdmin (only units needing no components, no App.Formulas, no data sources) to isolate the channel before anything else. **Claude maintains `docs/build-history.md`** — the human cannot write to this repo, so entries come from their chat reports — HANDOFF.md
- [2026-08-03] Component paste FAILED → root-caused against Microsoft's OFFICIAL pa-yaml v3.0 schema (fetched, vendored `tools/pa.schema.v3.0.yaml`): `Parameters` must be a SEQUENCE (was `{}`), and Output/OutputFunction/Action may NOT carry `Default` (formula belongs in the component's `Properties:` map). Built `tools/validate_pa_yaml.py` — run before EVERY hand-off; 17/17 now valid — docs/build-history.md
- [2026-08-03] scrAdmin LANDED — screen dialect + grounded control tokens CONFIRMED. Unstyled theme is expected (App.Formulas not yet pasted) — docs/build-history.md
- [2026-08-03] Kanban DEMOTED to a swap-in variant; scrProject ships a flat stage-grouped Vertical gallery. Why: `Variant: Horizontal` is ungrounded and sat mid-file gating the whole Tasks tab — under a one-way gap that risks the entire screen for a nicer layout. Kanban preserved at `src/Screens/` with swap instructions — src/Screens/scrProject.pa.yaml
- [2026-08-03] 2nd audit fixed: `IsError(Errors(...))` NEVER gates (Errors returns a TABLE → use IsEmpty); success Notify+Back() were unconditional (failed save said 'Saved' and navigated away); edit state now seeded into GLOBALS in OnVisible rather than trusting cmpSelection's internal selection (Reset can't reach inside a component instance → silent wrong write); rollup now REFUSES to write past the row limit rather than persisting a truncated average — src/Screens/scrTask.pa.yaml
- [2026-08-03] C10 — MM STAYS (user decision). Cascading term picker VALIDATED: Graph termStore `children` endpoints (GA Aug-2021) walk the hierarchy and yield term GUIDs → nesting detection falls out of the API; `TermStore.Read.All` is **DELEGATED ONLY** (app-only unsupported). The old 'Graph can't do MM' note is narrower than it read — it's the list-column VALUE, not the term store. WRITE via SharePoint connector `SPListExpandedTaxonomy` + `WssId:-1` is COMMUNITY-confirmed only = riskiest construct in the app. Recommend caching terms into a flat `taskmaster_terms` list so the cascade is delegable Filters with ZERO runtime dependency — docs/managed-metadata-picker.md
- [2026-08-03] Q12 (Power Automate / custom connector) is now **BLOCKING** — two required MM columns mean no project can be created from the app without a term source — .claude/context/open-questions.md
- [2026-08-03] C10 ARCHITECTURE DECIDED = **cache, not live Graph**. `taskmaster_terms` is now a real list in the golden source (flat, parent-pointer); the cascade is delegable `Filter`s on indexed `term_parent_guid`. Deciding argument was NOT speed — a flow call per dropdown level makes every create form depend on a flow being healthy at RUNTIME, the one thing this gap can't debug. Cache degrades to a stale vocabulary instead. **Narrows Q12 to population only** — the app pastes with no flow at all — schema/schema.yaml v1.8.0
- [2026-08-03] `cmpTermPicker` authored — 4 progressively-revealed vertical galleries. TWO load-bearing decisions: (1) a `"— select —"` SENTINEL row per level, because a gallery's `Selected` returns its first row until touched and would otherwise auto-pick a path the user never chose into a REQUIRED column (Coalesce treats `""` as blank, hence the empty-string guid); (2) CHAIN VALIDATION — a level's value counts only if that row is really a child of the level above, so a stale deeper pick after re-picking level 1 is discarded rather than written. Resolution runs once on a hidden `lblPick` label; all 4 outputs read it (a custom prop referencing another custom prop of the same component is unverified). `IsComplete` COUNTS children rather than trusting cached `term_is_leaf` — src/Components/cmpTermPicker.pa.yaml
- [2026-08-03] Four CRUD screens authored (`scrProjectEdit`/`scrTaskEdit`/`scrTransactionEdit`/`scrIssueEdit`) — shared pattern in `src/Screens/_EDIT-NOTES.md`: seed globals in OnVisible + write from them; normalised picker records ({DisplayName,Mail} / {Id,Value}, empty-record null state, never IsBlank on a Blank()-inferred type); overlay galleries declared LAST (positional z-order = dropdown behaviour without an ungrounded ComboBox); dates typed + `DateValue()` + echo label; optional fields as separate GUARDED patches (Power Fx can't conditionally omit a record field, and `{Value:""}` isn't legal) — src/Screens/_EDIT-NOTES.md
- [2026-08-03] SAVE-GATE pattern corrected repo-wide: `FirstError` exists ONLY inside `IfError`'s fallback, so the fallback stashes `FirstError.Message` into `g*Err` and success sits behind `Len(g*Err) = 0`. Every `IfError` argument is a SINGLE statement — no `;` chains inside a function argument (unverified across the gap). Supersedes the `IsBlank(gSaved)` gate — src/Screens/
- [2026-08-03] `scrProjectEdit` stages children locally (required Lookup needs the parent ID, which only exists post-insert) then writes them. On partial failure the parent is ALREADY saved and can't be rolled back → successes leave staging, failures stay + are listed, and the screen FLIPS to Edit mode against the project it just created. That flip is load-bearing: Save again retries only what failed and CANNOT create a second project. `IfError(value, fallback, default)` classifies each row in one pass — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-03] People pickers use `Office365Users.SearchUser` (first-party; returns DisplayName/Mail). Cost = a Studio PREREQUISITE — the Office 365 Users connection must be added BEFORE the edit screens are pasted, or the paste fails on an unrecognised name. Now a numbered HANDOFF step — HANDOFF.md
- [2026-08-03] C5 write landed on the transaction form: `transaction_notional_usd` is written in the SAME statement as the native notional, from a STATIC `FxToUsd` table in App.Formulas, with the USD figure echoed live. Rates are placeholders and will go stale → raised as **Q14**. Form REFUSES to save a notional whose currency has no rate rather than quietly using 1 — src/App.pa.yaml
- [2026-08-03] **Q12 ANSWERED = YES, Power Automate is available.** Unblocks three things that were queued behind it: the scheduled Graph-termStore flow that populates `taskmaster_terms` (C10), the flow-as-list-provisioner route (Q11-bis), and the extract flow (Q7). NOTE the app needs NO flow at runtime — that was the whole point of the C10 cache decision, and it still holds — .claude/context/open-questions.md
- [2026-08-03] **Q11-bis ADOPTED — flow-as-provisioner supersedes the manual-UI Q11 pick.** Its recommendation was explicitly conditional on Q12, which is now yes. Internal names set explicitly at creation kills the `_x0020_` risk, and it's re-runnable dev→test→prod (helps Q13). Manual UI stays the fallback. TO AUTHOR: the provisioning flow for 9 lists — .claude/context/open-questions.md
- [2026-08-03] **Q14 ANSWERED — C5 REVERSED. No FX in the app at all.** `transaction_notional_usd` is dropped (commented out in the golden source, DO NOT PROVISION), `FxToUsd` removed from App.Formulas, and the transaction form writes only the native notional + currency. Why: a write-time rate FREEZES whatever number the app held on the trade date and nothing downstream can correct it; report-time conversion can be restated, back-dated and audited. **CONSEQUENCE — no cross-currency figure can be shown ANYWHERE in the app**; scrProject's transactions tab now totals PER CURRENCY (five enumerated Sums, not Distinct/Concat — no novel scope-shadowing construct mid-file under a one-way gap) and the old USD column shows the product instead of a permanently blank number. **Power BI now OWES an FX dimension + a trade-date conversion measure** — schema/schema.yaml v1.9.0
- [2026-08-03] **CORRECTION — `Variant: Vertical` is NOT proven, and the validator had a hole.** User reports the `scrAdmin` they pasted carried `Variant: CONFIRM_BlankVertical` — a placeholder introduced in `2d7fad7` and only resolved to `Vertical` in `5a7e684`, so **they pasted a STALE copy**. Consequences: (1) scrAdmin's landing proves a Gallery *control* pastes and proves NOTHING about legal `Variant` names — my "four proven tokens" claim overstated it; (2) the official pa-yaml schema declares `Variant` as merely `type: string, minLength: 1` and `Control:` as `true`, so **both are unconstrained** and our validator passed the placeholder happily. FIXED: `validate_pa_yaml.py` now runs a **second pass** — a control-token allow-list (grounded vs unverified), a variant allow-list, and a CONFIRM_/TODO_/TBD_ placeholder scan. Verified it catches the original bug — tools/validate_pa_yaml.py
- [2026-08-03] **The work machine may be pasting STALE files.** The scrAdmin placeholder was fixed in the repo months of commits ago; it still reached Studio. Always re-copy from current `main` before a paste — docs/build-history.md
- [2026-08-03] **COMPONENT PASTE REGRESSED — blocker.** Whole-definition paste stopped working after earlier successes; screens don't load without components. Working theory (unconfirmed, no error text yet): a component is a **CONTRACT** (custom properties — *no paste gesture exists*, they're typed into the property pane) plus a **BODY** (child controls — ordinary dialect, pastes like a screen). A whole `ComponentDefinitions:` paste asks one channel to carry both. Response: `tools/split_components.py` generates `BUILD-SHEET.md` (contract, in creation order) + `bodies/*.children.pa.yaml` (comment-free control sequence). Add properties BEFORE pasting the body — controls reference them by name — src/Components/BUILD-SHEET.md
- [2026-08-03] **Only FOUR control tokens are actually PROVEN** — the ones in `scrAdmin`, the sole confirmed crossing: `Label@2.5.1`, `Rectangle@2.3.0`, `Classic/Icon@2.5.0`, `Gallery@2.15.0` (Vertical). `Classic/Button@2.2.0` and `Classic/TextInput@2.3.2` are NOT proven despite being used everywhere. Acted on the worst case: `cmpSelection` moved off `Variant: Horizontal` (self-flagged unconfirmed, instantiated by 7 screens) to **Vertical + `WrapCount = CountRows(Items)`**, which lays all items across one row — a horizontal strip from the only variant that has landed — src/Components/cmpSelection.pa.yaml
- [2026-08-03] **.msapp PACKAGING IS A DEAD END — settled, do not re-litigate.** Three independent reasons: (1) `pac canvas pack`/`unpack` are **DEPRECATED** by Microsoft (use Git integration instead), and PASopa carries an explicit "don't use in production" warning from its own maintainers; (2) the decisive one — **`.pa.yaml` files are READ-ONLY representations and are NOT used when an app loads** (MS Learn, power-apps-yaml). An app loads from the JSON control tree inside the .msapp, so dropping our authored YAML into `\Src` would produce an app that opens EMPTY. Building a real .msapp means hand-authoring CanvasManifest/Header/Properties JSON + Entropy + a computed Checksum.json — reverse-engineering with a high chance of a file Studio simply refuses to open, which under a one-way gap is the worst outcome (no diagnostic); (3) data-source bindings need real site URL + list GUIDs that don't exist until provisioning. **The code-view paste channel is PROVEN** (scrAdmin + components landed) — do not trade it for this — docs/msapp-and-git-integration.md
- [2026-08-03] **The one route that could retire the clipboard: Power Platform GIT INTEGRATION.** Studio commits/pulls canvas source as `.pa.yaml`, and *minor* edits made directly in the repo ARE restored on pull. BUT: it is **Azure DevOps**, not GitHub (the GitHub canvas integration was experimental and is RETIRED), it works on Dataverse **solutions**, and the flow is publish→commit. Worth investigating on the work machine; cannot be settled from this side — .claude/context/open-questions.md
- [2026-08-03] **CORRECTION (user-reported from Studio): components ARE code-view-pasteable, and several LANDED.** The repo had asserted the opposite since Phase 1 — that canvas components must be rebuilt by hand in the component editor and that their control tokens were therefore "documentation, not a paste payload". Both wrong. Consequence: component tokens are held to the same standard as screen tokens — `HtmlViewer@2.1.0` and `Classic/Timer@2.1.0` are now real paste risks and the first suspects if `cmpStatusPill`/`cmpChoicePill`/`cmpToast` are rejected — docs/build-history.md
- [2026-08-03] **C10 REVISED SAME DAY — the `taskmaster_terms` cache list is DELETED.** User challenged the double-store; they were right. `Choices([@list].mmColumn)` returns the term set from the term store with **Label, Path, Guid, WssId**, and **`Path` is the FULL hierarchical path** (`EMEA;UK;London`) — so the hierarchy is already in the data and the cascade is prefix matching (`StartsWith(childPath, parentPath & ";")`). No mirror, no refresh flow, no drift; term store stays the single source of truth. My delegation argument for the cache didn't survive either: a term set small enough to use is small enough to hold in memory — schema/schema.yaml v2.0.0
- [2026-08-03] **MM WRITE now hands the connector its OWN record back:** `LookUp(Choices([@list].col), Path = <picked path>)`. This RETIRES the hand-built `SPListExpandedTaxonomy` + `WssId:-1` literal that was the least-proven construct in the app, and sidesteps the live `Guid` vs `TermGuid` field-name ambiguity since nothing we author names it. Person is now the ONLY hand-built complex shape left (no `Choices()` exists for Person) — docs/managed-metadata-picker.md
- [2026-08-03] **The one real MM limit: `Choices()` on an MM column is capped at 20 TERMS** by the connector, not configurable (multiple independent sources + an open MS Ideas request). If a set outgrows it, swap ONE binding for a flow-fed collection in the same `{Label, Path}` shape — component unchanged, and it's an in-memory collection, still not a second store. **`Path` delimiter (`;`) is NOT first-party documented** → it's a `PathDelimiter` component input and the picker prints a raw path on screen, so first paste settles it — src/Components/cmpTermPicker.pa.yaml
- [2026-08-03] **Screens are HARD-COUPLED to components (a CanvasComponent instance = a paste-time dependency), so only the 2 shells were usable without the component build.** Broke the coupling with **navigable skeletons**: `src/Screens/scr{Home,Reports,Projects}.skeleton.pa.yaml` — component-free + data-free (header + nav + titled placeholder, same shell as scrReference/scrAdmin), needing only App.Formulas. Paste them in Phase A for a fully navigable 5-screen app before ANY component or list exists, then replace with the full `scr*.pa.yaml` once components are built + lists provisioned. Documented as step **A1b** in BUILD-BOOK.md. 3/3 validate. (Chose skeletons over inlining the display components; cmpSelection/cmpTermPicker stay components.) — BUILD-BOOK.md
- [2026-08-03] **`BUILD-BOOK.md` is the single linear build runbook** (merged to main via PR#7): tick-as-you-go, each needed component's contract inlined (Input/Output/Event/Action + backing formulas) so the build needs no cross-referencing. Order: smoke test → 11 screens → 7 composed components (contract typed, then body pasted) → App.Formulas → provision → data screens → verify. The other 4 components are skip-until-composed. HANDOFF.md now leads with validated channel facts + points to it. — BUILD-BOOK.md
- [2026-08-03] **pac has TWO packers with DIFFERENT power — this is the crux of msapp authoring.** (a) `--layout Experimental` (.fx.yaml, PASopa engine) **COMPILES** source → the app's control tree — PROVEN: our scrReference/scrAdmin shells packed this way opened in Studio with both screens (user-confirmed). (b) `--layout SourceCode` (.pa.yaml — our authored format) does **NOT compile**; it repackages a pre-existing `.msapr` (the already-compiled app) + the .pa.yaml as loose source. Feeding it the EXAMPLE's `.msapr` (obtained by bumping the example Header DocVersion 1.346→1.348 so SourceCode unpack accepts it) produced a "component library" msapp that **packs fine but whose RUNTIME control tree is still the example** (Dashboard/Screen1-6) — our 11 components sit only as inert `.pa.yaml` under `Other/Src/`, never compiled in. Verified by unpacking the SourceCode output in Experimental layout. **Consequence: authoring NEW content into a loadable msapp requires the Experimental `.fx.yaml` path (which compiles); SourceCode is only for round-tripping an app Studio already compiled.** **DEFINITIVELY CLOSED (researched + spiked 2026-08-03):** the Experimental component format IS fully grounded now — two files per component, `<Name>.fx.yaml` (`<Name> As CanvasComponent:` + base props + custom-prop default formulas + child controls) **plus** a companion `<Name>.json` whose `CustomProperties[]` carry `Category` (0=Input, 1=Output, 2=Behavior/Event), `PropertyDataTypeKey`, and a version-stamped `Type` descriptor `{"Version":"1.313","Type":{"Kind":"<Color/String/Number/Boolean>"...}}`. Also learned: in single-line `.fx.yaml` values a `#` triggers `PA3003` (comment) — hex must go in a `|-` block scalar. BUT converting one real component (cmpKpiRing) and packing hit the wall: **`Error PA3004: "This tool currently does not support adding new custom properties to components. Please use Power Apps Studio to edit component definitions."`** pac/PASopa HARD-REFUSES authoring component custom properties in any layout. So the component-library .msapp route is impossible with pac — the tool itself points back to Studio. **Components stay a code-view-paste item** (body paste + BUILD-SHEET contract). Do NOT re-attempt msapp packing of components. SQL note: the SourceCode `.msapr` is itself a zip — scrubbed the example's SQL connection/data sources out of it (Properties LocalConnectionReferences → "", References/DataSources → []) — src/Screens/components
- [2026-08-03] **.msapp packaging RE-TESTED empirically (refines the "dead end" call) — a data-independent smoke build WORKS.** Installed `pac` 2.10.1 (dotnet global tool; dotnet 10 present, pac was not). Findings: (1) only the **deprecated Experimental `.fx.yaml`** layout packs here — `pac canvas pack` round-trips the example fine, but `--layout SourceCode` (modern `.pa.yaml`) REJECTS it: *DocVersion 1.346 < min 1.348*, so our authored `.pa.yaml` is NOT directly packable and must be converted to old `.fx.yaml`; (2) old `.fx.yaml` uses a **GLOBAL control namespace** (names unique across ALL screens — hence the example's 1/4/6 suffixes) + explicit `ZIndex:` + `Name As type.variant:`; (3) packing regenerates `Controls/*.json` + `Checksum.json` from source — so the memory's old reason-(2) (".pa.yaml is read-only / app opens empty") does NOT hold for this format. Built `build/EQD-Taskmaster-smoke.msapp` (scrReference+scrAdmin shells, button-nav, NO data/components/App.Formulas) on the example scaffold; verified it round-trips through pac (both screens, control tree regenerated). **Studio-open still UNCONFIRMED — user tests (one-way gap).** Full-app packing NOT attempted (8 unprovisioned lists + O365 + 11 components can't resolve). Code-view paste stays the proven channel; this is a channel experiment — build/README.md
- [2026-08-03] **Pre-paste tooling was Windows-broken; authored .pa.yaml is example-conformant.** Checked the buildout against the genuine `example/Project Tracker.msapp` (unzipped → Studio-emitted modern-schema `Src/*.pa.yaml`): every control token + structure matches, `Control: CanvasComponent` matches the bundled schema's ENFORCED const (its snippet `Control: Component` is stale). **No authored source needed editing.** The errors were all in the Python tools: `read_text()`/`write_text()` default to cp1252 on Windows → validator crashed on the first em-dash and silently validated only 14/22 files; `split_components.py` had the same bug + rendered every function param as `None` (`q.get('Name')` on a single-key map). FIXED all with `encoding="utf-8"` + a param-parse fix; added `tools/requirements.txt` (jsonschema, ruamel.yaml, PyYAML — none were installed). Now 22/22 valid on Windows. Bare `|` block scalars + curly quotes in string literals left alone (valid, not errors) — tools/validate_pa_yaml.py
- [2026-08-03] **Power BI licence gate DECIDED = SOFT GATE.** Reports stays visible but greyed for unlicensed users and still opens, landing on the empty-state card + the three licence-free KPI rings. Never hidden — a hidden feature is one nobody knows to request a licence for. `gHasPowerBiLicence` stays `false` (everyone treated as unlicensed) until a real signal exists; worst case is a licensed user seeing the fallback rings and one extra click, vs. an unlicensed user hitting a broken embed. Nav + Reports both read that one line, so a future signal is a one-line change — src/App.pa.yaml
- [2026-08-06] **EVERY combo box is `Classic/ComboBox@2.4.0` — the modern one was never what Studio inserted.** A code-view export came back carrying the CLASSIC token for a control this repo had authored as `ModernCombobox`, with our own `Sort(Distinct(Filter(…)))` `Items` formula intact. So the formula was never the problem; **the display-column property was**. The classic family names it with STRING ARRAYS — `DisplayFields: =["Value"]` **and** `SearchFields: =["Value"]` — where the modern family uses `ItemDisplayText: =ThisItem.<col>`. The modern property on a classic control renders **blank rows**, which is exactly the reported *"all I see is select and a blue box"* and *"the region component does not work"*. All 10 instances converted (7 `scrProjectEdit`, 3 `cmpNestedSelect`); `ModernCombobox` appears nowhere in `src/`. `IsSearchable`/`SearchFields`/`DisplayFields`/`InputTextPlaceholder` verified first-party on MS Learn `controls/control-combo-box`, so nothing inferred was authored. **Likely root cause is a modern-controls-off app setting** — if so it implicates `ModernTextInput` (36), `ModernButton` (20), `ModernDatePicker` (10) too, but that is a HYPOTHESIS: get one export of a text input before converting anything else. `Items` from `Distinct()`, a `["a","b"]` literal or `Choices()` all expose a column named `Value` — tools/studio-enums.json
- [2026-08-06] **A DUPLICATE YAML KEY IS INVISIBLE, AND IT PASSED EVERY CHECK IN THIS REPO.** Commit `445f59d` doubled `scrProjectEdit.pa.yaml` from 2760 → 5984 lines: 132 controls carrying two copies of `Control`/`Variant`/`Properties`/`Children`, **360 duplicate keys**. PyYAML keeps the LAST duplicate silently, so the file parsed, schema-validated and reported **`24/24 valid`** while half of it was invisible to the parser and to every edit made against it. **This also explains the abandoned `FillPortions` sweep** — those 253 lines went into the copy the parser discards, which is why the parser "saw 0"; the discrepancy I could not explain at the time was this. Duplication was NESTED, not a flat 2×, so mechanical dedup was not provably correct: the file was **rebuilt from the last clean commit (`5edcd66`)** and all six intended fixes re-applied by hand, landing at 2752 lines / 183 controls — the same control count the parser had been seeing, so nothing was lost. **`duplicate_keys()` now runs in `validate_pa_yaml.py` before schema validation**, verified to FAIL the corrupt blob and pass the rebuild. **Two transferable lessons:** (1) never trust "valid" from a YAML tool that does not reject duplicate keys; (2) to recover an intended change set from a corrupted file, `comm` over `sort -u` of both versions — the sorted line-set diff is immune to duplication and reduced 3,200 junk lines to ~20 real ones — tools/validate_pa_yaml.py
- [2026-08-06] **SUPERSEDES the classic-combo-box decision above — the family was never the problem, the VERSION TOKEN was.** Modern controls are ENABLED in this app; the user activated them and confirmed *"all modern controls can remain"*. All 10 combo boxes are back to **`ModernCombobox@1.0.0`** with `ItemDisplayText: =ThisItem.Value`. **The rule worth keeping: `@x.y.z` in a pa-yaml token is the TEMPLATE version, NOT the revision number Studio shows in its properties pane.** Studio reports the combo box as **1.1.1**; authoring `ModernCombobox@1.1.1` rejects the entire paste as an unknown control, which is exactly the *"error on trying to input the nestedselect"* that started this whole detour. First-party proof: MS Learn's YAML example on `modern-controls/modern-control-combobox` emits **`@1.0.0`** while that same page's "Recent updates" documents `DelayOutput`, typed enums and `SelectMultiple` defaulting true — **revised control, unchanged template version**. Every other modern token in this repo is `@1.0.0`, which should have been the tell. **Never derive a `@version` from a number a human read off Studio's UI** — take it from an export or a first-party YAML sample. The property revision is still real and still matters (`TriggerOutput`→`DelayOutput`, `Fields`→`ItemDisplayText`, `SelectMultiple` default true, typed enums, `FontColor`/`FontSize`/`BorderRadius`→`Color`/`Size`/`Radius{...}`) — it just does not move the token. **The modern-controls-off hypothesis in the previous entry was WRONG and is retracted**; `Classic/ComboBox@2.4.0` stays catalogued only because Studio still generates it inside classic data cards — tools/studio-enums.json
- [2026-08-06] **`active` was a column I INVENTED, and filtering on a column the list does not have leaves a picker blank with NO error.** `cmpNestedSelect` filtered every level on `Coalesce(active, true)` and declared `active: true` in its `Terms` **Default** — which is what TYPES the input property, so binding a real `mapping_*` collection that lacks the column breaks the binding itself. User: *"The columns are level1, level2, level3.."* A mapping list has **exactly** `level1`/`level2`/`level3` plus the built-in ID and Title. Removed from all 6 formulas, from the `Terms` default, and from all three lists in `schema/schema.yaml` (where it had been a soft-delete design that was never provisioned). **The general lesson is the repo's own rule, which I broke: never author a field token that does not resolve to a `name:` in `schema/schema.yaml` — and `schema.yaml` is only authoritative for what has actually been PROVISIONED.** A `provisioned: pending` list is a proposal, not a fact — schema/schema.yaml
- [2026-08-06] **List name is `mapping_regions` — PLURAL — and a wrong data-source name fails silently.** `ClearCollect(colMapRegion, mapping_region)` against a non-existent source leaves the collection empty, so the picker renders blank rather than erroring; combined with the `active` defect above, that is the whole "classification combobox is blank" report. Renamed throughout `src/` and `schema/`, and `mapping_regions` marked `provisioned: live`. **All three names now CONFIRMED against SharePoint: `mapping_regions` is PLURAL, `mapping_activitytype` and `mapping_producttype` are SINGULAR.** The inconsistency is real — do not tidy it. All three `provisioned: live` — schema/schema.yaml
- [2026-08-06] **CORRECTION — `ModernCombobox@1.1.1` IS the right token; my "version trap" entry above was wrong and is retracted.** The user reads 1.1.1 off the live control in Studio and confirmed it twice. I overrode that on two pieces of evidence, and both were weak. (1) **MS Learn's YAML sample emits `@1.0.0`** — but doc code samples lag a control revision even when the prose beside them is updated, and that page's own prose documents the revised behaviour. A stale sample loses to the running app. (2) **A `@1.1.1` paste had failed** — but that happened while **MODERN CONTROLS WERE DISABLED in the app**, and a disabled template is unavailable whatever version you name it. The failure was fully explained without blaming the token; I attributed it to the token anyway. That misdiagnosis cost a pointless conversion of all 10 combo boxes to `Classic/ComboBox@2.4.0` and back. **Two lessons worth more than the version itself: (a) before concluding a token is wrong, rule out the app-level setting that makes a whole control FAMILY unavailable — a failed paste names only itself; (b) versions are PER-CONTROL — every other modern token here is `@1.0.0` and the combo box is not, so never pattern-match a version across the family.** `@1.0.0` stays allow-listed purely as a fallback — tools/studio-enums.json
- [2026-08-06] **A SAVE THAT INFERS SUCCESS FROM AN EMPTY ERROR MESSAGE CORRUPTS THE SCREEN PERMANENTLY.** Reported as *"sometimes the Project name will pop and the submission will fail. Then upon inputting the name again the project will just continue to fail."* Mechanism, and it is a general trap not a one-off: `btnPrSave` wrapped its Patch in `IfError(Set(gPrSaved, Patch(...)), Set(gPrErr, FirstError.Message))` and then gated the entire success block on `Len(gPrErr) = 0`. **A write can fail and still leave `FirstError.Message` EMPTY**, so the empty message read as success — the code then ran `Set(gEditProject, gPrSaved)` with gPrSaved BLANK and flipped `gEditMode` to `"Edit"`. From that moment `txtPrName.Default = If(gEditMode = "Edit", gEditProject.project_name, "")` evaluates to blank, which is the name "popping"; and every later save takes the Edit branch and patches a record that does not exist, so it fails forever regardless of what is typed back in. **Fix: trust the RECORD, never the message** — `If(IsBlank(gPrSaved.ID) && Len(gPrErr) = 0, Set(gPrErr, <explicit message>))`, and gate the success block on `Len(gPrErr) = 0 && !IsBlank(gPrSaved.ID)`. Plus a self-heal so anyone already stuck recovers: the New/Edit branch now tests `gEditMode = "New" || IsBlank(gEditProject.ID)`. **AUDIT THE OTHER THREE EDIT SCREENS for the same shape** — any `IfError` whose success path is inferred from the error variable rather than from the returned record — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-06] **cmpNestedSelect: the commit tick is GONE and the separator is `" | "`.** By instruction: *"have on change be the acceptance factor -> drop the checkbox"* and the path format `[l1] | [l2] | [l3]`. `icoCommit` deleted; `cmpNestedSelect.OnChange()` is now raised by all three combo boxes AND by the `+` / `✕` level buttons — closing a level SHORTENS the path, so it has to raise or the consumer keeps a value the user can no longer see. A selection is now the acceptance, so nothing can be left unrecorded by forgetting a second click. **The separator is part of the STORED DATA**: `DefaultPath` re-seeds via `StartsWith` on the path and `nsSeedDepth` derives depth by counting separators, so any row written with the old `" > "` opens at level 1 with nothing selected and must be re-picked. It is passed **explicitly as an instance property** on both pickers rather than left to the component default — a screen paste carries an instance property, while a component default must be retyped by hand in Studio — src/Components/cmpNestedSelect.pa.yaml
- [2026-08-06] **`Patch()`'s RETURN VALUE is not a full record — reading a column off it caused three symptoms that looked like three bugs.** Decisive clue from the user: *"the bug is fixed when I remove the single task added to the project."* One cause: `gPrSaved = Patch(taskmaster_projects, Defaults(...), {...})` does not reliably carry `project_name`, and three things read it. (1) Every child's parent Lookup was written as `{ Id: gPrSaved.ID, Value: gPrSaved.project_name }` — **a SharePoint Lookup write needs the target row's DISPLAY TEXT, and a blank Value is rejected**, so the task/transaction/issue insert failed. (2) `Set(gEditProject, gPrSaved)` then `Set(gEditMode, "Edit")` made `txtPrName.Default = If(gEditMode = "Edit", gEditProject.project_name, "")` re-evaluate to BLANK — **a text input re-reads Default when the Default's VALUE changes**, so the name box emptied in front of the user. That is the "project name pops". (3) The retry then had an empty name box against a required column, so it failed forever. **Why only with a child staged:** with none, `colChildErrors` is empty and the code `Navigate()`s away immediately, so the blanked name is never seen. **Fixes: hold the typed name in `gPrNameSaved` and use it for all three lookup writes; re-read the parent with `LookUp(taskmaster_projects, ID = gPrSaved.ID)` instead of reusing the Patch return.** **General rule: after a Patch, re-read the record before seeding a form from it — treat the return value as an ID receipt, not as the row.** Also: `Coalesce` is documented over VALUES not records, so the fallback is an explicit `If` — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-06] **A child-failure banner that reports only a COUNT is undiagnosable across the air gap.** `"N item(s) could not be created"` returns as "the submission failed" and the next fix is a guess — this defect survived two rounds because of it. The banner now appends the first failure's Kind, Name and **`Message`**, which is the text SharePoint actually returned and was already being collected into `colChildErrors` and thrown away. **Whenever an error is caught and counted, surface the MESSAGE somewhere the human can read it** — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-06] **`"Project_Type" is required` — a RETIRED column is blocking every project insert, and it is a SHAREPOINT fix, not a code fix.** Nothing in the app writes a column of that name; it writes `project_type_id` / `project_type_path`. So this is the **pre-C11 Managed Metadata column still on `taskmaster_projects` with Required = Yes**. SharePoint rejects the row before any new column matters, which is why it survived a fix aimed at the child writes. `migration:` steps 4-7 were never applied, so `project_region` and the old `client_type` / `client_coverage` / `product_type` columns are likely in the same state. **Do NOT make the app write a retired column to get past it** — set Required = No, or delete it. **The general point: `provisioned: live` in schema.yaml only ever meant "the NEW columns exist". It never meant the OLD ones were gone, and a leftover required column fails the insert with no reference to anything the app actually wrote.** Recorded as `migration.blocking_now` in the golden source — schema/schema.yaml
- [2026-08-06] **The staged-chip galleries were clipped by their own template arithmetic.** `TemplateSize: =34` with `TemplatePadding: =4` leaves **34 - 2x4 = 26px** of usable cell height for a **30px** chip, so the bottom was cut off — reported as *"just the space and part of the component"*. **`TemplatePadding` is taken off BOTH edges: usable height is `TemplateSize - 2*TemplatePadding`, and a chip must fit inside THAT, not inside TemplateSize.** Second fault in the same three galleries: `Width: =1316` with `WrapCount: =4` fixed TemplateWidth at exactly 329 for a 321px chip — no slack — while the gallery is a child of `frmPrScroll`, a VERTICAL auto-layout container that sizes it, so 1316 held at exactly one screen width and clipped sideways at any other. Now `Width: =Parent.Width - 48` like every sibling, `WrapCount: =3`, `TemplateSize: =46`, chip 300 wide — verified to fit any container >= 924px. **Compute cell-fit arithmetic explicitly; do not eyeball a gallery template** — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-07] **Never park a number in a Label's `Text` and read it back with `Value()`.** `cmpNestedSelect` held its seed depth in a hidden `nsSeedDepth` Label — a NUMBER assigned to a TEXT property — and 12 formulas read it back as `Coalesce(nsLevels, Value(nsSeedDepth.Text))`. That round trip is the only text→number conversion in the component and the only thing in it that can raise **"The Value xxxx cannot be converted to a number"**, which is what a region/type pick was throwing. Confirmed by elimination: selecting a term runs nothing else on the screen that converts. **The label existed only because a component cannot hold an initialised variable — but the derivation never needed a control at all.** It is now computed inline as a number, `Coalesce(nsLevels, If(IsBlank(DefaultPath), 1, CountRows(Split(DefaultPath, Separator))))`, factored through `With({depth: ...})` where a formula used it more than once. `Split`+`CountRows` also replaced the old `Len`/`Substitute`/divide arithmetic, which could divide by zero if `Separator` were ever blank. The hidden label is deleted. **General rule: a control's property is a rendering surface, not a variable — if you need a derived value in several formulas, derive it in each, or factor it with `With()`; do not launder it through a control's text.** If the error somehow persists, the next suspect is `project_region_id` / `project_type_id` being a **Lookup** column in SharePoint rather than a Number, which raises the identical message with the term name in it — src/Components/cmpNestedSelect.pa.yaml
- [2026-08-07] **SOLVED at last — clearing a component's internal state from outside is the built-in `OnReset` property + `Reset(<instance>)`.** This was open since `ResetSignal` was retired as ungrounded on 2026-08-06. MS Learn *Behavior formulas for components*: creating CUSTOM behaviour properties is experimental, **but "you can use the default OnReset property that is available on all components in your production environment"**, and it is fired by `Reset(ComponentName)` from the consuming screen. `cmpNestedSelect.OnReset` now does `Set(nsLevels, Blank()); Reset(cboL1); Reset(cboL2); Reset(cboL3)`, and `scrProjectEdit.OnVisible` calls `Reset(pickPrRegion); Reset(pickPrType)` **after** the `Set()`s that clear `gPrRegionPath`/`gPrTypePath`, since the inner combo boxes re-read `DefaultSelectedItems` only on reset. Without it a new project opened showing the previous project's classification. **`OnReset` is a BUILT-IN component property, so it belongs in the component's `Properties:` block, not in `CustomProperties:` — and like every component-level property it has to be entered by hand in Studio, it does not arrive with a screen paste** — src/Components/cmpNestedSelect.pa.yaml
- [2026-08-07] **`project_phase` now starts EMPTY on a new project, by instruction** (*"Project phase should be set to nothing - not planning"*). A default the user does not notice is a value they save by accident. It is a REQUIRED column, so the change is three coordinated edits, not one: `OnVisible` seeds `""`, `btnPrSave.DisplayMode` gains `Len(gPrPhase) > 0`, and `lblPrMissing` names "phase". Also `DefaultSelectedItems: =[gPrPhase]` became **`=Filter( [gPrPhase], !IsBlank(Value) )`** — `[""]` is a ONE-ROW table, so the literal form would have pre-selected a blank row rather than selecting nothing. **Whenever a seeded value is allowed to be empty, the combo box's DefaultSelectedItems needs filtering, not just the variable clearing** — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-07] **The staged children are THREE COLUMNS at the bottom of `scrProjectEdit`, at a fixed 248px.** By instruction. They were three stacked sections (divider + heading + Add button + list) that each GREW as items were added, so staging a few children pushed the action bar off the bottom of the scroll. `rowPrStaged` is one horizontal container of three vertical columns; each column is its Add button — which carries the live count, so no separate heading — over a 196px gallery that SCROLLS internally. Same height empty or full. **The row inside each gallery template is an auto-layout container, not absolute coordinates**: the ✕ is pinned right by `FillPortions: =0` against a label with `FillPortions: =1`, so it cannot drift over the text the way the frozen `Parent.TemplateWidth - 30` did on 2026-08-05. **A `Label` carries `OnSelect`, so the click-to-edit target is the label itself — no transparent overlay button to keep aligned.** All three edit handlers were verified call-for-call against the versions they replaced before committing — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-07] **`Remove` / `RemoveIf` return A TABLE, so `IfError(Remove(...), Set(...))` — MS Learn's own idiom — is rejected.** The repo's `IfError`-mixing lint caught it while I was adding project deletion, which is exactly why that lint exists (it was written for `Patch`). MS Learn `function-remove-removeif`: *"Remove and RemoveIf return the modified data source as a table"*, while `Set` and `Notify` return a boolean, and `IfError` currently requires ALL arguments to be type-compatible. Both arms must be the same kind: `IfError( Set(gPrDelScrap, RemoveIf(...)), Set(gPrErr, FirstError.Message) )`, where the scrap variable exists only to make the success arm a `Set`. **Also: `RemoveIf` is NOT delegable** — it pulls matching rows up to the data row limit and deletes them one at a time, so a project with more children than that limit needs a second press — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-07] **A project can now be DELETED from `scrProjectEdit`, children first.** Edit mode only, and TWO presses: the first arms it and the label changes to "Confirm delete", because there is no undo behind it. `OnVisible` and Cancel both disarm. Order matters and is not cosmetic — issues, then transactions, then tasks, then the project. **Every child carries a REQUIRED lookup to the project, so removing the parent first would leave rows SharePoint will not let anything edit afterwards** — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-07] **FillPortions has to be settled at EVERY nesting level, and it defaults to 1 at each.** The first fix pinned the button and gallery inside each column and still left the list cut off mid-row, because `rowPrStaged` is ITSELF a child of `frmPrScroll` — a vertical container, where FillPortions governs height — so the whole section was being squeezed before the column ever saw its declared height. Now pinned at all three levels: `rowPrStaged` `FillPortions: =0` + `LayoutMinHeight`, the Add button and empty label `=0`, the gallery `=1`. **Also: give a gallery a height that is an EXACT MULTIPLE of TemplateSize.** 332 = 36 + 8 + 288, and 288 is 9 whole rows of 32; anything else leaves a half-drawn last row, which is what "the 2nd is partially cut off" was. Original diagnosis below still stands as far as it went —
- [2026-08-07] **FillPortions is why a gallery inside an auto-layout column only showed 3 of 6 rows.** The column was 248px with an Add button (36) and a gallery (196) — but a child of an auto-layout container has flexible sizing ON by default, so `Height` is ADVISORY and the container splits its own height by `FillPortions`, which defaults to **1 on every child**. Two visible children therefore got (248-8)/2 = 120px each; 120/32 = 3.75 rows, and item 4 onward simply was not rendered. `FillPortions: =0` + `LayoutMinHeight` pins the button and the empty-state label; `FillPortions: =1` + `LayoutMinHeight` gives the gallery every remaining pixel. **This is the THIRD time this default has cost a round trip (cmpNestedSelect's nsRow, the chip galleries, now this). The rule: in an auto-layout container, an explicit Height/Width means nothing until FillPortions says who absorbs the slack** — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-07] **Issues are no longer staged on `scrProjectEdit`; they are raised from `scrProject`.** By instruction — an issue is raised against a project that already exists, so staging one alongside a project that does not yet exist was the wrong model. The staged section is now TWO columns. Removed: the `colStgNi` column, the whole issue modal (`mdNi*`, ~300 lines), `colNewIssues`, every `gNi*` global, the issue child-write block and the issue arm of all three staged counters. **No new work was needed on the receiving side — `scrProject` already had an Issues tab and a `btnNewChild` that navigates to `scrIssueEdit` when it is selected.** Screen is down to 148 controls from 183 — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-07] **Delete moved from `scrProjectEdit` to `scrProject`.** By instruction, and it is the better model: deleting is something done TO a project you are looking at, not a side effect of editing one. Same two-press arming, same children-first order, but keyed on `gSelProject` rather than `gEditProject`. `ProjName` / `ProjMeta` narrowed from `Parent.Width - (Gutter*2) - 200` to `- 480` to clear the second button; header band verified collision-free by arithmetic at 1366 wide (title 24..862, delete 876..1026, edit 1036..1186, ring 1202..1312) — src/Screens/scrProject.pa.yaml
- [2026-08-07] **`ModernTabList` `Default` is an ITEM; `ModernDropdown` `Default` is a VALUE. Same property name, two contracts.** `scrProject`'s tab list carried `Default: ="Tasks"` and errored. A string-array `Items` yields RECORDS with a `Value` column — which is exactly why the output is read as `.Selected.Value` — and MS Learn `modern-control-tabs-or-tabs-list` says `Default` "must match an item from the Items source", i.e. BE an item, not equal one. Now `=First(["Tasks", "Transactions", "Issues"])`. **The generalisable bit: a modern control's property NAME does not imply its contract — check the control's own page, not a sibling's.** Recorded in the enums and the controls skill — src/Screens/scrProject.pa.yaml
- [2026-08-07] **Never lay out a band beneath a control from a height you ASSUMED it takes.** `scrProject`'s whole tab band was positioned with a `+ 40` term while `tabProject.Height` is **48**, leaving 8px between the tab bar and the "N active task(s)" line — and MS Learn's Tab List *Limitations* warn that "very small or very large width and height values might not be fully respected by the control", so it renders at or past its stated height and runs into the line below, which reads as the callout sitting on the task list. The band now uses the real `+ 48` throughout, with `Theme.Space.Gap` under the tab bar and a further 8px under the meta line. Verified by arithmetic at 1366x768: tab 152..200, meta 216..240, lists 248..744, no two visible-at-once controls overlapping. **The source arithmetic looked clean the whole time — the defect was that one term did not match the control it described** — src/Screens/scrProject.pa.yaml
- [2026-08-07] **`scrProject` REWORKED — tabs removed entirely, layout moved to containers.** Three bands by instruction: (1) an info CARD stating the project outright — name plus 8 captioned facts (`project_phase`, `project_priority`, manager, target, `project_region_path`, `project_type_path`, `project_coverage`, start) — with the KPI ring beside stacked edit/delete ICONS; (2) tasks and transactions as two dynamically-sized sections stacked left, each titled with its count and carrying its New button on the same line; (3) **issues in their own full-height column on the right**, five columns wide — name, linked task-or-transaction, status, type, impact — because an issue is raised AGAINST a project and points at a unit of work rather than being one. All five issue columns exist in `schema.yaml` (`issue_task_name` / `issue_transaction_name` are Lookups, so the linked cell is `Coalesce(task.Value, transaction.Value, "—")`). **Tabs went because they hid two thirds of a project behind a click AND their `Default` had to be an item, which was the thing erroring.** 83 controls. **Two traps hit while writing it, both caught by the repo's own checks rather than by Studio: `Icon.TrashCan` does not exist (the armed delete icon is `Icon.Warning`), and `FillPortions`/`LayoutMinWidth` are NOT accepted on a `CanvasComponent` instance — the KPI ring is wrapped in a pinned GroupContainer instead, which is why scrProjectEdit's pickers only ever set Width/Height.** — src/Screens/scrProject.pa.yaml
- [2026-08-07] **Generating a screen from Python: re-indent EVERY embedded formula, never paste one at the indentation it had elsewhere.** Third rebuild broken this way. The generator now has one `fx(text, indent)` helper that strips each line and re-pads to the block scalar's column, and every multi-line formula goes through it. A formula lifted verbatim from another file carries that file's indentation, which is almost never the right depth in the new one, and the failure is a YAML parse error a long way from the cause — src/Screens/scrProject.pa.yaml
- [2026-08-07] **`scrTaskEdit` REWORKED — tab list out, containers in, seven changes by instruction.** (1) The Output tab is now an optional SECTION behind a `Classic/Toggle@2.1.0` — **no checkbox token is grounded in this repo, and the toggle is the same two-state control**; its `Default` reads `!IsBlank(gEditTask.task_output_format.Value)` so the RECORD says whether the section is relevant. **Switching it off on an edit CLEARS every output field on save**, which the old code did not do for the three lookups — otherwise the form hides values still on the record. (2) Stage/Health/Priority are `ModernCombobox@1.1.1` with `Color` bound to a `Switch` on the value (`Color` is the current control's text colour; it was `FontColor` before the revision). (3) Name + description lead, description is `TextMode.MultiLine` at 120px. (4) **Project is derived, never asked for** — `gTkProject` is set in OnVisible from the task on Edit and `gSelProject` on New, still written and still driving the C3 rollup. (5) Lead/supporter use scrProjectEdit's chip-replaces-search-box shape. (6) **Completion date has no picker: it is derived on save** — Complete stamps it and keeps the original on re-save, **Archived PRESERVES it because archiving a finished task must not erase when it finished**, anything else clears it; shown read-only so the value stays visible. (7) Due date flags itself through its CAPTION — **a date picker has no grounded colour property, a Label's `Color` is beyond doubt**. 114 controls, every field token verified against `schema.yaml` and all seven choice literals matched to the golden source — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-07] **MODERN CONTROL VERSIONS ARE PER-CONTROL AND NOT GUESSABLE — the `@1.0.0` pattern this repo relied on was never real.** User read them off the live Studio controls: **Text Input 1.1.1 · Combo box 1.1.1 · Dropdown 1.0.2 · Date picker 1.0.1 · Radio 1.0.1 · Data grid 1.5.0.** **Nothing is 1.0.0 and no two share a version.** The repo carried `@1.0.0` on every modern token for days purely because the first few grounded that way — I then wrote "every modern token here is `@1.0.0`" into the skill as if it were a rule, and used it as an argument AGAINST the user's correct combo-box version. **STANDING RULE, given by the user: if unsure of a modern control's version, author the CLASSIC control.** A wrong version rejects the whole paste. Applied: `ModernTextInput@1.0.0`→`@1.1.1` (33), `ModernDatePicker@1.0.0`→`@1.0.1` (9); **`ModernButton` (20) → `Classic/Button@2.2.0`** and **`ModernNumberInput` (2) → `Classic/TextInput@2.3.2`** because their versions are unknown. Converting a button drops `Appearance` (modern-only) for `Fill`/`Color`/`BorderColor`/`BorderThickness`; converting a number input drops `Min`/`Step`/`Precision` and every reader becomes `Value(ctl.Text)` since a classic text input outputs TEXT — `TextFormat` is not grounded here so no `Format` was authored — tools/validate_pa_yaml.py
- [2026-08-07] **SIBLING MODERN CONTROLS DO NOT SHARE PROPERTY NAMES — two errors on one screen, both from assuming a neighbour's spelling.** On `ModernTextInput` I authored **`Mode: =TextMode.MultiLine`** (that is the CLASSIC text input's property; the modern one takes **`Type: =TextInputType.Multiline`**) and **`DelayOutput: =true`** (that is the COMBO BOX's property; the text input keeps **`TriggerOutput: =TriggerOutput.Delayed`**, with values Keypress/FocusOut/Delayed). Both confirmed on MS Learn `modern-controls/modern-control-text-input`. The combo box is the control that renamed `TriggerOutput`→`DelayOutput`; the text input did NOT. **Check the control's OWN page before reusing a property name that worked on a sibling** — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-07] **`scrTaskEdit` refinements: completion date off the form, People/Dates as a 2x2 band, and a SECOND optional section.** (a) The read-only completion field is gone entirely — the derivation still runs in the save, it is simply not shown. (b) People and dates share one section as two columns of two rows: Lead over Supporter, Start over Due. Built with a `stack()` helper that re-indents a generated field block so it can nest one level deeper — the fields inside are `FillPortions: =0` so the column pins them rather than splitting its height between them. (c) The output toggle's caption is now the static **"Task Produces Output?"** rather than text that changed with the toggle. (d) New **Client** section on the same pattern (`tglTkClient`, caption "Client Section?") holding the client lookup and client lifecycle stage, both MOVED out of Output. **A section's toggle ROW is never gated on its own toggle** — that would hide the only way to switch the section back on; only the field rows below it are. In the save, client and client stage now follow `tglTkClient` while the rest follow `tglTkOutput`, so switching either section off still CLEARS its own fields — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-07] **A generator is source too — sweep it when you sweep `src/`.** The version/classic conversion was applied to `src/**` but not to the Python generator that writes `scrTaskEdit`, so regenerating silently reintroduced `ModernTextInput@1.0.0` and `ModernButton@1.0.0`. **The token allow-list caught it on the next run** — which is exactly what it is for, but the generator should have been swept at the same time. Any file that EMITS pa-yaml is subject to the same token rules as the pa-yaml itself — tools/validate_pa_yaml.py

- [2026-08-10] **`scrTask` DELETED — one row must have exactly ONE writer, and a stale DISPLAY is fixed with `Reset()`, not with a defensive write.** User reported both task screens "fail on changes", with NO red banner (which rules out the retired-MM `Required` columns — those fail loudly with a named column). Cause: `scrTask` contained **zero `Reset()` calls**; every control was seeded from `Default`/`DefaultId` off `gSelTask`, which are read once at control load, so the second task opened still showed the FIRST task's name/stage/health/priority — and `btnSave` wrote `txtTaskName.Text`, **renaming task B to task A**. The 2026-08-03 audit had hardened the WRITE (seed globals in `OnVisible`, Patch from them) but that leaves a hole: the globals only move on the strip's `OnChange`, so when the stale display already shows the value the user wants, clicking it fires nothing, `gTaskDirty` stays false, and the save button stays dead — the exact silent symptom. Deeper defect: **two writers, one invariant** — `scrTaskEdit` derives `task_date_completion` from stage, `scrTask` wrote `task_stage` without it, so completing from the quick screen left the date blank; `scrTask` also still guarded the rollup at `>= 2000` where the row limit is 500, so it could persist a truncated average that Power BI reads. `scrTask` was not in `NavMenu` and had ONE inbound reference; transactions and issues already went straight to their editors, so tasks were the odd hop out and `scrTaskEdit` is a strict superset. User was offered a one-tap stage flip to replace the lost `cmpSelection` strips and said it doesn't matter — none added — src/Screens/scrProject.pa.yaml
- [2026-08-10] **SUPERSEDED same day — see the OnReset correction below.** ~~`Reset()` cannot reach inside a component instance, so DON'T reuse `cmpSelection` for per-record seeded state.~~ The conclusion about `cmpSelection` still holds; the stated reason was wrong. Seeding a global and writing from it makes the write correct but leaves the display lying, and a lying display produces a no-op `OnChange` — which is its own failure mode. When a screen is reused for a different record, EVERY seeded input needs an explicit `Reset()` in `OnVisible`: text inputs, combo boxes (`DefaultSelectedItems`), date pickers (`DefaultDate`), toggles. `scrTaskEdit` resets all 16 of its inputs and documents why; `scrTask` never did — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-10] **Hand the editor a re-read row, not the gallery's collection record.** `galTasksHit.OnSelect` now does `Set(gEditTask, LookUp(taskmaster_tasks, ID = ThisItem.ID))` rather than `ThisItem`: `colProjectTasks` is a snapshot taken in `scrProject.OnVisible`, and the editor Patches against whatever record it is handed. One delegable ID lookup removes a whole class of write-stale-data bug. A LookUp can MISS (row deleted since the snapshot), so the `OnSelect` guards `IsBlank(gEditTask.ID)` and refuses with a Notify rather than opening an empty "Edit task" form that would Patch against nothing — src/Screens/scrProject.pa.yaml

- [2026-08-10] **The Managed Metadata blocker is CLEARED — the user retired every MM column and replaced them with CHOICE columns.** `schema.yaml -> blocking_now:` is now marked RESOLVED and `migration.status` is PARTIALLY APPLIED (step 6 done; steps 1-3 and 4-5,7 not). Kept as a record of the fault's shape: **a save rejected by a column the app never writes is a SharePoint-side fault and cannot be fixed in Power Fx** — two fixes were aimed at the wrong layer before the cause was found. **The replacement columns' internal names and values were NOT supplied**, so nothing in `src/` may reference them yet; a new block `choice_replacement_UNRECORDED:` records the two questions that must be settled first — (1) do the Choice columns REPLACE the C11 `*_id`/`*_path` + `mapping_*` + `cmpNestedSelect` apparatus or sit beside it, and (2) if they replace it, is the level1/level2/level3 hierarchy being dropped on purpose (a Choice column is flat). Until answered, the `*_id`/`*_path` writes stay as authored — they are what is landed and working — schema/schema.yaml
- [2026-08-10] **Transaction and issue rows now re-read the row on the way into their editor too, but the exposure there was NARROWER than the task one and I said otherwise.** Correction to the same-day claim of "the same stale-base-record exposure": `galTasks` bound to `colProjectTasks`, a ClearCollect SNAPSHOT, whereas the transaction and issue galleries bind to a LIVE `Filter` of the data source. So `ThisItem` there is a genuine data-source row with correct identity and Patch always hit the right ID — what was missing was FRESHNESS (a gallery's query result is cached between refreshes, so the editor could seed its form from values another user had since changed and patch them straight back, or open on a deleted row). Same `LookUp` + `IsBlank` guard applied to both, for the smaller reason — src/Screens/scrProject.pa.yaml

- [2026-08-10] **A DISABLED BUTTON CANNOT FIRE `OnSelect`, so it cannot explain itself — never gate a save with `DisplayMode.Disabled`.** User: "I added a description and changed the stage of a task — when I clicked save task, nothing happened." No toast, no navigation. Cause: `btnTkSave.DisplayMode` was `If(Len(lblTkMissing.Text) = 0, Edit, Disabled)`, and the task was missing a required field, so the click was swallowed with zero feedback. **The gate itself was CORRECT** — `task_name`, `task_lead` and `task_project_id` are all `required: true` in schema.yaml, so SharePoint would reject the write anyway; the defect was purely the silence. **It can only ever trip on an EDIT:** in New mode `gTkLead` defaults to `User()` and `gTkProject` to `gSelProject`, so both are always populated; in Edit mode they are whatever is stored, and a task saved without a lead makes the screen permanently dead. FIX: `DisplayMode` removed (always enabled), the whole `OnSelect` body wrapped in `If( Len(lblTkMissing.Text) > 0, Notify("Can't save yet — this task still needs: " & lblTkMissing.Text, Error), <body> )`. **The same pattern was on `scrIssueEdit` (3 fields) and `scrTransactionEdit` (SIX fields — the worst) and both were fixed identically.** `scrProjectEdit`'s three inline sub-form buttons were LEFT ALONE deliberately: those gate on fields inside the same modal the user is filling in, so the empty field is on screen next to the dead button — a different situation from an editor seeded off a stored record — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-10] **`lblTkMissing` / `lblIssMissing` / `lblTxMissing` must stay a BARE LIST — never add a static prefix.** The save guard now tests `Len(lbl*Missing.Text) > 0`, so a constant prefix would make it permanently non-empty and block every save on the screen. The sentence around the list belongs in the Notify. Noted in the label's own comment on all three screens — src/Screens/scrTaskEdit.pa.yaml

- [2026-08-10] **Saving a task went from 12 sequential round trips to 1 on the common edit.** User reported the save was "decently slow". Cause was not the query shape but the CALL COUNT: the main Patch plus **eight optional Patches to the SAME ROW**, and all eight fired on every edit because the else-branch of each was `gEditMode = "Edit"` -> clear, so nothing was ever skipped — then a LookUp, a Filter fetching up to 500 task rows, and a project write. Four changes: **(A) folded all eight optionals into the single Patch** as `If(condition, value, Blank())` (9 writes -> 1); **(B) the C3 rollup is SKIPPED unless the stage actually moved** — it is a weighted average over stages, so editing a description/date/priority/output field cannot change it, and skipping removes the LookUp, the 500-row fetch and the write from most saves; **(C) the project is only written when the rounded number actually changed**; **(D) `Concurrent()` around the two independent rollup reads.** Net: a typical edit is 1 round trip, a stage change 4. Both halves of (A) were grounded in the screen's own history — `task_date_completion` was always an `If(...)` inside that Patch record, and every `Blank()` was a live Patch value before the fold — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-10] **NEVER `Concurrent()` writes to the same row** — the obvious way to speed up a chain of Patches to one record, and it races version conflicts against itself. Concurrent is only safe here for the two INDEPENDENT rollup READS. Also rejected: `ShowColumns` to shrink the rollup fetch — whether it reduces the connector's `$select` or is applied client-side after the full fetch cannot be confirmed across a one-way gap, and (B) removes the fetch from most saves anyway, so there was no reason to ship a guess — src/Screens/scrTaskEdit.pa.yaml

- [2026-08-10] **CORRECTION: `Reset(<instance>)` DOES reach into a component — via its built-in `OnReset`.** The parent cannot reset a component's CHILD controls directly, but `Reset(instance)` fires the component's `OnReset` behaviour, and that handler can `Reset()` its own children and clear its own instance-scoped `Set()` state. Grounded in `cmpNestedSelect`, whose `OnReset` is documented as "Fired by Reset(<instance>) on the consuming screen" and does `Set(nsLevels, Blank()); Reset(cboL1); Reset(cboL2); Reset(cboL3)`. **`cmpSelection` is un-resettable because it HAS NO `OnReset`, not because components can't be reset** — a fixable omission, not a platform limit. Consequence: a component MAY hold per-record state provided it defines `OnReset` and the consuming screen calls `Reset(<instance>)` when it re-seeds. Any new stateful component must ship an `OnReset` — src/Components/cmpNestedSelect.pa.yaml

- [2026-08-10] **`cmpPeoplePicker` built — a MODAL people search, one full-screen instance PER SCREEN, not per field.** Requested flow: click a person box -> popup -> type -> ENTER -> list appears -> tap to highlight -> press the check to commit. **The per-screen shape is forced by a platform fact: a component CLIPS its children to its own Width/Height**, so an instance sized to a 36px form field cannot paint a dialog over the form. So the person BOX stays a screen control (`btnTkLeadPick` / `btnTkSuppPick` + the existing chips and their clear X) and only the DIALOG is the component; `gTkPicker` already named which field was being filled, so that survived unchanged and one instance serves both person fields. Contract is 4 hand-typed properties — `Title` (Input Text), `Picked` (Output Record), `OnConfirm` / `OnCancel` (Event) — plus the built-in `OnReset`, which costs nothing to type. **Search runs on SUBMIT, not per keystroke**: the gallery binds to the instance variable `ppQuery`, never to the search box's live text, and `TriggerOutput.FocusOut` makes a single-line input commit on ENTER, firing `OnChange`, which fills `ppQuery`. That is both the requested mechanic and one Office365Users call per search instead of one per character. Single-select, matching `multi: false` on every Person column — src/Components/cmpPeoplePicker.pa.yaml
- [2026-08-10] **`OnReset` is the load-bearing part of any component that is REUSED, and it is why this design is safe where `cmpSelection` was not.** One `cmpPeoplePicker` instance serves both person fields AND every record the screen is opened for, so without `OnReset` the dialog would open for Supporter still showing Lead's highlighted row and the check would commit the wrong person. `OnReset` clears `ppQuery`, `ppPicked`, `ppTried` and `Reset()`s the search box; the screen calls `Reset(cmpTkPeople)` in `OnVisible` AND in each box's `OnSelect`. `Picked` is read ONLY inside `OnConfirm` — reading it anywhere else would be reading whatever the last dialog left behind — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-10] **Coalesce() is grounded here for scalars, NOT for records or Booleans.** Authored `Coalesce(ppPicked, {DisplayName:"",Mail:""})` and `Coalesce(ppTried,false)`, then swapped both before hand-off: the record case became `If(IsBlank(ppPicked), {...}, ppPicked)` (If-over-records is used all over this repo) and the Boolean became `ppTried = true` (blank compares false). Text Coalesce — `Coalesce(ppQuery,"")`, `Coalesce(ppPicked.Mail,"")` — is left as is and is fine — src/Components/cmpPeoplePicker.pa.yaml

- [2026-08-10] **AUTHORING ERROR CAUGHT BY THE USER: `cmpPeoplePicker` v1 reached APP SCOPE 38 times with `AccessAppScope: false`.** 37 `Theme.*` references plus `Office365Users.SearchUserV2`. **`Theme` is an App NAMED FORMULA, not a language built-in** — with app scope off a component cannot read it, exactly as it cannot read a global. The repo already said so and I did not look: `cmpUiKit` line 12 — "isolated and cannot read app globals. Keep these hex values in step with Theme." Every other component has ZERO live `Theme.` references (the three greps that hit are all comments) and hardcodes literal RGBA; `cmpAppBar` uses `RGBA(0, 90, 158, 1)` where a screen would write `Theme.Color.Primary`. **And no component in this repo touches a data source at all** — `cmpAppBar` says "Bind to the NavMenu named formula", `cmpNestedSelect` says "Pass a collection, not the list itself". Data always arrives through an Input property. FIX: every Theme token became its literal, and the directory search moved to the SCREEN, which passes rows in via a new `Results` (Input, Table). **CHECK BEFORE AUTHORING ANY COMPONENT: is every name in this formula either a language built-in, a control in this component, or one of its own custom properties? If not, it is app scope and it will not resolve** — src/Components/cmpPeoplePicker.pa.yaml
- [2026-08-10] **An isolated component that SEARCHES costs 7 hand-typed properties, not 4** — that is the real price of `AccessAppScope: false`, and it is worth paying. `Title`/`Results` (Input), `Query`/`Picked` (Output), `OnSearch`/`OnConfirm`/`OnCancel` (Event). The flow: the box raises `OnSearch`, the screen reads `Query` into a global, and `Results` binds off THAT global — **not off `cmpTkPeople.Query` directly**, because feeding an instance's own output into its own input is a same-instance cycle Studio may reject, and a rejection costs a paste to find out. One global sidesteps the question entirely. `ShowColumns` narrows SearchUserV2's wide rows to the two the component declares, with column names as **IDENTIFIERS, not strings** (the repo's own validator caught that in a Description string) — src/Screens/scrTaskEdit.pa.yaml

- [2026-08-10] **`Variant: GridLayout` is the second `GroupContainer@1.5.0` variant — CONFIRMED from a Studio code-view photo, closing an "inferred" that both `studio-enums.json` and `powerapp-canvas-controls` had carried since 2026-08-04.** A grid CHILD places itself with four numeric properties — `LayoutGridColumnStart` / `LayoutGridColumnEnd` / `LayoutGridRowStart` / `LayoutGridRowEnd` — and **Studio writes `X`/`Y` on it anyway**, exactly as it does for auto-layout children whose X/Y are ignored, so the presence of X/Y is never evidence that absolute positioning applies. Same photo grounded **`ModernCard@1.3.0`** (new token) and re-confirmed `ModernCombobox@1.1.1` independently. **HALF-GROUNDED, AND THE MISSING HALF BLOCKS AUTHORING: nothing in the sample declares the grid's column count, row count or track sizes** — the container carries only X/Y. So a grid is PLACEABLE but not CREATABLE: safe to author the four child properties into a grid Studio already made, NOT safe to author a `GridLayout` container from scratch. Also inferred and NOT observed: that Start/End are CSS-grid LINE indices rather than cell indices (ColumnStart 3 / ColumnEnd 5 spans 2 columns under the line reading, 3 under the cell reading) — tools/studio-enums.json

- [2026-08-11] **`Parent.Width` in a padded auto-layout container is the FULL width, NOT the content box — every child on `scrProject` overflowed its parent's padding.** Reported as "the items in the task list have labels that are overlapping". A child written `Width: =Parent.Width` is PLACED at the content-box left edge but SIZED to the container's outer width, so it hangs off the right by exactly `PaddingLeft + PaddingRight`. Resolved at 1366x768: `rowWork` ran 24..1390 (24px off-screen) inside bodyRoot's 24px padding, `galTasks` ran 40..850 inside secTasks' 16px padding — 16px past its own card border and onto `colIssues`, which begins at 850 — so `cellTaskWho` ("Assigned to") drew at 615..842, over the issues column. Fixed to `- 48` under 24-padding and `- 32` under 16-padding on all 12 affected children; `rowWork.Height` likewise now subtracts bodyRoot's TOP+BOTTOM padding. **`scrProjectEdit`, the container reference, has ALWAYS written `=Parent.Width - 48` inside its 24-padded containers — the idiom was already proven in this repo and scrProject simply never applied it. When a container screen looks wrong on the right-hand side, check the padding subtraction FIRST.** — src/Screens/scrProject.pa.yaml

- [2026-08-11] **`cardInfo`'s eight generated fact VALUES dropped from `Theme.Size.Body` (14) to `Theme.Size.Small` (12), by instruction.** Caption and value are now the same 12pt; the hierarchy still reads because the value keeps `FontWeight.Semibold` + `Theme.Color.TextPrimary` against the caption's regular `TextMuted`. The card's project NAME was deliberately left at `Theme.Size.H2` — it is the card title, not one of the captioned facts — src/Screens/scrProject.pa.yaml


- [2026-08-11] **"Why not set Y from another control's dimension?" — because X/Y/Width/Height FREEZE at paste, and a sibling reference is evaluated MID-PASTE.** *(Corrected below: it is NOT "banned", and the rename half of this entry was wrong.)* MS Learn: layout formulas "will be overwritten with constant values" once the control is positioned in the canvas editor, and a paste positions controls. So `Y: =cardInfo.Y + cardInfo.Height + 16` is resolved at the instant of pasting, when the referenced control may not yet be where it ends up, and lands as a WRONG constant that never recovers. This repo has the scar: a gallery landed at `Y=193` on top of its own filter row exactly this way, and `validate_pa_yaml.py` still emits NOTEs for it on scrAdmin/scrReference/scrReports. Second failure mode: **Studio re-suffixes colliding names on paste** (`cardInfo` -> `cardInfo_1`), so the reference silently dangles and the control jumps. **What the question actually wants — "position follows the thing above it" — is AUTO-LAYOUT CONTAINERS, which these screens already use: children carry NO X/Y at all, so there is nothing to freeze.** Control-relative positioning is the fragile version of a problem already solved — src/Screens/scrProject.pa.yaml
- [2026-08-11] **The scrProject padding fix is CORRECT AS AUTHORED — it did not land for a transfer reason, not an authoring one.** Audited mechanically: every child on that screen now subtracts its parent's horizontal padding. **And the overflow was never a Y problem — it is horizontal**, so control-relative Y could not have fixed it in any case. **`scrTaskEdit` was the genuinely unswept screen: 20 children fixed 2026-08-11**, and they NEST — `secTk*` sections overflowed `bodyRoot` by 48 AND their own children overflowed the sections by a further 40, so the compound error was 88px. `scrIssueEdit`, `scrTransactionEdit`, `scrProjects`, `scrHome` audit clean; `scrProjectEdit`'s single hit (`lblPrMissing`, `- 512`) is a deliberate reservation for the action bar's buttons, not a bug — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-11] **`tools/audit_container_padding.py` — the padding rule is now MECHANICAL, not prose.** Walks a screen's indentation tree and reports every child whose `Width` does not subtract its parent's `PaddingLeft + PaddingRight`, resolving `Theme.Space.*` tokens. **Its first version had a bug worth remembering: the padding regex only matched literal integers, so `PaddingLeft: =Theme.Space.Gutter` read as 0 and it falsely accused the two controls that were actually CORRECT.** An audit that cannot read the codebase's own idiom will confidently indict the right answer — resolve the tokens before trusting the verdict — tools/audit_container_padding.py

- [2026-08-11] **CORRECTION, twice over: control-relative positioning is NOT banned, and the RENAME explanation for it was wrong.** (a) The validator emits a **NOTE**, and NOTEs are filtered out of the failure set — `scrAdmin`, `scrReference` and `scrReports` all position off a backdrop rectangle TODAY and report 24/24 valid. The rule is also narrower than stated: only **Y and Height** are checked, because anchoring X/Width to a backdrop is deliberately treated as harmless. (b) The validator's message blamed a paste-time rename (`SearchBox` -> `SearchBox_1`) — but `docs/build-history.md` 2026-08-04 explicitly rejects that: "the true origin of Y=193: **NOT an unresolved reference**, but `SearchBox.Y + SearchBox.Height + Gap` evaluated mid-paste and frozen." The tool and the history had drifted; the message now teaches the freeze. **The real distinction, since `Parent.Width - 48` freezes too: Parent's geometry is settled BEFORE its children are laid out, so that constant was correct when it froze and stays correct for a fixed-size app. A SIBLING's geometry is being established by the same paste, so its constant can be a value that was never correct at any instant. Both freeze — only one can freeze to something that was never true.** — tools/validate_pa_yaml.py

- [2026-08-11] **`cmpRecordPicker` — ONE picker for all TEN Lookup columns, NOT one per entity.** User proposed splitting it two ways (tasks/transactions vs clients/products); the split that actually exists is **create vs no-create**, not entity vs entity, and creating turns out not to belong in a picker at all (below). Picking is byte-identical for all ten lookups — task -> project/asset/client/product, transaction -> project/client/product, issue -> project/task/transaction — and the rows already arrive via `Results` because a component cannot touch a data source. A per-entity split would duplicate SEVEN hand-typed properties per copy and buy nothing. **The row shape is generic `{Id, Primary, Secondary}` and the SCREEN projects each list into it**; `Id` rides through `Picked` because a Lookup write needs `{Id, Value}`. Same seven-property contract as `cmpPeoplePicker`, deliberately, so there is one shape to learn — src/Components/cmpRecordPicker.pa.yaml
- [2026-08-11] **"Add a new client from the picker" is a FORM, not a "+", and it is BLOCKED.** `taskmaster_clients` has FIVE required columns (`client_name`, `client_type` Choice, `client_coverage` Choice, `client_sales` **Person**, `client_region_path`) and `taskmaster_products` THREE (`product_uid`, `product_type_path`, `product_description`). Three consequences: (1) a name-only create is impossible — SharePoint rejects the insert, the same class as the `"Project_Type" is required` failure; (2) `client_sales` is a Person, so an inline create would need a people picker INSIDE a picker, which a component cannot host because it clips children to its own bounds; (3) **`client_type`, `client_coverage`, `client_region_path` and `product_type_path` are exactly the columns hit by the Managed Metadata retirement, and `choice_replacement_UNRECORDED` is STILL OPEN** — the replacement Choice columns' names and values have never been supplied, so a create form cannot be authored without inventing tokens. Options put to the user: a dedicated create screen the picker navigates to; relaxing the required flags so a stub can be completed later; or supplying the Choice tokens — schema/schema.yaml

- [2026-08-11] **CHOICE BLOCKER CLOSED — the app never needed the Choice VALUES, only the column NAMES, which were in `schema.yaml` all along.** User's answer: read them with `Choices()` at runtime. `client_type` / `client_coverage` are filled by `ClearCollect(colX, Choices([@taskmaster_clients].client_type))` bound to a combo box — the pattern `scrProjectEdit` already lands for `project_coverage`. So the Managed Metadata retirement needs NO app change and no token is invented. `choice_replacement_UNRECORDED` is replaced by `choice_replacement_RESOLVED`. **The `*_path` columns are a DIFFERENT animal and are unchanged**: `client_region_path` / `product_type_path` are Text holding a composed mapping-list path, still driven by `cmpNestedSelect` over the three live `mapping_*` lists — not Choice, and `Choices()` does not apply — schema/schema.yaml
- [2026-08-11] **`scrClientEdit` + `scrProductEdit` — quick-create is a SCREEN, reachable from exactly two places.** scrAdmin (maintenance) and `cmpRecordPicker`'s "＋ New" (mid-flow). Deliberately NOT in `NavMenu`: reference data is maintained, not browsed to. **The return contract is `gClReturn` / `gPdReturn`**: the picker sets it to name the caller's field, and on a successful save the screen writes the new record into that global as `{Id, Value}` — the shape a Lookup write needs — then `Back()`s, so the user lands back on their form with it already selected. From scrAdmin the global is `""` and the save just returns. **People are NEVER created** — `client_sales` is filled by `cmpPeoplePicker` from Microsoft 365, per the user; only the CLIENT record is created. `product_uid` is a business key SharePoint does not enforce, so the save does a delegable duplicate check (`=` on indexed Text, excluding own ID on an edit) BEFORE writing — src/Screens/scrClientEdit.pa.yaml
- [2026-08-11] **A COMPONENT INSTANCE CANNOT CARRY `FillPortions` OR `LayoutMinHeight` — wrap it in a col container.** Caught by the validator as a paste failure: an instance accepts only its OWN custom properties plus the built-ins, so the auto-layout contract every child of a container needs has nowhere to live on it. The fix is the one the grid work already used ("components wrapped in cols"): a plain `GroupContainer` holds `FillPortions`/`LayoutMinHeight`, and the instance inside just sets `Width`/`Height`. Applies to every `cmpNestedSelect` / `cmpPeoplePicker` / `cmpRecordPicker` instance placed inside an auto-layout container — src/Screens/scrClientEdit.pa.yaml

- [2026-08-11] **A global READ but never `Set()` anywhere has NO TYPE — Studio reports it as unknown, and the repo cannot see it.** User: "Where do gEditClient and gEditProduct live? They show as unknown in each screen." Cause: `scrClientEdit` / `scrProductEdit` read those globals on their Edit path, but the ONLY entry points (scrAdmin's two buttons, the picker's ＋ New) opened both screens in **New** mode, so nothing in the app ever assigned them — and `gEditClient.client_name` cannot resolve against a variable with no type. **The YAML was perfectly valid the whole time**; this class is invisible to a schema validator and only surfaces when a human opens the screen, which across a one-way gap costs a round trip. **The real gap it exposed was functional: scrAdmin could only CREATE reference data, never open any**, so "reachable by the admin panel" was only half true. Fixed by building the maintenance half — two filtered galleries (clients, products) whose row `OnSelect` does `Set(gEdit*, LookUp(...))` behind an `IsBlank` guard — which both defines the globals and completes the screen — src/Screens/scrAdmin.pa.yaml
- [2026-08-11] **`tools/audit_globals.py` — reads every `g*` in `src/`, reports any that is never `Set`.** App NAMED FORMULAS are excluded by parsing them out of `App.pa.yaml`: `gUserEmail` and `gHasPowerBiLicence` are declared rather than assigned and are legitimately never `Set`. **Verified as a real regression test, not an assertion: run against the pre-fix tree it exits 1 and names both globals; against the fixed tree it exits 0.** Third mechanical check now in `tools/`, alongside `audit_container_padding.py` and the app-scope rule — all three encode a defect the user found before the tooling did — tools/audit_globals.py

- [2026-08-11] **`cmpRecordPicker` WIRED across all three edit screens — 8 inline galleries out, 3 instances in, 24 controls removed.** Scope was wider than "clients and products": the same inline search-box-plus-floating-gallery pattern also drove PROJECT on `scrTransactionEdit`/`scrIssueEdit` and TASK/TRANSACTION on `scrIssueEdit`. All eight became a box button + one shared instance per screen, keyed off the `g*Picker` global that already existed. `scrTaskEdit` also lost the now-dead shared picker chrome (`pnlTkPickerBg`, `lblTkPickerHead`, `icoTkPickerClose`), since the dialog brings its own card, heading and close. **Each screen now hosts TWO full-screen component instances (people + record) and their `Visible` gates are DISJOINT by construction** — Lead/Supporter vs Client/Product, Sales vs Project/Client/Product, Assignee/Owner vs Project/Task/Tx — which matters because a visible full-screen instance swallows every click on the screen. `AllowCreate` is on only where creation is in scope (client/product on task + transaction) and false on `scrIssueEdit` — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-11] **The screen projects each list into the picker's generic row shape with `AddColumns(ShowColumns(Filter(...)))`.** `Filter` is a delegable `StartsWith` on an indexed Text column; `ShowColumns` narrows to the three columns actually needed (identifiers, never strings); `AddColumns` renames them to the `{Id, Primary, Secondary}` contract the component declares. That projection is what lets ONE component serve ten lookups — the component never learns which list it is showing. `Id` carries `ID` so `OnConfirm` can write `{Id, Value}` straight into the caller's lookup global without a second read — src/Screens/scrIssueEdit.pa.yaml

- [2026-08-11] **`AddColumns` CANNOT add a column whose name differs from an existing one only by CASE — `ShowColumns(..., ID, ...)` then `AddColumns(..., Id, ID, ...)` is rejected.** User reported errors on all three edit screens "surrounding the add columns for project_name, task_name, transaction_name, client_name, client_type.Value, product_uid" — every one a valid column, which is what pointed away from the names and at the construct. Two defects in one expression: the `ID`/`Id` collision, AND the result carried SIX columns (`ID, client_name, client_type` kept by ShowColumns plus the three added) where `cmpRecordPicker.Results` declares exactly three. **FIX: `ForAll( FirstN(Filter(...), 50) As c, { Id: c.ID, Primary: c.client_name, Secondary: ... } )`** — a record literal emits exactly the declared shape and cannot collide, and `As` scoping is the idiom `scrProjectEdit` already lands. Delegation unchanged: `Filter` still delegates, `FirstN` caps the page at 50 because `ForAll` does not delegate. **The If has NO else branch**, matching the people picker's `Results` that is confirmed working in Studio, so an unmatched picker state yields blank rather than a type-mismatched fallback — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-11] **When a construct errors on arguments that are all individually valid, suspect the CONSTRUCT, not the names.** Six "invalid" tokens across three screens were all correct; the fault was one shared projection idiom copied to all three. A single wrong pattern replicated by a transformer fails everywhere at once, which reads like a data problem and is not — src/Screens/scrIssueEdit.pa.yaml

- [2026-08-11] **`Back()` FIRES `OnVisible` — so any screen with a sub-flow needs a RE-ENTRY GUARD, or returning wipes the form.** User: creating a client/product from the transaction picker cleared the transaction inputs. Cause: `OnVisible` exists to seed a record from scratch, and `Back()` from the create screen makes the caller visible again, re-running the whole thing — wiping what was typed AND overwriting the client/product the create screen had just handed back. FIX: `Set(gTxResume, true)` before navigating away, and `OnVisible` becomes `If( gTxResume, Set(gTxResume, false), <entire existing body> )`. On a return there is nothing to seed; the globals already hold the in-progress values. Applied to `scrTaskEdit` and `scrTransactionEdit`, the two screens with a ＋ New route — src/Screens/scrTransactionEdit.pa.yaml
- [2026-08-11] **A shared `gEditMode` CANNOT span a sub-flow — the create screens now carry their own `gClMode` / `gPdMode`.** `OnCreateNew` was doing `Set(gEditMode, "New")` so the create screen would open in New mode, which silently clobbered the CALLER's mode. Press ＋ New while EDITING a transaction and you returned with `gEditMode = "New"`, so the guarded OnVisible (had it run) would rebuild the screen as a blank new record — losing the record being edited, not merely the typed values. Found while fixing the clearing bug, not reported separately; the two faults compounded. **A global that means "which mode is THIS screen in" must not be shared across screens that can be open at once in a navigation stack** — src/Screens/scrTaskEdit.pa.yaml
- [2026-08-11] **`transaction_sales` no longer defaults to `User()`** — per the user, the sales owner is usually somebody else, so defaulting to whoever is entering the record writes a wrong name silently. It is `required: true`, so the empty state is caught by `lblTxMissing` and the user must choose. **NOT changed, because they are different judgements the user has not made: `gTkLead` (scrTaskEdit) and `gIssAssignee` (scrIssueEdit) still default to `User()`** — the person creating a task usually does lead it, and the person raising an issue usually is the assignee. Flagged rather than swept — src/Screens/scrTransactionEdit.pa.yaml

- [2026-08-11] **`scrProjectEdit`'s STAGED CHILD modals are now on the shared pickers too — the last four inline galleries in the app are gone.** The `mdNt*` (new task) and `mdNx*` (new transaction) modals still used the original search-box-plus-floating-gallery pattern for task lead, client, product and sales owner. `cmpPrPeople` grew from three states to FIVE (Manager/Requestor/Supporter + NtLead/NxSales) and a new `cmpPrRecord` covers NxClient/NxProduct. Both instances are declared AFTER both modals, so a picker dialog paints above the modal that opened it, and their `gPrPicker` gates are disjoint — verified mechanically that all seven states are both set and consumed — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-11] **`AllowCreate` is deliberately FALSE on `cmpPrRecord`, unlike the task and transaction editors.** ＋ New navigates away, and `scrProjectEdit`'s `OnVisible` clears `colNewTasks` / `colNewTransactions` — so a return would destroy STAGED CHILDREN, a far worse loss than the cleared inputs fixed the same day. The resume-guard pattern would fix it, but the failure mode if the guard has a hole is unsaved work rather than retypeable text, so creating a client mid-project stays on the transaction editor and scrAdmin. Flagged to the user rather than silently omitted — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-11] **A regex ending in `\s*` ate a newline and merged two controls — `git checkout --` and redo beats patching a mangled file.** Deleting `Reset(txtX)` from a chip's OnSelect with `re.sub(r'Reset\(...\);?\s*','')` swallowed the line break, so the next control's `- name:` landed inside the previous formula and PyYAML reported duplicate keys. The fix was to REPLACE rather than delete (`Reset(txtX)` -> `Reset(instance)`), which cannot change the line structure. **The validator caught it — a mangled screen was never at risk of being pasted** — src/Screens/scrProjectEdit.pa.yaml

- [2026-08-11] **Project is now DERIVED on all three child editors, not asked for — `scrIssueEdit` and `scrTransactionEdit` follow the pattern `scrTaskEdit` has had since 2026-08-07.** A transaction or issue is always created against the project whose screen it was launched from, so the field was a question with only one right answer. Removed `colTxProject` / `colIssProject`, promoted their grid row-mates (`colTxClient` / `colIssImpact`) into column 1, and dropped the "Project" state from both record pickers — verified every remaining `g*Picker` state is still both set and consumed. `gTxProject` / `gIssProject` are unchanged: still derived in `OnVisible` (from the record on Edit, `gSelProject` on New) and still WRITTEN by the save. The derived project now shows in the screen TITLE, as on `scrTaskEdit`, so it is visible without being editable — src/Screens/scrTransactionEdit.pa.yaml
- [2026-08-11] **A derived required field keeps its missing-fields guard even with no form field to point at.** Both saves still test `gTxProject.Id <= 0` / `gIssProject.Id <= 0` — the precedent `scrTaskEdit` set. The field is gone, but a blank project would still be rejected by SharePoint, and silently, since there would be nothing on screen to explain it. Reworded to say so ("not set — reopen from the project screen") because the user cannot fix it from this form. **Removing a FIELD is not the same as removing a REQUIREMENT** — src/Screens/scrIssueEdit.pa.yaml

- [2026-08-11] **`scrAdmin` DELETED — its reference-data galleries moved to `scrReference`, and NavMenu dropped from five entries to four.** The user's tabbed-Settings rework was scoped down to this consolidation first. `scrReference` had been an empty PLACEHOLDER all along (one label) while the clients/products maintenance lists built the day before lived on `scrAdmin` — so "move the reference data to the reference screen" was really "delete the duplicate destination". Controls renamed `*Admin*` -> `*Ref*` so nothing on the reference screen is called Admin. **NavMenu keys are load-bearing** (every nav gallery Switches on them): key 5 is gone and **key 4 is now the Switch DEFAULT** in all four remaining `OnNavigate` handlers, verified mechanically — every key set is handled and every screen's default arm points at `scrReference`. Two long-standing `positions off` NOTEs disappeared with the placeholder label. Return contracts (`gClReturn`/`gPdReturn` empty = "just go back") are unchanged; only the comments naming the caller moved — src/Screens/scrReference.pa.yaml
- [2026-08-11] **SUPERSEDED SAME DAY — saved settings were DROPPED, see below. Kept for the storage reasoning, which stands if it is ever revisited.** ~~PENDING DESIGN — tabbed Settings, and where settings are STORED.~~ `scrReference` is intended to become the Reference Data TAB of a tabbed Settings screen (user settings · reference data · admin settings); nothing built today needs to move for that. **Storage recommendation given, not yet accepted: TWO SharePoint lists with TYPED columns, not key/value** — `taskmaster_settings_app` (exactly one row, global, read with `First()`) and `taskmaster_settings_user` (one row per user, keyed by an indexed `us_user_email`, read with a delegable `LookUp`). Key/value was rejected because it reproduces this repo's recurring silent-failure class: a mistyped key returns BLANK, never an error, and every value arrives as untyped text. Typed columns also give `Choices()`, direct Power BI reads, and make each new setting a tracked `schema.yaml` change — the discipline already in force. Costs one column + one paste per new setting. **BLOCKED ON THE USER: which settings exist (name, type, per-user or global). Column names cannot be invented.** Would also finally give `gHasPowerBiLicence` a real source instead of the hardcoded `false` — schema/schema.yaml

- [2026-08-11] **SAVED SETTINGS DROPPED — no settings lists, no tabbed Settings screen.** User's call, immediately after the storage design was put to them. So `scrReference` is the reference-data screen full stop, not a tab of something larger, and the app stores **no per-user or global preferences at all**. Consequences worth knowing rather than rediscovering: **`gHasPowerBiLicence` stays hardcoded `false`** in `App.Formulas` — the settings column was the obvious place to give it a real source, and there is now no such place, so the soft gate remains "everyone is unlicensed" until some other signal is supplied; and any future preference has nowhere to persist, since `Set()` globals die with the session and `SaveData` is per-device. **The two-typed-lists recommendation is preserved in the superseded entry above** — if settings return, start there rather than relitigating key/value — src/Screens/scrReference.pa.yaml

- [2026-08-11] **`TriggerOutput.FocusOut` DOES NOT FIRE ON ENTER — confirmed in Studio, and there is NO Enter event on a canvas text input at all.** Both pickers were built with `FocusOut` on the assumption that a single-line input commits on Enter; it does not. FocusOut means only what it says, so Enter did nothing and results appeared only when the box lost focus. **I flagged this exact risk as unverifiable when authoring the people picker — "whether FocusOut fires OnChange on the Enter key specifically, or only on blur" — and the gap has now answered it.** Both components switched to **`TriggerOutput.Delayed`** (debounce: fires once typing stops), which is what the repo already used in all six other places — the pickers were the ONLY `FocusOut` users, i.e. the deviation was mine. Still one directory call per search term rather than per keystroke, so nothing regressed. **The user-facing text was ALSO wrong and had to be swept**: the placeholder said "Type a name, then press Enter", the empty-state said "press Enter to search", and both `Description:` fields — which get HAND-TYPED into Studio — claimed Enter submits. **A behaviour that turns out not to exist leaves lies in every string that described it** — src/Components/cmpPeoplePicker.pa.yaml
- [2026-08-11] **If a spec asks for "press Enter to search", say up front that it cannot be built.** The original picker spec asked for exactly that and it was accepted without flagging the constraint. Canvas offers only `Delayed` (debounce) or an explicit button — recorded in `powerapp-canvas-controls` so the next spec meets the constraint before authoring rather than after a paste — .claude/skills/powerapp-canvas-controls/SKILL.md

- [2026-08-11] **CODE REVIEW BATCH 1 — three correctness defects that were silently writing wrong data.** (a) **`cmpSelection` had no `OnReset`** and was bound to per-record `DefaultId` on three live instances (`scrIssueEdit` status/type/impact, `scrTransactionEdit` currency) with no screen resetting it — so the strip showed the PREVIOUS record's pick while the global, and therefore the save, held the new one. **This is the exact defect that got `scrTask` deleted on 2026-08-10**; the mechanism was recorded that day ("cmpSelection is un-resettable because it HAS NO OnReset") but the live consumers were never checked. Fixed at the source (`OnReset: =Reset(galSel)`) so every consumer benefits, plus `Reset()` calls in each screen's `OnVisible`. (b) **`gClSaved`/`gPdSaved` were never blanked before the Patch** — only the error string was — so the `IsBlank(.ID)` guard could pass on the PREVIOUS record and a silent-failure write reported success and handed the caller's form the WRONG client/product. `scrTaskEdit` always blanked its equivalent. (c) **An issue could never be UNLINKED**: `issue_task_name`, `issue_transaction_name` and `issue_owner` had no `gEditMode = "Edit"` clearing arm, unlike `issue_type`/`issue_impact`, so pressing the chip's ✕ and saving reported success while SharePoint kept the old value — src/Components/cmpSelection.pa.yaml
- [2026-08-11] **A stale comment misled two independent code reviewers into reporting a non-bug.** `scrIssueEdit`'s header said "Reset cannot reach inside a component"; two agents therefore reported all 16 `Reset(<instance>)` calls as no-ops. They are not — `Reset(<instance>)` fires the component's `OnReset`, which both pickers define. **The comment was itself the defect**, and it was written before the OnReset correction the same week. Corrected in place with a note that it misled reviewers. **Wrong comments do not merely fail to help; they actively manufacture false findings** — src/Screens/scrIssueEdit.pa.yaml

- [2026-08-11] **CODE REVIEW BATCH 2 — and one review finding REJECTED as a would-be regression.** Applied: (a) `txtProjSearch` was the ONLY text input in the repo without `TriggerOutput.Delayed`, so every keystroke fired a delegated query against `taskmaster_projects` on the busiest list screen — ~16 round trips to type "Portfolio Review"; (b) `scrProject`'s task list sorted `Sort(colProjectTasks, task_stage.Value)`, a TEXT sort over a Choice, which ordered it Complete, Drafting, Finalizing, Not Started, Planning, Under Review — **finished work at the top** — now sorted by `StageWeights` weight, which the app already owns and which costs nothing on an in-memory collection — src/Screens/scrProjects.pa.yaml
- [2026-08-11] **REJECTED REVIEW FINDING: the `LookUp(taskmaster_projects, …, project_name)` calls on the three edit screens are NOT redundant.** Two independent agents reported them as wasted queries because `schema.yaml` declares `lookup: { show: project_name }`, so `.Value` "already is" the name. **It is not: the LIVE display field on all three `*_project_id` columns is the target's ID**, confirmed in Studio 2026-08-09, so `.Value` returns a NUMBER — which is exactly why those LookUps were added, and the display column cannot be changed in place without UNASSOCIATING existing items. **The root cause of the false finding was that the divergence lived only in a commit message.** `schema.yaml` now records it on all three columns, and each call site carries a "do not optimise this away" note. **Where the golden source and the live system disagree, the golden source must say so — otherwise it manufactures confident wrong advice** — schema/schema.yaml

- [2026-08-11] **CODE REVIEW BATCH 3 — the save fold reached the last two screens that had not had it.** `scrIssueEdit`: one Patch plus FIVE guarded follow-ups became ONE Patch of 13 columns (**6 round trips -> 1**). `scrProjectEdit`: two near-identical Patches (insert vs update) plus FIVE follow-ups became ONE Patch of 16 columns with the base record chosen by `If()` (**9 round trips -> 4**, the remainder being the C3 rollup's LookUp + Filter + write, which is a separate concern and was left alone). Optionals fold as `If(condition, value, Blank())`; `gIssOpt` and `gPrOpt` — globals that existed only to make both `IfError` arms type-compatible — are gone. **The fold also killed the `scrIssueEdit` unlink bug at the root**: batch 1 had added three clearing arms that morning, and folding made them unnecessary because `Blank()` in the record IS the clear — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-11] **Two review findings were fixed INSIDE the fold rather than as a second pass, because the fold rewrites the exact expression they live in.** (a) `project_coverage: { Value: gPrCov }` was unguarded on the INSERT path while the Edit path guarded the identical write, and `gPrCov` seeds to `""` on New with no required-field gate — so an unpicked Coverage sent an empty Choice member, which is not a member, and the connector rejected the insert. Now `If(Len(gPrCov) > 0, {Value: gPrCov}, Blank())`. (b) `project_date_complete` read `gEditProject` unguarded, and **`gEditProject` is never cleared on New** — so a project created after editing a completed one inherited that project's completion date, and `Coalesce` kept the stale date instead of stamping `Now()`. Insert and update are now separated explicitly. Same stale-global class as `gEditTask` on `scrTaskEdit`, still open — src/Screens/scrProjectEdit.pa.yaml

- [2026-08-11] **CODE REVIEW BATCH 4 — four components DELETED: `cmpUiKit`, `cmpEditableGrid`, `cmpStatusPill`, `cmpChoicePill`. 462 lines, ZERO instances**, confirmed by an exhaustive `ComponentName:` and function-call scan. An unused component definition still ships inside the `.msapp` and loads at app start, so they were pure payload. `cmpUiKit` could not have worked as built — its `OutputFunction`s are uncallable without a screen-level instance and no screen had one — and its tone-to-colour logic was deliberately duplicated into `cmpStatusPill`, so there were three implementations of one pill and none in use. **What was lost, stated honestly: galleries render status as flat muted text where a coloured pill would read better.** Adopting the pills was the considered alternative (an `HtmlText` per gallery row plus a screen-level `cmpUiKit` instance); the definitions are recoverable at `e16688c^` if that is ever wanted. **The size win is only real once they are deleted in STUDIO too** — removing them from the repo does not shrink the running app. Component count 14 -> 10; validator 26 -> 22 files — src/Components/
- [2026-08-11] **`docs/notes/components.md` was documenting a ten-component set that no longer matched reality in TWO directions** — it listed four components that are now deleted, and had never listed `cmpAppBar`, `cmpNestedSelect`, `cmpPeoplePicker` or `cmpRecordPicker`, built after it was written. Corrected, and it now points at `.claude/CATALOG.md` as the generated always-current inventory while keeping itself to contract notes. The no-component-inside-a-gallery rule is KEPT even though the components that embodied it are gone: it still governs anyone building a per-row renderer — docs/notes/components.md

- [2026-08-11] **`cmpLookupField` built — the lookup FORM FIELD, companion to the picker dialogs.** The widget was hand-copied NINETEEN times across five screens: 882 lines in 35 controls, caption + "＋ Choose" button + chip + clear ✕. **It had already split into two drifting variants**: `scrProjectEdit`'s chip is a `Classic/Button` whose Text ends in a literal "✕" — which makes the WHOLE chip a clear button, so clicking it to read a truncated value wipes it — while the other four screens use a container with a real `Classic/Icon`. The component adopts the second, better variant. **STATELESS BY DESIGN, so there is no `OnReset` to forget**: the screen owns the value and passes `Display`/`HasValue` in, so the cmpSelection class of stale-display bug cannot occur here. FIVE hand-typed properties: `Caption`, `Display`, `HasValue`, `PickLabel` (Input), `OnPick`, `OnClear` (Event) — **`HasValue` is a separate input rather than `Len(Display) > 0` because callers key on different things** (`.Id > 0` for a lookup, `Len(.Mail) > 0` for a person); deriving it would force every caller to shape a display string just so, which breaks quietly — src/Components/cmpLookupField.pa.yaml
- [2026-08-11] **Proving ground: `scrTransactionEdit` converted first — 779 -> 631 lines, 44 -> 32 controls, for THREE fields.** ~49 lines saved per field, so the remaining 16 fields are worth roughly 780 more. **The instances need no new wrappers**: 18 of the 19 sites already sit inside a `col*`/`row*` container that holds `FillPortions` / `LayoutMinHeight` / `LayoutGrid*`, which a component instance cannot carry — only `scrClientEdit`'s sales field will need one. **YAML trap hit en route: `OnClear: =Set( g, { Id: 0, Value: "" } )` fails to parse** — the `{ Id: 0 …}` reads as a YAML mapping — so any behaviour formula containing a record literal must be a block scalar — src/Screens/scrTransactionEdit.pa.yaml

- [2026-08-11] **`cmpLookupField` ROLLED OUT to all 19 fields across 5 screens — 6261 -> 5417 lines (-844), 61 controls removed.** `scrTaskEdit` -253, `scrProjectEdit` -208, `scrIssueEdit` -184, `scrTransactionEdit` -148, `scrClientEdit` -51. Ahead of the ~470-line estimate because the clusters averaged ~49 lines, not ~25. **Every wrapper held exactly three controls — caption, pick button, chip — verified before replacing**, so blanket replacement of each container's children was safe; only `scrClientEdit`'s sales field sat directly in `bodyRoot` and needed a new `colClSales` wrapper. Verified after: no dangling control references on any screen, and every `g*Picker` state is still both set and consumed on all five screens (7 states on scrProjectEdit alone). **The two drifting chip variants are now one** — `scrProjectEdit`'s five `btn*Chip` buttons with a literal "✕" in their Text are gone, and with them the bug where clicking a chip to read a truncated value cleared the field — src/Components/cmpLookupField.pa.yaml

- [2026-08-12] **`cmpPeoplePicker` + `cmpRecordPicker` MERGED into one `cmpPicker`; the 9 screen instances became 5.** The two components were 802 lines of which ~91% was identical — same geometry constants, same scrim/card/header, same debounced search, same footer, same `OnReset`. **Only the KEY differed**: a record has an ID (`rpPicked.Id > 0 && rpPicked.Id = ThisItem.Id`), a person has none and keys on Mail. **Making the row shape a generic `{Key, Primary, Secondary}` with `Key` as TEXT is the whole merge** — the screen projects `Text(c.ID)` or `u.Mail` into it and writes back `{Id: Value(Picked.Key), Value: Picked.Primary}` for a lookup or `{DisplayName: Picked.Primary, Mail: Picked.Secondary}` for a person. A SharePoint ID is an integer, so `Text()` then `Value()` round-trips exactly. **The instance merge fell out of it for free**: `g*Picker` already named the field being filled, so it can select the row KIND too — one `Visible`, one `Title` Switch, one `Results` If chain, one `OnConfirm`, and ONE `Reset()` target per screen instead of two. `ShowColumns` could not survive the merge (it can only keep the source's own column names, and neither a directory row nor a list row has a `Key`), so **every branch is now `ForAll(… As alias, {record literal})`, people included** — uniform shape is what lets the If chain type-check. Net −337 lines across the screens plus −802 from the deleted components — src/Components/cmpPicker.pa.yaml
- [2026-08-12] **Three picker UX gaps closed in the same pass, because they all live in the file the merge rewrites.** (a) **Zero-results was invisible**: the hint label covered "nothing typed" and "too short" but not "searched and found nothing", so a query matching no rows rendered 588×264 of blank white, indistinguishable from still-loading or broken — now a three-state `If`, ending `"No matches for ""…""."`. (b) **`AccessibleLabel` added to the search box — and REVERTED everywhere else, because the property does not exist on `Classic/*` controls** (user, 2026-08-12). It survives only on `txtPkSearch`, which is a `ModernTextInput`. **The same property was already sitting on two `Classic/TextInput` notional fields on `scrProjectEdit` / `scrTransactionEdit`, so those were latent paste failures too; both removed.** The accessibility cost is real and accepted, not overlooked: `pkRowHit` is an unnamed transparent `Classic/Button` on every result row, so a screen reader still announces the list as "button, button, button", and `Tooltip` — the classic equivalent — is a hover hint, not a name. Converting the row hit target to a modern button is the only real fix and is a separate change. Recorded in the powerapp-canvas-controls skill. (c) **`SetFocus(txtPkSearch)` added to `OnReset` — and REMOVED 2026-08-12 when Studio rejected it.** **`SetFocus` cannot target a control inside a Component, a Gallery, an Edit form OR a Container** (MS Learn `function-setfocus#limitations`); the first exclusion covers every control in this file and the last covers most of the app. `Reset` on the line above was fine because it carries none of those restrictions — **when one of a pair throws and the other does not, that asymmetry is the tell.** **My stated reason for guarding it was WRONG**: I theorised a timing problem (target not yet visible when `OnReset` runs), which reads well and is unfalsifiable across a one-way gap. The restriction is categorical and documented. **When a function is rejected, read its Limitations section before theorising about evaluation order.** CONSEQUENCE, ACCEPTED: the dialog opens with nothing focused and the user must click into the search box; a PCF code component with an `InputEvent` property is the only documented workaround and is not worth it — src/Components/cmpPicker.pa.yaml

- [2026-08-12] **BUSY STATE on all six save buttons — `gSaving`, guarded in `OnSelect`, NOT via `DisplayMode`.** A SharePoint Patch takes seconds and every save button stayed live throughout, so a double-tap ran the whole handler twice and `Defaults()` built a SECOND record. The guard is `If( gSaving, false, <original handler>; Set(gSaving,false) )` with `Set(gSaving,true)` first: **`Set` applies immediately within a behaviour chain, so the flag is already true when the second tap is evaluated.** **Deliberately NO `DisplayMode` gate** — this repo removed those from three save buttons on 2026-08-10 precisely because a disabled button cannot fire `OnSelect` and so cannot explain itself, and a stranded flag would resurrect that dead-click bug wholesale. Feedback is the button TEXT instead (`"Saving…"`). **`Set( gSaving, false )` also runs in every editor's `OnVisible`, and that is not belt-and-braces**: closing an editor mid-save via the header X leaves the flag true, and without the reset the next editor opens on a permanently dead "Saving…" button — src/Screens/
- [2026-08-12] **`scrTaskEdit`'s `task_date_completion` was the one unguarded `gEditTask` read on the screen, and it silently dated new tasks wrong.** Every other read is behind `gEditMode = "Edit"`; this one was not, and **`gEditTask` is never cleared on New**. So a task CREATED straight after editing a completed task inherited that task's completion date — `Coalesce(gEditTask.task_date_completion, Now())` found the stale value and kept it instead of stamping `Now()`. Fixed by splitting insert from update explicitly (`If( gEditMode <> "Edit", …)`) rather than trusting `Coalesce` to notice a blank that isn't there. **Identical to `project_date_complete` on `scrProjectEdit`, fixed 2026-08-11 — the same defect twice means the pattern is the lesson: a Coalesce over a global that survives mode changes is not a default, it is a leak** — src/Screens/scrTaskEdit.pa.yaml

- [2026-08-12] **`scrProjectEdit`'s C3 rollup no longer runs on every save, and the guard could not be written where the rollup lives.** It cost a `Filter` over up to 500 task rows plus a write on EVERY project save — spent in full to rename a project or move its target date, neither of which a stage-weighted average can depend on. Only two things can move it: a NEW project, or child tasks staged in this save. **Neither is testable at step 5**: `Set(gEditMode,"Edit")` has already fired by then, and step 4's `RemoveIf` has stripped the SUCCESSFUL rows out of `colNewTasks`, so a count taken there sees only the failures and reads exactly backwards. `gPrRollup` is therefore captured at the TOP of the handler, before either mutation. **A New project always runs it, and that is load-bearing** — the main Patch never writes `project_perc_completion`, so with no tasks staged the rollup is the only thing that lands the initial 0. Also added scrTaskEdit's second guard: don't spend a write when the rounded average has not moved — src/Screens/scrProjectEdit.pa.yaml
- [2026-08-12] **The LAST `DisplayMode` gate on a save button is gone — `btnPrSave` was missed in the 2026-08-10 sweep.** Four required fields disabled it, and a disabled button cannot fire `OnSelect`, so a project missing one gave a click that did nothing and said nothing. **The blocker was that the gate could not simply be moved: `lblPrMissing.Text` carried a literal `"Still required:   " &` prefix, so `Len(Self.Text)` was ALWAYS non-zero** and the label's emptiness could not be tested — which is exactly why the rule had been restated in a second place to begin with. The prefix moved into the `Notify`, `Visible` became `=Len(Self.Text) > 0`, and the save gate now reads the same string. **Two copies of one rule is how they drift; the fix was to make the label testable, not to duplicate the condition again.** All four editors are now the same shape — src/Screens/scrProjectEdit.pa.yaml

- [2026-08-12] **`issue_owner` and `issue_date_open` RETIRED — provenance comes from SharePoint's system columns, and the best field is the one you don't have.** The user's framing settled it: who opened an issue and when are **informational and must not be user-manipulable**. Both columns were app-written, so both were editable, wrong-able and duplicable; `Created` and `Created By` are stamped by SharePoint on insert and **cannot be patched from the app at all** — `schema.yaml`'s `system_columns` line had said exactly that since the schema was written, so the mechanism was already grounded in the golden source and needed no new column, picker, write or clearing arm. Removed: the "Business owner" people-picker on `scrIssueEdit` (with `gIssOwner`), two record fields from the save, and the picker's fourth state (4 -> 3). Added: a read-only "Opened by <Created By> · <Created>" line in the vacated slot, which says "Recorded automatically when you save" on New rather than rendering a blank that looks like a load failure. **Columns NOT dropped in SharePoint** — they still hold their data; deleting a provisioned column is destructive and is the user's call — schema/schema.yaml
- [2026-08-12] **SUPERSEDED SAME DAY — the local `colArchivedProjects` + `RemoveIf` design is GONE.** It fetched the child rows and only THEN discarded them, so the data row limit still applied to the fetch and past it archived work silently reappeared; the collection also grew for the life of the app. Replaced by a denormalised `*_project_archived` Boolean on all three child lists (see below). The reasoning about WHY a join cannot delegate still stands — that is exactly what the new column buys around.
- [2026-08-12] **`scrHome`'s "My issues" NARROWED to assigned-to-me, and the delegation reason is why it was not replaced like-for-like.** The filter was `issue_assignee.Email = gUserEmail || issue_owner.Email = gUserEmail`. The obvious swap — `|| 'Created By'.Email = gUserEmail` — **would have been a silent disaster: `Created By` is not indexed, and one non-delegable arm inside an `Or` drops delegation for the WHOLE filter**, truncating the home queue at 500 rows with no warning. Semantics agree with the constraint anyway: a work queue is what I must ACT on, not what I happened to raise. Two identical copies of the filter on that screen, both changed — src/Screens/scrHome.pa.yaml

- [2026-08-12] **SCHEMA CHANGE: `task_stage` loses "Under Review" and "Archived"; `issue_status` becomes Open + three Closed outcomes.** Review is signalled by raising an ISSUE; archiving is a PROJECT state. **Removing "Archived" deletes the task-level archiving axis outright** — which is why the six-value Or-chain that existed only to exclude it is gone from `App.Formulas` and FIVE screens (scrHome ×2, scrReports, scrProject, scrTaskEdit, scrProjectEdit) and the one-day-old `UnarchivedTasks` named formula is deleted. **That chain is not "simplified away", it now selects everything** — and would silently drop any row with a blank stage. `issue_status`'s four-way Or collapses to a single delegable `= "Open"`. Knock-ons: `gProjArchived` deleted (it counted archived tasks, so it could only ever return 0, and its "N archived (hidden)" caption could never fire); scrTaskEdit's stage combo 7→5 values, its colour Switch loses two arms, both overdue guards lose `<> "Archived"`, and `task_date_completion` loses the Archived-preserves-the-date arm — src/App.pa.yaml
- [2026-08-12] **`selIssStatus` swapped from `cmpSelection` to a `ModernCombobox`, because the new VALUES do not fit the old CONTROL.** `cmpSelection` lays every option across ONE row (`WrapCount = CountRows(Items)`), so each chip gets `Width/N` — fine for "Open/Review/Waiting/Blocked/Closed", not for three ~19-character "Closed - …" labels at 11pt, which would clip with no way to widen them. **Shortening the text was not available: the Label IS the stored Choice value** (`Set(gIssStatus, …Label)`). `cboTkStage` is the in-repo proven pattern for a list that will not fit a strip; output moves from `.Selected.Label` to `.Selected.Value` — src/Screens/scrIssueEdit.pa.yaml
- [2026-08-12] **StageWeights: the survivors KEEP their original numbers, deliberately.** Dropping "Under Review" (60) leaves a visible 35 → 85 jump at Drafting → Finalizing. Re-spacing would move every project's stored `project_perc_completion` **without a single task changing**, so historical numbers would stop being comparable — a worse outcome than an uneven curve. Recorded in both `App.Formulas` and `schema.yaml` so it reads as a decision, not an oversight — src/App.pa.yaml

- [2026-08-12] **Issue -> task / transaction associations are now PROJECT-SCOPED DROPDOWNS, not tenant-wide search pickers.** An issue belongs to a project, so the only tasks and transactions it can sensibly reference are that project's — the old picker offered every row in the tenant and made you type two characters to find out. A bounded set does not need a search dialog: `colIssTaskOpts` / `colIssTxOpts` are ClearCollected in OnVisible (delegable `=` on the indexed Lookup FK, AFTER `gIssProject` is seeded) and shown in a `ModernCombobox`. **The "(none)" row at Id 0 IS the clear** — Collected first so it sorts to the top, and the save already treats `.Id > 0` as "linked", so picking it writes `Blank()` exactly as `cmpLookupField`'s ✕ did; that is why dropping cmpLookupField here costs no functionality. Both halves are projected to `{Id, Label}` because a record literal cannot be Collected alongside a raw SharePoint row. **`cmpIssPick` falls to ONE state (Assignee)** — its Title stops being a Switch and its Results loses two branches — src/Screens/scrIssueEdit.pa.yaml

- [2026-08-12] **SUPERSEDED SAME DAY — REVERTED, the delete workflow STAYS.** The user's "do not delete items, just filter them out" was about a misreading (they thought archiving a project would delete its children); once corrected, `scrProject`'s cascading hard delete was restored in full. **Do not re-propose replacing it.** The reasoning below is kept only because the mechanism it describes — archiving needs no cascade, because children are already excluded by their parent's phase — is still true and still what the filtering work buys. ~~[2026-08-12] **THE APP NO LONGER DELETES ANY RECORD. `scrProject`'s delete became an ARCHIVE.** It used to hard-delete across FOUR lists behind one confirm — `RemoveIf` issues, transactions, tasks, then `Remove` the project — irreversibly. A separate workflow now logs and removes rows on its own schedule; **the app's only job is to stop showing them.** Four Removes became ONE `Patch( taskmaster_projects, gSelProject, { project_phase: { Value: "Archived" } } )`, and **the children need no touching at all** because they are already excluded BY THEIR PARENT'S PHASE (scrProjects' Active filter + `ArchivedProjects` on scrHome/scrReports). That is why archiving needs no cascade — the filtering work done earlier the same day is what makes this a one-row write. Still two presses, but the reason changed: no longer "there is no undo" (archiving IS reversible — the Archived filter lists them and editing the phase brings one back) but "this removes a project from everyone's view". `Icon.Trash`/Danger -> `Icon.Hide`/Warning; `gPrDel*` -> `gPrArc*`~~ — src/Screens/scrProject.pa.yaml
- [2026-08-12] **REVIEW FINDING CONFIRMED AND FIXED: `colIssStatus`'s two children were both UNPINNED.** `cboIssStatus` declared `Height: =40` and its caption `Height: =18`, but **every auto-layout child is flexible along the parent's direction BY DEFAULT** — so the 54px of free space split 27/27, both declared heights were ignored, and the combo box would have rendered at 27px, below usable. The old `cmpSelection` chip strip tolerated being squashed; a combo box does not, which is why swapping the control exposed a latent bug rather than creating one. Both children now carry `FillPortions: =0` + `LayoutMinHeight`, and the container is 62 -> 66 (18+8+40) inside a 120-high grid cell. **Same latent pattern still in `colIssType`, `colIssImpact`, `colIssMissing`** — all still hold cmpSelection, so left alone — src/Screens/scrIssueEdit.pa.yaml

- [2026-08-12] **ADVERSARIAL REVIEW — one CRITICAL and three real logic holes, all fixed.** (F1) **`Theme.Color.Text` DOES NOT EXIST** — `Theme.Color` defines `TextPrimary` and `TextMuted`. The provenance label I added would have failed the paste, and across this gap that returns as "it didn't work" with no detail. Only occurrence in the repo. (F2) **`gPrRollup`'s capture predicate was not the complement of the Patch's own insert test**: it read `gEditMode <> "Edit"` while the Patch inserts on `gEditMode = "New" || IsBlank(gEditProject.ID)` — so the SELF-HEAL path inserted a project with the rollup skipped, defeating the initial-0 guarantee the guard exists to provide. Now ONE `gPrInsert`, captured once and used in all three places. (F3) **`newPc <> gEditProject.project_perc_completion` compares 0 against a NULL** on a just-inserted row; if Power Fx coerces blank to 0 the write is skipped and the column stays NULL. Invisible in-app (`Coalesce(...,0)` on scrProjects) but **not downstream — Power BI's AVERAGE drops blanks from the denominator, so portfolio completion reads HIGH**. Now compares against `Coalesce(..., -1)`, correct under either semantics. (F8) `issue_status` is `required: true` and written UNGUARDED, but `lblIssMissing` never checked it — same `{Value: ""}` rejection `project_coverage` already paid for — src/Screens/scrIssueEdit.pa.yaml
- [2026-08-12] **A STORED LINK THE OPTION SET DOES NOT CONTAIN IS STILL A LINK (F7).** The new project-scoped dropdowns build their options from `gIssProject`, but an issue can legitimately hold a task/transaction from ANOTHER project — the old picker searched the whole tenant — or one since deleted. `DefaultSelectedItems` then found nothing, the box read "Select…", **the screen told the user there was no related task while the save wrote the link straight back**. My comment claiming it "always resolves to a row" was simply false. OnVisible now Collects the stored `{Id, Value}` into the option set when it is missing, so the display matches the record and the user can change or clear it deliberately. This also closes a PLAUSIBLE silent-clear path (if `Reset()` fires `OnChange` with a blank Selected) — src/Screens/scrIssueEdit.pa.yaml

- [2026-08-12] **ARCHIVED WORK IS NOW EXCLUDED AT THE SOURCE — `*_project_archived` on all three child lists.** The aim the user set is a THRESHOLD one, not a speed one: **keep the rows in scope for every list safely under 2000 so nothing can silently truncate.** A local filter cannot serve that by construction — it drops rows that have already been fetched, and the fetch is what the limit applies to. A child row carries only `*_project_id`, never the parent's phase, so the only delegable answer is to denormalise: `task_project_archived` / `transaction_project_archived` / `issue_project_archived`, Boolean, INDEXED, default false. **Boolean not a phase copy** — a copy would be a second source of truth for something the project row owns. New named formulas `LiveTasks` / `LiveTransactions` / `LiveIssues`; `OpenIssues` now composes both axes (`issue_project_archived = false && issue_status.Value = "Open"`). `ArchivedProjects` and all the collect/RemoveIf machinery deleted from scrHome (x2) and scrReports — schema/schema.yaml
- [2026-08-12] **THE CASCADE HAS EXACTLY ONE HOOK, AND IT REFUSES RATHER THAN HALF-DOING IT.** `project_phase` is written in ONE place — scrProjectEdit's save Patch — so that is where the mirror is kept true. **Only on a FLIP** (`gPrArcFlip`, captured at the top of the handler alongside `gPrInsert`, because step 4 re-reads `gEditProject` and destroys the old phase): rewriting every child on every project save would be a ForAll over hundreds of rows to change nothing. **`ForAll` + `Patch` DOES NOT DELEGATE** — it pulls up to the data row limit and writes one row at a time — so a project with more children than that would be PARTIALLY cascaded, **which is worse than not at all**: the leftovers keep the old flag and quietly stay in or out of every query. Each list is therefore counted FIRST and the whole cascade refuses with a warning above 500, the same shape as the rollup's refusal. All three standalone child editors write the flag on EVERY save (not just insert) from `gSelProject.project_phase`, so a row whose project was archived while the editor sat open cannot come back live. The three New-child buttons on scrProject are hidden on an archived project — src/Screens/scrProjectEdit.pa.yaml

- [2026-08-12] **ANSWERED: `Filter(<named formula that is itself a Filter>, <more predicates>)` FOLDS into ONE server query.** Verified by the user in **Live Monitor** — a single `getRows` returning already-filtered rows, not the repeated walk a client-side filter produces. **Neither of my first two proposed tests could have answered it**: the blue underline is a one-way signal ("not every nondelegable case shows this warning" — MS Learn), and "the gallery looks right" is worthless because a non-delegable query looks perfect on a small list. Live Monitor is the only test that produces evidence a human can read back across the gap, and it is now §6 of the delegation reference.
- [2026-08-12] **ROLLOUT: every CROSS-PROJECT query now filters over a named formula; the rules have ONE definition.** `scrHome` (x4 — both OnVisible and the Refresh action), `scrReports` (x2), `scrProjects` (x1). `ActiveProjects` 2 consumers, `LiveTasks` 3, `OpenIssues` 2. **PROJECT-SCOPED queries deliberately stay on the raw list** — 13 of them — because opening an archived project should show its work, and one project's children are bounded anyway. **The sharpest case is scrProjectEdit's cascade: it must read EVERY child including the ones it is about to flag**, so filtering a Live* set there would make it skip exactly the rows it exists to fix. `scrProjects`' other two branches also stay raw: branch 3 IS the archived view, and the default means All — src/App.pa.yaml
- [2026-08-12] **`LiveTransactions` and `LiveIssues` DELETED after one day — zero consumers each.** Every transaction query in the app is project-scoped, and the only cross-project issue query wants OPEN issues, which `OpenIssues` covers. **A named formula nobody calls is not free**: it is a second place for the rule to drift from the schema, unnoticed precisely because nothing exercises it. Add one when a query needs it. Related call: `OpenIssues` writes its archived predicate out rather than layering over a `LiveIssues` — that would nest a named formula inside a named formula, and **what Monitor proved is that ONE level of composition folds; two is an inference**, with a silently truncated home queue as the failure mode — src/App.pa.yaml

- [2026-08-12] **REVERSED BY EVIDENCE — `Theme` MOVED TO `App.OnStart`, and I was wrong to refuse twice.** The user tested `Set` of the theme record in a control and it worked immediately, while the `Formulas` version did not resolve in the published app. **The docs say named formulas are always available; the running app said otherwise, and the running app wins.** MS's own theming example is in fact `App.OnStart: Set(BGColor, …)` -> `Label1.Fill: BGColor`, so this is a documented first-class pattern, not a workaround. **THE NAME DID NOT CHANGE, WHICH IS WHY THIS COST NOTHING: `Set(Theme, …)` makes `Theme` a GLOBAL with the same identifier, so all 939 `Theme.*` references across 11 screens and 9 components keep resolving — a change of DEFINITION SITE, not a rename, and zero screens were touched.** `NavMenu`, `StageWeights`, `ClaimPrefix`, `gHasPowerBiLicence` and `gUserEmail` moved with it, since they would fail the same way. **The three data-source filters STAY in `Formulas` and must not move**: `Set(x, Filter(...))` captures a table value, and filtering a variable does not delegate — it would silently cap every cross-project query at the data row limit. Accepted cost: OnStart is non-blocking, but every statement here is a literal with no round trip, so the render window is vanishingly small — src/App.pa.yaml
- [2026-08-12] ~~**REJECTED: moving `Theme` from `App.Formulas` to `App.OnStart`.** It is the intuitive fix for "the theme never lands" and it would make things WORSE, for a documented reason. A named formula's "value is always available — there is no timing dependency, no OnStart that must run first before the value is set, no time in which the formula's value is incorrect" (MS Learn, object-app#formulas-property). **`OnStart` is the opposite**: it is non-blocking by default, and "a screen can render and become interactive before App.OnStart finishes running" — so a `Set(gTheme, …)` there produces exactly the intermittent squished-and-black being diagnosed, only harder to reproduce. It would also cost a rename of every `Theme.*` reference in 11 screens and 9 components. **The symptom is a TRANSFER failure, not a placement problem**~~ — SUPERSEDED: the rename premise was also wrong (keeping the name costs nothing), and the theory lost to a test — src/App.pa.yaml

- [2026-08-12] **`Theme` COLLIDES WITH A BUILT-IN — renamed to `gTheme` across 946 references.** The user called it: enabling modern controls also enables modern THEMING, which defines an `App.Theme` property and loads theme objects into the global namespace by instance name (MS Learn: *"Reference any theme loaded into the app by its instance name"*, *"Theme Name ... must be unique within the app"*). **Studio TOLERATED a named formula called `Theme` for months, and that is what made it expensive — the collision never errored.** It surfaced as colours resolving inconsistently and the header vanishing, because `cmpAppBar`'s `Height` IS `Theme.Space.HeaderH`, so a wrong resolution silently became 0. **THE RULE: a contested identifier does not have to be REJECTED to ruin you; it only has to resolve to something other than what you meant, somewhere you were not looking.** `gNavMenu` / `gStageWeights` / `gClaimPrefix` renamed alongside for consistency with the repo's existing `g` convention for globals — src/App.pa.yaml

- [2026-08-12] **COMMENT PURGE — `src/` went from 2228 comment lines to 688 (0.27 -> 0.08 per line of code) with the CODE BYTE-IDENTICAL**, verified file by file against the previous commit. The user's objection was right and it was about my habit, not a one-off: I had been writing the same reasoning into the file, the commit message AND the memory ledger, so `src/` was carrying a third copy that rots the moment either of the others changes. **Deleted: every date, every "was X" / "moved" / "superseded", every account of what an earlier session got wrong, 45 lines of commented-out reference code, and seven file-header design essays (150 lines on scrProjectEdit alone).** Kept: only what stops someone breaking the code — AccessAppScope limits, delegation traps, "declared LAST" z-order, hand-typed component contracts, paste-freeze warnings. **The rule is now in CLAUDE.md** so it binds future sessions rather than depending on restraint — `.claude/memory/` holds reasoning, git holds history, comments hold constraints.

- [2026-08-12] **POST-RENAME CODE REVIEW — 8 defects fixed, and the worst one was a delegation trap hiding inside a correctness check.** `scrProductEdit`'s duplicate-`product_uid` guard was `LookUp(taskmaster_products, product_uid = … && ID <> If(gPdMode="Edit", gEditProduct.ID, 0))`. **Only `=` delegates on `ID`; the relational operators do not** (delegation.md `[id]`), so the whole predicate ran locally over one page and a clashing UID outside it saved silently. Self-exclusion moved OUT of the query and INTO the `If`, which restores folding and keeps the semantics. **The general shape: a non-delegable clause added to make a check STRICTER makes it weaker, because the query it poisons is the check.** Also fixed: `numTxNotional` was the only input never `Reset`, so the last transaction's notional was written onto the next new one; `scrIssueEdit`/`scrTransactionEdit` saves had no `IsBlank(g*Saved.ID)` re-entrancy guard (double-tap = duplicate row) while `scrTaskEdit`/`scrClientEdit` did; the `scrTaskEdit` rollup compared against a blank `project_perc_completion`, which coerces to 0 and so suppressed the write for genuinely-0% projects (now `Coalesce(…, -1)`); `gPrDelScrap` held four differently-shaped records under one name; `scrHome`/`scrReports` `OnVisible` now re-seeds `gUserEmail` when blank, because **OnStart is non-blocking and a screen can render before it finishes**. Staleness from the `gTheme` rename swept out of `schema.yaml`, `.claude/context/schema.md`, the `studio-transfer` skill, `formula_bar_body.py` and `docs/notes/*` — those still called `Theme`/`NavMenu`/`StageWeights`/`ClaimPrefix` named formulas — src/Screens/

- [2026-08-12] **CODE REVIEW BATCH 2 — three of the four were the same failure mode: a value read from the wrong place, with nothing on screen to show it.** (a) The three child editors stamped `*_project_archived` from `gSelProject.project_phase` — **a gallery snapshot taken when the project was opened**, so a project archived since then wrote `false` and the child stayed in scope forever, defeating the column's entire purpose. Now a live `LookUp(taskmaster_projects, ID = gSelProject.ID, project_phase.Value)` at write time: one delegable query, and it is the only reading that is true at the moment of the write. (b) `scrProject`'s transaction and issue headline counts read `CountRows(gal*.AllItems)`; **a gallery bound to a DATA SOURCE loads pages as the user scrolls**, so both undercounted until scrolled to the end — counted from the query now. (c) Project delete ran a non-delegable `RemoveIf` per child list; **a partial pass raises NO error**, so the parent could be removed on top of survivors, and a child holding a required lookup to a deleted project cannot afterwards be edited or deleted by anything. Counted first, refuses above 500, same shape as the archive cascade — and the failure message now admits the children may already be gone, because there is no transaction to roll back. (d) `scrTransactionEdit`'s notional check was literally `If( false, … )`; the column is optional so an empty box is fine, but unparseable text saved as blank silently — `Format: =TextFormat.Number` plus a real predicate. **`AllItems` and a cached parent record are both "the value I already have"; neither is the value the write needs.** — src/Screens/

- [2026-08-12] **`'Created By'` DOES NOT RESOLVE IN THE APP — the issue-provenance change is REVERSED, and the rule it teaches is bigger than this screen.** The user checked in Studio: the canvas SharePoint connector does not surface `Created By` on `taskmaster_issues`, so `gEditIssue.'Created By'.DisplayName` referenced a column the app cannot see. **THE COLUMNS THE LIST HAS AND THE COLUMNS THE APP CAN SEE ARE NOT THE SAME SET.** `system_columns:` in schema.yaml documented the former and I read it as the latter — it now says so explicitly and tells future sessions not to reference those tokens from Power Fx at all. `Created` was dropped with it rather than split the fix across a mechanism only half-confirmed. **`issue_owner` and `issue_date_open` un-retired** — neither was ever dropped from the live list, so nothing to provision; `join_cost` back to 5, still well under the block at 8. They are stamped by a **SECOND Patch that runs on insert only**, because **a field NAMED in a Patch is written, and on an edit there is no value to write that is not either a rewrite or a clear — omitting it is the only way to leave a column alone.** Failure warns rather than errors, since the issue itself is already saved by then. **Not-user-manipulable is preserved by offering no control, not by relying on SharePoint's read-only-ness** — which was always the actual requirement. `scrHome`'s "My issues" stays assigned-to-me: `issue_owner` is unindexed, so an Or-arm on it drops delegation for the whole filter — src/Screens/scrIssueEdit.pa.yaml

- [2026-08-12] **THE HARSH HOVER WAS A DEFAULT NOBODY WROTE.** A classic `Button` with no `HoverFill` derives one from `Fill` by **dimming it 20%** (MS Learn, Button control). Every transparent full-template hit button in this app therefore had a hover fill computed from `RGBA(0,0,0,0)` — and because **the hit button is declared LAST so it can catch the click, whatever it paints covers the row's own labels**. Hovering a row turned it into a solid block with the text gone. **The bug was in the properties NOT written, which is why no amount of reading the colour formulas would find it.** Fixed on all 11: `HoverFill` = 5% wash of the primary, `PressedFill` = 10%, matching what `cmpStatusCard.cardBtn` had been doing correctly all along. `gTheme.Color.Hover` / `.Press` added so screens share one definition; components keep literals because `AccessAppScope: false`. **`cmpAppBar`'s hamburger takes a WHITE wash** — it sits on the blue header bar, where a blue tint reads as nothing. **RULE: a transparent control that sits on top of content has no safe defaults — every state colour must be written, because each one is derived from a fill that means "invisible".** — src/Components/cmpAppBar.pa.yaml

- [2026-08-12] **`Classic/Icon` DOES take `HoverFill` / `HoverColor` / `HoverBorderColor` — user confirmed, and my caution was the wrong call.** MS Learn's own property lists omit Icon from `HoverColor` and `PressedColor`, so I shipped the button fix and left 16 icons alone rather than risk a rejected paste. **The docs' per-property "Applies to" lists are incomplete, and treating an omission as a prohibition cost a round trip.** The correct reading: an omission means UNCONFIRMED, not unsupported — ask, or ground it another way. All 16 icons now pin `HoverColor` explicitly, because the Icon default DIMS `Color` the way Button dims `Fill`: the white back arrows went grey on the blue bar, and decorative icons shifted under a passing cursor. Treatments are per-role, not uniform: back arrows keep white + a 15% white wash; **the three delete/remove icons go RED on hover** (the default dimmed the grey, which on a destructive control says nothing about what it does); `icoDeleteProject` takes a RED wash, not the shared blue one, because a blue tint behind a red icon reads as a different control; and **`navIcon` is pinned to its own `Color` expression** since `navHit` owns the click — an icon dimming under the cursor would fight the row wash and make one row look like two controls. **`Pressed*` was still NOT set on icons** — that family really is unconfirmed for Icon and nothing forced the question — src/Components/cmpAppBar.pa.yaml


## Threads          (open items; remove when closed)
- **THE RECORDS TRACK "IN STUDIO", NEVER "PUBLISHED" — and those are different apps.** 24 landings
  logged as in-Studio/landed, zero publishes, across the whole project. **Studio runs the latest
  SAVED version; end users run the last PUBLISHED one**, so the player can be arbitrarily far
  behind everything this ledger calls "landed". That gap is the likeliest reason the published app
  renders black while Studio is perfect. **From now on a hand-off is not done at "it works in
  Studio" — record SAVED + PUBLISHED separately**, because "landed" has silently meant only the
  first for the entire project.
- **CLOSED 2026-08-12 — the published app renders properly (user confirmed).** Kept because the
  diagnostic is the reusable part. **SQUISHED AND BLACK AND NO APP BAR = the theme record IS BLANK
  IN THE PUBLISHED APP (root-caused 2026-08-12, after I got it wrong twice).** ONE cause, every symptom, because blank coerces to 0
  and to black: 11/11 screens `Fill: =Theme.Color.Bg` -> black; 66 controls positioned off
  `Theme.Space.*` -> 0 -> stacked at the origin; and **all 4 `cmpAppBar` instances take
  `Height: =If(gNavOpen, Parent.Height, Theme.Space.HeaderH)` -> 0 -> the bar VANISHES.**
  **THE APP BAR IS THE TEST.** No display setting, aspect ratio or window size can delete a
  control — distortion stretches, it never removes. Missing bar = Theme; present-but-distorted =
  geometry. **FIX: REPUBLISH** (Studio runs the latest SAVED version, the player the last
  PUBLISHED one); if that fails, `App.Formulas` never committed — App checker, then
  `tools/formula_bar_body.py --bare`.
- **The Scale-to-fit / Lock-aspect-ratio divergence is REAL but was the WRONG ANSWER here** — it
  explains distortion and nothing else. Still worth setting Lock aspect ratio ON for this
  fixed-canvas app, but do it AFTER the Theme problem, and as its own change.
- **Still true as a hazard: `App.Formulas` defining nothing.** Reported 2026-08-12 after the
  formula-bar paste. Every named formula lives in ONE property, so one rejection takes out `Theme`,
  `NavMenu`, `StageWeights` and `ClaimPrefix` at once; every screen then reads blank for every
  colour and every `Theme.Space.*` dimension, and blank coerces to 0. **The repo source is
  structurally sound — 9 named formulas, all terminated, parens balanced (verified) — so this is a
  TRANSFER failure, not an authoring one.** Two causes: the leading `=` marker (yields `==`), or a
  COLLAPSED formula bar flattening the paste so the opening `//` comment swallows the property.
  **Fix: expand the formula bar, or paste `python3 tools/formula_bar_body.py --bare` (41 lines, no
  comments).** Decisive check: type `Theme.` in any Fill — no IntelliSense members means nothing
  was defined. **DO NOT move Theme to App.OnStart** — see the Decisions entry.
- **F6 is now MOOT** — the collection it described no longer exists. (F11) the C3 rollup no longer self-heals —
  guarding it removed the only repair path for a number already wrong; a "Recalculate completion"
  action beats reinstating the unconditional cost. (F12) the `RemoveIf` predicates shadow `ID`
  between two record scopes — correct as written (innermost wins) but worth `As` aliases.
- **SHAREPOINT IS DONE (user, 2026-08-12): the three `*_project_archived` columns are in, and the
  task_stage / issue_status value edits are placed.** Nothing SharePoint-side blocks the paste.
  **If tasks or issues go missing from Home after this lands, look FIRST at whether any child row
  has a NULL `*_project_archived`** — NULL is not `false`, so a `= false` predicate excludes it.
- **PASTE ORDER FOR THE UNLANDED BATCH — sequencing is the main risk, not tokens.** Reviewed
  2026-08-12; no invented columns, no ungrounded control tokens, paren-balance clean, 22/22 valid.
  1. **`App.Formulas` FIRST** (formula bar only — the App object has no code view; strip the
     standalone `=` marker line or you get `==`). `ActiveProjects` / `LiveTasks` / `OpenIssues` are
     NEW names: a screen pasted before them fails to resolve, and the human reports "didn't work"
     against the wrong file.
  2. **`scrIssueEdit` is the only CREATE-paste**, and go by container subtree — `colIssStatus`,
     `colIssTask`, `colIssTx`, `colIssOpened` — then rename off the `_1` suffix and delete the old
     `selIssStatus` / `fldTask` / `fldTx` / `fldOwner`. Paste creates, it never patches.
  3. Everything else is PROPERTY-LEVEL. Hand over control name + property + formula body (no
     leading `=`) rather than re-pasting screens, which re-freezes every X/Y/Width/Height.
  **Do NOT delete `cmpSelection` or `cmpLookupField`** — still consumed by `selIssType`,
  `selIssImpact`, `fldAssignee`.
- **Known edge, accepted:** an issue whose linked task/transaction belongs to a DIFFERENT project
  (only possible from the old tenant-wide picker) shows a BLANK dropdown while still being linked.
  `OnChange` never fires, so the save preserves the link — display-only, no data loss. Negligible
  with example data; revisit only if real cross-project links exist.
- **SharePoint migration for the Choice-value change: USER IS HANDLING IT, and it is a non-issue —
  the lists hold only example items (confirmed 2026-08-12). Closed; do not re-raise.**
- **Three auto-layout containers still have BOTH children unpinned** — `colIssType`,
  `colIssImpact`, `colIssMissing` on scrIssueEdit. Declared heights are ignored and the space
  splits evenly. Harmless while they hold `cmpSelection` strips, which squash gracefully; fix
  before ever swapping one for a combo box, as `colIssStatus` had to be.
- **PERF PROPOSAL ASSESSED 2026-08-12 — front-load non-archived items into app-scope collections.
  VERDICT: right instinct, wrong vehicle; do the named-formula half, not the collection half.**
  - **BLOCKER, grounded:** named formulas "can't use behavior functions or otherwise cause side
    effects" (MS Learn, object-app#formulas-property), so **`ClearCollect` CANNOT live in
    `App.Formulas`**. The "init in Formulas, call on refresh" design cannot be built as stated.
  - **The prize is smaller than it looks.** Archived is ALREADY excluded everywhere it can be
    delegably: `scrHome` + `scrReports` filter to the 6 live task stages / 4 live project phases,
    and `scrProjects` has an Active option with the same Or-chain. Those two screens ALREADY
    ClearCollect (colMyTasks / colMyProjects / colMyIssues) at screen scope.
  - **The "therefore" does NOT hold for children.** A task/transaction/issue does not carry its
    parent's phase, and `taskmaster_transactions` has no status/stage column AT ALL. Excluding
    children of archived projects needs a join or an `in` over a set — neither delegates. It needs
    a denormalised flag on the child lists, i.e. a schema change, or it cannot be done at source.
  - **Risks of app-scope collections here:** ClearCollect from SharePoint truncates SILENTLY at the
    Data Row Limit (max 2000); this is a MULTI-USER app so a launch snapshot goes stale and every
    save site would need write-through; and MS warns OnStart both slows launch and can race
    ("a screen can render and become interactive before App.OnStart finishes").
  - **CORRECTED 2026-08-12 — I HAD CROSSED TWO AXES, and the user caught it.** (1) PROJECT
    archiving is `project_phase = "Archived"`; (2) a CHILD'S OWN lifecycle is `task_stage =
    "Archived"` / `issue_status = "Closed"`. **My first draft filtered children on axis 2 while
    presenting it as axis 1.** Both are real — the C3 rollup genuinely depends on axis 2, and
    StageWeights omits Archived for that reason — but **archiving happens at the PROJECT level
    and does not live in the child row at all.** `ActiveTasks` renamed `UnarchivedTasks` and both
    child sets are now documented as axis 2 explicitly.
  - **STAGE 1 SHIPPED 2026-08-12** — `ActiveProjects`, `UnarchivedTasks`, `OpenIssues`,
    `ArchivedProjects` in `App.Formulas`; `scrProjects`' Active branch as the delegation canary;
    and the real ask implemented on the THREE cross-project child loads (`scrHome` OnVisible +
    Refresh, `scrReports`): collect `ArchivedProjects` in the same `Concurrent`, then `RemoveIf`
    the children **locally**. No `ActiveTransactions` on either axis — that list has no status
    column at all, so a transaction's liveness is ENTIRELY its project's.
  - **NO SCHEMA CHANGE WAS NEEDED, and that was the whole finding.** The denormalised-flag plan
    (stage 3) exists to make the exclusion delegable server-side — but **every cross-project child
    query is already narrow** (user-scoped "my tasks"/"my issues", or `FirstN 50` in a picker) and
    every other child query is scoped to ONE project, where you WANT its children. A local
    membership test over an already-local collection is correct and free. **Stage 3 is now
    unnecessary unless Monitor proves otherwise.**
  - **STILL LEAKING, deliberately not changed:** `scrIssueEdit`'s picker (lines ~795/801) can
    still offer a task or transaction belonging to an archived project when linking an issue.
    `FirstN 50` runs BEFORE any local removal, so filtering after would return fewer than 50 rows
    rather than the right 50. Low-value edge case; flagged rather than bodged.
  - **BLOCKING ON A BINARY:** does `Filter(ActiveProjects, StartsWith(...))` still delegate?
    Nested Filter over a named formula is the one unproven step, and **a delegation failure here
    is SILENT — it truncates at 500 rows rather than erroring**, so the signal is the BLUE
    UNDERLINE in Studio, not runtime behaviour. If it underlines: revert that branch to the inline
    Or-chain (git 72e19c4) and stop stage 1 there. If clean: tranche B converts the other 6 sites
    across scrHome (x4), scrReports (x2), scrProject, scrTaskEdit, scrProjectEdit.
  - **App.Formulas is FORMULA-BAR ONLY** — the App object has no code view, so tranche A is a
    formula-bar paste plus one screen re-paste.
  - **Honest sizing: stage 1 is a DE-DUPLICATION win, not a speed win.** A named formula still
    queries SharePoint per use; it does not cache across screens. The speed question is stage 2/3
    and should not be started before Monitor says where the time actually goes.
  - **(2)** collections ONLY for slow-changing reference data (clients, products, mapping_*), the
    genuine cache candidates. **(3)** child-of-archived-project exclusion LAST, only if Monitor
    justifies it, because it is the only step that costs a schema change.
- **The 2026-08-11 code review is CLOSED.** Its last item (index `issue_owner` /
  `issue_date_open`) was dissolved rather than done — both columns are retired in favour of
  SharePoint's system columns. **One index is still worth adding: `Created` on
  `taskmaster_issues`**, since `scrProject` now sorts the issue gallery on it and an unindexed
  sort fails outright past 5,000 items. SharePoint-side, not a paste.
- **Await the Studio binary on scrProject's padding fix** (13 formula-bar edits, handed off
  2026-08-11). If labels STILL overlap, padding overflow is ruled out — ask which two labels
  touch and whether the columns sit too far left or too far right.
- **Sweep `scrTaskEdit` and `scrIssueEdit` for `Width: =Parent.Width` inside a padded container.**
  Mechanical: for every auto-layout container declaring `PaddingLeft`/`PaddingRight`, each child's
  `Width` must subtract their sum. `scrProjectEdit` is known-clean (it is where the idiom came from).
- **Second hook candidate, now with evidence:** a write-time check on `src/` flagging any child
  whose `Width` is `=Parent.Width` while the parent declares horizontal padding. Pure arithmetic
  over the YAML tree, no Studio needed — it would have caught this at author time.
- **Ask the human for a `GridLayout` container's OWN code view** — one that has had its columns
  and rows configured in the Studio pane. That is the only missing piece before a grid can be
  authored here rather than merely filled.
- **Six superseded COMPONENTS still to delete in Studio** — `cmpPeoplePicker`, `cmpRecordPicker`,
  `cmpUiKit`, `cmpEditableGrid`, `cmpStatusPill`, `cmpChoicePill`. All instance-free now that
  `cmpPicker` has landed, but they still ship inside the `.msapp` and load at app start.
  **Deleting from the repo does not shrink the running app**; only the Studio delete does.
- **`scrIssueEdit` still saves with guarded follow-up Patches** (the pattern folded out of
  `scrTaskEdit` on 2026-08-10). Same fold available if its save also feels slow;
  `scrTransactionEdit` already writes in a single Patch and needs nothing.
- **The Choice columns that replaced Managed Metadata (2026-08-10) have NO recorded names or
  values, so no code can target them.** Needed from the user: the internal name of each, its
  allowed values, and whether they SUPERSEDE the `*_id`/`*_path` + `mapping_*` + `cmpNestedSelect`
  design or sit beside it. See `schema.yaml -> choice_replacement_UNRECORDED:`.
- Open questions Q3–Q10, Q13 + Q2b (PBI workspace/refresh/embed) + Q5 (index master?) + tmIndices taxonomy source → `.claude/context/open-questions.md`
- Propose upstream to claudeBrain: `studio-transfer` + `pre-paste-review` + the new `power-apps-svg` / `power-apps-editable-table` skills (all general); flag PnP/CSOM gap.
- Decide whether to build the column-token validator write-time hook.
- ~~Settle whether this tenant has MODERN CONTROLS OFF.~~ **ANSWERED 2026-08-06: they are ON.**
  (The follow-on claim that `@1.1.1` was the defect is RETRACTED — `@1.1.1` is correct for the
  combo box and the text input. Versions are per-control; unsure → author the classic control.)
- **Awaiting the user's Studio test of the `ModernTextInput@1.1.1` properties** (`Type:`,
  `TriggerOutput:`). If it fails, the fallback is `Classic/TextInput@2.3.2`.
- **Both reworked screens are unverified in Studio** — `scrProject` and `scrTaskEdit` need FULL
  re-pastes. Orphaned controls the human must delete by hand: `tabProject`, `ProjMeta`,
  `btnNewChild`, `lblTasksMeta`, `tabTaskEdit`, four `cmpSelection` instances, `dtpTkDone`,
  the `mdNi*` modal.
- **Owed by the user, in SharePoint (blocks saving a project):** set the retired Managed Metadata
  columns (`project_type`, likely `project_region`/`client_type`/`client_coverage`/`product_type`)
  to Required = No, or delete them.
- The transactions section on `scrProject` reads "N loaded" rather than "completed / total" —
  the schema carries no completion state for a transaction. Ask what split the user wants.
- **Verify in Studio that the 7 converted pickers on `scrProjectEdit` render.** The screen was
  rebuilt from `5edcd66` and needs a full re-paste, not a property edit.
- **AUDIT the other three edit screens** (`scrTaskEdit`, `scrTransactionEdit`, `scrIssueEdit`) for the
  two silent defects found in `scrProjectEdit` on 2026-08-05: (a) a `ModernDatePicker` with no
  `DefaultDate` on an edit path, which patches `Blank()` over the stored date with no error; and
  (b) hardcoded `Items: =[…]` arrays that have drifted from the Choice column's `values:` in
  `schema.yaml` (three invalid values shipped undetected). Not yet started.
- **Extend the column-token validator to look inside `Items: =[…]` array literals** and check them
  against the matching Choice column. This is the concrete case for the hook above — the three bad
  Choice values were invisible to every check the repo currently runs.
- **If `scrProjectEdit`'s paste fails, suspect the container NESTING DEPTH first.** Every token is
  photo-confirmed, but `frmPrScroll → row* → col*` is three deep and that depth has never crossed
  the gap. Fallback: flatten `col*` into single-column rows (caption immediately followed by its
  control) — keeps the label/field pairing fix, loses only the multi-column layout.
- **Order, if any of it is ever rebuilt:** screens exist first (nav references them by name), then
  `App.Formulas`, then the screen contents — the data screens read `StageWeights`/`ClaimPrefix`, so
  `App.Formulas` cannot come last. Tablet layout. Data row limit 2000.
- **Licence gate SETTLED (soft gate, greyed-but-reachable).** Only optional follow-up left: supply
  a real signal for `gHasPowerBiLicence` (config allow-list column or Entra group) — a one-line
  change, since nav and the Reports screen both read it.
- **Component transfer — SETTLED after two wrong turns.** A component is a **contract** (custom
  properties — typed by hand, no paste gesture, and `pac` refuses it with PA3004) plus a **body**
  (child controls — pastes like a screen). Consequence: body control tokens are a **real paste
  payload**, so `HtmlViewer@2.1.0` / `Classic/Timer@2.1.0` / gallery `Variant` are genuine risks,
  not documentation. → `components/_COMPONENTS-NOTES.md`.
- **Follow-up:** run `/reindex` to regenerate CATALOG (pull-reconcile now deprecated). Minor
  incidental two-way mentions left in `build-hooks.py`/`claudebrain-inventory.md`.
- **Phase-2 component composition DONE for 6/10** (static data on Home/Reports/Projects).
  Remaining 4 are gallery/data-bound → compose when data galleries wired: `cmpUiKit` pills +
  `cmpStatusPill`/`cmpChoicePill` in row templates, `cmpEditableGrid` on the Tickets tab.
- **Phase-2 DATA bound + audited (2026-08-03)**; blockers fixed, but still DO-NOT-PASTE on the PREREQUISITE: nothing is provisioned and paste-log is empty. Was blocked on provisioning (true internal names — `schema.md`
  ⟨capture⟩) + a confirmed pull. Every live query is a `TODO(Phase-2-data)` in the screens.

- **Schema intake COMPLETE (7 lists) and promoted to `.claude/context/schema.md`.** Outstanding: `asset_library` schema never supplied (blocks `task_output_asset`).
- **Schema open_recommendations (now EDITABLE — repo is golden source): C1** multi-person no delegable filter;  Settle BEFORE provisioning — names/types freeze at creation. → `schema/schema.yaml` open_recommendations
- **Schema consequences needing a call** (→ `context/schema.md` §Consequences): **C1** multi-person cols have no delegable filter; **C4** `task_date_start` is Calculated (nothing delegates); **C5** no USD-normalised notional; **C3** no writer for `project_perc_completion`; **C6** region modelled 3 ways; **C8** casing anomalies before provisioning.


- **Next physical step:** HANDOFF.md Stage 1 smoke test (paste scrReference) — proves the channel before the expensive component build.

- **C10 CLOSED — no cache, no seeding, nothing to provision.** Two things to watch at first paste,
  both visible on screen: the raw term **path** the picker prints (confirms the `;` delimiter), and
  whether any vocabulary is losing terms to the **20-term `Choices()` cap**.
- **One hand-built write shape is still UNEXECUTED against this tenant:** expanded USER for Person
  columns (`ClaimPrefix`). Not first-party; isolated in its own `Patch`. The MM write no longer
  belongs on this list — it hands back the connector's own record. Cheapest test = save ONE project
  with ONE region.
- **Power BI OWES the blended notional (consequence of Q14).** An FX dimension (currency, rate,
  effective date) + a measure converting `transaction_notional` at the trade date. Until it exists
  there is no cross-currency figure anywhere — accepted cost, not an oversight.
- **One Power Automate flow to author, unblocked (Q12 = yes):** the list-provisioning flow, **8
  lists** with explicit internal names — supersedes manual UI. *(The term-store sync flow that was
  here is no longer needed: the app reads the term store directly.)*
- **Studio prerequisite before the edit screens:** add the **Office 365 Users** connection. An
  unrecognised connector name is a PASTE failure, not a runtime one.


## Log              (append-only pointers)
- 2026-07-26 1726 | repo init: adopt + author .claude asset set; foundational decisions | sessions/2026-07-26-1726-repo-init-decisions.md
- 2026-08-02 1702 | review aprildunnam + PM-tracker; distillation + template decision + screen map | sessions/2026-08-02-1702-external-repo-review.md
- 2026-08-02 1411 | Phase-1 core shell authored (theme + NavMenu + 5 screens); pre-paste audit; dialect corrected to modern structured schema | sessions/2026-08-02-1411-phase1-core-shell.md
- 2026-08-02 1520 | 6 reusable components authored (v3.0 ComponentDefinitions); audit found+fixed cmpSelection double-fire + Output-reads-var | sessions/2026-08-02-1411-phase1-core-shell.md
- 2026-08-02 1545 | merged PR#2 shell + PR#3 components to main; +4 extra components (cmpSectionHeader/ConfirmDialog/Toast/KpiRing SVG), audit PASTE-clean | sessions/2026-08-02-1411-phase1-core-shell.md
- 2026-08-02 1610 | merged PR#4; Phase-2 composition — component instances (static data) on Home/Reports/Projects; instance dialect grounded; audit clean (only known gates) | sessions/2026-08-02-1411-phase1-core-shell.md
- 2026-08-03 | schema intake → golden source → data layer → pa-yaml validator → C10 cache decision + cmpTermPicker → four CRUD screens with staged children | sessions/2026-08-03-crud-screens-and-term-picker.md
- 2026-08-03 | Q12/Q14/licence-gate answered: FX conversion moved to Power BI (C5 reversed), flow-as-provisioner adopted, soft licence gate | sessions/2026-08-03-crud-screens-and-term-picker.md
- 2026-08-03 | C10 revised: cache list deleted, picker cascades on the term Path from Choices(), MM write hands back the connector's own record | sessions/2026-08-03-crud-screens-and-term-picker.md
- 2026-08-03 1526 | example .msapp reviewed → authored .pa.yaml confirmed conformant; fixed Windows cp1252 crash + param bug in validate/split tools; added tools/requirements.txt | sessions/2026-08-03-1526-windows-tooling-fix.md
- 2026-08-03 1540 | pac 2.10.1 installed; packed a verified data-independent smoke-test .msapp (build/) via deprecated .fx.yaml layout; empirically refined the msapp dead-end call; Studio-open unconfirmed | sessions/2026-08-03-1526-windows-tooling-fix.md
- 2026-08-05 | scrProjectEdit reviewed + rebuilt on auto-layout containers: floating-Save fixed at the anchor level, caption/field pairing restored, 6 defects found (unparsable OnVisible, date-wipe on Edit, 3 invalid Choice values, dropped issue_impact, always-open modal galleries) | sessions/2026-08-05-scrprojectedit-usability-rebuild.md
- 2026-08-06 2059 | all 10 combo boxes → Classic/ComboBox@2.4.0 (DisplayFields/SearchFields); found + repaired 360-duplicate-key corruption in scrProjectEdit from 445f59d; validator now rejects duplicate keys | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-06 2140 | modern controls confirmed ON; all 10 combo boxes back to ModernCombobox@1.0.0; the real defect was the @1.1.1 token — pa-yaml @version is the TEMPLATE version, not Studio's UI revision number | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-06 2215 | blank classification picker root-caused: invented `active` column + singular `mapping_region`; both fixed, other two mapping list names still unconfirmed | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-06 2245 | ModernCombobox restored to @1.1.1 per the live Studio control; my version-trap conclusion retracted — the earlier failure was modern controls being disabled, not the token | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-06 2320 | separator -> " | "; commit tick dropped (OnChange is the acceptance); save no longer infers success from an empty error message — the bug that blanked the name and failed forever | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-06 2350 | root-caused the save failure: Patch's return record lacks project_name, which blanked the child Lookup Value AND the name box; re-read the record + surface the child error message | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0010 | staged chip galleries re-fitted (TemplatePadding eats both edges); "Project_Type is required" traced to a retired MM column still Required on taskmaster_projects | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0035 | "cannot be converted to a number" on a pick: seed depth was laundered through a hidden Label's Text and read back with Value(); now computed inline via CountRows(Split(...)) and the label deleted | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0105 | staged tag strips cut 100px -> 38px (one row); component OnReset + Reset(instance) clears the pickers between records; phase starts blank and is gated | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0150 | staged children rebuilt as three fixed-height columns with auto-layout template rows; project delete added (children first, two-press) | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0230 | staged galleries scroll properly (FillPortions); issues off scrProjectEdit -> raised from scrProject; delete moved to scrProject; class note removed | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0300 | staged columns 332px / gallery 288 = 9 exact rows; FillPortions pinned at all THREE nesting levels, not just inside the column | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0330 | scrProject: tab Default made an item (was a string, erroring); tab band re-spaced off the real 48px tab height so the task callout stops colliding with the list | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0430 | scrProject fully reworked: info card + KPI ring + icon actions, tasks/transactions stacked left, issues detail list right; tabs and gProjTab gone | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0540 | scrTaskEdit reworked: tabs->optional output toggle, coloured state combos, derived completion date, overdue caption, project dropped | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0620 | real modern versions applied (TextInput 1.1.1, DatePicker 1.0.1); ModernButton+NumberInput converted to classic (version unknown); Mode->Type and DelayOutput->TriggerOutput fixed | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-07 0710 | scrTaskEdit: completion date off the form, People/Dates 2x2, static output caption, new Client toggle section | sessions/2026-08-06-2059-classic-combobox-and-corruption.md
- 2026-08-09 0110 | session log written; the 2026-08-06 "@1.0.0 correction" marked SUPERSEDED in place; INDEX State corrected (no `active` column on the mapping lists, no `@1.0.0` modern tokens) | sessions/2026-08-09-0108-modern-versions-and-screen-reworks.md
- 2026-08-10 1746 | scrTask DELETED (no Reset() anywhere -> stale Default seeds silently saved the previous task's values); scrProject task row now goes straight to scrTaskEdit via LookUp + blank guard; scrTaskEdit is the sole task writer | sessions/2026-08-10-1746-consolidate-task-editor.md
- 2026-08-10 1812 | transaction+issue rows given the same LookUp + blank guard (freshness, not identity - those galleries bind to a live Filter, not a snapshot); MM blocker recorded RESOLVED in schema.yaml, Choice replacement columns flagged UNRECORDED | sessions/2026-08-10-1746-consolidate-task-editor.md
- 2026-08-10 1905 | save buttons no longer gate on DisplayMode.Disabled (a disabled button can't fire OnSelect, so a missing required field = silent dead click); requirement moved into OnSelect with a Notify naming the field, on scrTaskEdit + scrIssueEdit + scrTransactionEdit | sessions/2026-08-10-1746-consolidate-task-editor.md
- 2026-08-10 2015 | task save optimised 12 round trips -> 1 on a typical edit: 8 optional Patches folded into the main one, C3 rollup skipped unless the stage moved, project write skipped when unchanged, Concurrent on the two rollup reads | sessions/2026-08-10-1746-consolidate-task-editor.md
- 2026-08-11 1325 | scrProject task-list overlap root-caused: `Parent.Width` is the OUTER width, so every child of a padded container overflowed by its padding; 12 widths + 1 height fixed, cardInfo fact values -> 12pt | sessions/2026-08-11-1325-scrproject-padding-overflow.md
- 2026-08-12 | cmpPeoplePicker + cmpRecordPicker merged into cmpPicker on a generic {Key, Primary, Secondary} row (Key is TEXT: Mail for a person, Text(ID) for a record); 9 screen instances collapsed to 5, one Reset target per screen; zero-results state added; AccessibleLabel (classic-only failure) and SetFocus (component/container restriction) both reverted after Studio rejected them | INDEX Decisions 2026-08-12
- 2026-08-12 | cmpPicker + cmpLookupField + all five re-pasted scr*Edit screens LANDED in Studio; both reverted properties confirmed by that landing (AccessibleLabel is modern-only, SetFocus cannot target a control inside a Component/Container); Studio-side deletions still outstanding | INDEX Decisions 2026-08-12
- 2026-08-12 | busy state (gSaving) on all six save buttons, OnSelect-guarded not DisplayMode-gated, with an OnVisible reset; task_date_completion stale-gEditTask leak fixed by splitting insert from update | INDEX Decisions 2026-08-12
- 2026-08-12 | scrProjectEdit: C3 rollup guarded (flag captured at the top of the handler, since RemoveIf and Set(gEditMode) both destroy the evidence by step 5) + no-op write guard; btnPrSave's DisplayMode gate removed, lblPrMissing made testable | INDEX Decisions 2026-08-12
- 2026-08-12 | issue_owner + issue_date_open retired in favour of the Created By / Created system columns; read-only provenance line on scrIssueEdit; scrHome's My issues narrowed to assigned-to-me to keep the filter delegable | INDEX Decisions 2026-08-12
- 2026-08-12 | post-rename code review: 8 defects fixed (scrProductEdit `ID <>` delegation trap in the duplicate check, numTxNotional never Reset, 2 missing save re-entrancy guards, blank-coerces-to-0 rollup guard, gPrDelScrap type conflict, gUserEmail re-seed for the non-blocking OnStart) + the gTheme-rename staleness sweep | INDEX Decisions 2026-08-12
- 2026-08-12 | code review batch 2: *_project_archived now read live at write time (gSelProject is a stale snapshot), gallery AllItems counts replaced with query counts, project delete pre-counts and refuses above 500, dead notional validation replaced + Format: TextFormat.Number | INDEX Decisions 2026-08-12
- 2026-08-12 | 'Created By' does not resolve in the canvas app (user confirmed in Studio); issue_owner + issue_date_open un-retired and stamped by an insert-only second Patch; schema.yaml system_columns now warns not to reference them from Power Fx | INDEX Decisions 2026-08-12
- 2026-08-12 | harsh hover root-caused: classic Button derives HoverFill from Fill by dimming 20%, so transparent hit buttons declared last painted a solid block over their row's text; HoverFill/PressedFill now explicit on all 11, gTheme.Color.Hover/.Press added | INDEX Decisions 2026-08-12
- 2026-08-12 | Classic/Icon accepts HoverFill/HoverColor/HoverBorderColor (user-confirmed; MS Learn's Applies-to lists are incomplete). All 16 icons pinned, per-role: back arrows white+wash, delete/remove icons go red, decorative icons pinned to their own Color | INDEX Decisions 2026-08-12


# ARCHIVE — second prune, 2026-09-04

INDEX.md was 552 lines against its ~80-line budget, with a State block of 123 lines
against its own ~10. Moved here VERBATIM, nothing rewritten or shortened:
the State block as it stood, the 2026-08-13..2026-08-20 Decisions bulk, and the
2026-08 Log pointers. INDEX.md keeps current truth, every 2026-09 decision, the
pre-2026-08-13 digest, and the entries promoted for still being load-bearing.

Append-only. Do not edit or reorder.

## State as it stood 2026-09-04 (superseded by the rewritten State in INDEX.md)
## State            (rewrite in place — current truth only, ≤ ~10 lines)
- **THE GITHUB BACKLOG IS EMPTY — ZERO OPEN ISSUES (verified 2026-09-04, 40 closed).** #45–#53
  are all closed and LANDED, #52's `scrProjects` included (`galProjects.Items` 155 -> 84 lines,
  phase vocab hoisted onto `ActiveProjects` / `OpenProjects`); `StartsWith(<Text col>, "")` is
  REJECTED OUTRIGHT so an off state must be a BRANCH (#53 won't-fix). **There is no queued work.
  Do not invent a backlog from these notes — ask.**
  Detail: sessions/2026-09-04-scrprojects-mine-and-items-epics.md
- **LANDED AND EXERCISED 2026-09-04 (merged, PR #56).** The picker empty-query fix
  (`scrTaskEdit` / `scrProjectEdit` / `scrTransactionEdit` — each record kind split into a typed
  branch and a no-Filter off state) and `client_sales` made OPTIONAL in `scrClientEdit` +
  `schema/schema.yaml`, Person write guarded by `Blank()`. **The user confirmed BOTH proof cases
  tested and working: opening a picker without typing, and clearing a set sales owner and saving.**
  This one is a genuine behavioural confirmation, not merely a paste that Studio accepted — the
  distinction the perf backlog below exists to warn about.
  Detail: sessions/2026-09-04-1650-picker-empty-query-defect.md
- **THE PERF BACKLOG #27–#40 IS COMPLETE — all fourteen landed and closed (user, 2026-08-19).**
  **NONE OF IT WAS EVER MEASURED**: every one landed on a paste confirmation, with no Live
  Monitor count, payload figure or wall-time reading taken before or after. The gains are what
  the source now does, not observed numbers. **Do not let a later session cite them as results.**
- **PASTE QUEUE — EMPTY as of 2026-09-04. Nothing is authored-but-unlanded.** `scrHome`'s Division-by-0 fix
  (`dfb5080`), the 2026-09-03 raw-list audit (5 sites) and the three health derivations moved onto
  `OpenIssues` are all **PASTED, TESTED AND CONFIRMED by the user**. The long-standing "authored
  but unverified" caveat on them is discharged — it no longer applies and should not be repeated.
  **#52's `scrProjects` IS ALSO CONFIRMED** (user, 2026-09-04) — the paste queue is fully empty.
- **THE APP IS PAST ITS DESIGN ASSUMPTION: 2000+ projects and 2000+ tasks backfilled (user,
  2026-09-02), plus a working archival flow.** Reported symptom: recent non-archived projects and
  their children are in the lists but not in the app. Three causes identified (see Decisions);
  **unresolved, and only the user can discriminate — it needs two SharePoint views: `project_phase`
  is empty, and `task_project_archived` is empty.** The 5,000 list-view threshold is now the
  live constraint on every unindexed filter (#47 checked the projects list against it and is closed).
- **`taskproduct_project_archived` is LIVE in SharePoint, recorded in `schema/schema.yaml`, and
  `scrTaskEdit` WRITES it false on every link it creates (writer gap closed).** Indexed/default
  state is still UNCONFIRMED (carried in the column's `review:`), and **links that predate the
  column are blank until back-filled** — the writer covers new rows only.
- **BRANCHES: `main` is authoritative.** Only `main` and `claude/powerapp-repo-init-xymvlm` exist;
  PR #41 merged the branch into `main` with identical trees, and the branch was restarted from
  `origin/main` before later work went on top.
- The `scrProbeDateNull` probe HAS RUN and its result is recorded; it can be deleted from Studio.
- **LIVE IN STUDIO, the whole perf set:** `scrProject` folds tasks/transactions/issues once in
  `Concurrent` (`colProjectTx`, `colProjectIss`) and every count, gallery, title and total reads
  the collections; the five edit screens cache 20 reference fetches once per session; the
  `scrTaskEdit` / `scrIssueEdit` save handlers hoist their pre-Patch reads into one `Concurrent`
  (`gTkParentProj` / `gIssParentProj` replace two LookUps each, keyed on the screen's OWN parent
  id, not `gSelProject.ID`); `scrTaskEdit` skips the taskproduct reconcile when the staged set
  matches `colTkProductsLoaded`; and `scrHome` holds its whole dashboard fold in ONE hidden
  button (`btnHomeRecompute`, two callers), resolves work-list project names from `colPrjNames`
  instead of per visible row, and counts `gIssLed` off one `colIssByPrj` fetch with a `>= 2000`
  fallback to the old per-project queries. **EVERY ONE OF THESE LANDED ON THE PASTE ALONE — no
  round-trip count was ever measured.** Unexercised: the `Concurrent` independent-failure path, the
  `CountRows(col) <= 1` guard against a genuinely empty fetch, a save that removes or clears
  products, and the archived mirror under a project archived mid-edit.
- **BUILT — editing and refining, not creating.** 11 screens, 10 components, the App object.
  22/22 valid. **The published app renders properly** (user, 2026-08-12).
- **App object is two formula-bar properties:** `OnStart` holds the constants (`gTheme`,
  `gNavMenu`, `gStageWeights`, `gClaimPrefix`, `gUserEmail`); `Formulas`
  holds only the three data-source filters, which **must stay named formulas**.
- **`src/` IS FULLY LANDED AS OF 2026-09-04** — all 11 screens, all 10 components and the App
  object are in Studio and current with the repo. The 2026-09-02/03 work (Division-by-0, the
  raw-list audit, the health derivations) and the 2026-09-04 work (#52's scrProjects, the picker
  empty-query fix, optional `client_sales`) are all pasted and confirmed.
  **`task_output_audience` and `task_output_approval_flag` are LIVE** on `taskmaster_tasks`.
- **#23 AND #24 ARE LANDED AND CLOSED (user, 2026-08-19).** `scrIssueEdit` carries required
  type/impact with defaults Question/Moderate; `scrHome` carries the Projects-I-lead gallery
  and, in place of the timeline, a clamped days-to-due bar chart and an effective-health donut.
- **#25 IS BUILT AND UNLANDED — `scrProjectEdit` QUEUED, AND SHAREPOINT MUST CHANGE FIRST.**
  `project_coverage` is `required: true` with default **Internal**; "(none)" is gone and the
  save is unguarded. **THE COVERAGE VOCABULARY IS STILL UNKNOWN TO THIS REPO** — schema.yaml
  carries `Internal` plus a PLACEHOLDER — so the seed falls back to the first offered option if
  `Internal` is not actually a member. Supply the real list before provisioning.
- **#26 IS BUILT AND UNLANDED — `scrReports` QUEUED (repo-only, no SharePoint change).**
  Band 4 is now three panels: the started/completed/net-open combo chart moved left, a NEW
  **projects-by-`project_phase` pie** (fixed 5-value sequence, not `Distinct`, so colours and
  order are stable), and a NEW **open-tasks-due bar chart, today + 14 days, keyed on `Today()`
  and NOT on the period strip**. `rowRptAge` is gone with `gRptAgeA..D`/`gRptStalled`;
  `gRptNoTarget` survives on the pie's subtitle. The combo chart's viewBox was cut 900 -> 480
  because `ImagePosition.Fit` would otherwise render its 15pt labels at ~7px in a third-width
  column. **Backlog is now empty.**
- **`src/` COMMENTS WERE PURGED 2026-08-19** — 2914 -> 1689 lines, density 18% -> 11%,
  1054 lines gone. Repeated notes now live ONCE per file as numbered header rules.
  **Do not re-inflate:** a comment earns its place only if it stops someone breaking the
  code. **This needed NO PASTE** — `#` comments never reach Studio, and `//` changes ride
  each file's next real paste.
- **SUBTITLE TEXT TRIMMED 2026-08-19 — `scrReports` AND `scrHome` ARE QUEUED FOR PASTE**
  (this half IS user-visible). Ten chart/panel subtitles cut 1471 -> 771 chars (48%).
  **Rule applied: cut the PROSE, keep the NUMBERS** — every reconciliation count (no
  supporter, no completion date, no target date, overdue, unassigned, unspecified)
  survives. Panel and label heights unchanged, so the geometry cannot regress.
- **ISSUE #14 IS CLOSED** (completed, by user, 2026-08-18). The multi-product junction write is
  no longer an open defect; do not reopen the diagnosis.
- **STANDING RISKS AND UNEXERCISED BEHAVIOUR — landed is not the same as exercised.** Do not
  claim any of these work:
  - **scrProjects contains-search is bounded by the DATA ROW LIMIT and fails SILENTLY.** Confirm
    Settings > General > Data row limit is 2000. Fine today because the list is small; nothing
    in the app will say when that stops being true.
  - **#22 leaves existing rows stale** — nothing back-fills `task_status` and there is no bulk
    recompute, so a task keeps its last user-chosen health until an issue on it changes or it is
    saved. The overdue half is live and correct immediately.
  - **#22's seven `issue_type` values are taken on trust.** If SharePoint's Choice does not hold
    exactly Blockage / Exception / Limitation, those issues read Amber instead of Red — silently.
  - The completion gate has never once blocked a save (unexercised under #13 and #21 alike), and
    the confirm dialog has never been seen to open. (The Division-by-0 fix IS now proven —
    confirmed by the user 2026-09-04.)
  - On scrReports: the truncation banner has never fired, `gRptDoneNoDate` has never been
    non-zero, the audience pie's `Other` slice has never been non-zero, and no trend bar has yet
    carried more than one currency.
- **`project_phase` is DERIVED BY THE APP, not picked** (2026-08-13): open issue -> Stalled;
  started task or any transaction -> Active; any child -> Planning; nothing -> Not Started.
  Vocabulary is exactly Not Started · Planning · Active · Stalled · Complete · Archived.
  Complete comes ONLY from scrProject's Mark-complete button; Archived ONLY from an external
  flow, which now also owns the three `*_project_archived` child flags. Complete and Archived
  are terminal. PENDING IN SHAREPOINT: set the column default to `Not Started`. **No orphaned controls: the user DELETES a screen before pasting it back.**
- Six superseded components still ship inside the `.msapp` — see Threads.
- **claudeBrain PR #36 is OPEN and unmerged** (2026-08-19) — the canvas-app family ported out.
  It asks one question back: genericise the ported skills' `tools/` / `schema/` path references,
  or leave them as worked-example voice? Answer it before assuming the port is finished.
- **DOCS: `docs/manuals/` EXISTS — a user manual and a maintainer manual, 7 chapters each**
  (2026-08-20). Docs only, no paste. They DESCRIBE; `schema/schema.yaml`, `src/`,
  `.claude/memory/` and `docs/build-history.md` still DEFINE. The stay-in-step table in
  `maintainer/03-making-a-change.md` is the only new synthesis — check it before a hand-off.
  **`docs/screen-map.md` is HISTORICAL and reads as current** (tmTickets, an Admin screen,
  tmLookups — none of which shipped); labelled as such in `docs/README.md`, not edited.



## Decisions 2026-08-13 .. 2026-08-20 (moved out of INDEX.md)
- [2026-08-13] **THE APP NO LONGER ARCHIVES ANYTHING, AND THAT MOVED A LOAD-BEARING JOB OUT OF IT.** Removing scrProjectEdit's phase picker also removed the only place the app wrote `project_phase`, and therefore the cascade that mirrored it onto `task_/transaction_/issue_project_archived`. **Those three flags are what let every cross-project query exclude archived work with a delegable `= false` instead of a join — the external cleanup flow now owns BOTH halves, and a flow that sets the phase without the child flags leaves the children in every query.** The column's whole vocabulary is **Not Started · Planning · Active · Stalled · Complete · Archived** (user-confirmed); `Under Review` and `Blocked` are gone from it and from every enumeration. **`ActiveProjects` is now that list minus Archived, so it must be kept in step with the column — an allow-list fails CLOSED, and a value added in SharePoint and not added there vanishes from the app rather than erroring** — INDEX Decisions
- [2026-08-13] **ROOT CAUSE, CONFIRMED FIXED — `Selected` was never declared as an OUTPUT property in Studio.** A custom property of any other kind cannot reference the component's own controls, so the formula was rejected and every consumer read BLANK: issue type and impact silently not writing, status coming back empty so the form said "needs status". **A component crosses the gap in TWO parts and only the CONTROLS paste — every custom property is declared and typed by hand, so this repo can be correct while Studio is not, silently.** Check the property KIND before touching a screen. The two entries below were both wrong about the cause: neither the control type nor the vocabulary was at fault. Checklist now lives at the top of `cmpSelection.pa.yaml` — INDEX Decisions
- [2026-08-13] **PARTLY WRONG (see above) — the cmpSelection conversion below was a fix for the wrong problem.** `issue_type` was not landing because the strip's LITERAL option list did not match the live column, so the app wrote a label the Choice would not accept. The controls are back to `cmpSelection`, now fed from `Choices()`. **`Choices()` returns only `Value`, and cmpSelection needs `{Id, Label}` — synthesise the Id POSITIONALLY with `Sequence(CountRows(ch))` + `Index(ch, n)`, and look the default up BY LABEL, because those Ids depend on the order SharePoint returns.** The lesson is about diagnosis, not controls: **the user's own follow-up ("use Choices") named the cause, and I kept acting on my earlier timing hypothesis instead of re-reading it** — INDEX Decisions
- [2026-08-13] **SUPERSEDED BY THE LINE ABOVE — plausible but never evidenced.** `cmpSelection` RAISES OnChange BEFORE ITS OWN OUTPUT SETTLES. `optBtn.OnSelect` is `Select(Parent); cmpSelection.OnChange()`, so a consumer's `Set(gX, sel.Selected.Label)` reads `Selected` in the same behaviour chain that just changed it — and can get the PREVIOUS pick. **It fails SILENTLY: the strip highlights correctly while the global, and therefore the write, holds something else.** `issue_status` was moved to a `ModernCombobox` on 2026-08-12 and has been right since; `issue_type` (user-reported not landing) and `issue_impact` followed on 2026-08-13. **`selTxCurrency` on scrTransactionEdit is the last consumer left on the strip** — same defect, on a REQUIRED column. Prefer `ModernCombobox@1.1.1` for any single-select bound to a global — INDEX Decisions
- [2026-08-17] **`transaction_sales` IS SALES-SIDE AND IS NOT A WORKLOAD ATTRIBUTION PATH** (user-confirmed). Desk person-productivity has exactly TWO paths, both from tasks: `task_lead` ("owns") and `task_supporter` ("supports"), counted separately and NEVER summed into one number. `transaction_sales` stays out of `colRptPerson` — the person who sold the trade did not do the desk work the screen measures. A sales-to-transaction analysis may come later; that is a SEPARATE VIEW, not a third column in the unpivot — INDEX Decisions
- [2026-08-17] **CROSS-CURRENCY CONVERSION IS OUT OF SCOPE — other tools handle FX.** C5/Q14 always sent `transaction_notional` per-currency only; dropping Power BI briefly left the blended figure unowned, and the user has settled it as DELIBERATELY unowned. The app never converts, never stores a converted amount, and never shows a blended total. **Do not "fix" this by adding a rate column or a conversion at write time** — a write-time rate freezes whatever number the app happened to see, which is exactly what C5 rejected. A request for one cross-currency number is a request for something the model is designed not to give — INDEX Decisions
- [2026-08-12] A gallery bound to a DATA SOURCE pages as the user scrolls, so `.AllItems` is what is on screen, not the total. Count from the query — INDEX Decisions
- [2026-08-12] A global's type unifies across every `Set`; one name holding differently-shaped records is a type conflict — INDEX Decisions
- [2026-08-06] Modern controls are ON in this tenant — ARCHIVE
- [2026-08-05] A component is a CONTRACT: custom properties are hand-typed in the editor, and `AccessAppScope: false` means it cannot read `gTheme` or any data source — colours are literals — ARCHIVE
- [2026-08-12] Layout formulas FREEZE at paste, so cross-control geometry references can land as a constant that was never correct — ARCHIVE
- [2026-08-12] COMMENTS ARE NOT A CHANGELOG (now enforced in CLAUDE.md): git holds history, `.claude/memory/` holds reasoning, comments hold constraints — ARCHIVE
- [2026-08-15] **The Power Platform skills flow OUT to `claudeBrain`, never in.** This repo was seeded from that factory, but its copies last moved 2026-07-24 and are now *wrong*, not merely stale — `sharepoint-list-architecture` there still teaches a 12-join ceiling where this repo proved **8**, and its `delegation.md` says flatly "`= Blank()` delegates" without the Person/Lookup/Choice trap or the Live Monitor procedure. **Never "sync from upstream" for a `power-*`/`sharepoint-*`/`graph-*` skill — it would regress corrections paid for in failed pastes.** The operational engines (`build-hooks.py`, `catalog.py`, `context.py`, `memory.py`, the shared hook fragments) ARE identical and safe to take — INDEX Decisions
- [2026-08-15] **A `README.md` in `.claude/commands/` or `.claude/agents/` becomes a live asset.** The harness registers every `.md` in those flat layers by filename — an imported `commands/README.md` appeared as a `/README` slash command. `catalog.py` skips READMEs, so the generated catalog looks clean while the namespace is polluted. Layer docs are fine in `hooks/`, `skills/` and `workflows/` (dir- or heading-scanned); never in the two flat layers — INDEX Decisions
- [2026-08-17] **THE POWER BI LICENCE GATE COMES OFF `scrReports` — it is now the native analytics surface for EVERYONE, and this REVERSES part of `app-structure.md`.** That brief says "do not rebuild the aggregate dashboard as native charts to dodge the licence gap; provide the empty state instead." The two things it was protecting still hold — navigation never depends on Power BI, and no user sees a broken embed — but the native surface is now PRIMARY rather than a fallback, and Power BI demotes to an optional deep-dive. **The gate was already a no-op: `gHasPowerBiLicence` is hard-set `false`, so every user saw only the empty-state card and three rings, and the gated Power BI content was dead code.** Power BI still owes the FX-converted notional (C5) — that is why the deep-dive is kept rather than dropped. User-decided 2026-08-17 — INDEX Decisions
- [2026-08-17] **A GAP IS AN ABSENT GROUP, so the axes of the offering-gap grid come from the DIMENSION LISTS, never from the facts.** Rows are `Choices([@taskmaster_clients].client_coverage)`, columns are `Distinct(mapping_producttype, level1)`, and the grid is the full cross product with transaction counts joined in. `GroupBy` over the transactions would produce a group only where a row exists — deleting precisely the empty cells the section exists to show. **This is the same lesson `scrHome`'s donut already records** (a written-out category table, because a group only exists when a row has that value); the gap grid is its sharpest case, since there the missing category IS the finding — INDEX Decisions
- [2026-08-17] **COVERAGE ON A TRANSACTION IS `client_coverage`, reached through `transaction_client_name`** — not `project_coverage` through the parent project. A transaction carries BOTH a required client and a required project, so this was a real fork, and it is purely semantic: both are client-side joins off small dimension lists, so neither costs delegation and either is cheap to swap. Chosen because trades are a client axis — the grid reads "this coverage team has never traded that product type". The grid's row vocabulary is read with `Choices()` at runtime, so **the screen is agnostic to the values and needs no change when they change** — `schema.yaml` carrying PLACEHOLDERs is a golden-source accuracy gap, NOT a build blocker. (Corrected 2026-08-17: this entry previously called the section "semantically empty until the real values land", which conflated the app with the data.) — user-decided 2026-08-17 — INDEX Decisions
- [2026-08-17] **"WHERE PEOPLE SPEND THEIR TIME" HAS NO BACKING COLUMN — the model has no hours or effort field anywhere.** It is measured as task VOLUME by activity family and output format, plus MEDIAN CYCLE TIME (`task_date_start` → `task_date_completion`), which is the only genuine duration signal in the schema. The screen says "volume and elapsed time" on its face and **must never call either one effort** — elapsed is not effort, and volume is not either. A `task_effort` column was offered and DECLINED for now: internal names freeze at creation and the column is worthless until people populate it. **Related and equally structural: the model stores NO point-in-time history**, so every trend on the screen is built from DATED EVENTS (`transaction_date`, `task_date_completion`, `project_date_start`, `project_date_complete`) and never from stateful columns like `project_perc_completion`. "Completion over time" is natively impossible and needs a snapshot list from a scheduled flow (Q12) — INDEX Decisions
- [2026-08-17] **EXPLICIT COLUMN SELECTION CAN SILENTLY DROP A COLUMN WHOSE LINEAGE IS LOST THROUGH A COLLECTION** (MS Learn, `fast-app-page-load` / `code-optimization`). ECS is ON BY DEFAULT and trims retrieved columns to those it can prove are used; Microsoft documents that lineage is "occasionally lost when the data moves through collections and variables", and ECS then drops the column. **The fix is `ShowColumns` on every `ClearCollect` — and it is CORRECTNESS, not optimisation.** A dropped column renders as a blank number, and across the one-way gap a blank number returns only as "it didn't work", which is undiagnosable. Any collection-heavy screen is exposed; `scrReports` most of all — INDEX Decisions
- [2026-08-17] **A FIX THAT MAKES A BOUNDARY MORE CORRECT CAN ALSO MAKE IT DEGENERATE — check a window at its own edges.** Review round 1 replaced "Quarter = 90 days" with a real calendar quarter; that meant on **1 April `gRptFrom = Today()`**, so the doubled prior-window span `gRptFrom2x` was ZERO and **every transaction fetch in `scrReports` came back empty** — blank trend, blank tiles, blank grid, one day in ninety and only on that day. The fixed 90 days could not degenerate. Now floored at 14 days each side. **Two review passes over the same file found 17 findings, all legitimate, and the sharpest one was introduced BY the previous pass** — INDEX Decisions
- [2026-08-17] **THREE MORE WAYS STRING-BUILT SVG FAILS SILENTLY, all found in `scrReports` review** (they extend, not replace, the 2026-08-13 empty-attribute rule). **(1) Put a unit suffix INSIDE every branch** — `fill-opacity` emitted `'1%'`, one percent instead of opaque, because the `"%"` was appended outside the `If`, so the empty cells a gap grid exists to show rendered as smudges. **(2) Round the RUNNING BOUNDARY of a stacked bar, never each segment** — `Round(112.5)+Round(187.5)=301` overflowed a 300 track and the last rect came out `width='-1'`, an SVG error that kills the entire drawing; cumulative rounding is monotonic so widths are non-negative by construction. **(3) Filtering a chart's rows is not enough — collapse the ORDINAL too**, or the surviving row draws at its index in the unfiltered grid and the chart looks empty. Where a datum can be a control property instead of a string, prefer it: the person overlay's bars are plain `Rectangle`s whose `Width` IS the number, so there is no string to build and no decimal separator to get wrong — INDEX Decisions
- [2026-08-17] **A COLLECTION SPANNING TWO WINDOWS WILL BE READ AS ONE.** `scrReports`' monthly bars read `colRptTx`, which holds the current AND prior windows; because `gRptFrom` rarely lands on the 1st, a transaction earlier in the same CALENDAR MONTH as the window start shared its month key and inflated the first bar — so the trend disagreed with the desk-pulse tile directly above it, which is how it was caught. Two disagreeing numbers on one screen is the cheap detector; **name the narrower fold (`colRptTxCur`) and make the wide one obviously wide.** Same family: a header that counts unfiltered while the list beneath it is filtered — the header and its gallery must share one predicate — INDEX Decisions
- [2026-08-17] **A TOTAL THAT DOES NOT RECONCILE IS WORSE THAN ONE THAT EXPLAINS ITSELF.** `scrReports` transactions whose client has no coverage, or whose product has no type path, land on fallback labels matching no axis — so they sit in NO heatmap cell while still counting in the KPI tile above. `gRptUnmapped` now states that count under the grid. Related sizing trap in the same file: `WrapCount: 5` fitting exactly the five currency values the column has today meant a SIXTH added in SharePoint would be silently unreachable — **a container sized to today's vocabulary is a bug, not a fit** — INDEX Decisions
- [2026-08-17] **GATE EVERY COMMIT WITH `&&`, NEVER `;`.** A validate-then-commit run with `;` committed a file that had already failed validation (`b45fc06`): an inline `DefaultSelectedItems: =Table( { Value: "..." } )` makes YAML read the inner `Value:` as a mapping key, so the screen must use a block scalar. With no CI on this machine the validator is the ONLY gate, and a separator that ignores its exit code disables it — INDEX Decisions
- [2026-08-17] **ONE BAD COLUMN NAME IN `ShowColumns` BREAKS EVERY READER OF THE COLLECTION, SO THE ERROR LIST IS NOT THE FAULT LIST.** `colRptPrj` named ten columns; one did not exist, the whole expression errored, the collection never got a valid schema, and Studio then reported "not recognized" on `p.project_name`, `p.project_manager`, `p.project_supporter` and a `project_perc_completion` LookUp on a DIFFERENT control. Four errors, one cause. **Read a burst of column errors that all read one collection as a single fault and go find the name the source rejects** — fixing it clears the rest. Corollary for this repo: `schema.yaml` naming a column proves nothing about SharePoint, and the app can only see what the connector exposes (see the 2026-08-12 `Created By` entry) — INDEX Decisions
- [2026-08-17] **PASTING A SCREEN IS THE ONLY VALIDATOR THAT READS SHAREPOINT, AND IT FOUND TWO NAME FAULTS IN ONE SESSION** — `task_supporter` (created as `task_other_resources`, only ever renamed, so the internal name never changed) and `project_perc_completion` (misspelt at creation). Both were invisible from this side: `schema.yaml` and all twelve-plus screen references agreed with each other the entire time, the validator passed 22/22, and a token-vs-schema check written specifically to catch this reported all 33 tokens clean. **THE REPO AGREEING WITH ITSELF IS NOT EVIDENCE THAT IT AGREES WITH SHAREPOINT** — the golden source defines what SharePoint SHOULD be, and drift is only ever detected downstream. A proposed write-time column-token hook would NOT have caught either one. Both fixed by delete-and-recreate, which discards stored values and any index: `task_supporter` owes its index back; `project_perc_completion` reads 0% on every project until the C3 rollup next writes it on a save — INDEX Decisions
- [2026-08-17] **THE ERROR LIST IS NOT THE FAULT LIST — COUNT THE ERRORS AGAINST HOW MANY PLACES THE SUSPECT IS USED.** Three separate times in one session a single unresolvable name produced a scatter of unrelated-looking errors, and every name Studio actually printed was innocent: `ShowColumns` naming one bad column reported four errors across two controls; an unconnected data source reported a missing **built-in** `ID`; a projected built-in reported three. The counting test is what settles it — `gTkSaved.ID` was read in FOUR places and only TWO errors appeared, so it could not be the fault. **A built-in that cannot be missing is the loudest possible signal that the SOURCE, not the column, failed to resolve** — INDEX Decisions
- [2026-08-17] **A PROPERTY IN COMPILE ERROR DOES NOT RUN AND SAYS NOTHING, AND A FRESH PROBE FORMULA IN THE SAME PROPERTY IS THE FASTEST WAY TO PROVE IT.** scrTaskEdit's Create button was inert — no toast, no label change, identical on a full and a minimal form — because one unresolvable data-source name sat inside its `OnSelect`. Static reading of the source could not find it: the file was correct. **Replacing the property with a one-line `Notify` via the formula bar costs no paste and splits the space in one click** — it fired cleanly, which proved the event path was fine and the handler was in error. Reach for it BEFORE reasoning further about layout, hit-testing or z-order; and read the App checker first of all, since it names the offending token outright — INDEX Decisions
- [2026-08-17] **THE USER SAYING "IT IS CORRECTLY REFERENCED ELSEWHERE" IS EVIDENCE, NOT AN OBJECTION TO ANSWER.** I inferred from one reading that `project_name` lived in the built-in `Title`, wrote it into the golden source, and sent the user at their SharePoint columns. The columns were fine; the cause was `ShowColumns` losing the schema. **The counting test would have refuted the Title theory immediately** — every other screen reads that column and none of them failed. Retracted in `schema.yaml`; the Title conversion pattern is kept only for a list that genuinely needs it. **Do not write an unverified inference into the golden source: a false claim there outlives the conversation that produced it** — INDEX Decisions
- [2026-08-18] **`Set()` CANNOT BE CALLED INSIDE `ForAll` — Studio rejects it outright — AND `IfError`'s ARMS MUST BE TYPE-COMPATIBLE, SO A BARE `Collect` CANNOT SIT AGAINST A `Set` EITHER.** Together those two rules leave NO WAY to catch a write in place inside a loop: you cannot wrap the loop (the arms clash) and you cannot wrap the call (no Set in ForAll). **`Errors(DataSource)` is the documented route** — it reports failures against a data source AFTER the fact, which is the shape a loop of writes needs. Hit on issue #14's junction adds; the repo has 69 `ForAll` bodies and this was the only violation. The validator already enforces the IfError rule and does NOT yet check the ForAll one — INDEX Decisions
- [2026-08-15] **`pre_read_guard` exempts `src/` and `schema/`.** The only files over its 60KB cap are the three largest authored screens, so unmodified it would fire almost exclusively on the golden source. A screen read at 1500 of 3000 lines is how a truncated read becomes a wrong paste — and the gap returns only "it didn't work", so the cause is undiagnosable. **Any future write-time guard must fail open for the same reason** — a misfiring hook here costs a paste — INDEX Decisions
- [2026-08-18] **A WRITE WITH NO `IfError` AND NO RE-READ IS A WRITE THAT CANNOT FAIL VISIBLY.** `scrTaskEdit` guards the task `Patch` and the project rollup, but the `taskmaster_taskproduct` reconcile between them is bare — so a rejected junction write still ends in `Notify("Task saved")` and `Back()`, and issue #14 arrived as "it doesn't save" with no error text to work from. Across a one-way gap the ONLY diagnostic channel is what the app itself reports, so **every write to a data source needs an `IfError` AND a verifying re-read** — the re-read is the stronger half, because it catches a silent no-op as well as a throw. Same class as the 2026-08-06 scrProjectEdit lesson (trust the record, never the message), one level out: there the write was checked and the message was not trusted; here the write was not checked at all — INDEX Decisions
- [2026-08-18] **A DERIVED SUMMARY COLUMN MUST BE WRITTEN AFTER THE THING IT SUMMARISES, NOT IN THE SAME PATCH.** `task_product_summary` is written inside the task `Patch` from the staging collection, before the junction is touched and regardless of whether the junction write succeeds — so the task row claims products that have no links behind it. The reopen then reads the JUNCTION, not the summary, and the products vanish. **Order denormalised copies after their source and gate them on its success**, or the two states disagree the moment the source write fails — INDEX Decisions
- [2026-08-18] **A VISIBILITY TOGGLE MUST NOT DRIVE A DELETE.** On `scrTaskEdit` the product set is gated on `tglTkOutput.Value` for BOTH display and reconcile, so switching the Output section off deletes every task↔product link silently on the next save. The toggle's own `Default` already special-cases `!IsEmpty(colTkProducts)` to stop hiding them — the design saying products are a relationship, not an output attribute. **When a toggle hides a control, it may decide what is SHOWN; it must never decide what is KEPT** — INDEX Decisions
- [2026-08-18] **CONDITIONAL REQUIREMENTS LIVE IN THE APP, AND THE LIST STAYS PERMISSIVE ON PURPOSE.** Issue #13 adds `task_output_audience` (Choice: Direct Retail · Indirect Retail · Client-Specific · Generic · Internal Only). Two forks in the issue's wording were settled with the user and must not be relitigated: **(1) the approval-id requirement bites ONLY at stage `Completed`**, not on every save — the external portal issues that id late in the lifecycle, so demanding it at draft time would block ordinary work; **(2) the audience itself IS required whenever the Output section is on**, which removes the blank third state the approval rule would otherwise have to reason about. Both columns stay `required: false` in `schema.yaml` because SharePoint cannot express "required only when another field is set" — marking either required there would reject every task that produces no output at all. **`scrTaskEdit`'s `lblTkMissing` is the ONLY enforcement, so anything writing outside that screen bypasses it.** Known consequence, accepted: an EXISTING task that has output data but no audience is blocked from saving at all until one is picked, not merely from completing — INDEX Decisions
- **#14's JUNCTION WRITE IS THE ONE LANDED BEHAVIOUR NOBODY HAS RUN.** `scrTaskEdit` now carries
  the error handling and the verifying re-read, so a multi-product save either WORKS or finally
  returns SharePoint's actual message. **The cause was never found** — the next multi-product save
  is the first run that can say what it is. Everything else from #13/#15/#17 is verified live:
  the confirm dialog opens (so `cmpConfirmDialog`'s custom properties ARE declared in Studio) and
  #13's completion gate blocks as designed.
- **`product_assetclass` (required) and `product_esg` are LIVE (2026-08-14)** — columns
  provisioned, scrProductEdit landed. **`schema.yaml` still carries PLACEHOLDER values for
  product_assetclass** — the app binds `Choices()` so it works, but the golden source now
  DISAGREES with SharePoint on that column's vocabulary. Same gap as `client_type`,
  `client_coverage` and `project_coverage`. Ask for the four real value lists.
- **`taskmaster_taskproduct` is live (2026-08-14)** — the task↔product junction, SINGULAR
  name. `task_product_summary` is provisioned; CASCADE delete on `taskproduct_task_id` and
  RESTRICT on `taskproduct_product_id` are set and confirmed. `task_product_id` is RETIRED
  but left in place. **No migration was written and none is owed** — the user is still on
  a throwaway example universe (3 projects) and will rebuild it.
- **`cmpStatusCard` and `cmpConfirmDialog` now have NO consumer in `src/`** — scrHome's
  rebuild dropped both (inert cards are plain containers; the archive button and its
  confirm modal are gone). Files kept; Studio housekeeping candidates.
- **ASSUMED, not confirmed: "issues I own" = `issue_assignee`**, not `issue_owner`.
  issue_owner is the un-indexed who-raised-it stamp and an Or-arm on it kills delegation.
- **scrHome's three SVG charts are on their SECOND correction.** Reported: the donut showed a
  phantom category and then turned entirely one colour when a task was added; the timeline drew
  only its grey tracks. All three symptoms are what EMPTY attributes look like, so every
  fractional coordinate is gone. **Still unconfirmed which of the three causes was actually at
  work** — the empty-attribute theory, the negative dashoffset, or a live `task_stage`
  vocabulary that does not match `schema.yaml`. The new "Other" slice answers the third on
  sight, and `colTimeline` is now inspectable in Studio's Collections pane.
- **The records track "IN STUDIO", never "PUBLISHED" — different apps.** Studio runs the latest
  SAVED version, end users the last PUBLISHED one. Record both, separately.
- **Six superseded COMPONENTS to delete in Studio** — `cmpPeoplePicker`, `cmpRecordPicker`,
  `cmpUiKit`, `cmpEditableGrid`, `cmpStatusPill`, `cmpChoicePill`. Instance-free but still
  shipping inside the `.msapp`.
- **UNTESTED follow-up to the freeze probe:** a reference to a name that is NOT in the paste at
  all. The probe only covered references that resolve. If an unresolvable one is what gets
  replaced by a constant, the 2026-08-04 `Y=193` report is fully explained and the rule becomes
  "relative geometry is fine as long as the name survives the paste".
- **`galProjects.Y` reads 193 in Studio, not the authored 210.** Nothing in the current source
  can evaluate to 193 and the literal appears nowhere in `src/`; 193 is exactly the PRE-REWORK
  `SearchBox.Y + Height + Gap`. Working theory: a NAME COLLISION — control names are unique
  app-wide, so a surviving `galProjects` forces the pasted one to `galProjects_1` and the panel
  keeps reading the old control. Awaiting a Studio tree search for the name. A rename in source
  (`galProjects` → e.g. `galPrjList`, plus four `=galProjects.*` refs in `ProjectsEmptyLabel`)
  is the permanent fix if confirmed.
- **`task_supporter` IS INDEXED AGAIN (2026-08-18) BUT ITS VALUES WERE NEVER BACK-FILLED.**
  The 2026-08-17 delete-and-recreate discarded every stored supporter, so any task predating
  it reads blank. **That is the first thing to check behind issue #10** ("support count not
  working") — a low count is most likely correct reporting over empty data, not a bug in the
  fold. Same for `project_perc_completion`, which self-heals via the C3 rollup on any save;
  `task_supporter` does NOT self-heal and needs entering by hand.
- **KNOWN, ACCEPTED HOLE NOW LIVE (#12): every NEW project shares the resume key `"N"`.**
  `scrProjectEdit`'s guard skips its seeds when `gPrResumeKey` matches, and a key left armed
  by abandoning "+ New client/product" can therefore match a LATER new project and skip its
  seeding. Chosen deliberately: the failure preserves the user's OWN draft and cannot match
  an existing project. **If a user reports "New Project opened with my old draft in it", this
  is why** — do not re-diagnose it as a seeding bug. The fix, if wanted, is a key unique per
  entry rather than per context.
- **OWED IN SHAREPOINT:** set the `project_phase` column default to `Not Started` (the value
  itself already exists).
- **`cmpSelection` is IN USE and WORKING** — issue status/type/impact, transaction currency,
  and now task stage/health/priority, all fed from `Choices()`. Fixed 2026-08-13 by declaring
  `Selected` as an Output property in Studio. Do not convert these to comboboxes; the control
  was never the fault. **The strips carry no colour input**, so scrTaskEdit's stage/health/
  priority lost the value-coloured text the comboboxes had.
- **Three auto-layout containers have BOTH children unpinned** (`colIssType` among them) —
  unpinned children split space evenly and ignore declared heights.
- **The Choice columns that replaced Managed Metadata (2026-08-10) have no recorded internal
  names** — verify against SharePoint before any formula binds them.
- **Column-token validator write-time hook** — proposed in CLAUDE.md, not built. "Never invent a
  column name" is prose a model can drift past; a hook would make it enforced. Scaffold it with
  `/add-hook` and copy `pre_read_guard.py`'s fail-open shape — a guard that misfires costs a paste.
- **This INDEX has drifted past its own budget again (noticed 2026-08-15)** — 378 lines / 150KB
  against the ≤80-line target at the top of the file. It was pruned to ~80 on 2026-08-13, so it
  has regrown 5× in two days: the Decisions ledger is append-only and nothing has been archived
  since. Next prune moves the settled entries to `sessions/ARCHIVE-2026.md` verbatim, as before.
- **One Power Automate flow to author:** the list-provisioning flow.
- **Known edge, accepted:** an issue whose linked task/transaction belongs to a DIFFERENT project.
- Open questions Q3–Q10, Q13 — see `.claude/context/open-questions.md`. Q2/Q2b are CLOSED (out of scope).
- **ISSUE WORKFLOW: PLAN AND COMMENT ON THE ISSUE, DO NOT IMPLEMENT** (user, 2026-08-18).
  An issue is a request for a design and a written plan on the thread, not a branch of code.
  Implementing #12 unasked was accepted after the fact ("this is fine") but was not what was
  wanted. Post the plan as an issue comment, and wait to be told to build.
- **A NAMES AUDIT IS WORTH BUILDING** — `tools/audit_names.py` beside `audit_globals.py`,
  checking collections that shadow a control name and collections seeded twice with
  different shapes. The ad-hoc version found two real bugs in one sitting. Note when
  building it that the column-token hook proposed in CLAUDE.md would NOT have caught either
  SharePoint name fault this session — scope it to authoring typos, not schema drift.
- **OWED IN SHAREPOINT — THREE NEW INDEXES (labelled 2026-08-18, NOT yet provisioned).**
  The user's sweep confirmed every `indexed: true` column live (55 of them), but these three were
  unlabelled at the time so it could not have covered them; they are labelled NOW and are the only
  gap between the golden source and SharePoint:
  **`task_date_completion`** (scrReports' window filter rides on it),
  **`issue_task_name`** and **`issue_transaction_name`** (issue #15's detach query filters on both).
  All three are delegable but unindexed, and **SharePoint refuses an unindexed filter past 5,000
  items** — so they fail exactly when the list is big enough for it to matter. Adding them takes
  tasks to 10 and issues to 8 against SharePoint's cap of 20, so there is room.
  **An index CANNOT be added past 20,000 items.**
  Once provisioned, `indexed: true` is again trustworthy as provisioned fact across the whole file.
- **ISSUE #14 IS DIAGNOSED AND THE FOUR FIXES ARE WRITTEN, UNPASTED.** Multi-product task links never
  reach `taskmaster_taskproduct` and the failure is silent. All four fixes are in `main` (guard +
  verify the reconcile; write the summary after it; take products out of the output gate; replace
  the non-delegable `RemoveIf`) and ride the next `scrTaskEdit` paste alongside issue #13.
  **WHY SharePoint rejects the write is still UNKNOWN** — it compiles, so it is a RUNTIME
  rejection and nothing on the repo side can see it. `tests/scrProbe-junction-write.pa.yaml`
  returns the actual message in one paste; the fixes stand whatever it says.
  **Do not re-derive the diagnosis** — it is in the session log with line numbers.
- **BACKLOG ORDER SETTLED (2026-08-19): #9 BEFORE #11, and #11 SUBTRACTS BEFORE IT ADDS.**
  #9 leads because it is the only WRITE-PATH change on the board and #11 is entirely read-path —
  every day `issue_date_close` is hand-entered or forgotten poisons the data any future
  issue-cycle report reads. Fix the writer before reworking the readers.
  #11's five steps: (1) drop median-cycle-time and the coverage-gap grid *including their folds*,
  which frees the band-3 column slot the new charts move into so the grid geometry is computed
  once; (2) extend `btnRptLoad` in ONE paste — invisible by design, so the only signal is "nothing
  regressed", which is what you want from the riskiest region; (3) people, because it is flagged
  WRONG rather than missing and a misleading number beats an absent one; (4) task grouping;
  (5) transactions LAST, so the pie renderer is proven in the simpler section before it has to
  carry a two-level path and a join.
- **PIE CHARTS USE THE `cmpKpiRing` DASHARRAY TRICK — NEVER ARC PATHS.** `scrReports.pa.yaml:1645`
  requires whole-number SVG coordinates: a fraction goes through `Text()`, which drags the locale
  decimal separator into an attribute, and an attribute Power Fx cannot render comes out EMPTY.
  `Cos`/`Sin` arc endpoints are exactly that hazard. `cmpKpiRing.pa.yaml:65` already dodges it with
  `stroke-dasharray` on a circumference-100 circle (`r=15.9155`), integer percentages only — a pie
  is N stacked circles with a running `stroke-dashoffset`. The technique is already landed in Studio.
- **`issue_date_close` BECOMES DERIVED, AND `StartsWith` IS THE RIGHT TEST HERE.** #9 removes the
  picker and the save stamps the date from the status transition instead. This repo's convention is
  to ENUMERATE Choice values rather than compare them — but that rule exists for DELEGATION, and
  this is a local expression on a text global with no query behind it. `StartsWith(gIssStatus,
  "Closed")` fails OPEN in the right direction: a future `Closed - Duplicate` gets stamped
  automatically, where an enumerated list would silently never stamp it. Opposite trade-off from
  the App.Formulas allow-lists, deliberately.
- **2026-08-19 — Porting to claudeBrain: the Power BI scope edits stay HERE.** The flow is
  outward (second port confirms it; nothing was imported). But this repo's "Power BI is OUT OF
  SCOPE" rewrites are **project-local** — claudeBrain still ships `power-bi-dax` and
  `power-query-m`, so porting them would make the factory contradict its own catalog.
  `power-apps-svg` went over with its boundary rewritten to defer *outward* reporting to the BI
  skills; the `graph-api-integration` / `power-fx-review` / `power-apps-components` diffs were
  **only** that scope rewrite and were dropped whole. Also never port: the meta-skill edits (they
  just swap worked examples VBA→Power Fx, correct here and wrong there),
  `pre_write_column_guard.py` (needs a `schema/schema.yaml` the example project lacks), and
  `delegation.md` — **claudeBrain's copy is better than ours**, generalised during the last port.
  Test each hunk: *true for a generic consumer, or only because we ruled Power BI out?*
- [2026-08-19] **"SEARCH ANYWHERE IN THE TEXT" CANNOT DELEGATE TO SHAREPOINT, SO WRAP THE DELEGATED QUERY RATHER THAN FLATTENING IT.** The connector's own table gives Text exactly `=`, `Filter`, `Lookup`, `Sort`, `SortByColumns` and `StartsWith`; **`Search` and the substring `in` have no row in it at all**, and the SQL table lists both, so the omission is a statement rather than a gap. A contains-search is therefore always local, and the only real question is HOW MUCH DATA IT RUNS OVER. `galProjects.Items` answers it structurally: the eight delegable branches stay exactly as they were and an OUTER `Filter(..., Trim(txt) in project_name)` wraps the whole `If`. Phase, coverage and lead still run on the server; the client filters only what came back. **Written as one flat Filter the entire query goes non-delegable** and the phase and lead tests degrade to an arbitrary page — the opposite of the intent. **The cost is a hard dependency on the data row limit (2000):** the outer pass cannot see a row the inner query did not return, and it fails SILENTLY. The schema's "keep every list safely under 2000 rows in scope" rule is what makes this sound; it stops being sound the moment that rule does. The blank-search test is written EXPLICITLY (`Len(Trim(...)) = 0 ||`) rather than leaning on `"" in text` — the empty-operand-matches-everything trick is what the deleted lead filter died of, and it is not worth re-learning even where the semantics are documented and local — INDEX Decisions
- [2026-08-19] **A DIVISOR THAT IS A GLOBAL VARIABLE MUST BE GUARDED WHERE IT IS DIVIDED BY, NOT WHERE IT IS SET — AN UNSET GLOBAL IS BLANK, AND BLANK IS ZERO.** MS Learn is explicit twice over: "all uninitialized variables start with a blank value" (*Power Fx overview*), and "Mathematical operations with *blank*, such as division, coerce the blank value to a zero. That value causes a division by zero error" (*Error handling*). `scrHome`'s timeline divides by `gTlSpan` in a DECLARATIVE `Image` property while `OnVisible` sets `gTlSpan` as its LAST statement — six statements AFTER `Collect(colTimeline, ...)` gives the chart rows to draw. The window between the two is a real render, and `scrHome` has no busy gate (`scrReports` opens its `OnVisible` with `Set(gRptBusy, true)` for exactly this reason). It presents as the user's "sometimes at startup": it needs `n > 0` (you lead an open project WITH a target date), and only the FIRST visit after a cold start can see it, because `gTlSpan` afterwards retains its previous value. **The fix is the guard at the point of division, not a reorder** — extend the existing `With` to `{ n: CountRows(colTimeline), sp: Max(7, gTlSpan) }` and divide by `sp`. `Max(7, Blank())` is 7, which is `gTlSpan`'s own floor, so nothing changes once the variable is populated; one frame draws at minimum scale instead of raising a banner. Reordering the three `Set`s above `Clear(colTimeline)` is a worthwhile complement but NOT the fix: it is invisible to a reader, a later edit reorders it back, and it does nothing if a render beats `OnVisible` entirely. **Every other chart in this app already divides by `Max(1, ...)` at the point of division — the timeline is the sole exception, and that consistency is the rule to keep.** Two corollaries worth not re-deriving: `If( false, 1/0, 3 )` raises NO error (MS Learn, *Observing errors*), so the `If(gPrjLed = 0, ...)` guards on the KPI cards are genuinely safe and blank `= 0` makes them cover the pre-`OnVisible` state too; and errors DO surface from control properties ("the system observes errors on the input to all control properties"), which is why a Div0 buried in an SVG string concat reaches the user as an app-level banner — INDEX Decisions
- [2026-08-19] **A CONDITIONAL REQUIREMENT SHOULD BE DRIVEN BY A COLUMN THAT MEANS IT, NOT INFERRED FROM A NEARBY VALUE.** #13 decided "does this task need an external approval id" by reading the OUTPUT AUDIENCE: `Internal Only` meant no, every other value meant yes. #21 replaces that with `task_output_approval_flag`, a plain per-task Boolean. The audience value now gates nothing. The inference was wrong in both directions — an internal piece can need sign-off and a client-specific one can be exempt — and it was invisible: nothing on the screen said that picking an audience had just made an approval mandatory. **The general rule: when a requirement is conditional, give the condition its own column and its own control.** A derived condition costs a column but buys a rule the user can SEE and SET, and it stops a descriptive field quietly acquiring enforcement semantics it was never designed to carry. Two supporting calls worth not relitigating: the gate still fires only at stage `Completed` (the external portal issues ids late, so demanding one at draft time blocks ordinary work), and **"remove the conditionality based on the audience type" was read as removing the Internal-Only test ONLY** — the separate rule that Output-on requires an audience at all was KEPT, because dropping it is a behaviour change #21 did not ask for — INDEX Decisions
- [2026-08-19] **SPLIT A DERIVED VALUE BY WHAT CHANGES IT, NOT BY WHAT IT MEANS — STORE THE PART THAT ONLY MOVES ON A WRITE, COMPUTE THE PART THAT MOVES ON ITS OWN.** #22 made task health derived. Its inputs have two different natures: the task's OPEN ISSUES change only when somebody edits an issue, but OVERDUE changes when nothing happens at all — a task turns red at midnight. A fully STORED health is therefore structurally unable to be correct: it is only ever as fresh as the last save, and the staleness lands on Red, the one value that matters. A fully LIVE health needs each task's issues at read time, which is a per-row join inside every gallery and aggregate — the thing this project forbids everywhere else. **The split resolves both:** `task_status` stores the issue-derived half (recomputed at every point a task's open-issue set can change), and readers fold overdue in live, which costs NO join because `task_date_target` is already on the task row. **The general test: for each input to a derived value, ask "can this change with no write?" Inputs that can must not be stored.** The price is real and must be written down where it will be read — the column no longer means what its name says, so a reader that shows `task_status` raw UNDER-REPORTS Red. That warning lives on the column in schema.yaml, in `rollups.task_health`, and in the context brief, because the cost of it being missed is silent — INDEX Decisions
- [2026-08-19] **AN ALLOW-LIST THAT DRIVES A WARNING MUST BE WRITTEN AS A RESIDUAL, SO AN UNKNOWN VALUE OVER-WARNS INSTEAD OF GOING QUIET.** #22's amber rule is "any other open issue", NOT the four amber issue types enumerated. Enumerated, a type added in SharePoint and not added to the app falls through to GREEN — the task reads healthy because the app did not recognise the problem. That is the same allow-list-fails-closed shape as `ActiveProjects`, but the consequence is inverted in severity: there, a missing value hides a project from a list, which somebody notices; here, it hides a WARNING, which nobody notices, because the absence of an alarm looks exactly like the absence of a problem. A blank type is Amber for the same reason. **Where a derivation drives a signal, the residual branch belongs on the loud side** — the failure mode you want is a spurious Amber someone corrects, not a silent Green nobody questions — INDEX Decisions
- [2026-08-19] **A CHART'S viewBox IS PART OF ITS LAYOUT, AND MOVING THE CONTROL WITHOUT RESCALING THE BOX SHRINKS THE TYPE, NOT THE CHART.** #26 moved scrReports' project-trend chart from full band width into a third-width column. `ImagePosition.Fit` scales the whole viewBox to the control, so a 900-unit box in a ~480px slot renders at 0.54 — and a `font-size='15'` label with it, at about 7px. **Nothing in the source looks wrong; the SVG is unchanged and every number in it is still right.** The fix is to halve the coordinate space (900 -> 480) so the downscale halves instead of the type, and to thin the category labels (`Mod(Idx - 1, sk) = 0`, `sk` 2 past 8 buckets) because a "d mmm" label is ~42 units wide and a quarter's buckets end up only 35 apart. **The general rule for every SVG chart in this app: the viewBox width should track the control's expected pixel width, and label density should track the bucket pitch in viewBox units.** A chart that changes column width needs both revisited — this is the one class of layout defect that the geometry audit cannot see, because the boxes never overlap — INDEX Decisions
- [2026-08-19] **A PIE OVER A CLOSED VOCABULARY SHOULD ENUMERATE IT, NOT `Distinct` IT — `Distinct` MAKES THE COLOURS AND THE ORDER DATA-DEPENDENT.** Every other pie on scrReports groups an OPEN dimension (product path, region path, lead) where the member list is unknown at author time, so `Distinct` + top-N + "Other" is correct there and the colour comes from `Mod(Idx - 1, 6)`. `project_phase` is different: five values, fixed, and MEANINGFUL IN ORDER (Not Started -> Planning -> Active -> Stalled -> Complete). Built with `Distinct` it would order by whatever the data happened to contain, and the moment one phase emptied, every phase after it would change colour on the next refresh — a chart a person reads twice a week must not repaint itself in new colours because a project closed. `ForAll(Sequence(5) As s, ...)` with a `Switch` for both the label and the hex pins both. **Test: can you write the member list down? If yes, enumerate it and pin the colours; if no, `Distinct` and accept data order.** (`ActiveProjects` excludes Archived, so five is genuinely the whole set on this screen — the sixth value cannot appear.) — INDEX Decisions
- [2026-08-19] **A FORWARD-LOOKING SERIES MUST BE KEYED ON `Today()` AND SAY SO NEXT TO THE PERIOD CONTROL THAT DOES NOT APPLY TO IT.** #26 put an "open tasks due, today + 14 days" chart into scrReports, a screen whose entire identity is a period strip (1W/1M/1QTR) that rewrites `gRptFrom` and reloads everything. This chart ignores it, and so does the new phase pie — one looks forward, the other is a snapshot. **The hazard is not the arithmetic, it is that the control is still on screen and still looks like it applies.** Three things carry the resolution together: the fold comment states the key, the panel subtitle states it in the user's words ("today first" / "not a window"), and the band comment states the reading order (past · present · future) so the mixture is deliberate rather than accidental. **Where a control's scope does not cover every panel it sits above, the exceptions must be legible ON the panels** — a reader cannot be expected to infer which of three charts a filter moved. Corollary held from the same change: the three populations a bounded chart CANNOT draw (overdue, beyond the window, no date at all) are counted on the subtitle, the same rule `gRptDoneNoDate` and `gRptNoSup` already follow — a total that does not reconcile is worse than one that explains itself — INDEX Decisions
- [2026-08-19] **YAML `#` COMMENTS NEVER CROSS THE GAP; POWER FX `//` COMMENTS DO — SO COMMENT WORK IN `src/` COSTS NO PASTE.** A `#` line is discarded by the YAML parser before Studio builds a single control (verified: parse-and-redump `cmpKpiRing.pa.yaml`, 17 `#` lines in, 0 out). A `//` line lives INSIDE a block-scalar formula string, so it is part of the stored formula and does reach the app. At the 2026-08-19 purge the split was 1750 `#` to 1216 `//`. **Two consequences worth not re-deriving:** deleting `#` comments changes nothing downstream at all, and deleting `//` comments changes only formula text, so it can ride each file's next real paste rather than forcing 22 of them. A repo-wide comment edit is therefore a **zero-paste change** — the one class of large edit this air gap does not tax. The corollary is the reason `tools/formula_bar_body.py --bare` exists: the App object's two properties go through the formula bar, where a collapsed bar flattens the paste to one line and the first `//` swallows the whole property — INDEX Decisions
- [2026-08-19] **A COMMENT REPEATED AT N SITES IS ONE COMMENT IN THE WRONG PLACE — HOIST IT TO THE FILE HEADER AND DELETE THE N COPIES.** The 2026-08-19 purge cut `src/` comments 2914 -> 1689, and the single biggest category was not verbosity but DUPLICATION: the sort-button note appeared verbatim 7 times in scrReports, the pie-construction essay twice more in the same file, the three gallery-row rules 3 times each in scrProject, the `colDueSrc` fold note twice in scrHome, the 8-line double-tap guard across 5 editors, the 9-line appBar note on 3 screens. Each copy was individually defensible and collectively they were 40% of the file. **The rule: if a note applies to more than one site in a file, it belongs in the file header as a numbered rule, and the sites get nothing or a four-word pointer.** scrReports, scrHome, scrProject and scrProjects now open with 3-5 numbered rules each, and that is where a reader looks first anyway. **The second-biggest category was CHANGELOG PROSE, which CLAUDE.md has forbidden since it was written and which nothing enforced** — scrTaskEdit's header was literally "REWORKED 2026-08-07 / WHAT CHANGED", cmpToast's was 40% "why the redesign", and scrProjectEdit carried THREE stacked generations of contradictory comment above one Set, one of them describing a control the screen no longer has. Git has the history; a comment that narrates it rots on the first edit — INDEX Decisions
- [2026-08-19] **BEFORE MIGRATING A COMMENT'S REASONING INTO MEMORY, CHECK WHETHER IT IS ALREADY THERE — IT USUALLY IS, AND THEN THE JOB IS DELETE, NOT MOVE.** The purge began expecting to distil ~2900 lines of prose into `.claude/memory/`. Checking the eight load-bearing invariants first (the locale-decimal SVG trap, the gallery N+1 rule, `Select()` queues, no component inside a gallery, `Max(1, ...)` at the point of division, the circumference-100 pie, Explicit Column Selection dropping a column, cumulative-not-independent bar boundaries) found **all eight already recorded** in memory, a skill, or `docs/reports-screen-design.md`. The comments were not the source; they were an uncontrolled COPY of notes that already had a better home, and copies drift. Almost nothing needed writing down — only three negative-space notes ("`gProjArchived` was removed because the query could only ever return 0") that existed nowhere else, and those went to the session log. **The general shape: a repo with a real memory layer accumulates comment prose that duplicates it, and the duplicate is always the one that goes stale. Grep the memory before you write anything down** — INDEX Decisions
- 2026-08-19 | Perf review filed #27–#40. Two finder claims verified FALSE and not filed: the health recompute is no project-wide N+1 (colHlTasks only ever holds 1-2 ids) and the task-edit picker queries nothing while closed (gTkPicker cleared on cancel). Donut-SVG inline recompute and a recompute staleness guard deliberately not filed | sessions/2026-08-19-1853-perf-review-issues.md
- 2026-08-19 | #30 is written as an AMENDMENT to the 2026-08-13 gIssLed decision, not a reversal: single truncation-guarded OpenIssues fetch, with the per-project counts kept as the >=2000 fallback | GitHub #30
- 2026-08-19 | #27 BUILT: scrProject fetches its three child lists once in Concurrent; the phase counts, both galleries, both section titles, the currency totals and the empty states all read colProjectTasks/colProjectTx/colProjectIss. The health block, the per-click delete guards and the cascade counts deliberately still query live | GitHub #27
- 2026-08-19 | #31 BUILT: 20 reference fetches across five edit screens guarded to once per session and issued in Concurrent. THE FIVE LISTS CARRYING AN APP-ADDED "(none)" ROW GUARD ON CountRows <= 1, NOT IsEmpty — the sentinel makes an empty fetch look populated and IsEmpty would latch a one-option dropdown for the session. A Concurrent arm is one formula, so the two-statement sentinel builds became one multi-item ClearCollect | GitHub #31
- 2026-08-19 | #27 and #31 LANDED and CLOSED — all six screens pasted clean first time. The perf work is live: scrProject reads three collections instead of re-querying three lists ~8 times, and the five edit screens fetch their vocabularies once per session. NEITHER GAIN IS MEASURED — landing is a paste confirmation, not a Live Monitor count | GitHub #27, #31
- 2026-08-19 | #29 BUILT: the save handlers on scrTaskEdit and scrIssueEdit hoist their pre-Patch reads into one Concurrent. scrTaskEdit read the PARENT PROJECT ROW TWICE per save (the archived mirror inline in the Patch record, then the C3 rollup) — now once into gTkParentProj, paired with the health fetch; the rollup's own Concurrent collapsed to a bare Set because CONCURRENT NEEDS TWO ARMS. **MERGING TWO LOOKUPS FORCES ONE KEY, AND THE KEY IS NOW THE SCREEN'S OWN PARENT ID** (gTkProject.Id / gIssProject.Id), NOT gSelProject.ID: that is the record's stored parent, where gSelProject is only equal to it because every entry path happens to come from that project. The "read LIVE, never a snapshot" invariant is untouched — the id is a key, the record is still fetched at save time. scrIssueEdit's close-date arm KEEPS its Edit-and-closing guard inside the Concurrent, because hoisting it unconditionally would ADD a read to the common save. **scrTransactionEdit was deliberately NOT changed** — one pre-Patch read, nothing independent to pair it with, so hoisting moves code without removing a round trip | GitHub #29
- 2026-08-19 | #36 BUILT: scrTaskEdit skips the whole taskproduct reconcile — three junction reads and a second task Patch — when the staged product set matches the set loaded in OnVisible (colTkProductsLoaded, copied from colTkProducts and seeded INSIDE the resume guard). **THE GATE IS ON THE STAGED SET, NEVER ON tglTkOutput**: gating on the toggle is what once made it a silent delete button, and clearing every product is still a set difference, so the deletes still run. Both tests are collection-against-collection, so the gate costs no round trip. Consequence accepted: task_product_summary is left UNWRITTEN on a products-untouched save, so a new task with no products keeps Blank() where it used to get "" — no control in src/ reads that column | GitHub #36
- 2026-08-19 | #29 and #36 LANDED and CLOSED (user) — scrTaskEdit and scrIssueEdit pasted clean. The save-path perf work is live: one parent-project read per save instead of two, the pre-Patch reads issued in parallel, and the taskproduct reconcile skipped on a save that never touched the products. **AS WITH #27/#31, LANDING IS A PASTE CONFIRMATION, NOT A MEASUREMENT** — no Live Monitor count was taken, and the remove/clear branch of the #36 gate has not been walked | GitHub #29, #36
- 2026-08-19 | #32 BUILT: the scrHome dashboard fold lived TWICE (OnVisible and the section header's Refresh Stats OnAction, byte-for-byte). Extracted to **btnHomeRecompute** — hidden, 1x1, declared FIRST — the grounded btnPrjHealth / btnRptLoad shape. **`Select()` QUEUES the target OnSelect for AFTER the calling formula finishes; it does not run it inline and does not wait** (MS Learn, function-select). So Select goes LAST in both callers: the OnVisible gUserEmail self-heal is guaranteed to land before the fold reads it, and **the Refresh toast now fires BEFORE the numbers** — "Refreshed" means the refresh was issued. The toast stays in OnAction, not in the button, or it would fire on every screen open. The three sequential Refresh calls became one Concurrent. **THE ONE UNGROUNDED THING IS `Select()` OF A SCREEN CONTROL NAMED FROM A COMPONENT INSTANCE'S EVENT HANDLER** — no prior case in the repo; the handler is authored in SCREEN scope and none of the four documented Select restrictions bite, but only paste will settle it. Fallback: inline the fold in OnAction, keep OnVisible on the button | GitHub #32
- 2026-08-19 | #28 BUILT: the two scrHome work-list galleries resolved their parent project's name with a LookUp against taskmaster_projects PER VISIBLE ROW, re-firing on scroll; now a local LookUp against colPrjNames, a fourth Concurrent arm projected with ForAll over ActiveProjects. EVERY active project, not colMyProjects — a task I lead can hang off a project someone else manages. **THE ARCHIVED-PARENT EDGE IS ACCEPTED, NOT SOLVED**: colPrjNames excludes archived projects, so a row whose parent archived since its *_project_archived flag was stamped shows "—". **REJECTED the `If( IsBlank(local), <server LookUp> )` fallback**: If-laziness is grounded in BEHAVIOUR formulas, but this is a DECLARATIVE Text property where the row may still register the data-source dependency — a construct that MIGHT not remove the per-row call is worse than one that definitely does, when nothing can be measured across the gap. The three LookUps left in the row OnSelect handlers are one-per-TAP live re-reads and correctly untouched | GitHub #28
- 2026-08-19 | #30 BUILT: gIssLed issued one delegable query PER PROJECT MANAGED on every dashboard open and refresh; now counted locally off colIssByPrj (a fifth Concurrent arm, ForAll projection over OpenIssues). **THIS AMENDS THE 2026-08-13 DECISION, IT DOES NOT REVERSE IT** — there is still no delegable single query for "children of the projects I manage" and `issue_project_id.Id in colMyProjects.ID` still does not delegate. What makes the local count safe is the guard that decision lacked: at `CountRows(colIssByPrj) >= 2000` the map is a partial page, so the OLD per-project queries run instead rather than undercount silently. Both If arms are live and the comment says so | GitHub #30
- 2026-08-19 | **`Select()` OF A SCREEN CONTROL FROM A CANVAS COMPONENT INSTANCE'S EVENT HANDLER WORKS — GROUNDED IN STUDIO** (user, 2026-08-19, `scrHome.hdrMyWork.OnAction` -> `Select( btnHomeRecompute )`). The instance's event binding is authored in SCREEN scope, so component isolation does not apply to it; isolation applies to formulas INSIDE the component definition, which still cannot see screen controls. This was the open paste-time risk on #32 and it resolved positively — the fallback (inline the fold in OnAction, keep OnVisible on the button) was never needed. First case of the pattern in this repo; reuse it rather than duplicating a block into a component handler | GitHub #32
- 2026-08-19 | #32, #28 and #30 LANDED and CLOSED (user) — `scrHome` pasted clean. The dashboard perf work is live: one copy of the fold reached by Select from two callers, three Refreshes in parallel, project names resolved from a local map instead of per visible row, and gIssLed counted off one open-issues fetch. **AS WITH #27/#31 AND #29/#36, LANDING IS A PASTE CONFIRMATION, NOT A MEASUREMENT** — #30's "Done when" asked for a Live Monitor count and that step was NOT performed. **THE `>= 2000` FALLBACK ARM HAS NEVER RUN** (needs a 2000+ open-issue tenant), and the toast-before-numbers ordering introduced by Select's queueing has not been judged in use | GitHub #28, #30, #32
- 2026-08-19 | **TAKE-NEXT ASSESSMENT of the 7 remaining perf issues.** The gate is not effort, it is how many air-gap round trips stand between authored and known-good. #39/#33/#37 authorable now. **#40 is ready but must NOT be batched with behaviour changes**: it touches App.OnStart (formula-bar-only, a different transfer mechanism) plus two screens for ZERO behaviour change, and a batched paste returns ONE binary works/doesn't — bundling a silent guard with real changes makes a failure signal ambiguous about its cause. #38 deferred (rewrites the destructive path, lowest frequency, needs a disposable project to verify — its own pass). #35 blocked on a probe that must be pasted first. #34 blocked on a USER DECISION: prefix search (delegable, option 1) vs substring plus a truncation banner (option 2). **#40's body is stale — it says six `>= 2000` literals; #30 landed a seventh at scrHome:127** — and #40 should precede #34 so an option-2 sentinel uses the constant | GitHub #33–#40
- 2026-08-19 | #39 BUILT: `transaction_product_id` gains `indexed: true` in schema.yaml — the only child-list FK on taskmaster_transactions without one. **REPO-ONLY, NO PASTE**: the live list still needs the index set in List settings, which the in-file comment records. Prevention, not a live defect — nothing leads on the column today, but transactions is the fastest-growing list and **SharePoint refuses to add an index past 20,000 items**, so the decision has an expiry date. `issue_type` / `issue_impact` stay unindexed deliberately (worked around, not overlooked) | GitHub #39
- 2026-08-19 | #33 BUILT: `scrProjectEdit.btnPrSave` created staged tasks and staged transactions in two back-to-back ForAll loops though both depend only on `gPrSaved`; they are now the two arms of one `Concurrent`. **THE SPLIT RESULT COLLECTIONS ARE FORCED, NOT COSMETIC** — Concurrent's arms must not touch shared state and both loops collected into `colChildResult`; each now writes `colChildResultT` / `colChildResultX`, merged straight after, so colChildErrors, colChildOk and the Save-to-retry RemoveIf pair are untouched. **Legal only because the bodies `Collect`**: the 2026-08-19 ForAll rule sanctions Patch and Collect and rules out Set, so a later "simplification" to Set would break it — the comment says so in place | GitHub #33
- 2026-08-19 | #37 BUILT: `scrProductEdit.OnVisible` rebuilt three reference collections per visit, one of them (`colPdUndAll`) a non-delegable `Concat` over the WHOLE products list — paid at the deepest point of another workflow, since the pickers open this screen mid-save. All three now `If(IsEmpty(...))`-guarded in one Concurrent (the #31 pattern, sixth application). **`btnPdSave` merges the tickers it just wrote into the cache LOCALLY rather than refetching**, staged through `colPdUndMerge` because the obvious shape would ClearCollect over the collection it is reading — unverifiable across the gap, so not used. **THE SORT IN THAT MERGE IS LOAD-BEARING**: `galPdUndSug.Items` is `FirstN(..., 12)` over the order, so an appended tail would never be seen. Staleness moves from per-visit to per-session for OTHER people's additions only | GitHub #37
- 2026-08-19 | **#33, #37 AND #39 CLOSED AS BUILT RATHER THAN AS LANDED** (user instruction). Every earlier perf issue was closed on the human's binary "it works" after a paste; these three were closed while `scrProjectEdit` and `scrProductEdit` were still authored-and-unpasted, and while `transaction_product_id` was indexed in the golden source but not on the live list. **CONSEQUENCE: closed-issue state is no longer a proxy for what is in Studio, and a paste failure on either screen must be filed as a NEW issue** — the closed one cannot track it. Each closing comment says plainly that it is not a landing, names what is outstanding, and lists what to check once pasted. #37's Live Monitor "Done when" was explicitly recorded as not taken and not planned | GitHub #33, #37, #39
- 2026-08-19 | **#39 COMPLETE ON BOTH SIDES: `transaction_product_id` is INDEXED ON THE LIVE `taskmaster_transactions` LIST** (user, 2026-08-19), so the golden source and SharePoint agree and the pending marker in schema.yaml is gone. The list now carries six indexes (name, project_id, client_name, product_id, project_archived, date) against a documented max of 20. **The constraint that made this urgent, grounded on MS Learn: the list limit to ADD OR REMOVE an indexed column is 20,000 items** — above it the create is itself blocked by the threshold, so the remedy is unavailable exactly when it is needed. List View Threshold is 5,000 and is NOT adjustable in SharePoint Online. Single-value Lookup is stored as an int and is indexable; LookupMulti is not | GitHub #39
- 2026-08-19 | #33 and #37 LANDED (user) — `scrProjectEdit` and `scrProductEdit` pasted clean. Concurrent over two ForAll+Collect loops is accepted by Studio, and so is the scratch-collection ticker merge. **Both had already been CLOSED as built**, so the landing is recorded as a follow-up comment rather than a state change. As always, a paste confirmation and not a measurement — #37's Live Monitor "Done when" was never taken | GitHub #33, #37
- 2026-08-19 | #40 BUILT: `Set( gDataRowLimit, 2000 )` in App.OnStart, and all SEVEN `>= 2000` sentinels (six in gRptTrunc, one in gIssLed) now read it. **THE ISSUE'S PROPOSAL AS WRITTEN WOULD HAVE SHIPPED A COLD-START REGRESSION** it does not mention: App.OnStart is NON-BLOCKING and **a blank compares as ZERO**, so an unseeded gDataRowLimit makes every `>=` test TRUE — gRptTrunc would show a false all-six-truncated banner and gIssLed would take the expensive per-project arm #30 removed. **Fixed by seeding it in the two OnVisible handlers beside the existing gUserEmail self-heal**, which is there for the identical reason. **The number therefore lives in THREE places, not one — a deliberate trade**: still down from seven, all three are `gDataRowLimit` sites one grep finds, and a mismatch now degrades gracefully instead of going silently dead. REJECTED a named formula in App.Formulas (the repo has a recorded published-app failure — it is why gTheme is a Set) and `Coalesce(gDataRowLimit, 2000)` per sentinel (seven literals again). **A wrong value is still UNDETECTABLE** — Power Fx cannot read the Studio setting; the checklist guarding it is in gitignored CLAUDE.local.md | GitHub #40
- 2026-08-19 | **PAREN-BALANCE CHECKING NEEDS A CHARACTER STATE MACHINE, NOT A REGEX** — now `tools/balance_check.py`. Stripping `//` comments before string literals eats `http://www.w3.org/2000/svg` in every SVG Image property and reports false unbalanced; stripping `'...'` first eats prose apostrophes ("the fold's") and the parens after them. Both wrong answers were produced against known-good committed source before the state machine agreed the whole repo balances. Do not re-derive a regex version | tools/balance_check.py
- 2026-08-19 | #40 LANDED and CLOSED (user) — App.OnStart via the FORMULA BAR plus scrReports and scrHome via code view, all clean. `gDataRowLimit` is live and no `>= 2000` literal remains in src/. **The perf backlog's buildable half is finished: #27–#33, #36, #37, #39, #40 are all done and closed.** What remains is blocked, not unstarted: #34 on a user decision, #35 on a probe, #38 deferred to its own pass. **A wrong gDataRowLimit is STILL UNDETECTABLE** — Power Fx cannot read the Studio setting — and the checklist guarding it lives in gitignored CLAUDE.local.md, so it would not survive a fresh clone; move it to docs/ if that matters. Neither the OnVisible seeds nor any sentinel has been observed firing (the latter needs a 2,000-row dataset) | GitHub #40
- 2026-08-19 | **#34 DECIDED BY THE USER: PREFIX SEARCH, DELEGABLE** — push `StartsWith(project_name, Trim(txtProjSearch.Text))` inside each existing branch of `galProjects.Items`, accepting that mid-word matching is lost ("Rollout" will no longer find "Alpha Rollout"). **DO NOT RELITIGATE.** The substring-plus-truncation-banner alternative was offered and declined. **It is cheaper than the issue body implies**: `StartsWith(col, "")` returns true, which is ALREADY a landed technique on this screen (from #17, where it kept the branch count at four instead of eight), so the predicate goes in unconditionally — one extra clause per branch, NOT a doubled branch count. The screen's own comment already grounds the delegability: SharePoint delegates `=`, `StartsWith`, `Filter` and `Sort` on Text and nothing else. **KEEP THE EXPLICIT PHASE GROUPS** — a bare `Filter(ActiveProjects, StartsWith(...))` without one drew "the query is not valid" from SharePoint, so the StartsWith goes INSIDE the existing shapes, never in place of them | GitHub #34
- 2026-08-19 | #34 BUILT, AND IT COST DOUBLE WHAT I TOLD THE USER. The outer local `Filter(..., Trim(txt) in project_name)` is gone and `StartsWith( project_name, ... )` sits inside each branch, so the whole query folds and a match past the data row limit is findable again. **I claimed it would add one predicate per branch on the strength of `StartsWith(col, "")` being a landed technique here. IT IS BANNED HERE** — #17 built the lead filter on it, it returned NOTHING for every choice in the app (#19), and the standing rule is that an "off" state must be a BRANCH. So the empty-search case is its own branch and `Items` is **16 branches** (search × show-completed × coverage × only-mine). Corrected to the user before building. A ~10-branch variant where a search OVERRIDES the other filters was offered and not taken — today's outer filter COMPOSES with them, and preserving that is the faithful choice | GitHub #34
- 2026-08-19 | #38 BUILT: the project delete cascade's independent stages now run together. Three guard counts in one `Concurrent` (they could NOT stay in the `With` — Concurrent takes behaviour formulas, so each count must land in a global before the threshold test reads it); issues still delete alone and first because `issue_task_name` / `issue_transaction_name` point at the other two children; transactions and tasks are the arms of a second `Concurrent` with **per-arm error globals**, since Concurrent's arms must not write the same state and both previously wrote `gPrDelErr`. `Coalesce` skips an EMPTY STRING as well as a blank, which is what lets the three be seeded `""` without masking a real message. Serial depth 6 stages -> 4. COUNT-THEN-REFUSE and children-before-parent untouched | GitHub #38
- 2026-08-19 | **A ` #` INSIDE A PLAIN (NON-BLOCK) YAML SCALAR IS EATEN AS A YAML COMMENT, AND `tools/validate_pa_yaml.py` DOES NOT CATCH IT.** `Text: ="… issue #35"` parsed to `="… issue` — closing quote and all — and the validator reported `ok`: by the time it inspects the parsed doc the `#` is gone, so `stray_hash_errors` only ever finds a `#` that SURVIVED parsing, i.e. one inside a block scalar. `tools/balance_check.py` caught it as an unterminated string. **Put any formula containing `#` in a block scalar.** **CLOSED THE SAME DAY: `eaten_comment_errors()` in tools/validate_pa_yaml.py** scans the SOURCE TEXT, skipping block scalars (where `#` is content, already covered) and quoted scalars (where `#` is literal), tracking block regions by indentation so formula bodies cannot false-positive. **It found two pre-existing instances on its first run**, both probe titles — `scrProbe-junction-write` (never pasted) and `scrProbeRerun` (pasted 2026-08-18, so its title label landed with an unterminated string and nobody noticed). `src/` was clean | tools/validate_pa_yaml.py
- 2026-08-19 | **PROBE RUN (#35): `task_date_completion = Blank()` DELEGATES on a DateTime column; `IsBlank(task_date_completion)` DOES NOT.** The asymmetry the issue was written on, confirmed. **THE PROBE WAS BADLY DESIGNED AND NEARLY UNREADABLE**: every sub-case is wrapped in `CountRows`, which is itself non-delegable, so all six labels carried an identical "CountRows Operation not supported" that says nothing about the Filter inside — **rule 4 again, the same instrument-sharing error that lost `scrProbeRerun` sub-case D.** It was saved by sub-case F, which was written to measure SCALE and happens to use `IsBlank()` where C/D/E use `= Blank()`: F returned "CountRows not supported **& task_date_completion - large dataset**", proving the checker reports predicate warnings ALONGSIDE the aggregate warning on the same label and is actively examining this column — which is what makes C/D/E's silence evidence of absence. **Next probe: no aggregate in the instrument** — a gallery `Items` or `First(Filter(...)).col` carries none. **RESIDUAL RISK IS NOT ZERO**: if the predicate does not fold, the fetch degrades from "completed tasks" to "first 2000 LIVE tasks then filtered", and `gRptTrunc` would NOT catch it because it counts AFTER the local filter | tests/README.md
- 2026-08-19 | #35 BUILT: `colRptDoneAll` is bounded on `gRptFrom` with `|| task_date_completion = Blank()` beside it, on the probe result above. **Corroborated at data row limit 1: the count went 0 -> 1 on SET CUTOFF** — a non-delegating filter would have tested one locally-fetched row and shown 0. That is corroboration, not proof: one row could match by chance, so the WARNINGS remain the evidence. `gRptFrom`, not `gRptFrom2x` — only transactions need the prior window. **`colRptDone` still re-applies the window LOCALLY and that is not redundant**: a SharePoint DateTime is compared server-side in the LIST's timezone, so the fetch boundary can sit up to a day off the client's; the local pass is the boundary every figure uses and can only narrow, never widen. `IsBlank()` is correct there because a collection has no query behind it. **THE IN-FILE COMMENT WARNS AGAINST "TIDYING" `= Blank()` INTO `IsBlank()`** — same-looking test, un-delegates the whole fetch, fails silently, and gRptTrunc cannot see it | GitHub #35
- 2026-08-19 | **#34, #35 AND #38 LANDED AND CLOSED (user) — THE PERF BACKLOG #27–#40 IS COMPLETE.** `scrProjects`, `scrReports` and `scrProject` all pasted clean. Live now: the projects search delegates as a PREFIX match (typing a word from the middle of a name finds nothing — deliberate, and the empty-state label says "starts with"); the completed-task report fetch is bounded on gRptFrom with the `= Blank()` null arm; and the project delete cascade issues its guard counts together and deletes transactions and tasks in parallel after issues. **ALL FOURTEEN ISSUES LANDED WITHOUT A SINGLE MEASUREMENT** — no Live Monitor count, payload figure or wall-time reading was ever taken. **UNEXERCISED AND WORTH A DELIBERATE RUN:** a project delete where transactions succeed and tasks fail, which is the only path that exercises #38's Coalesce of per-arm errors; the 500-item block path; and #35's `= Blank()` bound against a tenant large enough for delegation to matter | GitHub #34, #35, #38
- [2026-08-20] **DOCUMENTATION IS A SEPARATE LAYER FROM THE GOLDEN SOURCES, AND IT LINKS RATHER THAN COPIES.** `docs/manuals/` holds a user manual and a maintainer manual; `docs/` keeps the build-time notes. The contract is written at the top of `docs/manuals/README.md`: **a manual describes, the golden source defines, and where they disagree the manual is the thing to fix.** So a chapter needing a column name LINKS to `schema/schema.yaml` instead of quoting it — a manual that has to change because a column was renamed was quoting something it should have linked. This is the same reasoning as COMMENTS ARE NOT A CHANGELOG, applied one layer out. **The user manual was written from `src/` — real labels, real empty-state sentences, real banner wording — and NOT from `docs/screen-map.md`, which is stale enough to be misleading.** The maintainer manual's one original artifact is the **stay-in-step table**: `gDataRowLimit` ↔ the Studio data-row-limit setting, `gStageWeights` ↔ `rollups.stage_weights`, component-inlined colours ↔ `gTheme`, `gNavMenu` keys ↔ each screen's `Switch`, and the `ActiveProjects` phase allow-list ↔ `project_phase`'s values. Every one of those pairs was already documented in its own file header and nothing had collected them, and every one FAILS SILENTLY when it drifts — INDEX Decisions

## Log pointers, 2026-08 (moved out of INDEX.md)
- 2026-08-19 | issues #34 and #38 built and the #35 probe written: scrProjects search delegated as a prefix match at the cost of 16 branches, scrProject's delete cascade parallelised with per-arm error globals, tests/scrProbe-date-null-delegation.pa.yaml added | sessions/2026-08-19-2130-issues-34-38-probe35.md
- 2026-08-19 | issue #40: the data row limit named once as gDataRowLimit in App.OnStart, seven sentinels repointed, seeded in two OnVisible handlers because OnStart is non-blocking and blank compares as zero; rebuild checklist added to (gitignored) CLAUDE.local.md; tools/balance_check.py added | sessions/2026-08-19-2105-issue-40-datarowlimit.md
- 2026-08-19 | issues #39, #33, #37 built: transaction_product_id indexed in the golden source (no paste); scrProjectEdit's two child-creation loops issued concurrently with split result collections; scrProductEdit's three reference fetches cached per session with a local ticker merge on save | sessions/2026-08-19-2047-issues-39-33-37-built.md
- 2026-08-19 | comment purge across all 22 files in src/: 2914 -> 1689 comment lines, 1054 lines removed, density 18% -> 11%; repeats hoisted to file headers, changelog prose deleted, non-comment lines verified byte-identical per file | sessions/2026-08-19-1815-comment-purge.md
- 2026-08-19 | issue #26: scrReports band 4 reworked to three columns — trend chart moved left and its viewBox cut 900->480, NEW projects-by-phase pie (enumerated, not Distinct), NEW open-tasks-due bars keyed on Today() + 14 days; rowRptAge and gRptAgeA..D/gRptStalled deleted, gRptNoTarget kept on the pie subtitle | INDEX Decisions 2026-08-19
- 2026-08-18 | issue #15: delete for tasks/transactions/issues from scrProject and the three edit screens; issues DETACHED not cascaded; the OnVisible derivation moved into a hidden btnPrjRecompute called by four sites after the probe confirmed Select() fires on an invisible control | sessions/2026-08-18-issue15-delete-children.md
- 2026-08-18 | issue #17: scrProjects filters by project lead via cmpPicker; StartsWith(col,"") keeps the Items branch count at four instead of eight; filter row was full at 718/720 so a second row was forced and the gallery dropped 56px | sessions/2026-08-18-issue17-project-lead-filter.md
- 2026-08-18 | issue #13: task_output_audience added; approval id required only at stage Completed, audience required whenever Output is on; enforcement is app-side only because SharePoint cannot hold a conditional requirement. Paste queue confirmed down to scrTaskEdit alone | sessions/2026-08-18-issue13-output-audience.md
- 2026-08-13 | scrProjects rebuilt on auto-layout after the columns were found to collide below ~850px TemplateWidth; vertical stack now relative; status SVG inlined (a component cannot go in a gallery) | INDEX Decisions 2026-08-13
- 2026-08-13 | scrProjects reworked: coverage filter + show-completed toggle + six-column rows; new SVG cmpProjectStatus; project_phase gains "Not Started" as default | INDEX Decisions 2026-08-13
- 2026-08-13 | layout formulas SURVIVE a paste (probe run in Studio); design + transfer skills, validator check, build-history row and src comments all corrected | tests/README.md
- 2026-08-13 | cmpPicker opens on the first 10 records (current user for person fields); record branches un-gated from query length | INDEX Decisions 2026-08-13
- 2026-08-13 | INDEX pruned 620 lines/231KB to ~80; full prior contents archived verbatim | sessions/ARCHIVE-2026.md
- 2026-08-13 | project_phase derived in-app + Mark-complete button; cmpSelection root cause (Selected not an Output property); scrProject galleries rebuilt; scrProjects reworked | sessions/2026-08-13-1724-project-phase-derivation-and-selection-fix.md
- 2026-08-13 | scrHome rebuilt as a dashboard: four inert cards, my-tasks/my-issues galleries routing to the parent project, chart placeholders + the KPI ring; archive button and confirm modal removed | sessions/2026-08-13-1739-scrhome-dashboard.md
- 2026-08-13 | scrHome chart placeholders replaced with real SVG charts; power-apps-svg skill gains the data-driven section (Concat, Sequence+Index, running totals, literal category tables, the locale decimal trap) | .claude/skills/power-apps-svg/SKILL.md
- 2026-08-13 | scrTaskEdit stage/health/priority converted from ModernCombobox to cmpSelection strips fed from Choices(); stage hoisted to its own full-width row | INDEX Threads
- 2026-08-14 | taskmaster_taskproduct junction added to the golden source; scrTaskEdit's single product field replaced by a staged multi-select reconciled on save | INDEX Threads
- 2026-08-14 | product_assetclass + product_esg added to the schema; scrProductEdit gains a Choices()-fed cmpSelection strip and a Classic/Toggle | INDEX Threads
- 2026-08-15 | claudeBrain reviewed and mined: `/reindex` (a dead reference) plus the authoring, context-economy and GitHub layers imported and adapted; the domain skills confirmed to flow OUT, not in | sessions/2026-08-15-1738-claudebrain-asset-import.md
- 2026-08-17 | scrReports designed as native person-level analytics (fetch-bounded, fold-once); licence gate reversed, client_coverage chosen as the transaction coverage axis, effort proxied by volume + cycle time | sessions/2026-08-17-0019-scrreports-analytics-design.md
- 2026-08-17 | Power BI dropped from scope entirely; licence gate stripped from App/cmpAppBar/4 screens/scrReports, power-bi-dax + power-query-m skills removed, charts are SVG | INDEX Decisions 2026-08-17
- 2026-08-17 | transaction_sales confirmed sales-side and excluded from person productivity; licence gating and the no-native-dashboarding rule confirmed dead | INDEX Decisions 2026-08-17
- 2026-08-17 | main fast-forwarded to the feature branch; cross-currency conversion settled as out of scope | INDEX Decisions 2026-08-17
- 2026-08-17 | scrReports authored end to end (~2,560 lines, 6 bands + person overlay) on one Select()-queued load routine; two code-review passes, 17 findings, 16 fixed and the join cost accepted | sessions/2026-08-17-1448-scrreports-authoring-and-review.md
- 2026-08-18 | scrReports AND scrTaskEdit both LANDED; period narrowed to 1W/1M/1QTR, scope became a manager combobox, five literal Choice arrays bound to Choices(); four paste defects diagnosed | sessions/2026-08-18-0311-scrreports-scrtaskedit-paste-defects.md
- 2026-08-18 | issue #12 LANDED: create a client or product from a staged transaction; scrProjectEdit gains an identity-keyed resume guard, scrClientEdit and scrProductEdit gain NxClient/NxProduct return arms | GitHub #12
- 2026-08-18 | issue #16 CLOSED: project_perc_completion read 0% because gStageWeights was never loaded; task_stage corrected to "Completed" across 22 sites, scrProject now derives and back-fills the value | GitHub #16
- 2026-08-18 | issue #14 diagnosed read-only: the taskmaster_taskproduct reconcile is unguarded so a rejected write reports success, the summary is written before it and independently of it, the output toggle silently deletes links, and the RemoveIf does not delegate; probe-first plan posted, NOTHING written | sessions/2026-08-18-1118-issue14-multiproduct-diagnosis.md
- 2026-08-19 | backlog reviewed and ordered: only #9 and #11 remain; #14 confirmed closed and the stale State block corrected; #9 planned in full (not a pure deletion — the resolution date becomes a derived stamp) | sessions/2026-08-19-0019-backlog-ordering.md
- 2026-08-19 | issue #9 BUILT, unpasted: the resolution-date picker and the opened-by box removed from scrIssueEdit, issue_date_close now derived from the status transition; the stored date is read with a delegable LookUp because ECS would trim it off the gEditIssue snapshot once no control reads it | GitHub #9
- 2026-08-19 | #9 LANDED AND EXERCISED — all four checks clear. The re-save check passing is the one that matters: it proves the derived `issue_date_close` reads the STORED value, so the ECS/snapshot hardening was necessary and correct | GitHub #9
- 2026-08-19 | #11 planned in full and posted to the issue for review, nothing written. Five findings reshaped it: dropping the coverage grid orphans the Coverage combo; the people measures fail UPSTREAM (a Completed task with a blank task_date_completion never enters colRptDone at all, so Done/median/on-time read empty and the screen cannot say why); Tx is project-attributed among task measures; the overlay replacement can read colRptPersonSrc and refetch nothing if it gains 4 fields + a Dup flag; the pie is cmpKpiRing's stroke-dasharray, not arc math | GitHub #11
- 2026-08-19 | #11 BUILT in five commits: the coverage grid, the gap list, the median-cycle chart and the now-orphaned Coverage combo dropped; completed tasks fetched on stage alone because `task_date_completion >= gRptFrom` is FALSE for a blank date and silently emptied every completion measure; Tx dropped from the people table; the person overlay became a task list; band 3 is four columns with two pies; the transaction bars stack by currency beside product-type L1/L2 pies | sessions/2026-08-19-0111-issue11-scrreports-rework.md
- 2026-08-19 | #11 follow-up: the output pie switched from task_output_format to task_output_audience because scrTaskEdit ENFORCES the audience whenever the Output section is on and never enforces the format — so audience stays complete and format accumulates blanks; the young column's Unspecified count is stated in the subtitle because on a pie that case and a broken report look identical. Fam/Fmt dropped from colRptPersonSrc, taking a per-task project LookUp with them | GitHub #11
- 2026-08-19 | outbound port #2 to claudeBrain: the canvas-app family (7 skills, 2 agents, the air-gap brief) plus 4 generic corrections, opened as claudeBrain#36. Nothing imported — the factory's Power Platform copies remain downstream of this repo. Open question asked in the PR: whether the ported skills' `tools/` and `schema/` path references should be genericised | sessions/2026-08-19-0258-claudebrain-canvas-port.md
- 2026-08-19 | #11 LANDED — pastes clean, all charts render. Two paste defects: the task_name error type (ShowColumns over a named formula) and the overlay task row overlapping (absolute cells vs the auto-layout idiom); both fixed, the second unpasted | GitHub #11
- 2026-08-19 | overlay geometry: absolute cells replaced by the auto-layout idiom (LANDED), then the card widened to 1000 with status/health/opened set to zero FillPortions — a bounded vocabulary should never take a share of the slack from a free-text column | GitHub #11
- 2026-08-19 | #11 COMPLETE — scrReports rework landed end to end across five pastes: the subtraction, the data layer, the people section, four-column band 3, three-column band 5, then the overlay geometry, width and font refinements | GitHub #11
- 2026-08-19 | #19 BUILT (authored, not landed): the #17 lead filter, its second filter row and its `cmpPicker` instance deleted; an "Only Show my Tasks" toggle joins coverage/search/show-completed on ONE row; `Items` is eight delegable branches because `person.Email = gUserEmail` has no neutral value where `StartsWith(…, "")` pretended to; gallery back to Y 220. The "720 usable" that forced the second row was the design HEIGHT — the width is 1318 | sessions/2026-08-19-issue19-scrprojects-mine-toggle.md
- 2026-08-19 | #19 LANDED, then scrProjects re-authored: name search moved from delegable StartsWith to a LOCAL contains, as an outer Filter around the delegated eight-branch If — SharePoint delegates no substring op on Text. Ceiling is now the data row limit, silently | GitHub #19
- 2026-08-19 | #19 CLOSED — scrProjects landed in two pastes (the rework, then the contains-search) and the search is exercised. The screen now carries the app's only row-limit-bounded query; everything else on it still folds | GitHub #19
- 2026-08-19 | #20 built: 4 optional product columns. Two calls taken to the user first because names freeze — underlying is plain Text with an EMERGENT chip list (fill-in Choice rejected: Choices() cannot return fill-ins), maturity is YEARS not a date. Normalisation is at write time; maturity uses the classic number idiom because no modern number token is grounded | sessions/2026-08-19-issue20-product-columns.md
- 2026-08-19 | #20 CLOSED — four optional product columns landed with scrProductEdit and the scrReference rows. The paste also settled Split(...).Value, which had shipped as the change's one ungrounded token | GitHub #20
- 2026-08-19 | intermittent "Division by 0" at app start traced to scrHome's timeline dividing by gTlSpan before OnVisible sets it; every other divisor in src/ audited and cleared; fix specified, NOT applied | sessions/2026-08-19-1520-div0-scrhome-timeline.md
- 2026-08-19 | Division-by-0 FIXED in src: scrHome timeline divides by Max(7, gTlSpan) and the three Sets moved above the Collect in both duplicated blocks; awaiting a full-file scrHome paste | sessions/2026-08-19-1520-div0-scrhome-timeline.md
- 2026-08-19 | #21: scrTaskEdit approvals moved off the audience value onto task_output_approval_flag; new tglTkApprovalReq between the audience and the id; schema + two reference docs corrected. Authored, not landed | sessions/2026-08-19-1537-issue21-approval-flag.md
- 2026-08-19 | scrHome + scrTaskEdit both LANDED; task_output_approval_flag internal name confirmed; paste queue empty. Neither behaviour exercised yet | INDEX State
- 2026-08-19 | #21 debriefed and CLOSED (completed). Closed on landing; the completion gate remains unexercised | GitHub #21
- 2026-08-19 | #22: task health derived, not chosen. issue_type re-vocabularied to 7 values; stored issue-half + live overdue half; recompute wired to every issue change; scrReports folds overdue in | sessions/2026-08-19-1600-issue22-derived-health.md
- 2026-08-19 | #22 paste defect: Set() removed from the health-recompute ForAll on scrIssueEdit and scrProject; gHlScrapI/gHlScrapP/gHlErr were all write-only and are gone. LANDED clean. Scan of all 91 ForAll sites finds no other occurrence | GitHub #22
- 2026-08-19 | #22 (derived task health) LANDED and CLOSED after its ForAll paste defect was fixed; backlog is now #23, #24, #25, none started. State block consolidated — the closed-issue narrative moved out, the standing risks kept | GitHub #22
- 2026-08-19 | #23 BUILT: issue_type and issue_impact required with defaults Question/Moderate, "(none)" dropped from both pickers, save unguarded. Two traps handled that the bare requirement misses — the seed is tested against the VOCABULARY (a retired value is not blank) and the DefaultId fallback resolves the default BY LABEL, not Id 1 | GitHub #23
- 2026-08-19 | #24 BUILT: scrHome gains a Projects-I-lead gallery and swaps the timeline for a clamped days-to-due bar chart plus an effective-health donut; rowLists is now the widest band (1252 vs rowCharts 1204) and the old comment claiming the charts govern is corrected | GitHub #24
- 2026-08-19 | #23 and #24 LANDED and CLOSED. #25 BUILT: project_coverage required, default Internal, three-branch seed because the coverage vocabulary is the one this repo has never been told — a wrong default degrades to a real member rather than losing the insert | GitHub #25
- 2026-08-19 | perf review -> 14 issues #27–#40 | sessions/2026-08-19-1853-perf-review-issues.md
- 2026-08-19 | #27 and #31 built, six screens queued for paste | sessions/2026-08-19-1939-issues-27-31-built.md
- 2026-08-19 | #27 and #31 landed and closed; paste queue empty | sessions/2026-08-19-1939-issues-27-31-built.md
- 2026-08-19 | #29 and #36 built; scrTaskEdit and scrIssueEdit queued for paste | sessions/2026-08-19-2012-issues-29-36-built.md
- 2026-08-19 | #29 and #36 landed and closed; paste queue empty | sessions/2026-08-19-2012-issues-29-36-built.md
- 2026-08-19 | #32/#28/#30 built on scrHome in three commits: the fold deduped into btnHomeRecompute (Select is QUEUED, not inline), project names resolved from colPrjNames instead of per visible row, gIssLed counted off one open-issues fetch with a >=2000 fallback. Queued for paste; the Select-from-a-component-OnAction question is the only thing that could fail outright | sessions/2026-08-19-2031-home-32-28-30-built.md
- 2026-08-19 | #32/#28/#30 LANDED and closed; scrHome clean on the paste. Select-from-a-component-OnAction is now grounded, not a risk | sessions/2026-08-19-2031-home-32-28-30-built.md
- 2026-08-20 | docs/manuals/ built: user manual + maintainer manual, 7 chapters each, plus docs/README.md and a root-README documentation section. Root README's stale component count (12->10) and App.pa.yaml description corrected. Three findings left open: screen-map.md is historical, cmpAppBar's default Items carries a dead Admin entry, and the manuals have no staleness hook | sessions/2026-08-20-2346-manuals-section.md


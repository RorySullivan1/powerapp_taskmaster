# 2026-08-19 19:39 · issues-27-31-built

**Goal:** Build #27 (scrProject three concurrent collects) and #31 (edit-screen vocabulary caching)

## What happened
- **#27 BUILT** (`src/Screens/scrProject.pa.yaml`, commit b462cc4). `btnPrjRecompute` opens with
  `Concurrent()` of three ClearCollects — `colProjectTasks` (existing), **`colProjectTx`** and
  **`colProjectIss`** (new). Everything downstream reads the collections: `gPrjOpenIss`, the
  `nX`/`nI` counts in the phase derivation, `galTransactions.Items`, `galIssues.Items`, both
  section titles, `lblTxTotal`, and the two empty-state labels. ~8 reads -> 3, in parallel.
- **#31 BUILT** (five edit screens, commit d461e60). 20 reference fetches guarded to once per
  session and wrapped in `Concurrent()`: scrTaskEdit 7, scrProjectEdit 6, scrIssueEdit 3,
  scrClientEdit 3, scrTransactionEdit 1 (no Concurrent — it needs two arms).
- 22/22 valid after each commit. **Both are authored, NOT landed — the paste queue is no
  longer empty.**

## Gotchas & dead ends
- **The `"(none)"` sentinel defeats an `IsEmpty` guard.** scrTaskEdit's five output lists add
  their own `"(none)"` row, so a `Choices()` that returned nothing still leaves the list
  non-empty: `IsEmpty` would latch a one-option dropdown for the whole session and every save
  would write Blank() from it. Those five guard on **`CountRows(col) <= 1`**. The other 15
  carry no sentinel row, so `IsEmpty` is right for them. Do not "make them consistent".
- **A Concurrent arm is ONE formula**, so the two-statement builds
  (`ClearCollect(col, {sentinel}); Collect(col, ForAll(...))`) had to become one
  `ClearCollect( col, {sentinel}, ForAll(...) )` — ClearCollect takes several items, and order
  is preserved so "(none)" still sorts to the top.
- **Concurrent changes the failure mode, deliberately**: arms fail independently, so a failed
  `Choices()` no longer stops the whole OnVisible. Strictly better, but it retires the
  "order is defensive" comment on scrTaskEdit, which was rewritten rather than deleted.
- Verified before caching: all 20 collections have **exactly one write site** each, so nothing
  elsewhere can Clear a cached list mid-session.
- **Left alone on purpose:** the health-recompute block still reads the raw `taskmaster_issues`
  (its body must stay identical across scrProject and scrIssueEdit, and it runs on scrIssueEdit
  where the collections do not exist); the per-click delete-reference counts still query live
  (a delete guard must not read a snapshot); the delete cascade's three counts are #38's scope.
- `colMapRegion` (scrProjectEdit) and `colMapRegionCl` (scrClientEdit) hold the SAME
  `mapping_regions` list under two names — now one fetch each per session rather than one per
  visit, so not worth the cross-screen churn to unify. Left as is.

## State at end
- **#27 AND #31 LANDED AND CLOSED (user, 2026-08-19).** All six screens — scrProject,
  scrTaskEdit, scrIssueEdit, scrProjectEdit, scrClientEdit, scrTransactionEdit — pasted clean
  on the first attempt. No paste defects, so nothing in this build needed a second pass.
  Repo pushed to main at 3a44928 (fast-forward).
- Perf backlog remaining and unbuilt: #28, #29, #30, #32–#40.

## Open threads
- **Both gains are inferred from the source, not measured.** Landing confirms the pastes were
  accepted and the screens work; the checklist's Live Monitor read counts (3-not-8 on a project
  open, zero reads on a second edit-screen visit) were never reported back. One Live Monitor
  session would settle both if the number ever matters.
- **The Concurrent independent-failure path is unexercised** — arms failing independently only
  shows up when a Choices() actually fails, which it did not.
- The `CountRows(col) <= 1` guard on the five "(none)" lists is likewise untested against a
  real empty fetch; it is reasoning, not an observed repair.

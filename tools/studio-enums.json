{
  "_source": "References/Templates.json inside build/EQD-Taskmaster-smoke.msapp (a real Power Apps Studio export). First-party ground truth for enum values that MS Learn documents but never enumerates.",
  "Icon": [
    "3DPrinting",
    "Add",
    "AddDocument",
    "AddLibrary",
    "AddUser",
    "Airplane",
    "Alarm",
    "ArrowDown",
    "ArrowLeft",
    "ArrowRight",
    "ArrowUp",
    "ArrowsUpDown",
    "Attachment",
    "BackArrow",
    "Bell",
    "Blocked",
    "Bookmark",
    "BookmarkFilled",
    "Bug",
    "Bus",
    "Calculator",
    "Calendar",
    "CalendarBlank",
    "Camera",
    "CameraApertureFocus",
    "Cancel",
    "CancelBadge",
    "Cars",
    "Check",
    "CheckBadge",
    "ChevronDown",
    "ChevronLeft",
    "ChevronRight",
    "ChevronUp",
    "Clock",
    "CollapseView",
    "ColorPicker",
    "Compose",
    "Computer",
    "ComputerDesktop",
    "ComputerSimple",
    "Controller",
    "Copy",
    "Crop",
    "Currency",
    "Cut",
    "Database",
    "DetailList",
    "Diamond",
    "DockCheckProperties",
    "DockLeft",
    "DockRight",
    "Document",
    "DocumentPDF",
    "DocumentwithContent",
    "Download",
    "Draw",
    "Edit",
    "EditFolders",
    "EmojiFrown",
    "EmojiHappy",
    "EmojiNeutral",
    "EmojiSad",
    "EmojiSmile",
    "Enhance",
    "Erase",
    "Error",
    "ExpandView",
    "Export",
    "Filter",
    "FilterFlat",
    "FilterFlatFilled",
    "Flag",
    "Folder",
    "ForkKnife",
    "Globe",
    "GlobeChangesPending",
    "GlobeError",
    "GlobeNotConnected",
    "GlobeRefresh",
    "GlobeWarning",
    "HalfFilledCircle",
    "Hamburger",
    "Health",
    "Heart",
    "Help",
    "Hide",
    "History",
    "Home",
    "HorizontalLine",
    "Hospital",
    "Import",
    "Information",
    "Items",
    "Journal",
    "Key",
    "Leave",
    "LevelsLayersItems",
    "Lightbulb",
    "LightningBolt",
    "LikeDislike",
    "Link",
    "ListScrollEmpty",
    "ListScrollWatchlist",
    "ListWatchlistRemind",
    "Lock",
    "LogJournal",
    "Mail",
    "Manufacture",
    "Map",
    "Medical",
    "Message",
    "Microphone",
    "Mobile",
    "Money",
    "More",
    "Newspaper",
    "NextArrow",
    "Note",
    "Notebook",
    "OfficeBuilding",
    "OpenInNewWindow",
    "OptionsList",
    "Paste",
    "People",
    "Person",
    "Phone",
    "PhoneHangUp",
    "Phonebook",
    "PhotosPictures",
    "Pin",
    "Post",
    "Print",
    "Publish",
    "QuestionMark",
    "RadarActivityMonitor",
    "Redo",
    "Reload",
    "Reset",
    "Ribbon",
    "Save",
    "Scan",
    "Search",
    "Send",
    "Settings",
    "Share",
    "Shirt",
    "Shop",
    "ShoppingCart",
    "Signal",
    "Support",
    "Sync",
    "Tablet",
    "Tag",
    "Text",
    "ThumbsDown",
    "ThumbsDownFilled",
    "ThumbsUp",
    "ThumbsUpFilled",
    "Tools",
    "ToolsWrench",
    "Train",
    "Trash",
    "Tray",
    "Trending",
    "TrendingHashtag",
    "TrendingUpwards",
    "Undo",
    "Unlock",
    "Vehicles",
    "VerticalLine",
    "Video",
    "View",
    "Waffle",
    "Warning",
    "Waypoint",
    "Weather",
    "ZoomIn",
    "ZoomOut",
    "clearDrawing"
  ],
  "GalleryVariant": [],
  "GroupContainerVariant": [
    "AutoLayout",
    "GridLayout"
  ],
  "_containerNotes": {
    "control": "GroupContainer@1.5.0",
    "variant": "AutoLayout",
    "confirmed_from": "two photos of Studio code view, 2026-08-04 (horizontal, then vertical+scroll)",
    "container_properties": [
      "LayoutDirection  = LayoutDirection.Horizontal | LayoutDirection.Vertical",
      "LayoutAlignItems = LayoutAlignItems.Center (also Start/End/Stretch per docs)",
      "LayoutGap        = <number>",
      "LayoutOverflowX  = LayoutOverflow.Scroll  (also .Hide per docs)",
      "LayoutOverflowY  = LayoutOverflow.Scroll",
      "PaddingTop / PaddingBottom / PaddingLeft / PaddingRight",
      "X / Y / Width / Height  — on the container itself only"
    ],
    "child_properties": [
      "FillPortions   = <number>. How much of the SPARE space along the parent's Direction this child claims, as a share of what all children claim. 0 = none, i.e. the formula-bar equivalent of turning \"Flexible width/height\" OFF.",
      "LayoutMinWidth  = <number>. Minimum size along the parent's Direction.",
      "LayoutMinHeight = <number>.",
      "AlignInContainer = per-child override of the parent LayoutAlignItems (Set by container | Start | End | Center | Stretch) — display name confirmed, pa-yaml token NOT confirmed"
    ],
    "still_inferred": [
      "LayoutJustifyContent",
      "LayoutWrap"
    ],
    "why_it_matters": "Children of an AutoLayout container carry NO X/Y — the container places them. X/Y/Width/Height are exactly what Studio freezes to constants on paste, so container-laid-out content is the only layout that survives this gap intact.",
    "flexible_sizing_default": "A child of an auto-layout container has \"Flexible width\" (or height, along the parent's Direction) turned ON BY DEFAULT — MS Learn walks you through switching it off in \"Building responsive canvas apps\". While it is on, an explicit Width/Height on that child is ADVISORY: the container distributes space by FillPortions instead. This is why setting a child to a fixed width appears to do nothing. Set FillPortions: =0 to pin it, and LayoutMinWidth as a floor. Cost this repo four iterations of cmpNestedSelect, 2026-08-05.",
    "fillportions_provenance": "Documented on the Horizontal container control page as \"Fill portions\": \"Defines how the individual component grows when there is more screen real-estate assigned to its parent... if child A has Fill portions 1 and child B has 2, A gets 1/3 of the extra space.\" Doc-grounded 2026-08-05, not yet seen in a Studio export."
  },
  "_modernControls": {
    "confirmed_from": "user-supplied Studio YAML + MS Learn output properties, 2026-08-04",
    "_modernVersions": {
      "source": "Read off the LIVE Studio controls by the user, 2026-08-07. This is the only trustworthy source for a modern control's pa-yaml version.",
      "table": "Text Input 1.1.1 | Combo box 1.1.1 | Dropdown 1.0.2 | Date picker 1.0.1 | Radio 1.0.1 | Data grid 1.5.0 | Card 1.3.0",
      "the_rule": "VERSIONS ARE PER-CONTROL AND NOT GUESSABLE. Nothing in that table is 1.0.0 and no two entries share a version, so a version can never be pattern-matched across the modern family or assumed from a sibling. This repo previously carried @1.0.0 on every modern token purely because the first few grounded that way.",
      "when_unsure": "USE THE CLASSIC CONTROL. A wrong version rejects the WHOLE paste and comes back as \"it didn't work\", so an unverified modern token is never worth it. ModernButton and ModernNumberInput were converted to Classic/Button@2.2.0 and Classic/TextInput@2.3.2 on 2026-08-07 for exactly this reason — their versions are unknown.",
      "ModernRadio@1.0.1": "Version confirmed; properties not yet needed. Items, ItemDisplayText, Default (an ITEM from Items), Selected, Layout.",
      "ModernDataGrid@1.5.0": "Version confirmed; unused. Note the 1.5.x line — further evidence that modern versions do not cluster near 1.0.0.",
      "card_added": "ModernCard@1.3.0 read off Studio code view 2026-08-10, same photo as GridLayout. The same photo re-confirmed ModernCombobox@1.1.1, which this file already carried — an independent corroboration of the per-control version rule."
    },
    "ModernTextInput@1.1.1": {
      "output": "Text  — SAME as classic, so no formula that reads an input had to change",
      "props": "Placeholder, Type, TriggerOutput, MaxLength, Required, Default, Appearance, Align, AccessibleLabel, ValidationState, DisplayMode, Color/Size/Italic/Underline/Strikethrough",
      "two_traps_paid_for": "Type, NOT Mode. The modern text input takes Type: =TextInputType.Multiline; `Mode: =TextMode.MultiLine` is the CLASSIC text input's property and authoring it on the modern control is a property error. And TriggerOutput, NOT DelayOutput: =TriggerOutput.Delayed is right here — DelayOutput belongs to the COMBO BOX. Both authored wrongly on scrTaskEdit 2026-08-07 and both confirmed on MS Learn modern-control-text-input. Sibling modern controls do NOT share property names."
    },
    "_modernNumberInput_TOKEN_UNKNOWN": {
      "status": "VERSION NOT GROUNDED — do NOT author a ModernNumberInput token; the `@1.0.0` once keyed here was a GUESS and a wrong version rejects the whole paste. Per the powerapp-canvas-controls SKILL this control was CONVERTED to Classic/TextInput@2.3.2 (read as Value(ctl.Text)). Semantics below are reference only.",
      "output": "Value  — a real number, not text",
      "props": "Default, Min, Max, Step, Precision (DecimalPrecision.'0'|'1'|'2'), HintText, Align, Appearance, AccessibleLabel. NOTE: TriggerOutput was REMOVED from this control"
    },
    "ModernDatePicker@1.0.1": {
      "output": "SelectedDate  — Blank() when unset",
      "props": "Placeholder, DefaultDate, StartDate, EndDate, StartOfWeek, Format (DatePickerFormat.LongAbbreviated|Short|Long or a custom string), IsEditable, DateTimeZone"
    },
    "_modernTabList_TOKEN_UNKNOWN": {
      "status": "VERSION NOT GROUNDED — do NOT author a ModernTabList token; the `@1.0.0` once keyed here was a GUESS and a wrong version rejects the whole paste. Author a classic control until the version is read off a live Studio control. Semantics below are reference only.",
      "output": "Selected  — .Selected.Value is the tab label",
      "props": "Items (array literal), Default, Appearance (TabListAppearance.Underline|Filled|Subtle), Alignment (TabListAlignment.Start|Center|End), Align (Align.Left|Center|Right), Size, FontWeight, AccessibleLabel, OnChange, OnSelect, DisplayMode",
      "default_is_an_ITEM": "Default must be an ITEM FROM Items, not a value equal to one. `Default: =\"Tasks\"` ERRORS. A string-array Items yields RECORDS with a Value column — which is why the output is read as .Selected.Value — so the default must be a record too: =First([\"Tasks\",\"Transactions\",\"Issues\"]) or =LookUp([...], Value = \"Tasks\"). MS Learn modern-control-tabs-or-tabs-list: \"Default — The tab that is selected by default... Must match an item from the Items source.\" NOTE this DIFFERS from ModernDropdown, whose Default takes a VALUE. Same property name, two contracts.",
      "height_warning": "MS Learn Limitations: \"Very small or very large width and height values might not be fully respected by the control.\" Never lay out the band below a tab list from a height you ASSUMED it would take — use the height the control actually carries and leave a real gap under it."
    },
    "_modernButton_TOKEN_UNKNOWN": {
      "status": "VERSION NOT GROUNDED — do NOT author a ModernButton token; the `@1.0.0` once keyed here was a GUESS and a wrong version rejects the whole paste. Per the powerapp-canvas-controls SKILL this control was CONVERTED to Classic/Button@2.2.0 (carry the look on Fill/Color/BorderColor/BorderThickness). Semantics below are reference only.",
      "output": "none — OnSelect only",
      "props": "Text, OnSelect, Appearance (ButtonAppearance.Primary|Outline|Subtle), Icon (a STRING Fluent icon name, e.g. \"Checkmark\" — not the classic Icon enum), IconStyle, Layout (ButtonLayout.IconBefore), Tooltip, AccessibleLabel"
    },
    "kept_classic_deliberately": "Classic/Button for the ~30 transparent full-template gallery hit targets — they depend on Fill: =RGBA(0,0,0,0) and BorderThickness: =0, which a Fluent button does not expose. Classic/Icon throughout, for the same reason plus the 180-value Icon enum.",
    "Classic/ComboBox@2.4.0": {
      "output": "Selected / SelectedItems  — as the modern one",
      "seed": "DefaultSelectedItems  — CONFIRMED on this control (=Parent.Default inside a data card)",
      "props": "Items, SelectMultiple, DefaultSelectedItems, IsSearchable, SearchFields, DisplayFields, InputTextPlaceholder, OnChange, OnNavigate, SelectionColor, SelectionFill, Chevron{Fill,Background,HoverFill,HoverBackground,DisabledFill,DisabledBackground}, DisplayMode, Tooltip, AccessibleLabel, TabIndex, plus the usual colour/border set",
      "props_source": "MS Learn `controls/control-combo-box`, Key properties + Additional properties, read 2026-08-06. IsSearchable, SearchFields, DisplayFields and InputTextPlaceholder are all first-party documented on THIS control — they are not inferred.",
      "note": "NOT used in src/ — this repo's 10 combo boxes are ModernCombobox@1.1.1 (see that entry; user-confirmed from live Studio 2026-08-05/06). Modern controls ARE enabled in this app (confirmed 2026-08-06). Kept here because Studio still generates this CLASSIC control inside classic data cards. IsSearchable + SelectMultiple both false gives plain dropdown behaviour.",
      "display_column": "DisplayFields: =[\"Value\"] and SearchFields: =[\"Value\"] — STRING ARRAYS, not ItemDisplayText. Confirmed from a Studio export 2026-08-05. This is the classic family's way; the modern combo box uses ItemDisplayText: =ThisItem.<col> and the two are not interchangeable.",
      "export_confirmed": "Full code-view export 2026-08-05 over a Distinct() Items formula. Also confirms LayoutMinWidth / LayoutMinHeight are emitted on a control inside an auto-layout container. FillPortions did NOT appear — Studio emits only non-defaults, so that is consistent with it being at default rather than with it being invalid."
    },
    "Classic/DropDown@2.3.1": {
      "output": "Selected — a record. SelectedText is DEPRECATED; read .Selected.<Column>",
      "display_column": "Items.Value  — a DOTTED property name; the column of Items to show",
      "props": "Items, Items.Value, Default, AllowEmptySelection, Chevron{Fill,Background,HoverFill,HoverBackground,DisabledFill,DisabledBackground}, Selection{Color,Fill}, Padding*, plus the usual colour/border set",
      "note": "Capital D in DropDown. Single-select only — use a ListBox or ComboBox for multiple."
    },
    "ListBox@2.2.0": {
      "output": "Selected (record) · SelectedItems (table, when SelectMultiple)",
      "display_column": "Items.Value  — same dotted form as the dropdown",
      "props": "Items, Items.Value, Default, SelectMultiple, Selection{Color,Fill}, DisabledSelection{Color,Fill}, Padding*, plus the usual colour/border set",
      "note": "NO Classic/ prefix — it is plain `ListBox@2.2.0`, the same trap as `Timer`. Only ONE item can be pre-selected via Default; for multiple defaults use a combo box."
    },
    "ModernDropdown@1.0.2": {
      "output": "Selected — a record; .Selected.Value for a string array",
      "props": "Items, ItemDisplayText, Default (a VALUE, not a table), Appearance, AccessibleLabel, ValidationState, OnChange",
      "note": "Grounded from MS Learn's own YAML sample rather than a Studio export. Single-select; the combo box is the multi-select / searchable one. Note Default here takes a value, where ModernCombobox takes DefaultSelectedItems, a TABLE."
    },
    "Classic/Toggle@2.1.0": {
      "output": "Value — a boolean",
      "props": "Default (boolean), Value, TrueText/FalseText, ShowLabel, TextPosition, TrueFill/TrueHoverFill, FalseFill/FalseHoverFill, HandleFill, RailFill, OnCheck, OnUncheck, OnChange, plus the usual colour/border set",
      "studio_defaults": "BorderColor RGBA(0,0,0,0) · BorderStyle.None · Color RGBA(50,49,48,1) · DisabledBorderColor RGBA(0,0,0,0) · FalseFill RGBA(96,94,92,1) · FalseHoverFill RGBA(50,49,48,1) · FocusedBorderThickness 4 · Font.'Segoe UI' · HoverBorderColor RGBA(0,0,0,0) · PressedBorderColor RGBA(0,0,0,0) · TrueFill RGBA(0,120,212,1) · TrueHoverFill RGBA(16,110,190,1)",
      "note": "PREFIXED — `Classic/Toggle@2.1.0` — because a modern Toggle shares the name. OnCheck/OnUncheck fire on the transition; OnChange fires on both. A toggle is a two-state control only — for a tri-state (yes/no/unset) SharePoint Boolean, use a dropdown or you cannot express Blank()."
    },
    "RichTextEditor@2.7.0": {
      "output": "HtmlText — an HTML STRING, not plain text",
      "props": "Default (HTML in), HtmlText (out), EnableSpellCheck, DisplayMode, BorderColor, plus size/position",
      "studio_defaults": "BorderColor RGBA(245,245,245,1)",
      "note": "NO Classic/ prefix. The IN property is Default and the OUT property is HtmlText — they are NOT the same name, unlike a text input. What it emits is markup, so a SharePoint Multiple lines of text column receiving it must be Enhanced rich text; a plain-text column will store the tags literally. Read it back into an HtmlViewer@2.1.0, never into a Label."
    },
    "Rating@2.1.0": {
      "output": "Value — a number, 0 when unrated",
      "props": "Default, Max, ReadOnly, RatingFill, ShowValue, BorderThickness, FocusedBorderThickness, plus the usual colour/border set",
      "studio_defaults": "BorderColor RGBA(0,0,0,0) · BorderStyle.None · BorderThickness 2 · FocusedBorderThickness 4 · RatingFill RGBA(16,110,190,1)",
      "note": "NO Classic/ prefix. The modern Rating control adds Step (half stars); the classic one is whole stars only. Max defaults to 5 — set it explicitly if the column's scale is anything else."
    },
    "Classic/Slider@2.1.0": {
      "output": "Value — a number",
      "props": "Default (initial value), Min (default 0), Max (default 100), Layout (Layout.Horizontal|Vertical), HandleSize, ShowValue, ReadOnly, Reset, OnChange, RailFill/RailHoverFill, ValueFill/ValueHoverFill, HandleFill/HandleActiveFill/HandleHoverFill, plus the usual colour/border set",
      "studio_defaults": "BorderColor RGBA(0,0,0,0) · BorderStyle.None · BorderThickness 2 · DisabledBorderColor RGBA(0,0,0,0) · FocusedBorderThickness 4 · HandleActiveFill RGBA(255,255,255,1) · HandleHoverFill RGBA(255,255,255,1) · HoverBorderColor RGBA(0,0,0,0) · PressedBorderColor RGBA(0,0,0,0) · RailFill RGBA(184,187,184,1) · RailHoverFill RGBA(222,236,249,1) · ValueFill RGBA(96,94,92,1) · ValueHoverFill RGBA(0,120,212,1)",
      "note": "PREFIXED. RailFill is the track to the RIGHT of the handle, ValueFill the filled part to the LEFT — swap them and the slider reads backwards. A slider cannot express Blank(): it always has a number, so it cannot represent an unset optional column. Min/Max are inclusive; set them explicitly, because the 0–100 default silently clamps any real-world scale."
    },
    "_modernSlider_TOKEN_UNKNOWN": {
      "status": "VERSION NOT GROUNDED — do NOT author a ModernSlider token; the `@1.0.0` once keyed here was a GUESS and a wrong version rejects the whole paste. Author Classic/Slider@2.1.0 until the version is read off a live Studio control. Semantics below are reference only.",
      "output": "Value — a number, READ-ONLY on this control",
      "props": "Default (the input — NOT Value), Min, Max, LayoutDirection (LayoutDirection.Horizontal|Vertical), Size (SliderSize.Small|Medium|Large), BasePaletteColor, AccessibleLabel, Tooltip, OnChange",
      "note": "Semantics grounded from MS Learn's own YAML sample, not a Studio export — same provenance as ModernDropdown. VERSION TRAP: this control was revised. The OLD shape set `Value:` as the INPUT and `Layout: =\"Horizontal\"` as a STRING; the current shape splits them — `Default` in, `Value` read-only out — and takes typed enums (`LayoutDirection.Horizontal`, `SliderSize.Medium`). Any sample found online that assigns `Value:` is pre-revision and will not behave as written."
    },
    "Classic/Radio@2.3.0": {
      "output": "Selected — a record; .Selected.Value is the option text. SelectedText is DEPRECATED",
      "display_column": "Items.Value  — the same dotted form as DropDown and ListBox",
      "props": "Items, Items.Value, Default, Layout (Layout.Vertical|Horizontal), RadioSize, RadioBackgroundFill, RadioBorderColor, RadioSelectionFill, LineHeight, Padding*, Font, Size, Color/HoverColor/PressedColor/DisabledColor, plus the usual colour/border set",
      "studio_defaults": "BorderColor RGBA(50,49,48,1) · BorderStyle.None · BorderThickness 2 · Color RGBA(50,49,48,1) · DisabledBorderColor RGBA(0,0,0,0) · DisabledColor RGBA(161,159,157,1) · FocusedBorderThickness 4 · Font.'Segoe UI' · HoverColor RGBA(50,49,48,1) · HoverFill RGBA(0,0,0,0) · Items.Value =Value · PaddingTop/Bottom 5 · PressedColor RGBA(50,49,48,1) · PressedFill RGBA(0,0,0,0) · RadioBorderColor RGBA(50,49,48,1) · RadioSelectionFill RGBA(0,120,212,1)",
      "note": "PREFIXED. THIRD control to use the dotted `Items.Value:` display-column key, after DropDown and ListBox — that is now the classic-family convention, not a quirk. Single-select and always-visible, so it costs vertical space per option: right for 2–7 options, wrong beyond that (use a combo box). MS's guidance for the modern one is the same. LineHeight sets the gap between options."
    },
    "_modernRadio_TOKEN_UNKNOWN": {
      "status": "Semantics grounded on MS Learn; the pa-yaml TOKEN is NOT grounded — do not author it. The docs page is 'Radio modern control' (modern-controls-radio-group) and never names the token.",
      "semantics": "Items, ItemDisplayText, Default, Selected (out), Layout (Layout.Vertical|Horizontal), Required, TriggerOutput, RadioSize, RadioBackgroundFill/BorderColor/SelectionFill, BasePaletteColor",
      "enum_trap": "The modern Radio's orientation enum is `Layout.Vertical`, but the modern Slider's is `LayoutDirection.Horizontal` and a GroupContainer's is `LayoutDirection.Vertical`. Same concept, two enum names, no rule — check per control."
    },
    "ModernCombobox@1.1.1": {
      "output": "Selected  — a record; .Selected.Value for a string array",
      "props": "Items (array literal), ItemDisplayText, InputTextPlaceholder, SelectMultiple, IsSearchable, MultiValueDelimiter, Required, ValidationState",
      "seed": "DefaultSelectedItems — CONFIRMED on this control's own MS Learn page: 'the initial items selected… Must be a TABLE OF RECORDS from the Items data source.' With Items as an array literal, =[gVar] is the matching shape. No longer inferred.",
      "version": "VERSION IS 1.1.1 — user-confirmed twice from the live Studio control (2026-08-05, 2026-08-06). MS Learn's YAML sample on modern-controls/modern-control-combobox still emits @1.0.0 on the same page whose prose documents the revision; doc samples lag a control revision, so the live app wins and @1.0.0 is kept allow-listed only as a fallback. An earlier @1.1.1 paste DID fail, but modern controls were DISABLED in the app at the time — a disabled template is unavailable whatever version you name it, so that failure is not evidence against the token. Versions are PER-CONTROL and NO TWO MATCH: the grounded modern tokens are ModernTextInput@1.1.1, ModernCombobox@1.1.1, ModernDropdown@1.0.2, ModernDatePicker@1.0.1, ModernRadio@1.0.1, ModernDataGrid@1.5.0 — not one is @1.0.0. Never pattern-match a version across the family.",
      "revision_shape": "The revision is real even though the token does not move: TriggerOutput -> DelayOutput, Fields -> ItemDisplayText, SelectMultiple now defaults TRUE, Appearance/ValidationState take typed enums, FontColor/FontSize/BorderRadius -> Color/Size/Radius{TopLeft,...}. There is NO Reset PROPERTY — classic ListBox has one, this does not, and authoring it fails the paste. The Reset() function works, and now clears SearchText as well as the selection.",
      "display_column": "ItemDisplayText: =ThisItem.Value. NOT the classic pair DisplayFields/SearchFields — crossing the two families renders BLANK ROWS rather than erroring. Items from Distinct(), a [\"a\",\"b\"] literal or Choices() all expose a single column named Value."
    }
  },
  "_controlPrefixRule": {
    "rule": "`Classic/` appears on exactly those control names that a modern Fluent control also uses, and is absent where no modern namesake exists.",
    "prefixed": [
      "Classic/Icon@2.5.0",
      "Classic/TextInput@2.3.2",
      "Classic/Button@2.2.0",
      "Classic/ComboBox@2.4.0",
      "Classic/DropDown@2.3.1",
      "Classic/Toggle@2.1.0",
      "Classic/Slider@2.1.0",
      "Classic/Radio@2.3.0"
    ],
    "bare": [
      "Timer",
      "ListBox@2.2.0",
      "RichTextEditor@2.7.0",
      "Rating@2.1.0",
      "Gallery@2.15.0",
      "Image@2.2.3",
      "HtmlViewer@2.1.0",
      "Label@2.5.1",
      "Rectangle@2.3.0",
      "Form@2.4.4",
      "TypedDataCard@1.0.7",
      "GroupContainer@1.5.0"
    ],
    "status": "PREDICTIVE ONLY — derived from every token grounded so far, and re-tested against Slider and Radio on 2026-08-05 (both prefixed, both with modern namesakes). Use it to guess which form to ask for; never to author an ungrounded token. Casing is still per-token (`DropDown` has a capital D, `ComboBox` a capital B)."
  },
  "FormVariant": [
    "Classic"
  ],
  "DataCardVariant": [
    "ClassicTextualEdit"
  ],
  "_formNotes": {
    "confirmed_from": "two photos of Studio code view, 2026-08-04 (Edit form over asset_library)",
    "form": "Form@2.4.4 · Variant: Classic · Layout: Vertical · Properties.DataSource",
    "card": "TypedDataCard@1.0.7 · Variant: ClassicTextualEdit · IsLocked: true",
    "card_properties": [
      "DataField: =\"Title\"            — a STRING, the column's internal name",
      "Default: =ThisItem.<column>     — the card's value in from the form's Item",
      "Update: =DataCardValue1.Text    — the card's value OUT to SubmitForm",
      "DisplayName / MaxLength: =DataSourceInfo([@list], DataSourceInfo.X, 'Title')",
      "Required: =false"
    ],
    "card_children_contract": [
      "MetadataKey: FieldName     — the caption label, Text: =Parent.DisplayName",
      "MetadataKey: FieldValue    — the input, Default: =Parent.Default, MaxLength: =Parent.MaxLength,",
      "                             DisplayMode: =Parent.DisplayMode,",
      "                             BorderColor: =If(IsBlank(Parent.Error), Parent.BorderColor, Color.Red)",
      "MetadataKey: ErrorMessage  — the validation label"
    ],
    "schema_keys_confirmed_in_the_wild": "Layout:, MetadataKey:, IsLocked: — all accepted by pa.schema.v3.0",
    "note": "DataSourceInfo's column argument is a SINGLE-QUOTED IDENTIFIER ('Title'), not a double-quoted string — consistent with the 3.24042 identifier change."
  },
  "_powerFxNotes": {
    "Choices column argument": "Studio writes it as a SINGLE-QUOTED identifier — Choices([@asset_library].'asset_owner'). Quotes are only required when the identifier needs escaping, so this repo's unquoted snake_case form is equally valid; the quoted form is Studio being defensive.",
    "Self": "PaddingLeft: =If(Self.DisplayMode = DisplayMode.Edit, 5, 0) — Self is available in a control's own property formulas.",
    "Dotted property names": "A property key can be qualified with a dot: `Items.Value: =Value` on a classic DropDown or ListBox names the column of Items to display. Valid pa-yaml, and easy to mistake for a typo."
  },
  "_gridLayoutNotes": {
    "control": "GroupContainer@1.5.0",
    "variant": "GridLayout",
    "confirmed_from": "photo of Studio code view, 2026-08-10 (user-supplied). Answers the 'non-AutoLayout variant name' that this file and powerapp-canvas-controls both carried as INFERRED.",
    "observed_verbatim": [
      "- Container1:",
      "    Control: GroupContainer@1.5.0",
      "    Variant: GridLayout",
      "    Properties:",
      "      X: =40",
      "      Y: =40",
      "    Children:",
      "      - Card1:",
      "          Control: ModernCard@1.3.0",
      "          Properties:",
      "            X: =40",
      "            Y: =40",
      "      - Combobox1:",
      "          Control: ModernCombobox@1.1.1",
      "          Properties:",
      "            LayoutGridColumnEnd: =5",
      "            LayoutGridColumnStart: =3",
      "            LayoutGridRowEnd: =3",
      "            LayoutGridRowStart: =1",
      "            X: =40",
      "            Y: =40"
    ],
    "child_properties": [
      "LayoutGridColumnStart = <number>",
      "LayoutGridColumnEnd   = <number>",
      "LayoutGridRowStart    = <number>",
      "LayoutGridRowEnd      = <number>"
    ],
    "x_y_are_written_anyway": "Studio emitted X: =40 / Y: =40 on the grid child ALONGSIDE the four LayoutGrid* properties, exactly as it does for auto-layout children whose X/Y are ignored. Do not read the presence of X/Y as evidence that absolute positioning applies.",
    "INFERRED_not_observed": [
      "That Start/End are CSS-grid style LINE indices rather than cell indices. The sample reads ColumnStart 3 / ColumnEnd 5 and RowStart 1 / RowEnd 3, which under the line reading spans 2 columns and 2 rows. Plausible, NOT confirmed — a cell-index reading would make it 3 columns and 3 rows.",
      "Whether the indices are 1-based. The sample's lowest value is 1 (RowStart), which is consistent with 1-based but does not prove it.",
      "Whether omitting the four properties auto-places a child in the next free cell."
    ],
    "STILL_UNKNOWN_the_big_one": "HOW THE GRID ITSELF IS DEFINED. Nothing in the sample declares the column count, row count or their sizes — the container carries only X/Y. There must be a property (or a Studio-pane-only setting) that does this, and it is NOT grounded. Do not author a GridLayout container until it is: a child placed into a grid whose shape you cannot express is a guess.",
    "verdict": "SAFE to author the four CHILD properties on a grid that Studio already created. NOT safe to author a GridLayout container from scratch."
  }
}

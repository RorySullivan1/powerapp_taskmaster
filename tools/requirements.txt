# Python deps for the pre-paste tooling. Install once:
#   python -m pip install -r tools/requirements.txt
#
# validate_pa_yaml.py needs: PyYAML (parse), jsonschema (schema pass)
# split_components.py  needs: PyYAML + ruamel.yaml (round-trip that keeps block scalars)
PyYAML>=6.0
jsonschema>=4.0
ruamel.yaml>=0.17

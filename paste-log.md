# Paste log

Every crossing of the air gap, newest first. A row exists only once a human has pasted the unit
into Studio and confirmed the outcome. Nothing is "in the app" without a row here.
See the `studio-transfer` skill for the lifecycle and the rename-and-log rule.

| Date | Direction | Target screen | Intended name | Studio suffix | Channel | Source file | Outcome | Notes |
|------|-----------|---------------|---------------|---------------|---------|-------------|---------|-------|
| 2026-08-04 | repo → Studio | scrHome | cmpToastHome | — | Preview | `components/cmpToast.pa.yaml` | **RESOLVED — STALE STUDIO** | Reported first as *"the duration doesn't work"*, then as *"the toast doesn't even appear"*. **The real cause was Studio serving a stale component definition** — the editor kept running the old build until a browser refresh, so edits appeared to do nothing. **This is a channel fact, not an app bug, and it is the most expensive kind: a false negative is indistinguishable from a real failure across a one-way gap, and I rewrote `cmpToast` twice chasing it.** Refresh Studio before reporting any failure. The redesign is KEPT on its own merits — `IsOpen` as an Input with the timer on `Start: =IsOpen` / `Reset: =!IsOpen` has one source of truth, where the old `Show()` action plus an internal `_show` had two and raced a `Reset()` against the variable the timer's `Start` reads. But it should not be credited with the fix. |
| 2026-08-04 | Studio → chat (photo) | — | Classic/ComboBox | — | Code view | `tools/studio-enums.json` | **TOKEN GROUNDED + open question settled** | **`Classic/ComboBox@2.4.0`** — a different control from `ModernCombobox@1.0.0`; Studio generates the classic one inside data cards. Adds `Chevron*` styling and seeds its selection with **`DefaultSelectedItems: =Parent.Default`** — which **corroborates the one property still inferred in this repo**. It is now confirmed on the CLASSIC control and listed for the Fluent combo box family on MS Learn, but still never seen on ModernCombobox itself, so it stays flagged as corroborated-not-proven with a one-line fallback. Also recorded: Studio writes a column inside `Choices()` as a **single-quoted identifier** (`Choices([@asset_library].'asset_owner')`) — quotes are only required when the identifier needs escaping, so this repo's unquoted snake_case form is equally valid. |
| 2026-08-04 | Studio → chat (photos) | — | Form / DataCard | — | Code view | `tools/studio-enums.json` | **TOKENS GROUNDED** | The Edit form is **`Form@2.4.4`** (`Variant: Classic`, `Layout: Vertical`, `Properties.DataSource`) holding **`TypedDataCard@1.0.7`** (`Variant: ClassicTextualEdit`, `IsLocked: true`). The card is the contract — `Default` in, `Update: =DataCardValue1.Text` out, children bound via `Parent.DisplayName` / `Parent.Default` / `Parent.MaxLength` / `Parent.DisplayMode` / `If(IsBlank(Parent.Error), …)` and tagged `MetadataKey: FieldName|FieldValue|ErrorMessage`. **`Layout:`, `MetadataKey:` and `IsLocked:` are first-class pa-yaml keys** — the schema accepted them already; only the token allow-list rejected the controls. `DataSourceInfo`'s column argument is a single-quoted IDENTIFIER (`'Title'`). Registered and probe-validated. (The sample was supplied as control/YAML context, not as a statement about its data source.) |
| 2026-08-04 | Studio → chat (YAML) | scrProjectEdit | combobox / date picker | — | Code view | `src/authored/scrProjectEdit.pa.yaml` | **TOKENS GROUNDED + FORM COMPACTED** | User supplied working Studio YAML for both: **`ModernCombobox@1.0.0`** (`Items` as an array literal, `ItemDisplayText`, `InputTextPlaceholder`, `SelectMultiple`, `IsSearchable`, `MultiValueDelimiter`) and **`ModernDatePicker@1.0.0`** (`Placeholder`, `DefaultDate`, `StartDate`, `EndDate`, `StartOfWeek`, `Format`). Output properties confirmed from MS Learn: **`cbo.Selected.Value`** and **`dtp.SelectedDate`**. Replaced 6 cmpSelection strips and 4 typed-date + echo-label pairs on scrProjectEdit — each 40px row becomes one 32px control and the echo labels are deleted outright, because a date picker cannot produce an unparseable date. **Dates are now real date values end to end**: no `DateValue()` parsing survives on the screen, and the staging collections carry dates rather than text. One inferred property remains: `DefaultSelectedItems`, which seeds the current value on Edit. |
| 2026-08-04 | Studio → chat (photo) | — | container | — | Code view | `tools/studio-enums.json` | **TOKEN GROUNDED** | The auto-layout container is **`GroupContainer@1.5.0`** with **`Variant: AutoLayout`** — one control for both *Horizontal container* and *Vertical container*, direction set by `LayoutDirection`. Confirmed from the photo: `LayoutDirection`, `LayoutAlignItems`, the four `Padding*`, and child-side `LayoutMinWidth`/`LayoutMinHeight`. **Children carry no X/Y** — which means anything inside an auto-layout container is immune to the paste-freezing of X/Y/Width/Height. **A second photo confirmed the rest**: `LayoutGap`, `LayoutOverflowX`, `LayoutOverflowY`, and the enum `LayoutOverflow.Scroll` — every `Layout*` inference was right. Only `LayoutJustifyContent`, `LayoutWrap` and `FillPortions` remain unconfirmed (never non-default in either photo). Added to the validator's known controls and variants. |
| 2026-08-04 | Studio → chat | scrProjectEdit (+2 screens) | tab strips, people pickers | — | Preview | `src/authored/scr*.pa.yaml` | **TWO FIXES** | (1) *"people need to be searchable by standard name and not just email"* — `Office365Users.SearchUser` V1 matched only the address in practice, despite the doc table claiming display name / given name / surname. Switched all **8 people pickers** to **`SearchUserV2(...).value`**, the form MS's own current example uses. (2) *"switching between classification, tasks, etc, does not work"* — the three tab strips were `cmpSelection` instances setting the tab from `<strip>.Selected.Id` inside `OnChange`, i.e. **reading the gallery's selection in the same behaviour pass that `Select(Parent)` sets it** — the same race shape as cmpToast's `Reset()` vs `Start`. Replaced with plain `Classic/Button` controls doing `Set(gTab, n)`: the value travels in the button, so nothing has to propagate. **Corollary worth watching: the ~20 Choice strips still use cmpSelection and would lag by one click — invisible until save.** |
| 2026-08-04 | Studio → chat | taskmaster_projects | project_region, project_coverage | — | — | `schema/schema.yaml` | **RESOLVED — corrected to single-value** | Same arity class as `project_requestor`, one type across. Both Managed Metadata columns had been provisioned **multi-value**; corrected to single. The app already assumed single on both sides — `Coalesce(gEditProject.project_region.Value, "not set")` on the read and `LookUp(Choices(…), Path = …)` on the write — so no app change was needed. Note `Choices()` is arity-independent, so the PICKER would have kept working and only the current-value label and the save would have failed: **a multi MM column is quieter than a multi Person column, not safer.** All 8 MM columns now carry explicit `multi: false` alongside the 9 Person columns; 6 MM and 8 Person columns remain unverified. |
| 2026-08-04 | Studio → chat | scrProjectEdit | OnVisible | — | Studio error | `schema/schema.yaml` | **RESOLVED — column corrected to single** | *"the coalesce for displayname and mail in gPrRequestor is erroring because of the "" being passed instead of a Table."* `project_requestor` was provisioned in SharePoint with **Allow multiple selections = Yes**, so the connector returns a **table**; `col.DisplayName` is then a single-column table and `Coalesce(<table>, "")` fails — Coalesce takes its type from the first argument. Diagnostic tell: `gPrManager` and `gPrSupporter` use the IDENTICAL formula and are fine, so the fault is the column, not the code. The write would have failed next: a single expanded-user record is not a table. **Fix is in SharePoint — the repo is the golden source and says single.** All 9 Person columns now carry an explicit `multi: false` so the arity is part of the provisioning contract rather than an assumption. **Confirmed by the user and corrected in SharePoint to a single requestor — no app change was needed.** The 7 defined lists are now marked `provisioned: live` and the schema carries a verification block (arity, internal names, Choice values, indexes) for the rest of the drift this gap cannot detect. |
| 2026-08-04 | Studio → chat | scrProjects | galProjects | — | Code view | `src/authored/scrProjects.pa.yaml` | **CHANNEL FACT — layout formulas freeze on paste** | User: *"in github i see the dynamic calculation… but on paste the gallery's Y value becomes hardcoded."* **Documented first-party**: *"After you write formulas for the X, Y, Width and Height properties of a control, your formulas will be overwritten with constant values if you subsequently drag the control in the canvas editor"* — and a paste positions controls. So every layout formula lands as **the constant it evaluated to at the instant of the paste**. That is the true origin of `Y=193`: not an unresolved reference, but `SearchBox.Y + SearchBox.Height + Gap` evaluated mid-paste and frozen. Consequences: the landed app is **not responsive** (`Parent.Width - 48` → `1318`), a Theme change can never propagate to layout, and **a wrong position must be fixed in the formula bar — re-pasting re-freezes it**. Authoring rule adopted: plain integers for X/Y/Width/Height wherever the value is static, so what lands equals what was authored. |
| 2026-08-04 | Studio → chat | scrProjects | galProjects | — | Preview | `src/authored/scrProjects.pa.yaml` | **LAYOUT BUG** | *"the gallery and blue bar are overlapping the filter / search functions."* The authored bands are 88–148 header, 164–204 filter+search, 220–744 gallery — 16px gaps, no computed overlap. **The one mechanism that produces exactly this symptom: `galProjects` positioned itself off another control** — `Y: =SearchBox.Y + SearchBox.Height + Gap`. Studio suffixes a colliding name on paste, and an unresolved reference collapses the term, dropping the gallery to the top of the screen where — being declared after them — it covers the filter and search row. Now pure Theme arithmetic, like every other screen; `SearchBox` also renamed `txtProjSearch` to match the house convention and cut the collision risk. Validator NOTEs any control whose Y/Height references another control (8 remain, all labels inside their own backdrop card — small blast radius, left alone). |
| 2026-08-04 | Studio → chat | scrIssueEdit (+3 edit screens) | overlay pickers | — | Preview | `src/authored/scr*Edit.pa.yaml` | **LAYOUT BUG — swept** | *"the galleries overlap each dropdown and don't allow users to select some of the options. The selection bar also has too large of font."* Both real. The picker fields sit **66px apart** but every results gallery is **132px tall**, so (a) two galleries open together overlap by half and the later-declared one makes the earlier one's bottom rows unclickable, and (b) an open dropdown covers the next TWO search boxes. Fixed on all four edit screens, **21 pickers**: each search box sets a `g<X>Picker` key in `OnChange` and each gallery requires it, so only one is ever open; and each gallery moved BESIDE its input (`X = input.X + input.Width + 8`, same Y, W 280) so it covers the chip label and empty space instead of the fields below. The two inline pickers in scrProjectEdit's staged-transaction row have no space beside them and keep the gate only. Font: `cmpSelection` gains a **`FontSize` input (default 11)**, tuned down to 10 on the seven-option stage strips — tunable on the instance so a further tweak needs no round trip. |
| 2026-08-04 | Studio → chat | icons (all screens) | — | — | — | `tools/studio-enums.json` | **CORRECTED — enum finally grounded** | User: *"your use of icons is also incorrect in some places."* Correct. **`Icon.Back`, `Icon.Documents` and `Icon.Table` do not exist** → `Icon.BackArrow`, `Icon.Document`, `Icon.DetailList`. Found by recovering the **complete 180-value classic Icon enum from `References/Templates.json` inside the example `.msapp` already in this repo** — an `.msapp` is a zip and its Templates.json holds the enums as pipe-delimited runs. That is first-party ground truth, and it was available all along: I had declared the enum ungroundable and drawn a hamburger out of three Rectangles to avoid it — **`Icon.Hamburger` is in fact valid.** The list is committed as `tools/studio-enums.json`, the validator now rejects any icon off it (suggesting the nearest real name), and the extraction technique is written into the studio-transfer skill. |
| 2026-08-04 | Studio → chat | scrIssueEdit | btnIssSave | — | Studio error | `src/authored/scr*Edit.pa.yaml` | **REJECTED — swept** | *"The Notify function in OnSelect for the create issue button is expecting a record."* **`IfError` requires ALL its arguments to be type-compatible today** — MS Learn states the rule and adds a note that currently every argument must match, not just the ones that could be returned. `Patch` returns a record and `Notify` a boolean, so the idiom **the docs themselves print** is rejected. Swept the class: **28 call sites across 5 screens** — scrIssueEdit 6, scrTaskEdit 14, scrProjectEdit 7 (+3 three-arm `Collect` writers), scrTransactionEdit 1, scrTask 2. Every arm is now a `Set`; failures accumulate into one `gXWarn` and produce a single banner instead of up to ten. Inside `ForAll`, where `Set` is unsafe because iteration order is not guaranteed, the arms are forced to text: `IfError( Text(Patch(…).ID), FirstError.Message, "" )`. Validator now flags any IfError mixing behaviour functions. |
| 2026-08-04 | — (authored) | 6 screens, 24 galleries | *Hit buttons | — | — | `src/authored/scr*.pa.yaml` | **AUTHORED — pre-emptive fix** | **Every clickable gallery in the app had the dead-row bug.** The nav rail's fix on 2026-08-03 was applied only to the nav rail; an audit found 24 more galleries still relying on the gallery's own `OnSelect` — every person/lookup search dropdown on all four edit screens, the projects list, and the task / transaction / issue rows on the project detail. All of them would have looked right and done nothing. Each now carries a transparent full-template `Classic/Button` declared LAST doing `Select(Parent); <the original action>`; the gallery's own `OnSelect` is gone. Every body used only `ThisItem`, so nothing depended on the selection propagating. Validator now rejects any gallery `OnSelect` outright. |
| 2026-08-04 | repo → Studio | all 8 nav screens | appBar | — | Component editor + code view | `components/cmpAppBar.pa.yaml` | **LANDED ✅** | Header bar and nav rail merged into one component with a **fly-out** rail: closed it is a 64px header with a hamburger; tapping it slides the rail out over the content behind a scrim, and the scrim or any entry closes it. `cmpNavMenu` deleted, and `HeaderBar`/`AppTitle`/`ScreenTitle` removed from all 8 screens — one definition instead of four blocks × 8. Content reclaims the old 240px nav gutter. The instance `Height: =If(gNavOpen, Parent.Height, Theme.Space.HeaderH)` is load-bearing: a component swallows every click in its bounds, so it may only be full-screen while open (when the scrim *wants* the click). Hamburger drawn from three Rectangles — no first-party source enumerates the classic `Icon` enum, and one unknown token would fail the whole body paste. |
| 2026-08-03 | Studio → chat | NavMenu | — | — | — | `src/patches/App.Formulas.pa.fx` | **MY DIAGNOSIS WAS WRONG** | I claimed a screen reference can't live in a table and rewrote eight files on that basis. **It can** — the *"expecting a table"* error was a mistake in how the definition had been typed, not the design. The Key-based table is kept, but for a different and real reason: the nav is now a component, and a component cannot Navigate, so the Key is the only part it needs to hand back. |
| 2026-08-03 | repo → Studio | nav rail | NavGallery | — | Preview | `components/cmpNavMenu.pa.yaml` | **RUNTIME BUG + REFACTOR** | Nav items didn't navigate: a gallery's `OnSelect` does not reliably fire when the click lands on a child control, and every row was icon + label with no hit target. Each row now carries a transparent full-template `Classic/Button`, declared last so z-order puts it on top, doing `Select(Parent); OnNavigate()`. Extracted the rail into **`cmpNavMenu`** at the same time — one definition replacing the pair copy-pasted across 5 screens + 3 skeletons. |
| 2026-08-03 | repo → Studio | scrHome | cmpConfirmHome | — | Preview | `src/authored/scrHome.pa.yaml` | **RUNTIME BUG** | **Nothing on scrHome was clickable** — not nav, not buttons. Cause: `cmpConfirmHome` is a full-screen component instance declared LAST, and its `Visible` was never set, so it defaulted to true and sat on top swallowing every click. A transparent fill doesn't help — the component still hit-tests, and `IsOpen` only governs what is drawn INSIDE it. Now `Visible: =gConfirmOpen`. `cmpToastHome` had the same bug over the bottom-right corner; gated on a new `gToastShow`, cleared by its `OnDismiss`. Validator lints it. |
| 2026-08-03 | repo → Studio | all nav galleries | NavGallery | — | Studio error | `src/patches/App.Formulas.pa.fx` | **REJECTED** | `NavMenu` errored *"expecting a table"*. It held `Screen: scrHome` in each record — **a screen is a control reference, not a value**, so a table column cannot hold one and the whole named formula fails to type, breaking every gallery's `Items: =NavMenu`. Replaced with a numeric `Key` plus a `Switch` per gallery; the active-item highlight compares `ThisItem.Key` to the screen's own key instead of `App.ActiveScreen`. 8 files. |
| 2026-08-03 | repo → Studio | cmpTermPicker | — | — | Component editor | `components/cmpTermPicker.pa.yaml` | **RESOLVED** | Loads correctly after the gallery `Variant` fix. Confirms the black render was the **invalid variant**, not the zero-alpha `Fill` — so transparency works fine and that diagnosis was wrong. Galleries and row buttons reverted to transparent; the component's own Fill stays opaque white as deliberate panel styling. |
| 2026-08-03 | Studio → chat (screenshot) | galleries (all) | — | — | Code view | `src/authored/**` | **TOKEN CORRECTED** | Studio's generated YAML for a vertical gallery reads `Variant: BrowseLayout_Vertical_TwoTextOneImageVariant_ver5.0`. **`Variant: Vertical` was never a real token** — a guess carried since Phase 1. Replaced across **49 galleries** in 20 files; the kanban variant takes the Horizontal form. Likely the true cause of cmpTermPicker's black render: an unrecognised variant leaves the gallery with no template to style from. First hard evidence returned through the gap as a photo of code view rather than a yes/no. |
| 2026-08-03 | repo → Studio | cmpTermPicker (body) | galL2–galL4 | — | Code view | `components/bodies/cmpTermPicker.children.pa.yaml` | **REJECTED** | `Visible: =CountRows(galL2.Items) > 1` — a control's `Items` is **write-only**; only `AllItems` is readable. `Self.AllItems` would have been worse: a hidden gallery's rows aren't realised, so the formula would latch off permanently. Level visibility now asks the **source data** — does any term sit below the previous pick's path — which needs no depth arithmetic and no sentinel compensation. Validator lints any read of a declared control's `.Items`. |
| 2026-08-03 | repo → Studio | cmpToast (body) | tmrToast | — | Code view | `components/bodies/cmpToast.children.pa.yaml` | **TOKEN CORRECTED** | The control is plain **`Timer`**, not `Classic/Timer` — the authored token had the wrong class. Now written with no version suffix (optional; Studio uses current). **This was the last unverified token in the kit — the validator now emits no token warnings at all.** |
| 2026-08-03 | repo → Studio | cmpTermPicker | — | — | Component editor | `components/cmpTermPicker.pa.yaml` | **RENDER BUG** | Component showed a **black fill**. Cause: `Fill: =RGBA(0, 0, 0, 0)` is black at zero alpha, and where the alpha isn't honoured it renders opaque. Every component uses that value, but this is the first whose background is genuinely exposed — cmpSelection's single gallery covers 100% of its area. Fixed by removing all dependence on transparency here: component + 4 galleries get an explicit white surface, row buttons mirror their gallery's TemplateFill. Eight components still use a zero-alpha fill — see `_COMPONENTS-NOTES.md`. |
| 2026-08-03 | repo → Studio | cmpTermPicker (body) | galL1–galL4 | — | Code view | `components/bodies/cmpTermPicker.children.pa.yaml` | **REJECTED** | `Ungroup(…, "v")` — *"expecting an identifier name"*. **Power Apps 3.24042 (Apr 2024) changed column-name arguments from strings to IDENTIFIERS** across Add/Drop/Rename/ShowColumns, GroupBy/Ungroup, Search and DataSourceInfo. Existing apps were auto-migrated; this repo authors from scratch, so nothing migrated it for us. Fixed repo-wide (13 call sites): `Ungroup(t, v)`, `ShowColumns(x, Label, Path)`, `AddColumns(t, wgt, …)`. Validator now checks it per argument position. |
| 2026-08-03 | repo → Studio | cmpSelection (contract, retry) | Selected | — | Property pane | `components/cmpSelection.pa.yaml` | **RETRACTED** | Superseded by the root cause: the property had been created as an **Input**, not an Output. An Input's `Default` is evaluated in the consumer's scope and cannot see the component's child controls; an Output's formula is evaluated inside and can. The original conditional formula was correct all along, and has been restored. |
| 2026-08-03 | repo → Studio | cmpSelection (contract) | Selected | — | Property pane | `components/cmpSelection.pa.yaml` | **MISDIAGNOSED** | Looked like Studio rejecting a conditional child reference in an Output. **Real cause: the property had been created as an Input.** An Input's `Default` evaluates in the consumer's scope and cannot see child controls. Kept as the record of a wrong turn — if a property errors on a child reference, check its KIND first. |
| 2026-08-03 | repo → Studio | cmpStatusPill (body) | pillHtml | *(not reported)* | Code view | `components/bodies/cmpStatusPill.children.pa.yaml` | **LANDED** | **`HtmlViewer@2.1.0` is CONFIRMED** — the body pasted; the only failure was the HtmlText *value*, four `IsMatch` calls whose patterns carried their own `^`/`$` while IsMatch already defaults to MatchOptions.Complete. Rewritten as `in` membership over a word list. Clears the token risk for `cmpChoicePill` and `cmpUiKit` too; `Classic/Timer@2.1.0` is now the only unverified token left. |
| 2026-08-03 | repo → Studio | components | — | — | Code view | `src/authored/components/*.pa.yaml` | **REGRESSED** | Whole-definition paste stopped working after earlier successes; screens can't load without the components, so this is a hard blocker. Error text not yet reported. Two responses shipped without waiting for it: (1) components are now split into a typed **contract** (`BUILD-SHEET.md`) and a pasteable **body** (`bodies/*.children.pa.yaml`) — a custom property has no paste gesture, so a whole-definition paste asks one channel to carry two things; (2) `cmpSelection` moved off `Variant: Horizontal` (self-flagged unconfirmed, and used by 7 screens) to a vertical gallery with `WrapCount`. |
| 2026-08-03 | repo → Studio | components (retry) | — | *(not reported)* | Code view | `src/authored/components/*.pa.yaml` | **PARTLY LANDED** | Corrects the entry below and the repo's standing assumption: **components ARE code-view-pasteable** — the notes claimed they had to be rebuilt by hand in the component editor, which was wrong. User reports several landed after the schema fix; individual names not reported, so this row covers the batch. Consequence: component control tokens are a real paste payload, so `HtmlViewer@2.1.0` and `Classic/Timer@2.1.0` are now genuine risks rather than documentation. |
| 2026-08-03 | repo → Studio | scrAdmin | scrAdmin | *(not reported)* | Code view | `src/authored/scrAdmin.pa.yaml` | **LANDED (qualified)** | First successful crossing. Confirms the modern structured `.pa.yaml` screen dialect. **AMENDED 2026-08-03:** the copy actually pasted was a STALE one, predating commit `5a7e684`, and carried `Variant: CONFIRM_BlankVertical` on the nav gallery — a leftover placeholder. So this landing proves a Gallery control pastes; it proves NOTHING about which `Variant` names are legal, and the earlier claim that it made `Variant: Vertical` a proven token was wrong. Theme unstyled as expected — `App.Formulas` not yet pasted. |
| 2026-08-03 | repo → Studio | components (batch) | — | — | Component editor | `src/authored/components/*.pa.yaml` | **REJECTED** | YAML structural error: *"expected SequenceStart, got MappingStart"*. Cause found against Microsoft's official pa-yaml v3.0 schema: `Parameters` must be a **sequence**, and `Output`/`OutputFunction`/`Action` properties may not carry `Default`. All 10 components corrected and now validate. |

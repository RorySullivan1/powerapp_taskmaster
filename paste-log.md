# Paste log

Every crossing of the air gap, newest first. A row exists only once a human has pasted the unit
into Studio and confirmed the outcome. Nothing is "in the app" without a row here.
See the `studio-transfer` skill for the lifecycle and the rename-and-log rule.

| Date | Direction | Target screen | Intended name | Studio suffix | Channel | Source file | Outcome | Notes |
|------|-----------|---------------|---------------|---------------|---------|-------------|---------|-------|
| 2026-08-03 | repo → Studio | cmpTermPicker | — | — | Component editor | `components/cmpTermPicker.pa.yaml` | **RESOLVED** | Loads correctly after the gallery `Variant` fix. Confirms the black render was the **invalid variant**, not the zero-alpha `Fill` — so transparency works fine and that diagnosis was wrong. Galleries and row buttons reverted to transparent; the component's own Fill stays opaque white as deliberate panel styling. |
| 2026-08-03 | Studio → chat (screenshot) | galleries (all) | — | — | Code view | `src/authored/**` | **TOKEN CORRECTED** | Studio's generated YAML for a vertical gallery reads `Variant: BrowseLayout_Vertical_TwoTextOneImageVariant_ver5.0`. **`Variant: Vertical` was never a real token** — a guess carried since Phase 1. Replaced across **49 galleries** in 20 files; the kanban variant takes the Horizontal form. Likely the true cause of cmpTermPicker's black render: an unrecognised variant leaves the gallery with no template to style from. First hard evidence returned through the gap as a photo of code view rather than a yes/no. |
| 2026-08-03 | repo → Studio | cmpTermPicker (body) | galL2–galL4 | — | Code view | `components/bodies/cmpTermPicker.children.pa.yaml` | **REJECTED** | `Visible: =CountRows(galL2.Items) > 1` — a control's `Items` is **write-only**; only `AllItems` is readable. `Self.AllItems` would have been worse: a hidden gallery's rows aren't realised, so the formula would latch off permanently. Level visibility now asks the **source data** — does any term sit below the previous pick's path — which needs no depth arithmetic and no sentinel compensation. Validator lints any read of a declared control's `.Items`. |
| 2026-08-03 | repo → Studio | cmpToast (body) | tmrToast | — | Code view | `components/bodies/cmpToast.children.pa.yaml` | **TOKEN CORRECTED** | The control is plain **`Timer`**, not `Classic/Timer` — the authored token had the wrong class. Now written with no version suffix (optional; Studio uses current). **This was the last unverified token in the kit — the validator now emits no token warnings at all.** |
| 2026-08-03 | repo → Studio | cmpTermPicker | — | — | Component editor | `components/cmpTermPicker.pa.yaml` | **RENDER BUG** | Component showed a **black fill**. Cause: `Fill: =RGBA(0, 0, 0, 0)` is black at zero alpha, and where the alpha isn't honoured it renders opaque. Every component uses that value, but this is the first whose background is genuinely exposed — cmpSelection's single gallery covers 100% of its area. Fixed by removing all dependence on transparency here: component + 4 galleries get an explicit white surface, row buttons mirror their gallery's TemplateFill. Eight components still use a zero-alpha fill — see `_COMPONENTS-NOTES.md`. |
| 2026-08-03 | repo → Studio | cmpTermPicker (body) | galL1–galL4 | — | Code view | `components/bodies/cmpTermPicker.children.pa.yaml` | **REJECTED** | `Ungroup(…, "v")` — *"expecting an identifier name"*. **Power Apps 3.24042 (Apr 2024) changed column-name arguments from strings to IDENTIFIERS** across Add/Drop/Rename/ShowColumns, GroupBy/Ungroup, Search and DataSourceInfo. Existing apps were auto-migrated; this repo authors from scratch, so nothing migrated it for us. Fixed repo-wide (13 call sites): `Ungroup(t, v)`, `ShowColumns(x, Label, Path)`, `AddColumns(t, wgt, …)`. Validator now checks it per argument position. |
| 2026-08-03 | repo → Studio | cmpSelection (contract, retry) | Selected | — | Property pane | `components/cmpSelection.pa.yaml` | **RETRACTED** | Superseded by the root cause: the property had been created as an **Input**, not an Output. An Input's `Default` is evaluated in the consumer's scope and cannot see the component's child controls; an Output's formula is evaluated inside and can. The original conditional formula was correct all along, and has been restored. |
| 2026-08-03 | repo → Studio | cmpSelection (contract) | Selected | — | Property pane | `components/cmpSelection.pa.yaml` | **MISDIAGNOSED** | Looked like Studio rejecting a conditional child reference in an Output. **Real cause: the property had been created as an Input.** An Input's `Default` evaluates in the consumer's scope and cannot see child controls. Kept as the record of a wrong turn — if a property errors on a child reference, check its KIND first. |
| 2026-08-03 | repo → Studio | cmpStatusPill (body) | pillHtml | *(not reported)* | Code view | `components/bodies/cmpStatusPill.children.pa.yaml` | **LANDED** | **`HtmlViewer@2.1.0` is CONFIRMED** — the body pasted; the only failure was the HtmlText *value*, four `IsMatch` calls whose patterns carried their own `^`/`$` while IsMatch already defaults to MatchOptions.Complete. Rewritten as `in` membership over a word list. Clears the token risk for `cmpChoicePill` and `cmpUiKit` too; `Classic/Timer@2.1.0` is now the only unverified token left. |
| 2026-08-03 | repo → Studio | components | — | — | Code view | `src/authored/components/*.pa.yaml` | **REGRESSED** | Whole-definition paste stopped working after earlier successes; screens can't load without the components, so this is a hard blocker. Error text not yet reported. Two responses shipped without waiting for it: (1) components are now split into a typed **contract** (`BUILD-SHEET.md`) and a pasteable **body** (`bodies/*.children.pa.yaml`) — a custom property has no paste gesture, so a whole-definition paste asks one channel to carry two things; (2) `cmpSelection` moved off `Variant: Horizontal` (self-flagged unconfirmed, and used by 7 screens) to a vertical gallery with `WrapCount`. |
| 2026-08-03 | repo → Studio | components (retry) | — | *(not reported)* | Code view | `src/authored/components/*.pa.yaml` | **PARTLY LANDED** | Corrects the entry below and the repo's standing assumption: **components ARE code-view-pasteable** — the notes claimed they had to be rebuilt by hand in the component editor, which was wrong. User reports several landed after the schema fix; individual names not reported, so this row covers the batch. Consequence: component control tokens are a real paste payload, so `HtmlViewer@2.1.0` and `Classic/Timer@2.1.0` are now genuine risks rather than documentation. |
| 2026-08-03 | repo → Studio | scrAdmin | scrAdmin | *(not reported)* | Code view | `src/authored/scrAdmin.pa.yaml` | **LANDED (qualified)** | First successful crossing. Confirms the modern structured `.pa.yaml` screen dialect. **AMENDED 2026-08-03:** the copy actually pasted was a STALE one, predating commit `5a7e684`, and carried `Variant: CONFIRM_BlankVertical` on the nav gallery — a leftover placeholder. So this landing proves a Gallery control pastes; it proves NOTHING about which `Variant` names are legal, and the earlier claim that it made `Variant: Vertical` a proven token was wrong. Theme unstyled as expected — `App.Formulas` not yet pasted. |
| 2026-08-03 | repo → Studio | components (batch) | — | — | Component editor | `src/authored/components/*.pa.yaml` | **REJECTED** | YAML structural error: *"expected SequenceStart, got MappingStart"*. Cause found against Microsoft's official pa-yaml v3.0 schema: `Parameters` must be a **sequence**, and `Output`/`OutputFunction`/`Action` properties may not carry `Default`. All 10 components corrected and now validate. |

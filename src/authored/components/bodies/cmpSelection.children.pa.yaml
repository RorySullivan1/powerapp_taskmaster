# cmpSelection — BODY ONLY. GENERATED by tools/split_components.py; do not edit.
# Source of record: ../cmpSelection.pa.yaml
#
# Paste this into the component's canvas via code view, AFTER creating its
# custom properties (BUILD-SHEET.md). The controls below reference those
# properties by name, so a missing one fails the paste.
#
# This is a bare control sequence — the same shape a screen's Children: holds,
# and the same shape Studio produces when you copy selected controls.
- galSel:
    Control: Gallery@2.15.0
    Variant: BrowseLayout_Vertical_TwoTextOneImageVariant_ver5.0
    Properties:
      Items: =cmpSelection.Items
      Default: =LookUp(cmpSelection.Items, Id = cmpSelection.DefaultId)
      Height: =Parent.Height
      Width: =Parent.Width
      X: =0
      Y: =0
      WrapCount: =Max(CountRows(cmpSelection.Items), 1)
      TemplateSize: =Parent.Height
      TemplatePadding: =4
    Children:
    - optBtn:
        Control: Classic/Button@2.2.0
        Properties:
          Text: =ThisItem.Label
          Font: =Font.'Segoe UI'
          Size: =cmpSelection.FontSize
          Fill: =If(ThisItem.IsSelected, RGBA(0, 90, 158, 1), RGBA(255, 255, 255, 1))
          Color: =If(ThisItem.IsSelected, RGBA(255, 255, 255, 1), RGBA(32, 38, 45, 1))
          BorderColor: =RGBA(0, 90, 158, 1)
          BorderThickness: =1
          Height: =Parent.TemplateHeight - 4
          Width: =Parent.TemplateWidth - 8
          X: =4
          Y: =2
          OnSelect: =Select(Parent); cmpSelection.OnChange()

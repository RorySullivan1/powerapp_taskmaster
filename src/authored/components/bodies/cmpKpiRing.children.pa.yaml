# cmpKpiRing — BODY ONLY. GENERATED by tools/split_components.py; do not edit.
# Source of record: ../cmpKpiRing.pa.yaml
#
# Paste this into the component's canvas via code view, AFTER creating its
# custom properties (BUILD-SHEET.md). The controls below reference those
# properties by name, so a missing one fails the paste.
#
# This is a bare control sequence — the same shape a screen's Children: holds,
# and the same shape Studio produces when you copy selected controls.
- imgRing:
    Control: Image@2.2.3
    Properties:
      ImagePosition: =ImagePosition.Fit
      Height: =Parent.Height
      Width: =Parent.Width
      X: =0
      Y: =0
      Image: |
        =With(
            { pct: Min(100, Max(0, RoundDown(cmpKpiRing.Percent, 0))) },
            With(
                { lbl: If(Len(cmpKpiRing.Label) > 0, cmpKpiRing.Label, Text(pct) & "%") },
                "data:image/svg+xml," & EncodeUrl(
                    "<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 36 36'>" &
                    "<circle cx='18' cy='18' r='15.9155' fill='none' stroke='" & cmpKpiRing.TrackHex & "' stroke-width='3'/>" &
                    "<circle cx='18' cy='18' r='15.9155' fill='none' stroke='" & cmpKpiRing.AccentHex & "' stroke-width='3' stroke-dasharray='" & pct & " " & (100 - pct) & "' stroke-dashoffset='25' stroke-linecap='round' transform='rotate(-90 18 18)'/>" &
                    "<text x='18' y='20.5' text-anchor='middle' font-size='7' font-family='Segoe UI, sans-serif' fill='#20262D'>" & EncodeHTML(lbl) & "</text>" &
                    "</svg>")
            )
        )

# cmpToast — BODY ONLY. GENERATED by tools/split_components.py; do not edit.
# Source of record: ../cmpToast.pa.yaml
#
# Paste this into the component's canvas via code view, AFTER creating its
# custom properties (BUILD-SHEET.md). The controls below reference those
# properties by name, so a missing one fails the paste.
#
# This is a bare control sequence — the same shape a screen's Children: holds,
# and the same shape Studio produces when you copy selected controls.
- toastBg:
    Control: Rectangle@2.3.0
    Properties:
      Fill: |
        =Switch(cmpToast.Tone,
            "Success", RGBA(49, 130, 93, 1),
            "Warning", RGBA(214, 152, 40, 1),
            "Danger",  RGBA(197, 58, 58, 1),
            RGBA(0, 90, 158, 1))
      Visible: =_show
      Height: =Parent.Height
      Width: =Parent.Width
      X: =0
      Y: =0
- toastIcon:
    Control: Classic/Icon@2.5.0
    Properties:
      Icon: |
        =Switch(cmpToast.Tone,
            "Success", Icon.Check,
            "Warning", Icon.Warning,
            "Danger",  Icon.Error,
            Icon.Information)
      Color: =RGBA(255, 255, 255, 1)
      Visible: =_show
      Height: =22
      Width: =22
      X: =16
      Y: =(Parent.Height - 22) / 2
- toastLabel:
    Control: Label@2.5.1
    Properties:
      Text: =cmpToast.Message
      Color: =RGBA(255, 255, 255, 1)
      Visible: =_show
      Font: =Font.'Segoe UI'
      Size: =13
      X: =50
      Y: =0
      Width: =Parent.Width - 66
      Height: =Parent.Height
      VerticalAlign: =VerticalAlign.Middle
- tmrToast:
    Control: Classic/Timer@2.1.0
    Properties:
      Duration: =cmpToast.Duration
      AutoStart: =false
      Repeat: =false
      Start: =_show
      Visible: =false
      OnTimerEnd: =Set(_show, false); cmpToast.OnDismiss()

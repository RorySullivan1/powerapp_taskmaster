# cmpStatusCard — BODY ONLY. GENERATED by tools/split_components.py; do not edit.
# Source of record: ../cmpStatusCard.pa.yaml
#
# Paste this into the component's canvas via code view, AFTER creating its
# custom properties (BUILD-SHEET.md). The controls below reference those
# properties by name, so a missing one fails the paste.
#
# This is a bare control sequence — the same shape a screen's Children: holds,
# and the same shape Studio produces when you copy selected controls.
- cardBg:
    Control: Rectangle@2.3.0
    Properties:
      Fill: =RGBA(255, 255, 255, 1)
      Height: =Parent.Height
      Width: =Parent.Width
      X: =0
      Y: =0
- accentBar:
    Control: Rectangle@2.3.0
    Properties:
      Fill: =cmpStatusCard.Accent
      Width: =6
      Height: =Parent.Height
      X: =0
      Y: =0
- lblTitle:
    Control: Label@2.5.1
    Properties:
      Text: =cmpStatusCard.Title
      Color: =RGBA(110, 120, 130, 1)
      Font: =Font.'Segoe UI'
      Size: =12
      FontWeight: =FontWeight.Semibold
      X: =22
      Y: =16
      Width: =Parent.Width - 38
      Height: =20
- lblValue:
    Control: Label@2.5.1
    Properties:
      Text: =cmpStatusCard.Value
      Color: =RGBA(32, 38, 45, 1)
      Font: =Font.'Segoe UI'
      Size: =28
      FontWeight: =FontWeight.Semibold
      X: =22
      Y: =38
      Width: =Parent.Width - 38
      Height: =40
- lblTrend:
    Control: Label@2.5.1
    Properties:
      Text: =If(cmpStatusCard.Trend >= 0, "▲ ", "▼ ") & Text(Abs(cmpStatusCard.Trend), "0.0%")
      Color: =If(cmpStatusCard.Trend >= 0, RGBA(49, 130, 93, 1), RGBA(197, 58, 58, 1))
      Visible: =cmpStatusCard.Trend <> 0
      Font: =Font.'Segoe UI'
      Size: =12
      FontWeight: =FontWeight.Semibold
      X: =22
      Y: =84
      Width: =100
      Height: =20
- lblCaption:
    Control: Label@2.5.1
    Properties:
      Text: =cmpStatusCard.Caption
      Color: =RGBA(110, 120, 130, 1)
      Visible: =Len(cmpStatusCard.Caption) > 0
      Font: =Font.'Segoe UI'
      Size: =11
      X: =122
      Y: =84
      Width: =Parent.Width - 138
      Height: =20
      Align: =Align.Right
- cardBtn:
    Control: Classic/Button@2.2.0
    Properties:
      Text: =""
      Fill: =RGBA(0, 0, 0, 0)
      HoverFill: =RGBA(0, 90, 158, 0.05)
      PressedFill: =RGBA(0, 90, 158, 0.1)
      BorderColor: =RGBA(0, 0, 0, 0)
      Height: =Parent.Height
      Width: =Parent.Width
      X: =0
      Y: =0
      OnSelect: =cmpStatusCard.OnSelect()

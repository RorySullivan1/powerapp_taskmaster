# cmpStatusPill — BODY ONLY. GENERATED by tools/split_components.py; do not edit.
# Source of record: ../cmpStatusPill.pa.yaml
#
# Paste this into the component's canvas via code view, AFTER creating its
# custom properties (BUILD-SHEET.md). The controls below reference those
# properties by name, so a missing one fails the paste.
#
# This is a bare control sequence — the same shape a screen's Children: holds,
# and the same shape Studio produces when you copy selected controls.
- pillHtml:
    Control: HtmlViewer@2.1.0
    Properties:
      Height: =Parent.Height
      Width: =Parent.Width
      X: =0
      Y: =0
      HtmlText: |
        =With(
            { s: Trim(cmpStatusPill.Label) },
            With(
                { t: If( cmpStatusPill.Tone <> "auto",
                         cmpStatusPill.Tone,
                         Switch( true,
                             s in ["done", "complete", "completed", "closed", "approved", "active", "on track"], "Success",
                             s in ["blocked", "overdue", "rejected", "failed", "error", "breach"], "Danger",
                             s in ["at risk", "pending", "review", "waiting", "on hold", "hold"], "Warning",
                             s in ["in progress", "open", "new", "todo", "to do"], "Info",
                             "Neutral"
                         ) ) },
                With(
                    { c: Switch(t, "Success","#31825D", "Warning","#D69828", "Danger","#C53A3A", "Info","#005A9E", "#6E7882") },
                    "<span style='display:inline-block;background:" & c &
                    ";color:#fff;padding:2px 10px;border-radius:11px;font:600 12px Segoe UI,Arial,sans-serif'>" &
                    EncodeHTML(s) & "</span>"
                )
            )
        )

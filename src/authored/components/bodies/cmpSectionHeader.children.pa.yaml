# cmpSectionHeader — BODY ONLY. GENERATED by tools/split_components.py; do not edit.
# Source of record: ../cmpSectionHeader.pa.yaml
#
# Paste this into the component's canvas via code view, AFTER creating its
# custom properties (BUILD-SHEET.md). The controls below reference those
# properties by name, so a missing one fails the paste.
#
# This is a bare control sequence — the same shape a screen's Children: holds,
# and the same shape Studio produces when you copy selected controls.
- lblTitle:
    Control: Label@2.5.1
    Properties:
      Text: =cmpSectionHeader.Title
      Color: =RGBA(32, 38, 45, 1)
      Font: =Font.'Segoe UI'
      FontWeight: =FontWeight.Semibold
      Size: =20
      X: =0
      Y: =6
      Width: =Parent.Width - 160
      Height: =28
      VerticalAlign: =VerticalAlign.Middle
- lblSubtitle:
    Control: Label@2.5.1
    Properties:
      Text: =cmpSectionHeader.Subtitle
      Visible: =Len(cmpSectionHeader.Subtitle) > 0
      Color: =RGBA(110, 120, 130, 1)
      Font: =Font.'Segoe UI'
      Size: =12
      X: =0
      Y: =34
      Width: =Parent.Width - 160
      Height: =18
- btnAction:
    Control: Classic/Button@2.2.0
    Properties:
      Text: =cmpSectionHeader.ActionLabel
      Visible: =cmpSectionHeader.ShowAction
      Fill: =RGBA(0, 90, 158, 1)
      Color: =RGBA(255, 255, 255, 1)
      Font: =Font.'Segoe UI'
      Size: =13
      BorderColor: =RGBA(0, 90, 158, 1)
      Height: =36
      Width: =140
      X: =Parent.Width - 140
      Y: =(Parent.Height - 36) / 2
      OnSelect: =cmpSectionHeader.OnAction()
- divider:
    Control: Rectangle@2.3.0
    Properties:
      Fill: =RGBA(225, 229, 235, 1)
      Height: =1
      Width: =Parent.Width
      X: =0
      Y: =Parent.Height - 1

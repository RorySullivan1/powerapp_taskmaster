# =============================================================================
# scrAdmin  —  Admin (screen-map §9). CORE-SHELL: header + nav + titled
# placeholder. Phase 2/3 wires tmLookups maintenance, bulk archive, orphan
# detection (FK integrity is app logic — no real joins on SharePoint).
# DIALECT: modern structured schema; positional z-order. The nav rail is now
# the cmpNavMenu component, not inline controls.
# =============================================================================
Screens:
  scrAdmin:
    Properties:
      Fill: =Theme.Color.Bg
    Children:
      - HeaderBar:
          Control: Rectangle@2.3.0
          Properties:
            Fill: =Theme.Color.Primary
            Height: =Theme.Space.HeaderH
            Width: =Parent.Width
            X: =0
            Y: =0
      - AppTitle:
          Control: Label@2.5.1
          Properties:
            Text: ="EQD Taskmaster"
            Color: =Theme.Color.OnPrimary
            Font: =Theme.Font
            FontWeight: =FontWeight.Semibold
            Size: =Theme.Size.H2
            Height: =Theme.Space.HeaderH
            VerticalAlign: =VerticalAlign.Middle
            X: =Theme.Space.Gutter
            Y: =0
            Width: =360
      - ScreenTitle:
          Control: Label@2.5.1
          Properties:
            Text: ="Admin"
            Color: =Theme.Color.OnPrimary
            Font: =Theme.Font
            Size: =Theme.Size.H3
            Align: =Align.Right
            Height: =Theme.Space.HeaderH
            VerticalAlign: =VerticalAlign.Middle
            X: =Parent.Width - Self.Width - Theme.Space.Gutter
            Y: =0
            Width: =360
      # The whole rail is one component now — see components/cmpNavMenu.pa.yaml.
      # It cannot Navigate (a component can't see app screens), so it reports the
      # chosen entry and this screen does the navigating.
      - navMenu:
          Control: CanvasComponent
          ComponentName: cmpNavMenu
          Properties:
            X: =0
            Y: =Theme.Space.HeaderH
            Width: =Theme.Space.NavW
            Height: =Parent.Height - Theme.Space.HeaderH
            Items: =NavMenu
            ActiveKey: =5
            HasLicence: =gHasPowerBiLicence
            OnNavigate: |
              =Switch( navMenu.SelectedKey,
                       1, Navigate(scrHome, ScreenTransition.Fade),
                       2, Navigate(scrReports, ScreenTransition.Fade),
                       3, Navigate(scrProjects, ScreenTransition.Fade),
                       4, Navigate(scrReference, ScreenTransition.Fade),
                          Navigate(scrAdmin, ScreenTransition.Fade) )
      # ---- Content placeholder --------------------------------------------
      - AdminEmpty:
          Control: Rectangle@2.3.0
          Properties:
            Fill: =Theme.Color.Surface
            Height: =Parent.Height - Theme.Space.HeaderH - (Theme.Space.Gutter * 2)
            Width: =Parent.Width - Theme.Space.NavW - (Theme.Space.Gutter * 2)
            X: =Theme.Space.NavW + Theme.Space.Gutter
            Y: =Theme.Space.HeaderH + Theme.Space.Gutter
      - AdminEmptyLabel:
          Control: Label@2.5.1
          Properties:
            Text: ="Lookups, bulk archive and orphan detection will live here (Phase 2/3)."
            Font: =Theme.Font
            Size: =Theme.Size.Body
            Color: =Theme.Color.TextMuted
            Align: =Align.Center
            VerticalAlign: =VerticalAlign.Middle
            X: =AdminEmpty.X
            Y: =AdminEmpty.Y
            Width: =AdminEmpty.Width
            Height: =AdminEmpty.Height

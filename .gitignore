# Personal, machine-local notes — never committed.
/CLAUDE.local.md

# Transient, dated raw Studio pulls (working scratch). The committed baseline is studio/pulled/.
/studio/snapshots/

# Local session working logs (NOT .claude/memory/, which is committed shared memory).
*.session.log
/.session-logs/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
